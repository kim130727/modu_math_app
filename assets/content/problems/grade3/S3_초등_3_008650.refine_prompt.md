# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_008650.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_008650

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
[전체적인 구성]
- 화면 상단 왼쪽에 문제 번호 `21.`이 있고, 그 뒤로 지문이 2줄로 이어져 있습니다.
- 중앙에는 원의 그림이 크게 배치되어 있습니다.
- 원 그림 아래에는 주황색 테두리의 큰 사각형 박스가 있고, 그 안에 선택형 문장이 들어 있습니다.
- 더 아래 왼쪽에는 `(정답)` 표시와 함께 짧은 답안이 있으며, 그 아래에 `(해설)` 문장이 한 줄로 제시되어 있습니다.
- 전체적으로 “그림 관찰 → 문장 속 빈칸/선택 구조 → 정답 및 해설” 순서의 배치입니다.

[보이는 텍스트]
- 상단 지문:
  - `그림을 보고 지름과 반지름 사이의 관계를 알아보려고 합니다. 알맞은`
  - `것을 선택하세요.`
- 그림 안의 표시:
  - `4 cm`
  - `2 cm`
  - 작은 원형 표시 주변에 아주 작은 문자/기호처럼 보이는 부분이 있으나 선명하지 않습니다.
- 주황색 박스 안 문장:
  - `한 원에서 ( 지름 , 반지름 )은 ( 지름 , 반지름 )의 2배입니다.`
- 하단:
  - `(정답)지름, 반지름`
  - `(해설)한 원에서 지름은 반지름의 2배입니다.`

[반복되는 시각 패턴]
- 원 내부에 중심으로 보이는 점이 있고, 그 점에서 바깥 원둘레로 이어지는 선분이 반복적으로 사용됩니다.
- 점선 곡선/점선 표시가 두 군데 있어 길이 관계를 강조하는 방식으로 보입니다.
  - 왼쪽의 `4 cm` 표시는 원 내부의 한 선분 구간을 가리키는 듯한 점선 곡선과 함께 배치되어 있습니다.
  - 오른쪽의 `2 cm` 표시는 중심에서 오른쪽 원둘레로 향하는 선분 근처에 점선 곡선이 있습니다.
- 선택형 문장에서는 괄호로 두 단어를 나란히 제시해 둘 중 하나를 고르는 형태가 반복됩니다.
- 하단의 `(정답)`과 `(해설)`은 문제 풀이 형식에서 자주 쓰이는 반복 패턴입니다.

[수식 단계]
- 별도의 등식 변형 단계는 보이지 않습니다.
- 다만 그림 속 길이 표시는 `4 cm`, `2 cm`처럼 숫자와 단위가 함께 제시되어 관계를 비교하게 되어 있습니다.

[등호별 변환 의미]
- 해당 없음. 화면에 연속된 등호식이나 변형 과정은 보이지 않습니다.

[세부 내용]
- 원 그림:
  - 바깥 윤곽은 하나의 원입니다.
  - 원의 중심 근처에 분홍색 작은 점이 하나 있습니다.
  - 중심에서 오른쪽 원둘레까지 수평에 가까운 선분이 하나 그려져 있습니다.
  - 중심에서 원의 위쪽-오른쪽 방향 원둘레까지 사선 선분이 하나 그려져 있습니다.
  - 중심을 지나 아래쪽-왼쪽 원둘레까지 이어지는 사선 선분도 보입니다.
  - 이 사선은 원 내부를 가로지르는 형태라 지름을 나타내는 그림 요소처럼 보입니다.
- 길이 표기:
  - `4 cm`는 원의 왼쪽 안쪽에 위치하며, 점선 곡선이 위쪽 원둘레와 아래쪽 원둘레 사이를 연결하는 듯 표시되어 있습니다.
  - `2 cm`는 중심에서 오른쪽으로 뻗은 선분 아래쪽에 배치되어 있습니다.
- 주황색 박스:
  - 가로로 긴 사각형 테두리 안에 선택 문장이 중앙 정렬에 가깝게 배치되어 있습니다.
  - 문장 내부의 `( 지름 , 반지름 )` 형태는 선택지처럼 보이며, 두 곳 모두 쉼표로 구분된 두 단어가 괄호 안에 들어 있습니다.
- 하단 정답/해설:
  - `(정답)`은 굵게 보이며, 그 뒤에 쉼표로 구분된 답이 바로 이어집니다.
  - `(해설)` 뒤에는 문장 하나가 붙어 있고, 그림의 관계를 말로 설명합니다.

[Python DSL 작성 힌트]
- 화면을 크게 나누면 다음 요소로 구성하는 것이 좋습니다.
  1. 상단 문제 번호와 지문 2줄
  2. 중앙의 원 그림
  3. 주황색 테두리의 선택 문장 박스
  4. 하단 정답 및 해설 텍스트
- 원 그림은 다음 구성요소를 분리해서 그리면 좋습니다.
  - 바깥 원
  - 중심점
  - 중심에서 오른쪽으로 가는 반지름 선분
  - 중심에서 위쪽-오른쪽으로 가는 사선 선분
  - 중심을 지나는 왼쪽 아래 방향의 긴 사선 선분
  - 길이 표시용 점선 곡선 2개
  - `4 cm`, `2 cm` 텍스트 라벨
- 선택 박스는 주황색 테두리만 있는 긴 직사각형으로 처리하면 됩니다.
- 박스 안 문장은 가운데 정렬에 가깝게 배치하는 것이 자연스럽습니다.
- 하단의 `(정답)`과 `(해설)`은 왼쪽 정렬로 두 줄 배치하면 됩니다.
- 답안 내용을 새로 추론하여 별도로 넣지 말고, 보이는 텍스트만 그대로 옮기는 방식이 좋습니다.

[주의할 점]
- 문제를 풀어 `지름`, `반지름`의 관계를 설명하는 해설을 새로 생성하지 마세요.
- 주황색 박스 안의 괄호는 실제 선택 구조로 보이므로, 빈칸으로 단정하지 말고 “괄호로 된 선택지”로 묘사하세요.
- `4 cm`, `2 cm`가 각각 정확히 어떤 선분 길이를 뜻하는지는 그림만으로 일부 해석이 필요하지만, 답을 확정해 쓰지 마세요.
- 원 내부의 작은 기호는 해상도가 낮아 정확히 읽기 어렵습니다.
- 점선 곡선의 정확한 시작점과 끝점은 이미지 해상도 때문에 약간 모호합니다.

[불확실한 부분]
- 중심점 옆의 작은 기호가 정확히 무엇인지 선명하지 않습니다.
- `4 cm`와 `2 cm`의 각 기준선이 정확히 어떤 선분을 나타내는지, 점선 곡선의 연결 의미가 완전히 또렷하지 않습니다.
- 원 내부의 사선들이 서로 정확히 어떤 관계인지(지름, 반지름 표시용인지)는 그림 해석이 일부 필요합니다.
- 주황색 박스 안 문장의 괄호가 실제 선택지인지, 편집용 표기인지 경계가 약간 애매하지만, 화면상으로는 선택형 문장처럼 보입니다.

--- RAW VISION DRAFT END ---
