# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_008541.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_008541

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
[전체적인 구성]
- 화면 상단에 문제 번호와 지시문이 한 줄로 배치되어 있습니다.
- 그 아래에는 연한 베이지색의 긴 직사각형 강조 영역이 있고, 그 안에 3개의 곱셈식이 가로로 나란히 놓여 있습니다.
- 중간 영역에는 보기 선택지처럼 보이는 원형 번호 표식 ①, ②, ③, ④가 두 줄로 분산 배치되어 있으며, 각 번호 옆에 동그라미로 표시된 기호들이 붙어 있습니다.
- 하단에는 `(정답)` 표기와 `(해설)` 표기가 순서대로 있고, 해설에는 각 식의 계산 결과와 부등호 비교식이 이어집니다.
- 가장 아래에는 왼쪽을 가리키는 회색 화살표 모양이 있으며, 그 뒤로 정답을 설명하는 문장이 길게 이어집니다.

[보이는 텍스트]
- 상단 지시문: `2. 계산 결과가 큰 것부터 차례대로 나열한 것을 고르시오.`
- 강조 박스 안의 식:
  - `① 397 × 6`
  - `ㄴ 549 × 4`
  - `ㄷ 456 × 5`
- 보기 영역의 번호와 기호 배치:
  - `①` 옆에 `ㄱ ㄴ ㄷ`
  - `②` 옆에 `ㄱ ㄷ ㄴ`
  - `③` 옆에 `ㄴ ㄷ ㄱ`
  - `④` 옆에 `ㄷ ㄴ ㄱ`
- 정답 표시: `(정답) ②`
- 해설 시작: `(해설)`
- 계산 결과:
  - `① 397 × 6 = 2382`
  - `ㄴ 549 × 4 = 2196`
  - `ㄷ 456 × 5 = 2280`
- 비교식:
  - `2382 > 2280 > 2196`
- 해설 문장 일부:
  - `이므로 계산 결과가 큰 것부터 차례로 기호를 써 보면 ①, ㄷ, ㄴ입니다.`
- 하단에 회색 화살표가 있고, 그 뒤 해설 문장이 이어집니다.

[반복되는 시각 패턴]
- 원형 번호 표식 `①, ②, ③, ④`가 보기 항목의 시작 표시로 반복됩니다.
- 각 곱셈식은 `기호 + 수 × 수`의 형태로 같은 형식을 유지합니다.
- 해설 부분에서도 각 항목은 `기호 + 식 = 결과`의 형태로 반복되어 정렬되어 있습니다.
- 비교 결과는 `>` 기호를 연속으로 사용해 큰 수에서 작은 수로 순서를 나타냅니다.
- 동그라미로 둘러싼 한글 기호 `ㄱ, ㄴ, ㄷ`가 문제와 해설 양쪽에서 반복 사용됩니다.

[수식 단계]
- 해설에 보이는 수식은 각 항목별로 한 번의 등호를 사용하여 곱셈 결과를 나타냅니다.
- 이어서 세 결과를 비교한 부등식 `2382 > 2280 > 2196`이 제시됩니다.
- 등호가 여러 번 연속되는 변형식은 보이지 않습니다.
- 각 계산식은 `곱셈식 → 계산 결과` 형태로만 제시되어 있습니다.

[등호별 변환 의미]
- `397 × 6 = 2382`는 왼쪽의 곱셈식을 계산한 결과가 오른쪽 수라는 뜻으로 보입니다.
- `549 × 4 = 2196`도 같은 방식으로 계산 결과를 적은 것입니다.
- `456 × 5 = 2280` 역시 동일한 형식입니다.
- 부등식 `2382 > 2280 > 2196`은 세 계산 결과의 크기 비교를 나타냅니다.
- 다만 여기서는 계산 과정을 보여 주지 않고 결과만 제시합니다.

[세부 내용]
- 문제는 세 개의 곱셈식의 계산 결과를 비교하여, 큰 것부터 작은 것 순서로 배열한 보기 번호를 찾는 형식입니다.
- 베이지색 박스 안의 세 식은 문제의 핵심 대상이며, 각각 왼쪽에 `①`, `ㄴ`, `ㄷ` 같은 표지가 붙어 있습니다.
- 보기 영역은 각 번호 `①~④` 아래에 세 기호의 순서를 다르게 배치한 선택지 구조입니다.
- 해설에서는 각 식의 결과를 먼저 적고, 그 결과를 비교한 뒤 어떤 기호 순서가 되는지를 문장으로 설명합니다.
- 하단의 회색 화살표는 해설 문단 시작을 시각적으로 강조하는 역할로 보입니다.

[Python DSL 작성 힌트]
- 상단 문제문을 한 줄 텍스트로 배치하고, 그 아래에 연한 색 배경의 긴 사각형 박스를 그리면 좋습니다.
- 박스 안에는 3개의 식을 가로 정렬로 배치하되, 각 식 앞에 동그라미 기호를 포함합니다.
- 보기 영역은 2행 2열 느낌으로 배치되어 있으나, 실제로는 좌우로 떨어진 두 묶음처럼 보이므로 위치를 주의해 재현합니다.
- 각 보기마다 원형 숫자 `①~④`와 그 옆의 세 기호 순서를 함께 넣습니다.
- 정답 표시는 `(정답) ②`처럼 작은 텍스트로 따로 두고, 해설은 아래에 `(해설)` 라벨 뒤로 이어서 작성합니다.
- 해설의 각 계산식은 `기호 + 식 + = + 결과` 구조로 반복 배치합니다.
- 비교식 `>`는 결과 세 개를 한 줄에 이어 적는 형태로 구성합니다.
- 하단 화살표는 회색의 두꺼운 좌향 아이콘처럼 보이므로 단순 텍스트 화살표보다 도형 요소로 처리하면 더 비슷합니다.

[주의할 점]
- 문제를 실제로 풀어서 답을 확정하는 방식으로 작성하지 말고, 화면에 이미 보이는 정답 표기만 사실로 서술해야 합니다.
- 보기의 기호 순서를 임의로 재배열하거나 추론해서 설명하지 않도록 주의합니다.
- `①`이 식 앞의 표시인지 보기 번호인지 문맥에 따라 달라 보일 수 있으므로, 문제 영역과 보기 영역을 구분해서 적는 것이 좋습니다.
- 계산 결과 수치는 해설에 이미 제시된 값만 그대로 옮기고, 별도 계산 과정은 덧붙이지 않습니다.
- 베이지색 박스의 정확한 폭과 좌우 여백은 이미지상 약간의 오차가 있으므로, 상대적 배치 중심으로 재현합니다.

[불확실한 부분]
- 왼쪽 상단의 작은 네모 표시가 선택지용 체크박스인지 단순 인쇄 기호인지 정확히는 판단하기 어렵습니다.
- 보기 영역의 각 기호 묶음이 완전히 같은 간격인지, 실제로는 미세하게 다른지는 이미지 해상도상 확정하기 어렵습니다.
- 하단 회색 화살표의 정확한 모양은 둥근 화살표인지 단순 좌향 화살표인지 약간 불명확합니다.
- `(해설)` 아래 문장의 줄바꿈 위치는 이미지에서 다소 흐릿하여, DSL에서는 자연스러운 문단 배치로 처리하는 편이 안전합니다.

--- RAW VISION DRAFT END ---
