# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_008698.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_008698

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
[전체적인 구성]
- 화면 상단 왼쪽에 문항 번호와 문제 문장이 한 줄로 배치되어 있다.
- 오른쪽 상단에는 원과 선분이 그려진 도형 그림이 있다.
- 왼쪽 하단과 가운데 하단 쪽에는 4개의 객관식 보기(①~④)가 두 줄 배치로 나뉘어 있다.
- 맨 아래 왼쪽에는 `(정답) ③` 표시와 해설 문장이 있다.
- 전체적으로 “문항 → 도형 → 보기 → 정답/해설” 순서로 읽히는 단순한 시험지형 레이아웃이다.

[보이는 텍스트]
- `□ 87. 원을 둘로 똑같이 나누는 선분은 어느 것인가요?`
- 보기:
  - `① 선분 ㄱㄴ`
  - `② 선분 ㄱㅇ`
  - `③ 선분 ㄷㄷ`처럼 보일 수 있으나, 실제로는 도형 안의 두 점 표기를 따라 읽어야 해서 `③ 선분 ㄱㄷ`로 보이는 부분이 핵심이다.
  - `④ 선분 ㅇㄹ`
- 아래쪽:
  - `(정답) ③`
  - `(해설) 원을 둘로 똑같이 나누는 선분은 원의 지름이므로 선분 ㄱㄷ입니다.`
- 도형 주변 문자:
  - 원 위쪽 오른쪽에 `ㄱ`
  - 원 왼쪽에 `ㄴ`
  - 원 아래쪽 왼편에 `ㄷ`
  - 원 오른쪽에 `ㄹ`
  - 원 중심 부근에 `ㅇ`
- 도형에는 원을 가로지르는 선분들이 있고, 중심 점처럼 보이는 작은 표시가 있다.

[반복되는 시각 패턴]
- 원형 도형 내부에 여러 개의 선분이 겹쳐 그려져 있어, “어떤 선분이 지름인지 고르는” 유형임을 시각적으로 강조한다.
- 원 위의 점 표기(`ㄱ`, `ㄴ`, `ㄷ`, `ㄹ`)와 중심 근처 점 표기(`ㅇ`)가 함께 나타나며, 보기의 선분 이름도 이 문자들을 조합한 형태로 반복된다.
- 보기 번호 `①~④`는 파란색 원형 번호 표시로 통일되어 있고, 각 보기 뒤에 `선분`이라는 단어가 반복된다.
- 해설에서는 `원의 지름`이라는 표현이 다시 등장해 문제의 핵심 개념을 반복적으로 강조한다.

[수식 단계]
- 해당 없음.  
- 등호로 연결된 계산식이나 변형식은 보이지 않는다.

[등호별 변환 의미]
- 해당 없음.  
- 화면에는 수식의 등호 변환 단계가 없다.

[세부 내용]
- 도형은 하나의 원이며, 내부에 적어도 3개의 선분이 그려져 있다.
- 선분 중 하나는 왼쪽 위의 `ㄴ`에서 오른쪽 위의 `ㄱ`으로 이어지는 긴 사선이다.
- 또 다른 선분은 아래쪽의 `ㄷ`에서 위쪽의 `ㄱ`으로 이어지는 사선이다.
- 중심 부근의 점 `ㅇ`에서 오른쪽 `ㄹ`로 향하는 수평 선분이 있다.
- 중심 점처럼 보이는 작은 점은 분홍색으로 표시되어 있어, 중심을 강조하는 것으로 보인다.
- 원의 왼쪽 바깥에는 `ㄴ`, 아래쪽 왼편 바깥에는 `ㄷ`, 오른쪽 바깥에는 `ㄹ`, 위쪽 오른편에는 `ㄱ`이 배치되어 있다.
- 문제는 “원을 둘로 똑같이 나누는 선분”을 묻고 있으며, 보기 중 하나를 고르는 형식이다.
- 하단 해설은 정답의 이유를 한 문장으로 설명한다.

[Python DSL 작성 힌트]
- 원 도형을 먼저 그리고, 중심점 표시를 별도 객체로 두는 구성이 적절하다.
- 원 안의 선분들은 서로 다른 색 없이 검은 선으로 그려져 있으며, 각각의 끝점과 방향을 구분해야 한다.
- 문자 라벨 `ㄱ, ㄴ, ㄷ, ㄹ, ㅇ`은 원 주변과 중심 근처에 배치하되, 선분 끝점과 겹치지 않도록 약간 떨어뜨려 배치하는 것이 좋다.
- 보기 영역은 좌우 2열 배치처럼 구성하면 원본과 유사하다.
- 정답과 해설은 하단 별도 텍스트 블록으로 분리해 넣는 편이 좋다.
- 도형에서 지름으로 보이는 선분과 다른 선분의 길이/방향 차이를 시각적으로 그대로 옮기는 것이 중요하다.

[주의할 점]
- 문제를 풀어 답을 계산하거나 추론한 내용은 시각 분석에 섞지 말아야 한다.
- 보기의 선분 이름은 도형의 문자 배치와 정확히 대응시켜야 한다.
- 원 중심을 나타내는 점은 작은 점이므로, 일반적인 큰 점이나 원호처럼 과장하지 말아야 한다.
- 해설에 나온 정답 표시는 원문에 보이지만, DSL 작성 시에는 문제 본문과 정답 표시를 분리해 처리하는 것이 좋다.
- 그림 속 선분이 원의 중심을 지나가는지 여부는 핵심 시각 정보이지만, 이를 수치적으로 단정할 필요는 없다.

[불확실한 부분]
- 일부 문자 표기가 해상도 때문에 다소 흐릿하게 보일 수 있다.
- 보기 `③`의 선분 표기는 이미지에서 약간 겹쳐 보일 가능성이 있으나, 해설 문장에 따라 `ㄱㄷ`로 해석되는 구성이 가장 자연스럽다.
- 원 내부의 중심 점 `ㅇ`는 정확히 문자라기보다 중심 표시용 기호처럼 보일 수 있다.
- 왼쪽 상단의 작은 네모 표시는 체크박스처럼 보이지만, 실제 의미는 문제 번호 앞의 장식 요소일 수 있다.

--- RAW VISION DRAFT END ---
