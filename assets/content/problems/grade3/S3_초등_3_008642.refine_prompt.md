# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_008642.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_008642

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
[전체적인 구성]
- 상단에 문제 번호와 설명 문장이 있고, 그 아래 중앙 오른쪽에 원 그림이 배치되어 있습니다.
- 원 아래에는 연녹색 테두리의 큰 선택지 상자가 가로로 놓여 있습니다.
- 선택지 상자 아래에는 `(정답)`과 `(해설)`이 각각 문장 형태로 인쇄되어 있습니다.
- 전체적으로 객관식 문제 형식이며, 그림과 보기 상자가 분리되어 있습니다.

[보이는 텍스트]
- 상단 문제 문장:
  - `□ 14. 은지, 진아, 현준이가 한 원에 지름을 그은 것입니다. 한 원에 지름을 몇 개 그을 수 있는지 알맞은 것을 선택하세요.`
- 원 그림 주변 이름:
  - 위쪽: `은지`
  - 오른쪽: `진아`
  - 오른쪽 아래: `현준`
- 그림 내부/근처 설명:
  - `지름은 모두 원의 중심을 지나요.`
- 선택지 상자 안:
  - `( 무수히 많이 그을 수 있습니다. , 3개 )`
- 하단 설명:
  - `(정답)무수히 많이 그을 수 있습니다.`
  - `(해설)지름은 원 위의 두 점을 이은 선분이 원의 중심을 지나도록 그은 것이므로 무수히 많이 그을 수 있습니다.`

[반복되는 시각 패턴]
- 원 안에 중심을 지나는 선분이 여러 개 그려져 있습니다.
- 같은 원을 기준으로 서로 다른 방향의 선들이 반복적으로 배치되어 있어, “지름”이 여러 개 가능함을 시각적으로 보여 줍니다.
- 중심점은 작은 점으로 표시되어 있고, 여러 선분이 그 점을 공통으로 지나갑니다.
- 선분마다 색이 다르게 표시되어 구분됩니다.
- 중심에서 아래쪽으로 향하는 초록색 곡선 화살표와 함께 설명 문구가 붙어 있습니다.
- 선택지 상자는 하나의 큰 직사각형 안에 두 개의 답 후보가 괄호와 쉼표로 나열된 형태입니다.

[수식 단계]
- 해당 없음. 등호를 포함한 수식 변형 단계는 보이지 않습니다.

[등호별 변환 의미]
- 해당 없음. 등호가 연속되는 수식 전개는 보이지 않습니다.

[세부 내용]
- 원은 화면 중앙보다 약간 오른쪽에 위치합니다.
- 원의 테두리는 회색 선으로 그려져 있고 내부는 비어 있습니다.
- 원 중심에는 작은 분홍색 점이 있으며, 여러 선분이 이 점을 기준으로 방사형으로 뻗어 있습니다.
- 선분은 적어도 3개 방향으로 보입니다.
  - 수직에 가까운 파란색 선분: 원의 위쪽과 아래쪽을 잇는 형태
  - 왼쪽 위에서 오른쪽 아래로 내려가는 초록색 대각선 선분
  - 왼쪽 아래에서 오른쪽 위로 올라가는 연한 보라색 대각선 선분
- 각 선분은 원의 가장자리까지 닿아 있어 지름을 나타내는 시각 요소처럼 보입니다.
- 가운데 근처에 작은 초록색 원 표시가 하나 더 보이며, 화살표와 연결된 안내문이 그 점을 가리키는 느낌입니다.
- 초록색 글씨 `지름은 모두 원의 중심을 지나요.`는 원의 오른쪽 아래에 세 줄로 배치되어 있습니다.
- 선택지 상자는 연녹색 테두리의 긴 직사각형이며, 내부 텍스트가 중앙 정렬되어 있습니다.
- `(정답)`과 `(해설)`은 본문보다 작지 않지만, 문제 아래쪽 왼편에 연속 문장처럼 배치되어 있습니다.

[Python DSL 작성 힌트]
- 상단 문제 문장은 좌측 상단에 텍스트 박스로 배치하면 됩니다.
- 원은 도형 객체 1개로 그리고, 중심점은 작은 점으로 따로 표시하는 구성이 적절합니다.
- 중심을 지나는 여러 선분은 서로 다른 색의 직선 객체로 추가하면 좋습니다.
- 각 선분의 끝점은 원 둘레에 닿도록 배치하고, 길이를 달리하지 말고 원을 관통하는 형태로 맞추면 됩니다.
- 원 주변의 이름 텍스트는 각각 해당 선분 끝 근처에 붙여 배치합니다.
- 초록색 설명 문구는 원 오른쪽 아래에 2~3줄 텍스트로 배치하고, 연결 화살표를 함께 넣는 구성이 어울립니다.
- 보기 상자는 큰 사각형 테두리로 만들고, 내부 텍스트는 가운데 정렬합니다.
- 하단의 `(정답)`, `(해설)`은 별도 텍스트 블록으로 두 줄 이상 배치합니다.

[주의할 점]
- 답을 추론해서 빈칸이나 보기 내용을 바꾸지 마세요.
- 그림 속 선분 개수를 임의로 늘리거나 줄이지 마세요.
- 이름 텍스트의 정확한 위치는 이미지상 약간 흐릿할 수 있으므로, 원 주변에만 상대적으로 배치하면 됩니다.
- `지름은 모두 원의 중심을 지나요.` 문구는 초록색 강조 텍스트로 처리해야 합니다.
- 선택지 상자 내부의 괄호, 쉼표, 공백 형태를 가능한 한 유지하세요.
- `(정답)`과 `(해설)`은 실제 풀이가 아니라 인쇄된 안내 문구로 보아야 합니다.

[불확실한 부분]
- 원 내부의 선분 색상 중 일부는 화면 해상도 때문에 연한 보라색/파란색 계열로 보일 수 있습니다.
- 중심점과 초록색 작은 표시의 정확한 위치 관계는 약간 अस्पष्ट할 수 있습니다.
- 상단 문제 문장의 줄바꿈은 이미지에서 자동 개행된 결과로 보이며, 실제 원문은 한 줄 문장일 수 있습니다.

--- RAW VISION DRAFT END ---
