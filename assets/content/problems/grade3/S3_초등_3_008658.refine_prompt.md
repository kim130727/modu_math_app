# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_008658.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_008658

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
# [전체적인 구성]
- 문제 번호 **33.** 이 왼쪽 상단에 있고, 그 오른쪽에 문제 지시문이 한 줄로 시작된다.
- 지시문 아래쪽, 화면 중앙 오른쪽에 **큰 정사각형 격자**가 배치되어 있다.
- 격자 안에는 **원 4개가 겹쳐 그려진 도형**이 있으며, 각 원의 중심은 같은 수평선 위에 놓여 있다.
- 격자 아래 왼쪽에는 **(정답)** 표시와 함께 선택 결과가 텍스트로 적혀 있다.
- 그 아래에는 **(해설)** 문장이 한 줄로 제시되어 있다.
- 전체적으로 “문제 지시문 → 도형 제시 → 정답 표시 → 해설”의 순서로 구성되어 있다.

# [보이는 텍스트]
- `33.`
- `주어진 모양을 그리기 위하여 컴퍼스의 침을 꽂아야 할 곳을 모두 선택해 보세요.`
- `(정답) ㄱ, ㄴ, ㄷ, ㄹ`
- `(해설) 4개의 원의 중심이 모두 다르므로 컴퍼스의 침을 4군데에 꽂아야 합니다.`

# [반복되는 시각 패턴]
- 격자는 **하늘색 점선**으로 이루어진 동일 간격의 정사각형 칸들이 반복된다.
- 원은 모두 **검은색 얇은 선**으로 그려져 있고, 서로 중심이 다른 상태로 겹쳐 있다.
- 원들의 중심처럼 보이는 위치에는 **분홍색 점 4개**가 한 직선 위에 놓여 있다.
- 분홍색 점들 위쪽에는 **ㄱ, ㄴ, ㄷ, ㄹ** 표지가 각각 대응되도록 배치되어 있다.
- 각 원은 왼쪽의 한 점을 지나며, 중심이 오른쪽으로 갈수록 이동하는 형태로 배열되어 보인다.

# [수식 단계]
- 해당 없음.  
- 이미지에는 등호, 식 변형, 계산 과정이 보이지 않는다.

# [등호별 변환 의미]
- 해당 없음.  
- 수식의 등호 단계가 제시되지 않았다.

# [세부 내용]
- 격자는 거의 정사각형에 가까운 큰 영역이며, 화면 중앙 오른쪽에 위치한다.
- 격자 내부 왼쪽 가장자리 부근에서 여러 원호가 시작되어 오른쪽으로 크게 확장된다.
- 원 4개는 모두 서로 다른 크기로 보이며, 작은 원부터 큰 원까지 순차적으로 겹친다.
- 4개의 분홍색 점은 동일한 수평선 위에 있으며, 왼쪽에서 오른쪽으로 일정한 간격으로 놓여 있다.
- 각 분홍색 점의 위에 작은 회색/검은색 문자로 **ㄱ, ㄴ, ㄷ, ㄹ** 이 하나씩 표시되어 있다.
- 가장 왼쪽의 작은 원은 첫 번째 점 근처를 중심으로 한 듯 보이고, 이후 원들이 점점 더 큰 반지름으로 바깥쪽을 감싼다.
- 전체 도형은 “어느 점에 컴퍼스 침을 꽂는가”를 판단하게 하는 시각 자료로 보인다.
- 해설 문장은 정답의 이유를 설명하지만, 실제로 어떤 점이 왜 중심인지 계산식은 없다.

# [Python DSL 작성 힌트]
- 큰 도형 영역을 **격자 정사각형**으로 먼저 배치하는 구조가 필요하다.
- 격자는 동일 간격의 점선 수직선/수평선 반복으로 표현하면 된다.
- 그 위에 **원 4개**를 겹쳐 배치하고, 모두 같은 높이의 중심을 갖는 형태로 그린다.
- 중심 후보처럼 보이는 **분홍색 점 4개**를 수평선 위에 일정 간격으로 배치한다.
- 각 점 위에 **ㄱ, ㄴ, ㄷ, ㄹ** 텍스트를 작은 크기로 올려놓는다.
- 문제 문장은 상단 좌측에 줄바꿈을 포함해 배치한다.
- 하단에는 `(정답)`과 `(해설)` 텍스트를 별도 블록으로 배치한다.
- 도형과 텍스트 사이의 세로 간격을 충분히 두어 문제지 레이아웃을 재현하는 것이 좋다.

# [주의할 점]
- 정답 텍스트가 이미 보이지만, 분석 초안에서는 이를 근거로 문제를 풀거나 추가 해석을 확장하지 말아야 한다.
- 분홍색 점이 실제로 “컴퍼스 침을 꽂는 위치”를 뜻하는지, 혹은 단순 표시점인지는 이미지의 맥락상 추정이지만, 서술에서는 **보이는 표시**와 **의미 추정**을 구분해야 한다.
- 원의 정확한 중심 좌표, 반지름 값, 격자 칸 수는 이미지 해상도상 정확히 확정하기 어렵다.
- ㄱ, ㄴ, ㄷ, ㄹ 표시는 점 위에 작게 놓여 있어 일부 글자가 원선과 겹쳐 보일 수 있다.
- 격자 바깥 여백과 도형의 상대적 위치는 대략적이며, 정확한 픽셀 단위 수치는 불확실하다.

# [불확실한 부분]
- 격자의 정확한 가로세로 칸 수는 화면에서 완전히 또렷하게 세기 어렵다.
- 네 개의 원 각각의 정확한 중심 위치와 반지름은 이미지에서 근사적으로만 읽힌다.
- ㄱ, ㄴ, ㄷ, ㄹ 표지가 각각 어떤 점과 정확히 대응되는지는 시각적으로는 보이지만, OCR 수준에서 일부 글자 판독이 흔들릴 수 있다.
- 해설의 마지막 문장 중 “4군데”의 정확한 띄어쓰기나 문장부호는 이미지 품질에 따라 다소 달라질 수 있다.

--- RAW VISION DRAFT END ---
