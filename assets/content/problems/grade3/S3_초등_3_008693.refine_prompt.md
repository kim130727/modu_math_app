# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_008693.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_008693

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
# [전체적인 구성]
- 한 문제짜리 초등 수학/도형 감각 관련 문항 화면이다.
- 상단에는 문제 번호와 문제 지문이 왼쪽 정렬로 배치되어 있다.
- 중앙에는 연한 노란색 직사각형 영역 안에 작은 점 5개와 번호 1~5가 가로로 배열된 그림이 있다.
- 그림 위에는 왼쪽 쪽에 압정(또는 핀) 모양, 오른쪽 쪽에 연필 모양이 표시되어 있다.
- 하단 왼쪽에는 `(정답)` 표시와 함께 동그라미 친 숫자 2가 보인다.
- 그 아래에는 `(해설)` 문장이 한 줄로 제시되어 있다.

# [보이는 텍스트]
- `□ 82.`
- `그림과 같이 띠 종이를 누름 못으로 고정한 후 연필을 구멍에 넣어 원을`
- `그리려고 합니다. 원의 중심이 되는 곳을 선택하세요.`
- 그림 안의 번호: `① ② ③ ④ ⑤`
- 하단: `(정답) ②`
- 하단 해설: `(해설) 누름 못이 꽂혔던 점이 원의 중심이 되므로 ②입니다.`

# [반복되는 시각 패턴]
- 가로로 긴 직사각형 띠 영역 안에 점 5개가 거의 일정 간격으로 배치되어 있다.
- 각 점 아래에 원형 번호 표시가 있고, 번호가 1부터 5까지 순서대로 반복된다.
- 점 위/근처에 작은 표시가 하나씩 있으며, 그중 하나는 압정 위치, 하나는 연필 위치로 강조된다.
- 연필은 세로 방향으로 길게 그려져 있고, 압정은 짧은 금속 물체처럼 표현되어 있다.
- 전체적으로 선택형 문항의 시각 패턴: 그림 + 번호 선택 + 정답/해설 영역이 반복적으로 구성된다.

# [수식 단계]
- 해당 없음.  
- 수식 변형이나 등호가 연속된 식은 보이지 않는다.

# [등호별 변환 의미]
- 해당 없음.  
- 등호가 포함된 단계적 계산식이 없다.

# [세부 내용]
- 문제 문장은 “그림과 같이”라는 표현으로 시작해, 아래 그림을 참고하도록 안내한다.
- 띠 종이를 누름 못으로 고정한 뒤 연필을 구멍에 넣어 원을 그리는 상황을 묘사한다.
- 질문은 “원의 중심이 되는 곳”을 고르라는 형태의 선택 문제이다.
- 중앙 그림:
  - 연한 노란색 배경의 긴 직사각형 상자가 있고,
  - 그 안에 작은 점 5개가 왼쪽에서 오른쪽으로 일정하게 놓여 있다.
  - 각 점 아래에는 원형 테두리 안에 `1, 2, 3, 4, 5`가 적혀 있다.
  - 두 번째 위치 근처에는 회색 압정/못 그림이 위에서 아래로 꽂힌 듯 배치되어 있다.
  - 다섯 번째 위치 근처에는 붉은색 연필 그림이 위에서 아래로 세워져 있다.
- 하단의 `(정답) ②`는 화면상 정답 표시로 보이지만, 이는 문제 풀이 결과를 그대로 옮긴 시각 요소로만 기록한다.
- 해설 문장은 “누름 못이 꽂혔던 점이 원의 중심이 된다”는 설명을 담고 있으나, 작성 시에는 이것을 풀이 내용으로 확장해 쓰지 않는 것이 좋다.

# [Python DSL 작성 힌트]
- 문제 텍스트 영역과 그림 영역, 정답/해설 영역을 분리해서 레이아웃을 구성하는 방식이 적합하다.
- 그림은 다음 요소로 나누어 표현하면 좋다:
  - 배경 직사각형 띠
  - 띠 위의 5개 점
  - 각 점 아래 원형 번호 라벨 1~5
  - 특정 위치의 압정 아이콘
  - 특정 위치의 연필 아이콘
- 번호와 점의 상대 위치가 중요하므로, 좌우 간격을 일정하게 배치하는 구조가 필요하다.
- 원형 번호는 모두 같은 스타일로 반복 생성하는 것이 자연스럽다.
- 정답 표시와 해설 문장은 별도 블록으로 두는 것이 좋다.
- 필요하다면 그림 요소를 좌표 기반으로 배치하되, 각 번호의 수평 정렬을 유지하도록 한다.

# [주의할 점]
- 문제를 풀어 정답 의미를 새로 추론하지 말고, 화면에 보이는 정답 표시는 별도로만 기록한다.
- “원의 중심”이라는 개념을 설명 문장으로 확장하더라도, 실제 답을 서술형으로 대신 쓰지 말아야 한다.
- 압정과 연필의 정확한 위치는 그림 해석에 중요하므로, 임의로 위치를 바꾸지 않는다.
- 띠 종이의 색감은 연한 노란색 계열로 보이지만, 채도나 명도는 구현 환경에 따라 달라질 수 있다.
- 점과 번호의 크기는 작고 단순하므로 과도한 디테일은 생략해도 된다.
- 상단 왼쪽의 네모 기호 `□`는 문제 번호 앞의 표시처럼 보이지만, 정확한 용도는 단정하지 않는다.

# [불확실한 부분]
- 압정이 정확히 “누름 못”인지, 또는 단순한 핀/못 아이콘인지 화면만으로는 세부 명칭을 단정하기 어렵다.
- 그림 속 각 작은 점이 실제 구멍인지, 표시용 점인지 화면만으로는 확정하기 어렵다.
- 문제 번호 앞의 `□` 기호의 정확한 의미는 명확하지 않다.
- 정답 `②`는 화면에 보이지만, DSL 작성 시 이를 정답 데이터로 넣을지 여부는 별도 판단이 필요하다.

--- RAW VISION DRAFT END ---
