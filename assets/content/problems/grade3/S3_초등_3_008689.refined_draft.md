[문제 개요]
- 초등 수학 도형 문제로 보이며, 원 그림을 보고 **원의 반지름과 지름을 나타내는 선분을 각각 찾아 기호를 고르는** 형식이다.
- 화면 상단 왼쪽에 문항 번호와 지시문이 있고, 오른쪽 중앙에 원 그림이 배치되어 있다.
- 원 그림 아래에는 선택 문장이 길게 한 줄로 제시되고, 좌하단에는 `(정답)`, `(해설)` 표기가 함께 보인다.
- **보이는 사실:** 이 화면에는 계산식 전개나 등호 연쇄는 없다.
- **추론되는 수학적 의미:** 각 기호가 붙은 선분이 반지름인지 지름인지 분류하는 문제로 보인다.

[원본에 보이는 요소]
- 상단 문장:
  - `□ 78. 원의 반지름과 지름을 나타내는 선분을 각각 찾아 기호를 선택해 보세요.`
- 원 그림 안의 기호:
  - `ㄱ`, `ㄴ`, `ㄷ`
- 원 내부 중심 부근:
  - 작은 점과 `O`처럼 보이는 중심 표기가 함께 보인다.
- 그림 아래 선택 문장:
  - `원의 반지름 ( ㄱ , ㄴ , ㄷ ), 원의 지름 ( ㄱ , ㄴ , ㄷ )`
- 좌하단 표기:
  - `(정답) ㄷ, ㄴ`
  - `(해설) 원의 반지름 ㄷ, 원의 지름 ㄴ`
- **보이는 사실:** 위의 정답/해설 문구는 화면에 실제로 인쇄되어 보인다.
- **추론되는 수학적 의미:** 정답과 해설이 별도 표시로 제시된 구성처럼 보인다.

[수식/관계 구조]
- 본 문항에는 수식, 계산식, 등호(`=`)로 이어지는 관계식이 보이지 않는다.
- 구조는 다음과 같은 **분류형 관계**로 읽힌다.
  - `원의 반지름` ↔ `( ㄱ , ㄴ , ㄷ )` 중 선택
  - `원의 지름` ↔ `( ㄱ , ㄴ , ㄷ )` 중 선택
- **보이는 사실:** 원 안에 여러 선분이 겹쳐 그려져 있고, 각 선분에 기호가 붙어 있다.
- **추론되는 수학적 의미:** 중심을 기준으로 한 길이 관계(반지름/지름)를 시각적으로 판별하는 문제다.

[등호별 변환 의미]
- 해당 없음.
- 등호 연쇄나 식 변형이 보이지 않으므로, 단계별 변환 의미를 쓸 내용이 없다.

[빈칸 정책]
- 본 화면에는 전형적인 숫자 빈칸은 보이지 않지만, 선택지 괄호 `( ㄱ , ㄴ , ㄷ )`가 **선택 대상 자리**처럼 기능한다.
- **빈칸 정책:** 추론값으로 채우지 말고, 원본에 보이는 괄호와 선택지 구조를 그대로 유지한다.
- 원문에 보이는 `(정답) ㄷ, ㄴ`, `(해설) 원의 반지름 ㄷ, 원의 지름 ㄴ`은 **관찰 사실로만 기록**하고, 이를 근거로 빈칸을 재작성하지 않는다.
- 입력 칸처럼 보이는 별도 박스는 없으므로, DSL에서는 선택 문장과 해설 표기를 분리된 텍스트 요소로 다루는 편이 좋다.

[반복 패턴]
- 원 안의 도형 후보가 여러 개 반복적으로 배치되어 있다.
- 기호 `ㄱ`, `ㄴ`, `ㄷ`가 각각 선분 후보 옆에 붙어 있어, **기호-도형 매칭 패턴**이 반복된다.
- 선택 문장에서는 같은 형식이 반복된다.
  - `원의 반지름 ( ... )`
  - `원의 지름 ( ... )`
- 좌하단의 `(정답)`, `(해설)`도 동일한 안내 형식으로 반복 배치되어 있다.
- 원 내부에는 파란색 계열의 점선 강조가 여러 개 보이며, 서로 다른 선분 후보를 구분하는 패턴처럼 보인다.

[DSL 작성 힌트]
- 전체 레이아웃은 다음처럼 분할하면 자연스럽다.
  - 상단 좌측: 문항 번호 + 지시문
  - 중앙 우측: 원 도형
  - 원 아래: 선택 문장
  - 좌하단: `(정답)`, `(해설)` 텍스트
- 원 도형 구성 요소:
  - 바깥 원 1개
  - 내부 선분 후보 3개 정도
  - 중심점 1개
  - 각 선분 옆 기호 라벨 `ㄱ`, `ㄴ`, `ㄷ`
- **렌더링/레이아웃 힌트**
  - 원은 검은 외곽선으로, 내부 후보 선분은 연한 파란색 점선처럼 보이게 표현하는 것이 원본에 가깝다.
  - 한 선분은 원 내부를 거의 세로로 내려오고, 한 선분은 비스듬히 지나가며, 한 선분은 중심을 지나가는 긴 선처럼 보인다.
  - 중심 부근의 작은 점과 문자 `O`는 서로 가까이 배치되어 있으므로 겹침 가능성을 고려해 구성한다.
  - 선택 문장은 한 줄에 길게 놓이고, 괄호와 쉼표 간격을 원본처럼 유지하는 것이 좋다.
- `(정답)`, `(해설)`은 하단 좌측에 작은 텍스트 박스로 따로 두는 배치가 적절하다.
- **보이는 사실:** 원문에는 출력 예시처럼 보이는 정답/해설 텍스트가 함께 있다.
- **추론되는 수학적 의미:** 학습자용 풀이 공간이 아니라 해설 포함형 화면일 가능성이 있다.

[semantic/solvable 힌트]
- 문제는 **원의 반지름과 지름의 정의를 시각적으로 판별**하는 유형으로 보인다.
- 원의 중심을 지나는지, 원 둘레까지 연결되는지 같은 시각 정보가 핵심이다.
- **보이는 사실:** 하단에 `(정답) ㄷ, ㄴ`, `(해설) 원의 반지름 ㄷ, 원의 지름 ㄴ`이 인쇄되어 있다.
- **추론되는 수학적 의미:** 어떤 기호가 반지름인지, 어떤 기호가 지름인지 구분하는 도형 개념 학습 문제다.
- 다만, 이 문서는 문제를 푸는 용도가 아니라 원본을 DSL로 옮기기 위한 초안이므로, 정답 판단을 별도로 전개하지 않는다.

[주의 및 TODO]
- 정답을 추론해서 빈칸을 채우지 말 것.
- 원 내부 중심 표기가 정확히 `O`인지, 점과 문자가 겹친 것인지 해상도상 다소 불확실하므로 TODO로 남긴다.
- 각 선분의 정확한 끝점이 원 둘레와 일치하는지, 혹은 약간 안쪽에서 끝나는지 해상도상 단정하지 말 것.
- 상단 문장의 줄바꿈은 화면에서 `보세` / `요.`처럼 끊겨 보이므로, 원문 줄바꿈 위치는 TODO로 남긴다.
- `(정답) ㄷ, ㄴ` 및 `(해설) 원의 반지름 ㄷ, 원의 지름 ㄴ`은 화면에 보이는 텍스트이지만, 이것이 본문 일부인지 별도 해설인지 맥락은 완전히 확정하지 말고 TODO로 둔다.
- **보이는 사실**과 **추론되는 수학적 의미**를 계속 분리해서 기록해야 한다.
