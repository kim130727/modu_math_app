# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_008711.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_008711

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
[전체적인 구성]
- 한 페이지형 초등 수학 문제 이미지이다.
- 상단 왼쪽에 문제 번호와 본문 지시문이 있고, 중앙에는 연필심을 표현한 그림이 배치되어 있다.
- 그림 아래 오른쪽에는 보기처럼 보이는 괄호 묶음이 있으며, 왼쪽 아래에는 `(정답)` 표기와 해설 문장이 이어진다.
- 전체적으로 **문장 → 그림 → 선택지/정답 표기 → 해설** 순서로 구성되어 있다.

[보이는 텍스트]
- `□ 9.` 형태의 문제 번호가 보인다.  
  - 맨 앞의 작은 사각형은 체크박스처럼 보인다.
- 본문 문장:
  - `띠 종이를 누름 모양으로 고정한 다음 연필심을 구멍에 끼려고`
  - `합니다. 원을 가장 작게 그릴 때 연필심을 넣어야 하는 구멍의 기호를 선택`
  - `하세요.`
- 그림 아래 오른쪽의 기호 묶음:
  - `( ㅁ, ㄴ, ㄷ, ㄹ, ㅁ )`처럼 보이는 원형 문자 표기가 있으나, 실제 인쇄된 글자는 그림 내 기호와 대응되는 항목으로 보인다.
- 왼쪽 아래:
  - `(정답) ㄱ`
- 해설 문장:
  - `(해설) 원을 가장 작게 그리려면 연필심을 누름 못에서 가장 가까운 쪽의 구멍에 넣어야`
  - `합니다.`
  - `따라서 연필심을 ㄱ에 넣어야 합니다.`
- 주의: 해설의 문장은 문제의 의미를 설명하는 문장으로 보이며, 답 자체를 이 초안에서 추론해 적는 것은 아니다.

[반복되는 시각 패턴]
- 그림 아래에는 여러 개의 구멍 기호가 **가로 방향으로 반복**된다.
- 각 구멍 아래에는 **원 안의 한글 기호**가 하나씩 들어가 있고, 위쪽의 구멍과 아래쪽 기호 사이에 **세로 화살표**가 연결되어 있다.
- 구멍들은 거의 동일한 크기와 간격으로 배열되어 있어, 선택지 비교를 위한 **등간격 반복 패턴**으로 보인다.
- 왼쪽 첫 번째와 두 번째 구멍은 위쪽 회색 직사각형 내부에 있고, 나머지 구멍들도 같은 높이 선상에 정렬되어 있다.
- 회색 직사각형 상단 쪽에는 타원형의 연필심/누름점처럼 보이는 표시가 하나 있다.

[수식 단계]
- 해당 없음. 수식 변형이나 등식 단계는 보이지 않는다.

[등호별 변환 의미]
- 해당 없음.

[세부 내용]
- 중심 그림은 **가로로 긴 회색 직사각형** 안에 구성되어 있다.
- 직사각형 내부에는 작은 원형 구멍이 여러 개 있으며, 왼쪽에서 오른쪽으로 일정한 간격으로 배치되어 있다.
- 맨 왼쪽 쪽에는 회색 타원형 표시가 위에 있고, 그 아래에 작은 원이 있어 **연필심 위치 또는 누름점**을 나타내는 것으로 보인다.
- 아래쪽에는 각 구멍에 대응하는 원형 문자 라벨이 있고, 각 라벨에서 위쪽 구멍으로 향하는 화살표가 있다.
- 화살표는 모두 위를 향하는 단일 화살표이며, 선택해야 할 구멍의 위치를 가리키는 구조이다.
- 오른쪽 아래의 괄호 묶음은 여러 선택지를 한 줄로 제시한 형태로 보이며, 각 항목이 원 안의 글자로 표시되어 있다.
- 하단 해설은 문제의 의도를 설명하는 문단 형태이며, 본문과는 분리되어 있다.

[Python DSL 작성 힌트]
- 전체 화면을 상단 텍스트 영역, 중앙 그림 영역, 하단 해설 영역으로 나누어 배치하면 좋다.
- 중앙 그림은 다음 요소로 분해해 표현할 수 있다:
  - 회색 긴 직사각형 1개
  - 직사각형 내부의 작은 원형 구멍 여러 개
  - 왼쪽 상단 근처의 타원형 표시 1개
  - 각 구멍 아래의 원형 라벨 5개
  - 라벨에서 구멍으로 향하는 위쪽 화살표 여러 개
- 선택지처럼 보이는 괄호 묶음은 그림 오른쪽 아래에 별도 텍스트 객체로 배치한다.
- `(정답)`과 `(해설)`은 하단 좌측에 세로로 이어지게 배치한다.
- 문제 본문은 줄바꿈이 포함된 문장 블록으로 넣고, 그림과 충분히 떨어뜨려 배치한다.
- 라벨 문자나 선택지 문자는 이미지에서 정확히 판독되는 경우에만 텍스트로 넣고, 불확실하면 자리표시만 해두는 편이 안전하다.

[주의할 점]
- 이 문제는 **도형 위치 선택형**이므로 계산식으로 처리하면 안 된다.
- 빈칸형 수식은 없고, 답을 채워 넣는 칸도 보이지 않는다.
- 그림 안의 문자 기호는 해상도상 혼동될 수 있으므로, OCR 결과를 그대로 단정하지 말아야 한다.
- 해설에 이미 답이 적혀 있어도, 초안에서는 그 답을 새로 추론해 적지 않는다.
- 회색 직사각형 내부의 점/구멍 개수는 확대 전후에 따라 헷갈릴 수 있으니, DSL 작성 시 위치 기준으로 먼저 잡는 것이 좋다.

[불확실한 부분]
- 그림 아래 원형 라벨 안의 한글 기호가 정확히 무엇인지 일부 अस्प릿하거나 흐릴 수 있다.
- 오른쪽 아래의 괄호 묶음 안 문자 배열이 정확히 어떤 기호들인지 해상도상 확정이 어렵다.
- 왼쪽 상단의 작은 사각형은 체크박스인지 단순 인쇄 기호인지 단정하기 어렵다.
- 회색 타원형 표시가 연필심인지, 누름점의 일부인지 시각적으로는 유사하게 보여 완전 확정은 어렵다.
- `(정답)`과 `(해설)`에 적힌 내용은 인쇄 상태가 선명하지만, 문제 본문 속 기호와의 대응은 별도 확인이 필요하다.

--- RAW VISION DRAFT END ---
