# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_008701.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_008701

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
[전체적인 구성]

- 화면 왼쪽 상단에 문제 번호와 지시문이 있고, 오른쪽에는 원과 여러 선분이 그려진 도형이 배치되어 있습니다.
- 도형 아래쪽 왼편에는 `(정답)` 표시와 함께 기호 3개가 나열되어 있습니다.
- 맨 아래에는 `(해설)` 문장이 한 줄로 제시되어 있습니다.
- 전체적으로는 **문장형 지시 + 도형 선택** 구조이며, 빈칸 입력형 수식은 보이지 않습니다.

[보이는 텍스트]

- `□ 94. 원의 지름을 모두 찾아 기호를 선택해 보세요.`
- 원 주변의 기호:
  - `ㄴ`
  - `ㄷ`
  - `ㄹ`
  - `ㅁ`
  - `ㄱ`
- `(정답) ㄴ, ㄷ, ㅁ`
- `(해설) 원의 중심을 지나도록 원 위의 두 점을 이은 선분을 찾습니다.`

[반복되는 시각 패턴]

- 원 중심에서 여러 방향으로 뻗은 선분들이 여러 개 반복적으로 보입니다.
- 선분 색이 서로 다르게 표시되어 있어 각 선분을 구분하도록 만든 형태입니다.
- 원 둘레 바깥에 작은 원형 표시로 기호가 붙어 있으며, 각 기호는 특정 선분 끝점 근처에 위치합니다.
- 선분들은 모두 원의 중심 부근에서 만나는 형태로 배치되어 있어, 여러 반지름/지름 후보를 동시에 비교하는 시각 패턴입니다.

[수식 단계]

- 해당 없음.  
  - 등호로 연결된 식 변형이나 계산 과정은 보이지 않습니다.

[등호별 변환 의미]

- 해당 없음.  
  - 식의 변형 단계가 아니라 도형 판별 문제입니다.

[세부 내용]

- 오른쪽 도형은 **원 하나**가 크게 그려져 있고, 내부에 중심점이 보입니다.
- 중심점에서 바깥으로 뻗는 선분이 여러 개 있습니다.
- 보이는 선분 색상과 방향은 다음과 같이 구분됩니다.
  - 위아래로 거의 수직인 **주황색** 선분
  - 왼쪽 위에서 중심을 거쳐 오른쪽 아래로 이어지는 **초록색** 선분
  - 왼쪽 위에서 중심으로 향하는 **빨간색** 선분
  - 중심에서 오른쪽 위로 향하는 **파란색** 선분
  - 중심을 지나 좌우로 수평에 가깝게 뻗은 **보라색/분홍색** 선분
- 각 선분의 끝부분 또는 근처에 작은 원형 라벨이 붙어 있습니다.
  - 위쪽에 `ㄴ`
  - 오른쪽 위에 `ㄷ`
  - 오른쪽 중간에 `ㄹ`
  - 오른쪽 아래에 `ㅁ`
  - 왼쪽 위에 `ㄱ`
- `(정답)` 옆에는 `ㄴ, ㄷ, ㅁ`이 원형 테두리처럼 표시된 기호와 함께 제시되어 있습니다.  
  - 다만 이것은 정답 제시 요소로 보이며, 사용자가 선택해야 할 대상 자체를 미리 추론해 쓰는 것은 주의가 필요합니다.
- 하단 해설 문장은 지름의 정의를 설명하는 문장입니다.
  - 보이는 의미상: “원 위의 두 점을 이은 선분” 중에서 “원의 중심을 지나도록” 하는 것을 찾으라는 안내입니다.

[Python DSL 작성 힌트]

- 도형 영역은 오른쪽 상단에 배치하고, 원과 중심점을 먼저 그린 뒤 여러 색의 선분을 중심에서 방사형으로 추가하는 구조로 작성하면 됩니다.
- 기호 라벨 `ㄱ, ㄴ, ㄷ, ㄹ, ㅁ`은 선분 끝점 근처에 작은 원형 또는 말풍선형 표식으로 배치하면 시각적으로 유사합니다.
- 문제 문장은 왼쪽 상단에 한 줄로 배치하고, `(정답)`과 `(해설)`은 아래쪽에 별도 텍스트 블록으로 두는 구성이 적절합니다.
- 중심을 통과하는 선분과 통과하지 않는 선분이 함께 있으므로, 선분의 위치 관계를 분명하게 드러내는 좌표 배치가 중요합니다.
- 색상 구분이 핵심이므로 선분마다 서로 다른 색을 유지해야 합니다.

[주의할 점]

- 정답을 추론하여 설명문에 덧붙이지 마세요.
- 기호가 붙은 위치를 실제 선분의 이름처럼 단정하지 마세요.
- 이 문제는 계산식이 아니라 도형 판별 문제이므로, 수식 변형처럼 해석하지 말아야 합니다.
- 원의 중심점이 매우 작게 표시되어 있어도, 중심을 지나가는지 여부를 판단하는 기준점으로 중요하게 다뤄야 합니다.
- `(정답)` 영역에 보이는 기호는 화면에 이미 제시된 정보이므로, 별도의 추론 결과처럼 쓰지 않도록 주의하세요.

[불확실한 부분]

- 오른쪽 아래의 기호 `ㅁ`이 어떤 색 선분과 정확히 대응되는지는 이미지 해상도상 다소 헷갈릴 수 있습니다.
- 선분 색상은 화면의 인쇄/표시 상태에 따라 약간 다르게 보일 수 있습니다.
- 작은 원형 라벨의 경계가 선분과 겹쳐 보여, 일부 위치는 정확한 접점처럼 보이지 않을 수 있습니다.

--- RAW VISION DRAFT END ---
