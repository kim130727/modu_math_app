# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_008590.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_008590

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
[전체적인 구성]
- 전체 화면은 초등 수학 문제 1문항으로 보이며, 상단에 문제 번호와 지시문이 있고, 가운데에는 나눗셈 식의 빈칸형 수식이 배치되어 있습니다.
- 수식 아래에는 말풍선을 가진 인물 3명이 가로로 나란히 배치되어 있고, 각 인물 아래에는 이름표가 있습니다.
- 왼쪽 아래에는 `(정답)` 표시와 함께 이름이 따로 제시되어 있고, 그 아래에는 `(해설)` 문장들이 여러 줄로 적혀 있습니다.
- 화면은 크게 “문제 제시 영역 → 선택지/설명 인물 영역 → 정답 및 해설 영역”의 흐름으로 구성되어 있습니다.

[보이는 텍스트]
- 상단 문제 문구: `57. 문제를 바르게 설명한 사람을 선택하세요.`
- 수식 영역: `68 ÷ 5 = □ … □`
  - 가운데에 빈 사각형 2개가 있고, 두 빈칸 사이에 점 3개처럼 보이는 기호가 있습니다.
- 말풍선 속 문구:
  - 왼쪽 인물: `몫은 한 자리 수야.`
  - 가운데 인물: `나머지는 5보다 크네.`
  - 오른쪽 인물: `나누어떨어지지 않아.`
- 인물 이름표:
  - 왼쪽: `형우`
  - 가운데: `희영`
  - 오른쪽: `성태`
- 하단 텍스트:
  - `(정답)성태`
  - `(해설)68 ÷ 5 = 13···3`
  - `형우 : 몫은 13으로 두 자리 수입니다.`
  - `희영 : 나머지는 3으로 5보다 작습니다.`
  - `성태 : 나누어떨어지지 않으므로 나누어떨어지지 않습니다.`
  - `따라서 문제를 바르게 설명한 사람은 성태입니다.`
- 위 하단 해설 문장들은 이미지에 그대로 보이는 텍스트로, 문장 일부는 줄바꿈에 따라 나뉘어 있습니다.

[반복되는 시각 패턴]
- 인물 3명이 거의 같은 크기와 형식으로 가로 정렬되어 있습니다.
- 각 인물 위에는 타원형 말풍선이 있고, 말풍선 꼬리가 아래쪽 인물 쪽으로 연결됩니다.
- 각 인물 아래에는 연한 초록색 테두리의 이름표 박스가 붙어 있습니다.
- 세 명의 배치는 좌→중→우 순서로 균등하게 떨어져 있어 선택지 형태로 읽히게 되어 있습니다.
- 상단 수식의 빈칸 2개는 같은 크기의 정사각형 박스로 보이며, 같은 줄에 좌우로 배치되어 있습니다.

[수식 단계]
- 화면에 보이는 수식은 `68 ÷ 5 = □ … □` 형태입니다.
- 하단 해설에는 `68 ÷ 5 = 13···3`이 제시되어 있습니다.
- 이때 등호 왼쪽의 나눗셈식에서, 등호 오른쪽에는 몫과 나머지를 나타내는 형식으로 보이는 값이 이어져 있습니다.
- 다만 상단 문제의 빈칸에는 실제 숫자가 채워져 있지 않으므로, 빈칸이 무엇을 뜻하는지의 최종 값은 여기서 적지 않습니다.

[등호별 변환 의미]
- 첫 번째 등호: `68 ÷ 5 = □ … □`
  - 나눗셈 결과를 두 칸의 빈칸으로 쓰는 형식입니다.
  - 가운데의 점 3개는 나눗셈에서 몫과 나머지를 구분하는 표기처럼 보입니다.
- 두 번째 등호: `68 ÷ 5 = 13···3`
  - 해설 부분에서 등호 오른쪽에 몫과 나머지가 함께 적힌 예시가 제시되어 있습니다.
  - 여기서 무엇이 몫이고 무엇이 나머지인지는 이미지의 해설 문장을 통해 설명되고 있지만, 실제 계산 과정 자체는 단계적으로 전개되어 있지 않습니다.

[세부 내용]
- 문제는 “세 사람 중 누가 68 ÷ 5를 올바르게 설명했는지”를 고르는 유형입니다.
- 세 인물의 발화 내용은 서로 다른 판단 기준을 말하고 있습니다.
  - 왼쪽 인물은 몫의 자릿수에 대해 말합니다.
  - 가운데 인물은 나머지의 크기 비교를 말합니다.
  - 오른쪽 인물은 나누어떨어지는지 여부를 말합니다.
- 인물들의 그림은 단순한 캐릭터 아이콘이고, 말풍선과 이름표로 선택지 역할을 합니다.
- 하단 해설은 각 인물의 말이 왜 맞거나 틀린지 문장으로 설명하는 구조입니다.
- `(정답)` 표시는 문제 풀이 결과를 나타내는 영역으로 보이지만, DSL 작성 시에는 정답값을 자동으로 채우지 않는 방식이 필요합니다.

[Python DSL 작성 힌트]
- 상단 제목 문구를 먼저 배치하고, 그 아래에 `68 ÷ 5 = □ … □` 형태의 수식 박스를 중앙 정렬로 배치하면 좋습니다.
- 수식의 두 빈칸은 정사각형 입력칸 2개로 표현하고, 그 사이에는 점 3개 또는 생략/나머지 표기용 기호를 별도 요소로 두는 것이 좋습니다.
- 선택지는 3열 구조로 배치합니다.
  - 각 열: 말풍선 → 인물 그림 → 이름표 순서
  - 말풍선은 위쪽, 인물은 아래쪽, 이름표는 인물 바로 아래에 정렬
- 각 말풍선은 타원형 테두리와 아래쪽 꼬리를 갖는 형태로 표현합니다.
- 이름표는 연한 초록색 테두리의 둥근 직사각형 박스로 표현하면 화면과 유사합니다.
- 하단 해설 영역은 왼쪽 정렬의 텍스트 블록으로 구성하고, `(정답)`, `(해설)`을 별도 줄로 두면 구조를 살리기 좋습니다.
- 전체적으로 여백이 넓은 편이므로 요소 간 간격을 크게 두는 레이아웃이 적합합니다.

[주의할 점]
- 빈칸 안의 값을 추론해서 넣지 마세요.
- 하단 해설에 보이는 정답 문구가 있더라도, DSL 작성 단계에서는 문제 영역의 빈칸 채우기와 분리해서 다뤄야 합니다.
- `13···3`의 점 표기는 이미지에서 보이는 그대로 처리해야 하며, 임의로 다른 기호로 바꾸지 않는 것이 좋습니다.
- 말풍선 문장은 실제로는 선택지의 주장 문구이므로, 해설 문장과 혼동하지 않도록 구분해야 합니다.
- 인물 이름과 말풍선 위치를 바꾸지 않도록 주의해야 합니다.

[불확실한 부분]
- 상단 수식의 가운데 기호는 점 3개처럼 보이지만, 실제 인쇄 의도는 나눗셈의 몫·나머지 표기일 수 있어 정확한 문자 해석은 불확실합니다.
- `(해설)`의 마지막 문장 일부는 화면상 문맥상 자연스럽게 읽히지만, 이미지 해상도 때문에 문장 끝 표현이 약간 흐릿하게 보일 수 있습니다.
- `(정답)성태`에서 괄호와 이름 사이에 공백이 있는지 여부는 화면상 매우 좁아 정확히 단정하기 어렵습니다.

--- RAW VISION DRAFT END ---
