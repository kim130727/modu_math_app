[문제 개요]
- 문제 번호는 `6.`으로 보인다.
- 지시문은 `나머지가 다른 하나를 찾아 기호를 선택해 보세요.`로 읽힌다.
- 중앙에 파란색 테두리의 큰 직사각형 박스가 있고, 그 안에 4개의 나눗셈 식이 2행 2열로 배치되어 있다.
- 박스 아래에는 보기 기호를 다시 나열한 괄호 묶음이 한 줄로 있다.
- 하단에는 `(정답)` 표시와 `(해설)` 줄, 그리고 문장형 결론이 보인다.
- 원본 이미지 기준으로 정답 표시는 `③`이 아니라 원형 기호로 된 `ㄷ`처럼 보이는 마커로 표시되어 있다.

[원본에 보이는 요소]
- 상단 왼쪽: `6.`
- 상단 문장: `나머지가 다른 하나를 찾아 기호를 선택해 보세요.`
- 중앙 박스 내부 보기:
  - `① 29 ÷ 7`
  - `② 31 ÷ 6`
  - `③ 74 ÷ 8`
  - `④ 64 ÷ 9`
- 박스 아래 괄호 묶음:
  - `( ① , ② , ③ , ④ )`
- 정답 표시:
  - `(정답) ㄷ`로 보이는 원형 기호
- 해설 표시:
  - `(해설) ① 29 ÷ 7 = 4··1   ② 31 ÷ 6 = 5··1   ③ 74 ÷ 8 = 9··2   ④ 64 ÷ 9 = 7··1`
- 하단 문장:
  - `따라서 나머지가 다른 하나는 ㄷ입니다.`
- 원본 이미지에서는 보기 기호와 정답 기호가 모두 원 안에 들어간 형태처럼 보인다.

[수식/관계 구조]
- 보기 4개는 모두 같은 형식의 나눗셈식이다.
- 각 식은 `숫자 ÷ 숫자` 형태로 주어져 있다.
- 해설 줄에서는 각 나눗셈이 `= 몫··나머지` 형태로 다시 적혀 있다.
- 여기서 `··`는 나머지 표기처럼 보이지만, 정확한 글리프는 점 두 개 형태로 보인다.
- 별도의 연쇄 등호는 없고, 각 항목이 독립적으로 한 번씩만 변환되어 있다.

[등호별 변환 의미]
- `29 ÷ 7 = 4··1`
  - 보이는 의미: 29를 7로 나눈 결과를 몫과 나머지로 적은 형태.
- `31 ÷ 6 = 5··1`
  - 보이는 의미: 31을 6으로 나눈 결과를 몫과 나머지로 적은 형태.
- `74 ÷ 8 = 9··2`
  - 보이는 의미: 74를 8로 나눈 결과를 몫과 나머지로 적은 형태.
- `64 ÷ 9 = 7··1`
  - 보이는 의미: 64를 9로 나눈 결과를 몫과 나머지로 적은 형태.
- 각 식은 원래의 나눗셈식을 `= 몫··나머지`로 바꿔 적은 한 단계 설명이다.
- 변형 순서를 따로 이어서 보여 주는 등호 연쇄는 없다.

[빈칸 정책]
- 이 문제에는 채워야 할 빈칸이 직접적으로 보이지 않는다.
- 정답을 추론해서 빈칸을 메우는 방식은 사용하지 않는다.
- 원본에 인쇄된 정답 표시나 해설은 관찰 사실로만 옮기고, 빈칸이 있다면 그대로 비워 둔다.
- 보기에 해당하는 기호는 원본에 보이는 그대로 유지한다.

[반복 패턴]
- 4개의 보기가 모두 같은 문법 구조를 따른다: `번호 + 나눗셈식`.
- 박스 안 배치는 2행 2열의 대칭 구조로 보인다.
- 해설 줄에서는 보기 번호가 다시 반복되며, 각 번호 뒤에 대응하는 나눗셈 결과가 붙는다.
- 번호, 수식, 등호, 몫, 나머지의 배열이 일정한 간격으로 이어져 있어 좌→우 대응이 분명하다.
- 박스 아래의 `( ① , ② , ③ , ④ )`는 보기 번호의 반복 나열로 보인다.

[DSL 작성 힌트]
- 상단 지시문은 한 줄 텍스트로 배치하는 것이 자연스럽다.
- 중앙에는 파란색 테두리의 큰 직사각형 박스를 먼저 두고, 그 내부에 2행 2열 배치를 구성한다.
- 각 보기 앞의 원형 번호 마커는 일반 숫자와 다른 기호로 처리해야 한다.
- 박스 아래의 번호 목록은 괄호와 쉼표를 포함한 한 줄 텍스트로 배치한다.
- 하단의 `(정답)`과 `(해설)`은 별도 라벨로 분리하는 것이 좋다.
- 해설 줄은 가로로 길게 이어지므로, 항목 사이 간격을 일정하게 두는 것이 중요하다.
- 원본에서 `4··1`처럼 보이는 표기는 그대로 보존하되, 실제 DSL에서 나머지 표기 규칙이 필요하면 별도 처리해야 한다.
- 박스 내부는 글자 크기가 비교적 크고 중앙 정렬 느낌이 강하므로, 각 항목을 충분히 간격 두고 배치하는 것이 적절하다.

[semantic/solvable 힌트]
- 문제의 요구는 “나머지가 다른 하나”를 고르는 선택형이다.
- 해설은 각 보기의 나눗셈을 몫과 나머지로 적어 비교하도록 구성되어 있다.
- 원본 이미지에서 `(정답)` 옆 기호와 마지막 문장의 기호는 같은 대상을 가리키는 것으로 보인다.
- 다만 이 문서는 풀이를 새로 계산하거나 정답을 확정하는 용도가 아니라, 화면에 보이는 구조를 정리하는 용도이다.

[주의 및 TODO]
- 원본 이미지에서는 정답 표시가 `③`처럼 보이지 않고 원형 기호 `ㄷ`처럼 보이므로, 초안의 표기와 충돌 시 이미지를 우선해야 한다.
- 해설의 `··`가 정확히 어떤 나머지 표기인지, 텍스트로는 불명확하므로 TODO로 남긴다.
- `( ① , ② , ③ , ④ )`의 정확한 글자 간격, 괄호 폭, 폰트는 이미지 기준으로만 대략 반영한다.
- 정답을 추론해 다른 기호로 바꾸지 않는다.
- `나누기 n은 분모의 ×n으로 반영된다.`는 이 문제의 화면에는 해당하지 않는 변환이므로 적용하지 않는다.
