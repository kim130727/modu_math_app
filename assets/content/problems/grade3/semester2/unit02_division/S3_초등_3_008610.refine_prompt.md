# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_008610.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_008610

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
# [전체적인 구성]
- 화면 상단 왼쪽에 문제 번호 **“80.”** 이 있고, 그 오른쪽에 지시문 **“바르게 계산한 것을 찾아 기호를 선택해 보세요.”**가 한 줄로 배치되어 있습니다.
- 본문 중앙에는 **연한 초록색 테두리의 큰 직사각형 박스**가 있으며, 그 안에 보기 2개가 **좌우로 나란히** 놓여 있습니다.
- 각 보기는 **동그라미 기호 + 식** 형태로 되어 있습니다.
  - 왼쪽 보기: **① 54 ÷ 3 = 17**
  - 오른쪽 보기: **ㄴ 96 ÷ 8 = 12**
- 박스 아래쪽 왼편에는 정답 표시처럼 보이는 **“(정답) ㄴ”**가 있습니다.
- 그 아래에는 해설이 작은 글씨로 이어지며, **“(해설) ① 54 ÷ 3 = 18”** 이 보이고, 마지막 줄에 **“따라서 바르게 계산한 것은 ㄴ입니다.”**라고 적혀 있습니다.

# [보이는 텍스트]
- `80.`
- `바르게 계산한 것을 찾아 기호를 선택해 보세요.`
- `① 54 ÷ 3 = 17`
- `ㄴ 96 ÷ 8 = 12`
- `(정답) ㄴ`
- `(해설) ① 54 ÷ 3 = 18`
- `따라서 바르게 계산한 것은 ㄴ입니다.`

# [반복되는 시각 패턴]
- 보기들이 **같은 형식**으로 반복됩니다.
  - 기호(원 안의 숫자/문자) + 나눗셈 식 + 등호 + 결과
- 두 보기는 모두 **가로 방향으로 한 줄**에 배치되어 비교하게 되어 있습니다.
- 답안과 해설 부분도 아래쪽에 **텍스트 줄 단위로 순차 배치**되어 있습니다.
- 문제 본문에서 **연산 기호 “÷”와 등호 “=”**가 반복적으로 사용됩니다.

# [수식 단계] (해당될 때)
- 해설에는 한 개의 수식이 별도로 제시되어 있습니다.
  - `54 ÷ 3 = 18`
- 이 수식은 보기 ①에 적힌 식 `54 ÷ 3 = 17`과 대응되는 형태로 보이며, **보기의 계산 결과를 바로잡아 제시한 식**처럼 배치되어 있습니다.
- 수식 단계가 여러 번 이어지는 변형식은 보이지 않습니다.

# [등호별 변환 의미] (해당될 때)
- 보기 ①의 `54 ÷ 3 = 17`은 **등호 오른쪽 결과가 제시된 값**입니다.
- 해설의 `54 ÷ 3 = 18`은 같은 좌변에 대해 **다른 결과를 보여 주는 수정식**으로 보입니다.
- 즉, 등호 오른쪽 숫자가 **오답/정답 판단의 근거**로 쓰이고 있습니다.
- 다만 실제로 어떤 계산이 맞는지는 여기서 풀지 않고, **화면에 제시된 해설 구조만** 확인합니다.

# [세부 내용]
- 상단 문제 번호 왼쪽에는 작은 **빈 사각형 체크박스처럼 보이는 네모**가 있습니다.
- 본문 박스는 **연녹색 선으로 둘러싸인 큰 직사각형**이며, 내부는 흰색 배경입니다.
- 왼쪽 보기와 오른쪽 보기는 박스 내부에서 **좌우 균형**을 이루도록 배치되어 있습니다.
- 각 보기의 기호는:
  - 왼쪽: **동그라미 안의 “1”처럼 보이는 기호**
  - 오른쪽: **“ㄴ” 모양 기호**
- 해설의 `(정답)`과 `(해설)`은 괄호가 붙은 라벨 형태이며, 본문보다 아래쪽에 작게 표시됩니다.
- 마지막 문장은 평서문으로, **“따라서”**로 시작해 정답 기호를 다시 알려 주는 구조입니다.

# [Python DSL 작성 힌트]
- 큰 외곽 박스를 먼저 배치하고, 그 안에 **좌우 2열 보기 구조**를 넣는 방식이 적합합니다.
- 각 보기는 다음과 같은 요소로 분리하면 좋습니다.
  - 기호
  - 나눗셈 식
  - 등호
  - 결과
- 해설 영역은 본문 박스 아래에 **세로로 이어지는 텍스트 블록**으로 배치하면 자연스럽습니다.
- 텍스트 크기 차이를 반영해:
  - 상단 지시문은 비교적 크게
  - 보기 식은 본문 크기
  - 정답/해설은 더 작은 글씨
  로 표현하면 화면 구조를 잘 재현할 수 있습니다.
- 체크박스, 문제 번호, 지시문, 보기 박스, 해설 문장을 **서로 독립된 객체**로 나누어 작성하는 것이 좋습니다.

# [주의할 점]
- **정답 자체를 추론해서 새로 계산하지 마세요.**
- 해설에 이미 보이는 식이 있더라도, 그 값을 별도로 다시 판단해 채워 넣지 마세요.
- 왼쪽 보기의 기호는 일반 숫자 1이 아니라 **동그라미 기호 안의 표식**으로 보이므로 단순 숫자로 바꾸지 않는 것이 좋습니다.
- 오른쪽 보기의 기호는 **ㄴ**처럼 보이며, 문자 형태를 그대로 유지해야 합니다.
- 본문 박스의 테두리는 **초록색 계열**이며, 너무 진한 색으로 단정하지 않는 것이 좋습니다.
- 해설의 “정답” 표시는 화면에 실제로 적혀 있는 문자만 반영하고, 추가 강조 표시가 있다고 단정하지 마세요.

# [불확실한 부분]
- 왼쪽 보기의 기호가 정확히 **동그라미 숫자 1인지, 다른 표기인지**는 이미지 해상도상 다소 애매합니다.
- 상단 왼쪽의 작은 네모는 **체크박스인지 단순 인쇄 요소인지** 확정하기 어렵습니다.
- 본문 박스의 테두리 색은 연녹색으로 보이지만, 실제 인쇄 색상은 이미지 품질에 따라 다르게 보일 수 있습니다.
- 해설 문장의 줄바꿈 위치는 화면상 보이는 대로 추정했으며, 실제 원본 편집에서는 다를 수 있습니다.

--- RAW VISION DRAFT END ---
