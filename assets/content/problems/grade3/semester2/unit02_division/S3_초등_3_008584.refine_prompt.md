# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_008584.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_008584

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
# 시각 분석 초안

[전체적인 구성]
- 화면 상단 왼쪽에 문제 번호와 지시문이 있고, 그 아래에 선택지처럼 보이는 3개의 그림이 가로로 배치되어 있습니다.
- 하단 왼쪽에는 `(정답)` 표시가 붙은 별도의 그림이 하나 더 있으며, 그 아래에는 `(해설)` 문장과 계산식이 한 줄로 제시되어 있습니다.
- 전체적으로 “선택 → 정답 표시 → 풀이 설명”의 흐름으로 구성된 초등 수학 문제 화면입니다.
- 상단의 3개 그림은 비슷한 모양의 틀 안에 나눗셈 식이 들어간 형태입니다.

[보이는 텍스트]
- `□ 51. 묶이 다른 것을 선택하세요.`
- 첫 번째 그림: `72 ÷ 3`
- 두 번째 그림: `84 ÷ 2`
- 세 번째 그림: `96 ÷ 4`
- 왼쪽 아래 정답 표시 옆 그림: `84 ÷ 2`
- `(정답)`
- `(해설)72 ÷ 3 = 24, 84 ÷ 2 = 42, 96 ÷ 4 = 24`
- `따라서 묶이 다른 것은 84 ÷ 2입니다.`

[반복되는 시각 패턴]
- 상단의 3개 선택지는 모두 같은 형태의 연한 청록색 트럭/상자 모양 틀로 되어 있습니다.
- 각 틀 안에는 가운데 정렬된 나눗셈 식이 1개씩 들어 있습니다.
- 식의 배치는 모두 비슷하며, 숫자와 `÷` 기호가 한 줄로 표시됩니다.
- 각 선택지 아래쪽에는 작은 원형 바퀴처럼 보이는 청록색 요소가 붙어 있어 동일한 패턴을 반복합니다.
- 하단의 정답 그림도 같은 모양을 사용하지만, `(정답)` 텍스트가 왼쪽에 따로 붙어 있어 선택지와 구분됩니다.

[수식 단계]
- 화면에는 직접적인 등호 변형식이 길게 이어지지는 않지만, 해설 부분에 각 나눗셈 식의 결과가 `=`를 사용해 정리되어 있습니다.
- 표시된 식은 다음처럼 각각 결과와 연결되어 있습니다.
  - `72 ÷ 3 = 24`
  - `84 ÷ 2 = 42`
  - `96 ÷ 4 = 24`

[등호별 변환 의미]
- 해설에서 각 `=`는 앞의 나눗셈식과 그 계산 결과를 연결하는 역할을 합니다.
- 첫 번째 식에서는 `72 ÷ 3`이 어떤 값과 같다고 적혀 있습니다.
- 두 번째 식에서는 `84 ÷ 2`가 어떤 값과 같다고 적혀 있습니다.
- 세 번째 식에서는 `96 ÷ 4`가 어떤 값과 같다고 적혀 있습니다.
- 다만 여기서는 실제 값이 무엇인지 다시 계산하거나 검증하지 않고, 화면에 적힌 등호 관계만 관찰합니다.

[세부 내용]
- 문제 번호 `51`은 왼쪽 상단에 있고, 번호 앞에 작은 빈 사각형 기호처럼 보이는 요소가 있습니다.
- 지시문 `묶이 다른 것을 선택하세요.`는 상단 왼쪽에서 시작하여 한 줄로 표시됩니다.
- 선택지 3개는 위쪽 공간에 넓게 분산 배치되어 있으며, 좌우 간격이 꽤 있습니다.
- 첫 번째 선택지는 왼쪽 상단 쪽, 두 번째는 상단 중앙 쪽, 세 번째는 상단 오른쪽 쪽에 위치합니다.
- 하단의 `(정답)` 표시는 왼쪽 아래에 세로로 내려간 위치에 있으며, 그 오른쪽에 정답 그림이 붙어 있습니다.
- 해설 문장은 정답 그림 아래쪽에 이어서 표시됩니다.
- 마지막 문장 `따라서 묶이 다른 것은 84 ÷ 2입니다.`는 해설 계산식 아래에 한 줄로 배치되어 있습니다.

[Python DSL 작성 힌트]
- 상단 지시문과 문제 번호를 먼저 배치하는 구조로 작성하면 좋습니다.
- 선택지 3개는 동일한 도형 컴포넌트를 반복 사용하고, 각 도형 내부의 수식 텍스트만 바꾸는 방식이 적합합니다.
- 각 선택지는 같은 크기와 같은 스타일로 맞추고, 수평 방향으로 일정 간격을 두어 배치합니다.
- 하단의 `(정답)` 영역은 별도 라벨 텍스트와 그림 1개를 묶어서 배치합니다.
- 해설은 `(해설)` 라벨 뒤에 계산식 문자열을 한 줄로 넣고, 그 아래에 결론 문장을 넣는 형태가 자연스럽습니다.
- 트럭/상자 모양의 외곽선, 내부 채움색, 바퀴처럼 보이는 하단 원형 요소를 개별 도형으로 나누어 표현할 수 있습니다.

[주의할 점]
- 답을 추론해서 빈칸을 채우지 마세요. 화면에 이미 보이는 텍스트만 기록해야 합니다.
- 문제의 핵심 판단이 무엇인지는 분석해도 되지만, 실제 정답을 다시 계산하여 확정하는 서술은 피하는 것이 안전합니다.
- `묶이`라는 표현은 원문 그대로 적되, 오타인지 의미인지 임의로 바꾸지 마세요.
- 해설의 계산 결과가 화면에 적힌 그대로인지와 별개로, 시각 분석 초안에서는 보이는 문자열만 우선적으로 서술하세요.
- 정답 표시와 선택지는 시각적으로 같은 도형이지만, 위치와 라벨로 구분된다는 점을 명시해야 합니다.

[불확실한 부분]
- 왼쪽 상단의 작은 빈 사각형 기호가 장식인지 체크칸인지 화면만으로는 단정하기 어렵습니다.
- 선택지 도형이 정확히 트럭 모양인지, 또는 단순한 받침이 있는 상자 모양인지는 스타일상 유사해 보이지만 단정하지 않는 것이 좋습니다.
- `(정답)` 영역의 그림이 실제로는 선택지 중 하나를 복제한 것인지, 별도 강조 표시인지 화면만으로는 확정하기 어렵습니다.
- 해설의 수식 결과가 읽기 쉬운 위치에 있으나, 실제 의도와 표기 방식 사이에 혼동 가능성이 있어 보이므로 문자 그대로 옮기는 것이 안전합니다.

--- RAW VISION DRAFT END ---
