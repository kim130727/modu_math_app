[문제 개요]
- 문항 번호는 `71.`로 보인다.
- 지시문은 `나머지가 가장 작은 것을 찾아 기호를 선택하세요.`로 읽힌다.
- 보기 3개가 상단의 연한 분홍색 테두리 박스 안에 가로로 배치되어 있다.
- 박스 아래에는 `(정답)` 표시와 `(해설)`이 순서대로 배치되어 있다.
- 해설 아래 마지막 줄에는 결론 문장이 있다.

[원본에 보이는 요소]
- 상단 텍스트: `71. 나머지가 가장 작은 것을 찾아 기호를 선택하세요.`
- 보기 박스 내부의 3개 보기:
  - `① 77 ÷ 6`
  - `ㄴ 264 ÷ 5`
  - `ㄷ 307 ÷ 3`
- 박스 아래:
  - `(정답) ㄷ`
  - `(해설)`
  - `① 77 ÷ 6 = 12···5`
  - `ㄴ 264 ÷ 5 = 52···4`
  - `ㄷ 307 ÷ 3 = 102···1`
  - `따라서 나머지가 가장 작은 것은 ㄷ입니다.`
- 원본 이미지에서도 정답 기호는 `(정답)` 옆에 따로 표시되어 보인다.
- 해설 줄의 나머지 표기는 점 2개/점 3개처럼 보이는 기호로 인쇄되어 있다.

[수식/관계 구조]
- 보기들은 모두 같은 형식의 나눗셈식이다.
  - `기호 + 자연수 ÷ 자연수`
- 해설은 각 보기마다 대응하는 결과를 같은 줄에 적는 구조이다.
  - `식 = 몫···나머지`
- 등호는 계산 과정의 중간 단계라기보다, 왼쪽의 나눗셈식과 오른쪽의 몫·나머지 결과를 연결하는 역할로 보인다.
- 세 식의 나머지를 비교해 가장 작은 것을 찾는 문제 구조이다.
- 원본에 이미 정답과 해설이 인쇄되어 있어, 풀이 결과는 관찰 사실로만 기록한다.

[등호별 변환 의미]
- `77 ÷ 6 = 12···5`
  - 왼쪽: 77을 6으로 나누는 식.
  - 오른쪽: 몫 12와 나머지 5를 함께 적은 형태로 보인다.
- `264 ÷ 5 = 52···4`
  - 왼쪽: 264를 5로 나누는 식.
  - 오른쪽: 몫 52와 나머지 4를 함께 적은 형태로 보인다.
- `307 ÷ 3 = 102···1`
  - 왼쪽: 307을 3으로 나누는 식.
  - 오른쪽: 몫 102와 나머지 1을 함께 적은 형태로 보인다.
- 이 문제의 등호 연쇄는 변형 과정이 아니라, 각 나눗셈의 결과를 바로 적은 대응 관계이다.
- 따라서 각 줄은 “식 → 결과”의 의미로 읽으면 된다.

[빈칸 정책]
- 이 문항에는 빈칸 입력란이 보이지 않는다.
- 정답은 원본에 `(정답) ㄷ`으로 인쇄되어 보인다.
- DSL 작성 시 빈칸을 새로 만들지 않는다.
- 설령 문제 유형이 선택형이더라도, 관찰되지 않은 답칸이나 추론값을 임의로 추가하지 않는다.
- 해설의 몫·나머지 값도 원본에 보이는 범위만 기록하고, 보이지 않는 값은 추론하지 않는다.

[반복 패턴]
- 보기 3개가 동일한 구조로 반복된다.
  - `기호 + 수식`
- 해설도 동일한 구조로 반복된다.
  - `기호 + 수식 + = + 몫/나머지`
- 마지막 결론 문장도 정형화된 문장 패턴을 따른다.
  - `따라서 나머지가 가장 작은 것은 ...입니다.`
- 선택지 기호는 서로 다른 문자 체계처럼 보이지만, 각 해설 줄과 정확히 대응한다.
- 반복 배치 덕분에 DSL에서는 각 보기를 같은 스타일의 컴포넌트로 묶는 것이 적절하다.

[DSL 작성 힌트]
- 상단 문장은 한 줄 텍스트로 배치한다.
- 그 아래 연한 분홍색 테두리의 큰 직사각형 박스를 두고, 내부에 보기 3개를 가로 정렬한다.
- 각 보기는 다음 요소를 한 묶음으로 처리하면 좋다.
  - 기호 텍스트
  - 나눗셈식 텍스트
- 보기 간 간격은 균등하게 두고, 각 항목이 같은 기준선에 맞도록 정렬한다.
- 박스 아래에는 `(정답)` 라벨과 정답 기호를 붙여 한 줄로 배치한다.
- `(해설)` 아래에는 세 줄의 풀이를 세로로 배치한다.
  - 각 줄은 기호, 수식, 등호, 몫·나머지 결과를 한 줄에 유지한다.
- 마지막 결론 문장은 아래쪽에 왼쪽 정렬로 둔다.
- 수식 기호 `÷`, 등호 `=`, 나머지 표기 `···`처럼 보이는 점 표시는 원본 표현을 우선 반영한다.
- 선택지와 해설의 기호가 서로 일치하도록 묶어 배치한다.
- 분홍색 테두리 상자는 내용보다 약간 여유 있는 padding을 두는 것이 자연스럽다.

[semantic/solvable 힌트]
- 이 문제는 나눗셈 결과의 나머지를 비교하는 선택형 문항이다.
- 원본 해설에는 각 식의 몫과 나머지가 이미 제시되어 있다.
- 따라서 DSL 관점에서는 “세 보기의 결과를 비교하는 구조”가 핵심이다.
- 다만 답을 새로 계산해 채워 넣지는 말고, 원본에 보이는 결과만 시각적으로 옮긴다.
- 정답 기호는 원본의 `(정답)` 표기와 결론 문장에서 동일하게 보인다.

[주의 및 TODO]
- 보기 기호가 `①, ㄴ, ㄷ`처럼 보이지만, OCR 혼동 가능성이 있으므로 실제 렌더링에서는 이미지 기준으로 최종 확인이 필요하다.
- 해설의 나머지 기호 `···`는 점 2개/3개 또는 다른 생략 표기처럼 보일 수 있어, 해상도에 따라 재확인이 필요하다.
- 상단 박스의 정확한 여백, 테두리 두께, 각 보기의 정확한 간격은 이미지 기준 미세 조정이 필요하다.
- 정답을 추론해서 바꾸지 말고, 원본에 인쇄된 `ㄷ` 표기를 유지한다.
- 계산 과정 추가, 보조 설명 추가, 빈칸 생성은 하지 않는다.
