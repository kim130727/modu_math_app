# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_008604.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_008604

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
[전체적인 구성]
- 화면 상단 왼쪽에 문제 번호와 지시문이 있고, 그 아래 중앙 상단에 주어진 식 `67 ÷ 5`가 주황색 테두리의 직사각형 상자 안에 배치되어 있습니다.
- 중앙에는 두 명의 인물 그림이 좌우로 나란히 배치되어 있으며, 왼쪽은 남자아이, 오른쪽은 여자아이입니다.
- 각 인물의 머리 위에는 말풍선이 하나씩 있고, 말풍선 안에 각 사람의 설명 문장이 들어 있습니다.
- 인물 그림 아래에는 각각 이름이 적혀 있고, 이름은 사람의 얼굴과 대응되게 배치되어 있습니다.
- 중간 하단에는 선택지처럼 보이는 괄호형 표기 `( 현태 , 은수 )`가 있습니다.
- 왼쪽 아래에는 정답 표기와 해설 문장이 별도로 인쇄되어 있습니다.

[보이는 텍스트]
- `□ 74. 문제를 바르게 설명한 사람의 이름을 선택하세요.`
- `67 ÷ 5`
- 왼쪽 말풍선: `몫은 13이야.`
- 오른쪽 말풍선: `나머지는 0으로 나누어떨어져.`
- 왼쪽 인물 이름: `현태`
- 오른쪽 인물 이름: `은수`
- 가운데 아래: `( 현태 , 은수 )`
- 하단 왼쪽: `(정답)현태`
- 하단 해설:
  - `(해설)67 ÷ 5 = 13···2이므로 몫은 13이고 나머지는 2입니다.`
  - `따라서 문제를 바르게 설명한 사람은 현태입니다.`

[반복되는 시각 패턴]
- 인물 2명 구조가 좌우 대칭에 가깝게 반복됩니다.
- 각 인물마다 `말풍선 + 얼굴 그림 + 이름`의 동일한 구성 패턴이 반복됩니다.
- 말풍선은 위쪽에, 인물은 아래쪽에 배치되어 말하는 대상과 발화 내용이 연결되도록 구성되어 있습니다.
- 선택지 표시는 이름 2개를 괄호 안에 쉼표로 나열한 형태로 반복적이고 단순한 목록 구조입니다.

[수식 단계]
- 본문 문제 화면에는 수식 변형 단계가 직접 제시되어 있지 않습니다.
- 다만 하단 해설에는 `67 ÷ 5 = 13···2` 형태의 등식이 보입니다.
- 이 등식은 원래 나눗셈 식에서 몫과 나머지를 함께 나타낸 표기로 바뀐 형태입니다.

[등호별 변환 의미]
- `67 ÷ 5 = 13···2`
  - 보이는 의미: `67 ÷ 5`의 결과를 몫과 나머지 형태로 적어 놓은 것입니다.
  - 시각적으로는 일반 계산식에서 결과 표기로 이어진 단일 등식입니다.
- 해설 문장에서는 이 식을 바탕으로
  - 몫을 설명하는 문장,
  - 나머지를 설명하는 문장
  이 이어집니다.
- 단, 이 문서에서는 결과값을 다시 계산해 쓰지 않고, 해설에 적힌 표현만 관찰 사실로 정리합니다.

[세부 내용]
- 문제는 “문제를 바르게 설명한 사람의 이름”을 고르는 선택형입니다.
- 주어진 식 `67 ÷ 5`를 보고, 두 사람의 설명 중 올바른 사람을 찾는 유형으로 보입니다.
- 왼쪽 인물 `현태`의 말풍선에는 `몫은 13이야.`라고 적혀 있습니다.
- 오른쪽 인물 `은수`의 말풍선에는 `나머지는 0으로 나누어떨어져.`라고 적혀 있습니다.
- 선택지처럼 보이는 `( 현태 , 은수 )`는 두 이름 중 하나를 고르도록 유도하는 형식입니다.
- 하단 해설 텍스트는 정답을 직접 제시하고, 왜 그런지 문장으로 설명합니다.
- 화면 구성상 본문 문제와 해설이 한 장에 함께 인쇄된 형태로 보입니다.

[Python DSL 작성 힌트]
- 상단 문제 문구를 좌상단에 두고, 그 아래 중앙 상단에 주황색 테두리의 식 상자를 배치하면 좋습니다.
- 식 상자는 가운데 정렬된 직사각형 프레임으로 만들고, 내부에 `67 ÷ 5`를 크게 넣습니다.
- 좌우 인물은 동일한 크기의 캐릭터 그림으로 배치하고, 각 캐릭터 위에 타원형 말풍선을 연결합니다.
- 말풍선은 흰 배경, 회색 또는 검은색 윤곽선으로 처리하며, 꼬리 방향은 각 인물의 입 쪽을 향하게 합니다.
- 인물 아래에는 이름 텍스트를 각각 중앙 정렬로 배치합니다.
- 선택지 `( 현태 , 은수 )`는 인물들 아래 중앙에 작은 글씨로 넣습니다.
- 하단에는 정답 및 해설 영역을 별도 텍스트 블록으로 배치합니다.
- 해설은 본문 문제와 시각적으로 구분되도록 왼쪽 아래에 작은 글씨로 배치하면 원본과 유사합니다.

[주의할 점]
- 말풍선 안의 문장은 실제 인쇄된 문구 그대로 옮겨야 하며, 의미를 바꿔 쓰지 않아야 합니다.
- 정답을 문제 작성 단계에서 미리 채워 넣지 않도록 주의해야 합니다.
- `몫`, `나머지`, `나누어떨어져` 같은 용어는 텍스트 그대로 유지해야 합니다.
- 해설에 포함된 식 표기 `13···2`는 점의 개수와 배치를 임의로 바꾸지 않는 것이 좋습니다.
- 인물 이름과 말풍선 위치가 서로 대응되므로 좌우 반전 없이 배치해야 합니다.

[불확실한 부분]
- 왼쪽 상단의 작은 네모 표시는 인쇄 상태상 체크박스처럼 보이지만, 실제 편집 의도는 문제 번호 앞의 기호인지 불명확합니다.
- 하단의 `13···2` 표기는 점이 세 개로 보이지만, 인쇄 해상도 때문에 구분이 다소 불명확합니다.
- 말풍선 테두리의 색상은 회색 계열로 보이나, 정확한 색상값은 화면만으로 단정하기 어렵습니다.
- 선택지 `( 현태 , 은수 )`의 정확한 용도는 보기 목록인지 단순 안내인지 화면만으로는 완전히 확정하기 어렵습니다.

--- RAW VISION DRAFT END ---
