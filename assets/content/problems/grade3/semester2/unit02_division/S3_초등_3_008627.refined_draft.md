[문제 개요]
- 문제 ID는 `97.`로 보이며, 좌상단에 작은 빈 네모(□)가 함께 있습니다.
- 문제 문장은 `나머지가 다른 하나는 어느 것일까요?`입니다.
- 객관식 보기 5개가 있으며, 나눗셈식 형태로 제시됩니다.
- 보기 아래에 `(정답) ④`가 인쇄되어 보입니다.
- 그 아래 `(해설)`과 함께 각 보기의 나눗셈 결과가 나열되고, 마지막에 결론 문장이 있습니다.

[원본에 보이는 요소]
- 좌상단: 작은 빈 네모(□), 그 옆에 `97.`
- 문제 문장: `나머지가 다른 하나는 어느 것일까요?`
- 보기:
  - `① 37 ÷ 7`
  - `② 22 ÷ 5`
  - `③ 26 ÷ 4`
  - `④ 58 ÷ 9`
  - `⑤ 66 ÷ 8`
- 정답 표기: `(정답) ④`
- 해설 표기: `(해설)`
- 해설식:
  - `① 37 ÷ 7 = 5··2`
  - `② 22 ÷ 5 = 4··2`
  - `③ 26 ÷ 4 = 6··2`
  - `④ 58 ÷ 9 = 6··4`
  - `⑤ 66 ÷ 8 = 8··2`
- 결론 문장: `따라서 나머지가 다른 하나는 ④입니다.`

[수식/관계 구조]
- 보기들은 모두 `A ÷ B` 형태의 나눗셈식입니다.
- 해설에서는 각 나눗셈식의 결과가 `몫··나머지` 형태로 보입니다.
- 등호 왼쪽은 나눗셈식, 등호 오른쪽은 몫과 나머지를 점 2개(`··`)로 구분한 표시입니다.
- 보이는 식의 구조는 다음과 같습니다.
  - `37 ÷ 7 = 5··2`
  - `22 ÷ 5 = 4··2`
  - `26 ÷ 4 = 6··2`
  - `58 ÷ 9 = 6··4`
  - `66 ÷ 8 = 8··2`

[등호별 변환 의미]
- 각 항목의 등호는 계산 과정을 길게 전개한 것이 아니라, 나눗셈 결과를 몫과 나머지 표기로 옮긴 것으로 보입니다.
- 단계별로 읽으면:
  - `37 ÷ 7 = 5··2`
    - 왼쪽: 나눗셈식
    - 오른쪽: 몫 5, 나머지 2로 보이는 결과
  - `22 ÷ 5 = 4··2`
    - 왼쪽: 나눗셈식
    - 오른쪽: 몫 4, 나머지 2로 보이는 결과
  - `26 ÷ 4 = 6··2`
    - 왼쪽: 나눗셈식
    - 오른쪽: 몫 6, 나머지 2로 보이는 결과
  - `58 ÷ 9 = 6··4`
    - 왼쪽: 나눗셈식
    - 오른쪽: 몫 6, 나머지 4로 보이는 결과
  - `66 ÷ 8 = 8··2`
    - 왼쪽: 나눗셈식
    - 오른쪽: 몫 8, 나머지 2로 보이는 결과
- 계산 방법 자체는 화면에서 보이지 않으며, 결과 형식만 관찰됩니다.

[빈칸 정책]
- 이 문제 화면에는 채워야 하는 수학적 빈칸은 보이지 않습니다.
- 좌상단의 작은 네모(□)는 빈칸처럼 보이지만, 실제 입력 칸인지 장식 요소인지는 확실하지 않습니다.
- 따라서 추론값을 넣지 말고, 보이는 빈 칸/네모는 그대로 빈 상태로 유지하는 정책이 적절합니다.
- 해설의 `··`도 빈칸이 아니라 점 표기이므로 임의 숫자로 대체하지 않습니다.

[반복 패턴]
- 보기 번호 `①~⑤`는 원형 번호로 반복됩니다.
- 모든 보기가 `숫자 ÷ 숫자` 형태로 반복됩니다.
- 해설에서는 각 항목이 `번호 + 식 + = + 몫··나머지` 형식으로 반복됩니다.
- 결과 표기에서 점 2개(`··`)가 반복적으로 사용됩니다.
- 보기 배치는 2줄 구조입니다.
  - 첫 줄: ①, ②, ③
  - 둘째 줄: ④, ⑤
- 해설도 한 줄에 여러 항목이 붙는 방식으로 배치되어 있습니다.

[DSL 작성 힌트]
- 화면 구성은 위에서 아래로 다음 순서가 자연스럽습니다.
  1. 좌상단의 작은 빈 네모와 문제 번호
  2. 문제 문장
  3. 보기 5개
  4. `(정답) ④`
  5. `(해설)` 및 각 식의 결과 나열
  6. 결론 문장
- 보기와 해설의 각 항목은 서로 같은 번호 체계를 유지해야 합니다.
- 보기는 번호와 식이 한 묶음으로 보이도록 배치하세요.
- 해설의 `··`는 원본의 시각적 표기이므로, 일반 쉼표나 괄호로 바꾸지 않는 것이 좋습니다.
- 보기의 배열은 3개 + 2개로 나뉜 줄바꿈을 반영하는 것이 중요합니다.
- 전체적으로 텍스트 중심이며, 박스나 표는 보이지 않습니다.
- 정답 표시는 별도 텍스트 블록으로 두면 원본 구조를 잘 반영할 수 있습니다.

[semantic/solvable 힌트]
- 이 문항의 핵심은 여러 나눗셈 결과 중 나머지가 다른 항목을 찾는 것입니다.
- 화면에 이미 `(정답) ④`가 인쇄되어 보입니다.
- 다만, 문제 자체를 여기서 새로 계산해 정답을 추론해 적지는 않습니다.
- 해설에 보이는 결과들은 비교용으로 제시된 것으로 보이며, 나머지 값이 서로 같은지/다른지 확인하는 구조입니다.
- `나누기 n은 분모의 ×n으로 반영된다.`는 형태의 변환은 이 화면에는 보이지 않습니다.

[주의 및 TODO]
- 정답이 화면에 보이긴 하지만, DSL 작성 시 정답을 추론하여 새로 채우지 말고 관찰 사실로만 다루세요.
- 좌상단의 작은 네모는 체크박스인지 장식인지 불확실하므로 TODO로 남겨도 좋습니다.
- `··` 표기의 정확한 의미는 인쇄 체계에 따라 다를 수 있으니, 원본 그대로 유지하는 것이 안전합니다.
- 해설의 각 결과는 보이는 그대로 기록하되, 계산 과정이나 검산을 추가하지 마세요.
- 보이는 사실과 추론되는 수학적 의미를 분리해서 서술해야 합니다.
