# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_008574.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_008574

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
[전체적인 구성]
- 화면 상단에 문제 번호와 지시문이 있고, 그 아래에 두 명의 어린이 그림이 좌우로 배치되어 있습니다.
- 각 어린이는 노란색 표지판(직사각형)을 들고 있으며, 표지판 안에 곱셈식이 크게 적혀 있습니다.
- 왼쪽 어린이 옆에는 이름표 형태의 작은 라벨이 있고, 오른쪽 어린이 옆에도 동일한 형태의 이름표가 있습니다.
- 화면 하단에는 `(정답)`과 `(해설)`이 텍스트로 표시되어 있으며, 해설에는 계산식과 비교 결과가 이어서 적혀 있습니다.
- 전체적으로 “두 계산 결과를 비교하여 더 작은 사람의 이름을 고르는” 선택형 문제처럼 구성되어 있습니다.

[보이는 텍스트]
- `40. 계산 결과가 더 작은 사람의 이름을 선택해 보세요.`
- 왼쪽 이름표: `은우`
- 왼쪽 표지판: `51 × 18`
- 오른쪽 이름표: `도윤`
- 오른쪽 표지판: `23 × 41`
- 하단: `(정답) 은우`
- 하단 해설: `(해설) 은우: 51 × 18 = 918, 도윤: 23 × 41 = 943  →  918<943`

[반복되는 시각 패턴]
- 사람 그림이 좌우로 2개 반복됩니다.
- 각 사람마다 이름표 1개와 노란 계산 표지판 1개가 함께 배치되어 있습니다.
- 표지판의 형태와 크기가 거의 동일하며, 가운데에 곱셈식만 다르게 들어 있습니다.
- 이름표도 흰색 테두리의 둥근 직사각형 형태로 반복됩니다.
- 하단 해설에서는 두 사람의 이름과 계산식이 콜론(`:`)으로 구분되어 나열되고, 마지막에 비교 기호(`<`)가 사용됩니다.

[수식 단계]
- 하단 해설에는 다음과 같은 단계가 보입니다.
  - `은우: 51 × 18 = 918`
  - `도윤: 23 × 41 = 943`
  - 이어서 `918<943`가 표시됨
- 등호가 들어간 계산 결과 표현이 2번 보이며, 각 사람의 곱셈식을 계산한 뒤 비교하는 흐름입니다.
- 마지막에는 화살표 모양 기호처럼 보이는 회색 표시 뒤에 부등호 비교식이 이어집니다.

[등호별 변환 의미]
- 첫 번째 `=`는 `51 × 18`이라는 곱셈식의 계산 결과가 오른쪽의 수로 적혔다는 뜻으로 보입니다.
- 두 번째 `=`는 `23 × 41`의 계산 결과를 오른쪽의 수로 적었다는 뜻으로 보입니다.
- 마지막 비교식 `918<943`는 두 계산 결과를 비교해 왼쪽 값이 더 작음을 나타내는 표시입니다.
- 다만, 이 초안에서는 실제 계산 과정을 풀지 않고, 보이는 결과 구조만 정리합니다.

[세부 내용]
- 문제 문장은 상단 왼쪽에 있고, 번호 `40.`이 먼저 나온 뒤 문장이 이어집니다.
- 왼쪽 인물은 머리카락이 갈색인 어린이 그림이며, 표지판은 몸 앞쪽에 가로로 들고 있습니다.
- 오른쪽 인물은 안경을 쓴 어린이 그림이며, 동일한 방식으로 표지판을 들고 있습니다.
- 왼쪽 표지판에는 `51 × 18`, 오른쪽 표지판에는 `23 × 41`이 중앙 정렬에 가깝게 배치되어 있습니다.
- 이름표 `은우`, `도윤`은 각 인물의 가까운 옆에 작은 말풍선/라벨처럼 따로 떠 있는 형태입니다.
- 하단의 `(정답)`은 짧게 한 줄로 제시되고, `(해설)`은 그 아래에서 계산식과 비교식을 함께 보여 줍니다.
- 오른쪽 하단의 회색 화살표는 해설의 계산 결과와 비교 결과를 연결하는 시각적 연결 표시로 보입니다.

[Python DSL 작성 힌트]
- 전체 레이아웃을 상단 문제문장, 가운데 좌우 2인 비교 영역, 하단 정답/해설 영역으로 나누어 배치하면 좋습니다.
- 좌우 비교 영역에서는 다음 요소를 각각 독립 객체로 두는 것이 좋습니다.
  - 사람 그림 2개
  - 이름표 2개
  - 노란 계산판 2개
- 계산판은 동일한 크기의 직사각형으로 두고, 내부 중앙에 곱셈식을 텍스트로 넣는 구조가 적합합니다.
- 이름표는 작은 둥근 사각형 박스 형태로 따로 생성하고, 사람 옆쪽에 배치합니다.
- 하단 해설은 텍스트 블록으로 처리하고, 필요하면 `은우: ...`, `도윤: ...`, `→ ...` 순서로 줄 간격을 두어 배치합니다.
- 비교식 `918<943`는 별도의 텍스트 요소로 취급하는 편이 자연스럽습니다.

[주의할 점]
- 실제 답을 다시 계산하거나 추론해서 채우지 마세요. 화면에 보이는 값만 기록해야 합니다.
- `(정답) 은우`는 화면에 이미 보이는 정보이므로 그대로 관찰 사실로만 적습니다.
- 왼쪽과 오른쪽의 인물, 이름표, 계산판의 대응 관계를 혼동하지 않도록 주의해야 합니다.
- 해설의 비교식은 결과를 설명하는 텍스트로 보이므로, 이를 새로운 문제 요소로 오해하지 않도록 합니다.
- 숫자와 기호(`×`, `=`, `<`)의 정확한 형태를 유지해 적는 것이 중요합니다.

[불확실한 부분]
- 사람 그림의 세부 표정, 옷 색, 손 위치 등은 일부 작게 보여 정확한 묘사가 어려울 수 있습니다.
- 회색 화살표 모양이 정확히 일반 화살표인지, 강조용 아이콘인지 화면 해상도상 다소 애매합니다.
- `(정답)`과 `(해설)`의 줄바꿈 위치는 화면에서 보이는 대로 읽었지만, 실제 문서 구조에서는 약간 다를 수 있습니다.
- 이름표의 테두리 두께나 그림자 효과는 보이지만, DSL에서 반드시 재현해야 하는 수준인지는 불명확합니다.

--- RAW VISION DRAFT END ---
