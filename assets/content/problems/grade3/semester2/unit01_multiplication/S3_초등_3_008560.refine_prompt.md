# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_008560.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_008560

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
[전체적인 구성]
- 상단 왼쪽에 문제 번호와 지시문이 한 줄로 배치되어 있습니다.
- 중앙 상단에는 초록색 테두리의 작은 사각형 박스가 있고, 그 안에 세로셈 형태의 곱셈식이 들어 있습니다.
- 상단 박스 아래 왼쪽에는 `(정답)` 표시와 함께 선택지 기호가 한 개 보입니다.
- 하단 왼쪽에는 `(해설)`이라는 글자와 함께 더 큰 세로셈 풀이가 제시되어 있습니다.
- 전체적으로 “문제 제시 → 작은 문제 박스 → 정답 표시 → 해설용 세로셈” 구조입니다.

[보이는 텍스트]
- `24.`
- `다음 계산에서 3 × 8 = 24의 4를 써야 할 곳을 찾아 기호를 선택하세요.`
- 작은 박스 안:
  - `3 0`
  - `× 8 0`
  - 아래에 기호가 원 안에 `ㄱ`, `ㄴ`, `ㄷ`, `ㄹ` 순서로 배치됨
- 왼쪽 중간:
  - `(정답) ㄴ`
- 아래 해설 영역:
  - `(해설)`
  - `3 0`
  - `× 8 0`
  - 가로선
  - `2 4 0 0`
  - 각 자리 아래에 위쪽 화살표 4개
  - 맨 아래 원 안 기호 `ㄱ`, `ㄴ`, `ㄷ`, `ㄹ`

[반복되는 시각 패턴]
- 작은 박스와 해설 영역 모두 같은 세로셈 구조를 사용합니다.
- 숫자 `3`, `0`, `8`, `0`이 자릿값을 맞춰 세로로 정렬되어 있습니다.
- 결과 숫자 `2400`도 자리별로 띄어져 보이며, 각 자리 아래에 화살표와 기호가 대응되도록 반복 배치되어 있습니다.
- 원 안의 기호 `ㄱ, ㄴ, ㄷ, ㄹ`은 가로로 나란히 반복 표시되어, “어느 자리인지 선택”하는 보기 역할을 합니다.

[수식 단계]
- 해설 영역에는 한 번의 곱셈 세로셈만 보입니다.
- 위에 `30`, 아래에 `80`이 놓인 형태이고, 아래 가로선 아래에 `2400`이 적혀 있습니다.
- 중간 과정(부분곱, 자리올림 표시 등)은 별도로 보이지 않고, 완성된 결과만 제시되어 있습니다.

[등호별 변환 의미]
- 화면에 여러 단계의 등호 변형은 보이지 않습니다.
- `3 × 8 = 24`라는 문장이 문제 지시문 안에 텍스트로만 등장하며, 별도의 계산식 전개나 `=` 연쇄는 없습니다.

[세부 내용]
- 작은 초록색 박스:
  - 테두리가 연두색이며 내부는 흰색입니다.
  - 세로셈의 상단 숫자 `3 0`과 하단 숫자 `8 0`이 가운데 정렬에 가깝게 배치되어 있습니다.
  - 곱셈 기호 `×`는 왼쪽 아래 줄에 위치합니다.
  - 가로선은 숫자 아래에 짧게 그어져 있습니다.
  - 가로선 아래에는 원형 테두리 안에 `ㄱ`, `ㄴ`, `ㄷ`, `ㄹ`이 좌에서 우로 배치되어 있습니다.
- 정답 표시:
  - `(정답)` 텍스트 바로 뒤에 원 안의 `ㄴ`이 표시되어 있습니다.
  - 이 표시는 해설 이전에 답의 위치를 알려주는 형식입니다.
- 해설 영역:
  - 문제와 같은 세로셈이 더 크게 다시 제시됩니다.
  - 결과 `2400`은 가로선 아래에 있고, 각 자리 숫자 아래에는 위쪽 화살표가 하나씩 대응됩니다.
  - 화살표 아래에는 다시 `ㄱ, ㄴ, ㄷ, ㄹ` 원형 기호가 가로로 배열되어 있습니다.
  - 숫자와 기호가 정확히 자릿수별 대응을 이루도록 아래쪽으로 정렬되어 있습니다.

[Python DSL 작성 힌트]
- 상단 문제 문장, 초록색 테두리 박스, 정답 표기, 해설 영역을 각각 독립된 블록으로 나누어 배치하면 좋습니다.
- 세로셈은 숫자 위치 정렬이 핵심이므로, 문자열을 단순 나열하기보다 자리별 좌표 또는 행 단위 레이아웃으로 구성하는 것이 좋습니다.
- 원형 보기 `ㄱ, ㄴ, ㄷ, ㄹ`은 동일한 크기와 간격으로 반복 배치하세요.
- 해설 영역의 `2400` 아래에는 각 자리별 화살표를 따로 그려서, 숫자와 화살표가 1:1로 대응되게 처리하는 것이 좋습니다.
- 정답 표시는 `(정답)` 뒤에 원형 기호 하나를 별도 텍스트 객체로 두는 방식이 적합합니다.

[주의할 점]
- 문제를 풀어서 “4가 어느 자리인지”를 서술하지 마세요.
- `(정답) ㄴ`이 화면에 보이지만, 이를 바탕으로 답의 의미를 추가로 해석해 적지 마세요.
- `3 × 8 = 24`의 변형이나 계산 과정을 임의로 확장하지 마세요.
- 작은 박스의 기호 위치와 해설 영역의 기호 위치를 혼동하지 않도록 주의하세요.
- 화살표와 원형 기호는 결과 숫자 `2400`의 각 자리와 대응되는 배치로 보이지만, 그 정확한 의도는 문제 풀이 없이 단정하지 않는 것이 안전합니다.

[불확실한 부분]
- 작은 박스 안의 가로선 길이와 각 원형 기호의 정확한 좌표는 이미지 해상도상 정밀하게 확정하기 어렵습니다.
- 해설 영역의 화살표가 “어느 자리에 4를 써야 하는지”를 직접 표시하는지, 또는 자리 대응을 안내하는 장치인지는 화면만으로는 완전히 단정하기 어렵습니다.
- 초록색 박스 안의 세로셈은 결과가 생략된 선택형 안내 도식으로 보이지만, 세부 의도는 텍스트만으로 확정하지 않는 것이 좋습니다.

--- RAW VISION DRAFT END ---
