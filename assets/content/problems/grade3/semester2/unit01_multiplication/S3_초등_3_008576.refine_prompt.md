# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_008576.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_008576

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
[전체적인 구성]
- 화면은 초등 수학 문제 1개가 중심인 단일 문항 구성입니다.
- 왼쪽 위에 문제 번호와 지시문이 있고, 가운데에는 세로셈 형태의 곱셈 풀이 그림이 배치되어 있습니다.
- 오른쪽에는 연한 초록색 테두리의 직사각형 박스가 있으며, 그 안에 선택지처럼 보이는 두 개의 식이 세로로 적혀 있습니다.
- 아래쪽에는 `(정답)`과 `(해설)` 문장이 텍스트로 따로 표시되어 있습니다.
- 전체적으로 “실제 어떤 수의 곱인지 찾아 선택”하는 형태의 객관식/선택형 문제처럼 보입니다.

[보이는 텍스트]
- 상단 문제 문장:
  - `42. 색칠된 부분은 실제 어떤 수의 곱인지를 찾아 선택하세요.`
- 가운데 세로셈 그림에 보이는 숫자와 기호:
  - 위쪽에 `9 5 1`
  - 그 아래 왼쪽에 `×`
  - 아래쪽에 `6`
  - 중간 계산 결과로 보이는 `6`
  - 색칠된 부분 안에 `3 0 0`
  - 그 아래에 `5 4 0 0`
  - 맨 아래 합계처럼 보이는 `5 7 0 6`
- 오른쪽 초록색 박스 안 텍스트:
  - `5 × 6`
  - `50 × 6`
- 하단 텍스트:
  - `(정답)50 × 6`
  - `(해설)951에서 5는 십의 자리 숫자이므로 50 × 6 = 300입니다.`
- 주의: 하단 해설 문장은 화면에 보이지만, 문맥상 정답 설명용으로 보입니다. 실제 선택 과정의 결과를 직접 계산해 적는 용도로는 사용하지 않는 것이 좋습니다.

[반복되는 시각 패턴]
- 세로셈 영역에는 세로로 정렬된 숫자 칸이 여러 열로 나뉘어 있습니다.
- 가운데 계산 부분에서 같은 자릿수 구조를 따라 아래로 결과가 쌓이는 형태입니다.
- 색칠된 영역은 연한 파란색 배경으로 강조되어 있으며, 숫자 `3 0 0`이 이 배경 위에 놓여 있습니다.
- 오른쪽 선택 박스는 테두리만 있는 직사각형 안에 식이 두 줄로 배치된 반복 패턴입니다.
- 숫자와 기호는 주로 세로 정렬되어 있고, 계산 결과는 아래쪽으로 누적되는 전형적인 곱셈 세로셈 형태입니다.

[수식 단계]
- 보이는 세로셈은 다음과 같은 구조로 읽힙니다.
  - 위쪽 수: `951`
  - 아래 수: `6`
  - 중간에 부분 계산 결과가 아래로 나타남
- 색칠된 부분의 `300`은 한 단계의 부분곱으로 보입니다.
- 그 아래의 `5400`은 또 다른 자리값을 반영한 부분곱처럼 보이며, 마지막 줄 `5706`은 전체 결과로 보이는 형태입니다.
- 다만 이 문항의 핵심은 계산 전체를 푸는 것이 아니라, 색칠된 부분이 어떤 곱을 나타내는지 선택하는 것이므로, 단계 해석은 시각 구조 설명에만 사용해야 합니다.

[등호별 변환 의미]
- 화면에 등호 기호는 명시적으로 보이지 않습니다.
- 대신 아래 해설 문장에 `50 × 6 = 300`이 텍스트로 제시되어 있습니다.
- 이 문장은 `951`의 `5`가 십의 자리라는 설명과 함께, 색칠된 부분이 `50 × 6`과 대응된다는 의미를 전달하는 것으로 보입니다.
- 그러나 이 해설은 결과 설명 텍스트이므로, 본 분석에서는 “보이는 텍스트” 수준으로만 다루고 실제 정답 추론 근거로 과하게 확장하지 않습니다.

[세부 내용]
- 문제 번호 `42`는 문장 앞에 붙어 있으며, 번호 주변에 작은 네모형 표시가 보입니다.
- 본문 문장은 한 줄로 길게 이어지고, 문장 끝의 마침표가 보입니다.
- 세로셈 그림은 중앙에 위치하며, 숫자들은 열 간격이 일정한 편입니다.
- `×` 기호는 왼쪽 아래 수의 시작 부분 근처에 놓여 있어 세로곱셈임을 나타냅니다.
- 색칠된 부분은 세로셈 중간의 한 줄에 해당하며, 파란색 배경으로 강조되어 있어 문제에서 찾으려는 대상임을 시각적으로 드러냅니다.
- 오른쪽 박스는 답안 후보처럼 보이며, 상하 두 줄이 같은 크기의 글씨로 배치되어 있습니다.
- 하단의 `(정답)`과 `(해설)`은 본문과 구분되는 별도 영역에 배치되어 있습니다.

[Python DSL 작성 힌트]
- 먼저 전체 캔버스에 대해 상단 문제 문장, 중앙 세로셈, 우측 선택 박스, 하단 정답/해설 영역을 분리해서 배치하는 것이 좋습니다.
- 세로셈은 숫자 열 간격을 맞춰 정렬하고, 중간 결과 줄과 밑줄을 포함한 형태로 구성하세요.
- 색칠된 부분은 별도 사각형 배경 요소로 처리하고, 그 위에 `300` 텍스트를 얹는 방식이 자연스럽습니다.
- 오른쪽 박스는 연한 초록색 테두리의 직사각형 1개와 그 안의 두 줄 텍스트로 구성하면 됩니다.
- 하단의 `(정답)`과 `(해설)`은 본문과 다른 작은 텍스트 영역으로 분리해 주세요.
- 숫자 `951`, `6`, `300`, `5400`, `5706`의 세로 정렬이 중요하므로 문자 간격과 줄 높이를 신경 써야 합니다.

[주의할 점]
- 정답을 직접 계산하거나 빈칸에 추론값을 넣지 마세요.
- 색칠된 부분을 단순한 정답 박스로 오해하지 말고, 세로셈 내부의 강조된 부분으로 표현해야 합니다.
- `300`이 보이더라도 이것을 문제의 최종 답으로 단정하지 말고, 시각적으로 강조된 부분으로만 다루세요.
- 오른쪽 박스 안의 두 식은 선택지처럼 보이지만, 실제로는 화면상 제시된 후보 목록입니다.
- 해설 문장의 수식 의미를 분석에 사용할 수는 있으나, 문제 해결 서술로 확장하지는 마세요.

[불확실한 부분]
- 가운데 세로셈의 각 숫자 배치에서 작은 점선/칸 경계가 일부 흐릿하여, 정확한 열 너비는 화면만으로 단정하기 어렵습니다.
- 색칠된 파란 영역이 정확히 어느 칸부터 어느 칸까지 덮는지는 해상도상 약간 애매합니다.
- 오른쪽 박스가 선택지인지, 예시 표시인지 문맥상 선택지로 보이지만, 화면 구성만으로 100% 단정하기는 어렵습니다.
- 하단 해설의 수식 표현은 문장형 텍스트로 보이며, 별도의 수식 박스인지 일반 문장인지 경계가 약간 흐릿합니다.

--- RAW VISION DRAFT END ---
