# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_008562.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_008562

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
[전체적인 구성]
- 화면 상단 왼쪽에 문제 번호와 지시문이 있고, 오른쪽 상단에는 보라색 테두리의 큰 직사각형 박스가 있습니다.
- 박스 안에는 두 개의 계산식이 좌우로 나란히 배치되어 있으며, 각각 동그라미 기호와 곱셈식이 함께 표시됩니다.
- 화면 왼쪽 아래에는 “(정답)” 표시와 “(해설)” 표시가 세로로 내려가며, 해설 부분 아래에 두 개의 세로 계산식이 좌우로 비교되듯 배치되어 있습니다.
- 왼쪽 계산식 아래쪽에는 회색 화살표와 함께 기호 비교 문장이 있습니다.
- 전체적으로 “두 계산 결과 중 더 큰 것을 고르는” 선택형 문제 구조이며, 시각적으로는 비교 대상 2개와 그 계산 과정이 함께 제시된 형태입니다.

[보이는 텍스트]
- 문제 번호와 문장: `27. 계산 결과가 더 큰 것을 찾아 기호를 선택하세요.`
- 오른쪽 상단 박스 안:
  - `① 4 × 93`
  - `ㄴ 7 × 54`
- 왼쪽 중간:
  - `(정답) ㄴ`
  - `(해설)`
- 해설 영역:
  - 왼쪽 계산식 위쪽에 `①`
  - 노란색 배경 숫자 `1`
  - `4`
  - `× 93`
  - 결과로 보이는 `372`
  - 아래에 `① < ㄴ`
  - 오른쪽 계산식 위쪽에 `ㄴ`
  - 노란색 배경 숫자 `2`
  - `7`
  - `× 54`
  - 결과로 보이는 `378`
- 맨 아래 왼쪽에 회색 화살표 모양이 있습니다.

[반복되는 시각 패턴]
- 두 계산식이 거의 같은 형식으로 반복됩니다.
  - 위에 기호 표시가 있고
  - 그 아래 세로 곱셈식이 있으며
  - 아래에 계산 결과가 적혀 있습니다.
- 각 계산식 위쪽에 노란색 배경의 숫자 `1`, `2`가 각각 붙어 있어 항목 구분용 번호처럼 보입니다.
- 왼쪽과 오른쪽 모두 세로 곱셈 배치가 동일하게 구성되어 있어 비교 구조가 강조됩니다.
- 박스 안의 식과 해설 아래의 식이 서로 대응되는 반복 패턴을 이룹니다.

[수식 단계]
- 왼쪽 식:
  - `4 × 93`이 세로 곱셈 형태로 정리되어 있습니다.
  - 아래에는 결과로 보이는 `372`가 적혀 있습니다.
- 오른쪽 식:
  - `7 × 54`이 세로 곱셈 형태로 정리되어 있습니다.
  - 아래에는 결과로 보이는 `378`이 적혀 있습니다.
- 해설 맨 아래에는 `① < ㄴ` 형태의 비교식이 보입니다.
- 다만, 이 비교식이 실제로 어떤 계산 규칙으로 적힌 것인지는 화면만으로 추가 설명이 생략되어 있습니다.

[등호별 변환 의미]
- 화면에는 등호 `=`가 연속으로 등장하는 변형식은 보이지 않습니다.
- 대신 각 곱셈식 아래에 결과값이 직접 제시되고, 그 값을 바탕으로 기호 비교가 이어지는 구조입니다.
- 따라서 “등호별 변환”보다는 “계산식 → 결과값 → 비교”의 흐름으로 이해하면 됩니다.

[세부 내용]
- 오른쪽 상단 박스는 보라색 외곽선으로 둘러싸인 긴 직사각형이며, 내부에 두 항목이 좌우 균등하게 배치되어 있습니다.
- 왼쪽 항목은 `① 4 × 93`처럼 동그라미 숫자 기호와 곱셈식이 한 줄에 놓여 있습니다.
- 오른쪽 항목은 `ㄴ 7 × 54`처럼 동그라미 대신 한글 자모 형태의 기호가 사용된 것으로 보입니다.
- 해설 영역의 왼쪽 계산식은 숫자 `4`가 위, `× 93`이 아래로 내려간 세로 곱셈 형태입니다.
- 오른쪽 계산식도 숫자 `7`이 위, `× 54`가 아래로 내려간 동일한 형식입니다.
- 두 계산식 각각의 중간 또는 아래에 결과값이 적혀 있으며, 왼쪽은 `372`, 오른쪽은 `378`으로 보입니다.
- 맨 아래의 `① < ㄴ`은 두 선택지의 크기 관계를 나타내는 비교식처럼 배치되어 있습니다.
- 왼쪽 아래의 회색 화살표는 정답이나 결론으로 시선을 유도하는 장치처럼 보입니다.

[Python DSL 작성 힌트]
- 상단 문제 문장과 오른쪽 상단 박스는 별도 텍스트 블록으로 배치하는 것이 좋습니다.
- 박스는 얇은 보라색 테두리의 직사각형으로 표현하고, 내부에는 두 식을 좌우 정렬하세요.
- 두 식은 같은 높이에서 시작하도록 맞추고, 각 식 앞의 기호(①, ㄴ)를 식 왼쪽에 붙여 배치하세요.
- 해설 영역에서는 좌우로 두 개의 세로 곱셈식을 나란히 배치하세요.
- 각 계산식 위에 작은 기호와 노란색 배경 숫자 라벨(`1`, `2`)을 별도로 두면 원본의 시각적 강조를 재현하기 쉽습니다.
- 결과 숫자는 곱셈식 아래쪽에 배치하되, 실제 계산 과정을 풀어 쓰기보다 화면에 보이는 결과 위치만 맞추는 것이 중요합니다.
- 맨 아래 비교식 `① < ㄴ`은 화살표와 함께 왼쪽 하단에 둡니다.
- 전체 간격은 왼쪽 해설 영역과 오른쪽 박스 영역이 서로 겹치지 않도록 충분히 분리하세요.

[주의할 점]
- 문제를 풀어서 정답을 새로 계산하지 마세요.
- 빈칸이나 선택 결과를 추론해 채우지 마세요.
- 화면에 보이는 기호가 동그라미 숫자인지, 한글 자모인지 혼동하지 않도록 그대로 기록하세요.
- 결과값 `372`, `378`은 화면에 보이는 숫자로만 다루고, 그 의미를 추가로 해석해 단정하지 마세요.
- `① < ㄴ`의 비교식은 화면에 보이는 형태만 기록하고, 그 자체를 일반화하여 서술하지 마세요.

[불확실한 부분]
- 오른쪽 상단 박스 안의 두 번째 기호는 동그라미 숫자처럼 보이지 않고 `ㄴ` 모양에 가깝게 보입니다. 다만 인쇄체 특성상 실제 기호 종류는 약간 불확실합니다.
- 해설 아래의 비교식 `① < ㄴ`은 화면상 분명히 보이지만, 이것이 최종 정답 표기인지 설명용 표기인지 문맥상 완전히 분리되지는 않습니다.
- 왼쪽의 `372`와 오른쪽의 `378`은 분명히 보이지만, 각 숫자가 어떤 자리 계산으로 구성되었는지는 이미지에 단계적으로 나타나지 않아 세부 계산 과정은 확정할 수 없습니다.

--- RAW VISION DRAFT END ---
