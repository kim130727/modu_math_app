# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_008575.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_008575

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
[전체적인 구성]
- 화면 상단 왼쪽에 문제 번호 `41.`이 있고, 그 오른쪽으로 문제 문장이 한 줄로 배치되어 있습니다.
- 가운데에는 세로로 정렬된 곱셈 계산 과정이 크게 제시되어 있습니다.
- 오른쪽에는 연두색 테두리의 직사각형 선택 상자가 있으며, 그 안에 보기 4개가 세로로 나열되어 있습니다.
- 하단에는 `(정답)` 표시와 함께 답이 한 줄로 적혀 있고, 그 아래에는 `(해설)` 문장이 이어집니다.
- 전체적으로 “세로셈 계산 과정 + 보기 선택 + 해설” 구조입니다.

[보이는 텍스트]
- 문제 번호: `41.`
- 문제 문장: `색칠된 부분은 실제 어떤 수의 곱인지 찾아 선택하세요`
- 가운데 계산식에 보이는 숫자와 기호:
  - `473`
  - `× 3`
  - 중간 결과로 보이는 `9`
  - 색칠된 부분 안에 `210`
  - 아래쪽에 `1200`
  - 맨 아래 결과로 보이는 `1419`
- 오른쪽 보기 상자 안의 항목:
  - `7 × 3`
  - `73 × 3`
  - `70 × 3`
  - `700 × 3`
- 하단 텍스트:
  - `(정답)70 × 3`
  - `(해설)473에서 7은 십의 자리 숫자이므로 70 × 3 = 210입니다.`

[반복되는 시각 패턴]
- 가운데 계산은 세로셈 형태로, 숫자들이 자릿수별로 세로 정렬되어 있습니다.
- 곱셈 기호 `×`가 왼쪽에 있고, 오른쪽에 곱하는 수 `3`이 위아래 자리와 맞춰 배치되어 있습니다.
- 중간 과정 중 `210`이 분홍색 배경으로 강조되어 있어, 문제에서 찾으려는 대상이 시각적으로 두드러집니다.
- 오른쪽 보기들은 모두 같은 형식의 `숫자 × 3` 구조로 통일되어 있습니다.
- 보기 상자는 외곽선만 있는 단순 직사각형이며, 내부 항목은 세로로 균등 간격 배치되어 있습니다.

[수식 단계]
- 가운데에는 세로곱셈의 일부 계산 과정이 제시되어 있습니다.
- 보이는 단계는 다음과 같이 읽힐 수 있습니다.
  - 위쪽에 `473`이 있고, 그 아래에 `× 3`이 놓여 있음
  - 첫 번째 부분 결과로 보이는 `9`가 오른쪽 아래쪽에 표시됨
  - 그 아래 분홍색으로 색칠된 `210`이 있음
  - 그 아래 `1200`이 표시됨
  - 맨 아래에 최종 결과처럼 보이는 `1419`가 표시됨
- 단, 이 시각 분석 초안에서는 실제 계산 과정을 완성하거나 값의 의미를 확정하지 않습니다. 보이는 위치 관계만 기술합니다.

[등호별 변환 의미]
- 화면에는 일반적인 가로식의 `=`가 연속된 형태로 직접 보이지 않습니다.
- 대신 세로셈의 중간 결과가 위에서 아래로 누적되는 방식으로 배치되어 있어, 각 줄이 계산 단계의 의미를 가질 가능성이 있습니다.
- 분홍색 `210`은 해설 문장과 연결될 때 `70 × 3`에 해당하는 부분 결과로 설명되고 있습니다.
- 그러나 이 문서는 문제를 풀지 않는 초안이므로, 각 숫자의 정확한 산술 결론은 단정하지 않습니다.

[세부 내용]
- 문제 문장은 “색칠된 부분”을 찾는 형식이므로, 가운데 계산식에서 분홍색으로 칠해진 영역이 핵심 대상입니다.
- 분홍색 영역에는 `210`이 들어 있고, 이 영역은 숫자 3개가 가로로 나란히 놓인 작은 직사각형 색칠 칸처럼 보입니다.
- 가운데 세로셈은 점선으로 세로 구획이 나뉜 듯 보이며, 자릿수 정렬을 돕는 안내선처럼 보입니다.
- 오른쪽 보기 상자에는 `700 × 3`, `70 × 3`, `73 × 3`, `7 × 3`이 들어 있어, 자릿수에 따른 곱셈 식을 고르게 한 선택형 문제입니다.
- 하단 해설 문장에는 `473에서 7은 십의 자리 숫자이므로 ...`라는 설명이 보이며, 이는 자리값에 주목하게 만드는 의도입니다.

[Python DSL 작성 힌트]
- 가운데 세로셈 영역은 별도의 그룹으로 처리하면 좋습니다.
- 권장 구성:
  - 문제 번호와 문장
  - 중앙 계산식 블록
  - 오른쪽 보기 박스
  - 하단 정답/해설 텍스트
- 중앙 계산식 블록에서는 다음 요소를 분리해 표현하면 좋습니다.
  - 상단 숫자 `473`
  - 곱하는 수 `3`
  - 중간의 각 계산 결과 위치
  - 분홍색 강조 박스 `210`
  - 하단의 `1200`, `1419`
- 보기 상자는 연두색 테두리 직사각형으로 표현하고, 내부 텍스트는 세로로 정렬합니다.
- 색칠 영역은 별도 배경색 박스로 처리해야 하며, 숫자와 배경을 분리해 좌표 지정하는 방식이 적합합니다.
- 점선 세로 안내선이 여러 개 있으므로, 필요하면 계산 블록 내부의 열 기준선으로 표현할 수 있습니다.

[주의할 점]
- `210`을 정답으로 단정하는 식의 서술은 피해야 합니다. 다만 화면 하단 해설에 그렇게 적혀 있는 사실은 기록할 수 있습니다.
- 가운데 세로셈의 각 숫자가 어떤 중간 계산값인지, 모든 자릿수 의미를 완전히 확정하지 마세요. 보이는 위치만 기준으로 적는 것이 안전합니다.
- 오른쪽 보기의 순서는 위에서 아래로 `7 × 3`, `73 × 3`, `70 × 3`, `700 × 3`입니다.
- 화면 하단의 `(정답)`과 `(해설)`은 인쇄된 텍스트로 보이므로, 입력용 DSL에서는 별도 텍스트 영역으로 분리하는 것이 좋습니다.
- 문제 문장에 “찾아 선택하세요”가 있으므로, 선택형 UI처럼 보이지만 실제로는 보기 박스만 제공된 정적 이미지입니다.

[불확실한 부분]
- 가운데 세로셈의 점선 구획과 숫자 배치가 정확히 어떤 자릿수 계산 절차를 나타내는지, 이미지 해상도만으로는 일부 판독이 어렵습니다.
- `9`, `210`, `1200`, `1419`가 각각 어떤 단계의 결과인지 화면만으로 100% 확정하기 어렵습니다.
- 맨 아래 `1419`가 최종값인지, 또는 다른 표기와 겹친 것인지도 시각적으로는 그렇게 보일 뿐 완전히 단정하지는 않습니다.
- 오른쪽 보기 상자의 테두리 색은 연두색 계열로 보이지만, 정확한 색상명은 단정하지 않습니다.

--- RAW VISION DRAFT END ---
