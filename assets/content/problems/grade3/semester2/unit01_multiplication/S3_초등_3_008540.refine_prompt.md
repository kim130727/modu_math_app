# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_008540.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_008540

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
[전체적인 구성]
- 화면 상단에 문제 번호와 지시문이 한 줄로 배치되어 있습니다.
- 왼쪽에는 세로형 곱셈 계산 과정이 크게 있고, 오른쪽에는 주어진 선택지들이 주황색 테두리 박스 안에 2행 2열로 배치되어 있습니다.
- 하단 왼쪽에는 `(정답)` 표시와 함께 정답 표기가 보이고, 그 아래에 `(해설)` 문장이 한 줄로 이어집니다.
- 전체적으로 “계산 과정 그림 + 보기 박스 + 해설 문장”의 3부분 구조입니다.

[보이는 텍스트]
- `1. 색칠된 부분은 실제 어떤 수의 곱인지를 찾아 선택하세요.`
- 세로 곱셈식 안에 보이는 수:
  - `869`
  - `× 4`
  - 중간 계산 결과로 보이는 `36`
  - 색칠된 부분 안의 `240`
  - 아래쪽에 `3200`
  - 맨 아래 결과 `3476`
- 보기 박스 안 텍스트:
  - `6 × 4`
  - `69 × 4`
  - `60 × 4`
  - `600 × 4`
- 하단:
  - `(정답)60 × 4`
  - `(해설)869에서 6은 60을 나타내므로 색칠된 부분 240은 실제 60 × 4의 결과입니다.`

[반복되는 시각 패턴]
- 보기 항목들이 모두 `숫자 × 4` 형태로 반복됩니다.
- 곱셈 세로셈의 각 자릿수 계산이 위에서 아래로 단계적으로 쌓이는 형태입니다.
- 가운데의 색칠된 부분은 연한 분홍색 배경으로 강조되어 있으며, 한 줄의 일부 숫자만 선택적으로 하이라이트되어 있습니다.
- 세로셈의 자리 구분은 점선 세로줄로 나뉘어 있어 각 자릿값 위치를 강조하는 구조입니다.

[수식 단계]
- 이 문제 화면에는 등호로 이어지는 여러 변형식이 직접적으로 보이지는 않습니다.
- 다만 세로 곱셈에서 중간값들이 위에서 아래로 차례로 계산되는 구조가 보입니다.
- 보이는 계산 순서의 형태:
  - 한 자리 곱의 결과로 `36`이 보임
  - 다음 자리의 결과로 색칠된 `240`이 보임
  - 이어서 `3200`이 보임
  - 최종 결과 `3476`이 맨 아래에 제시됨
- 단, 이 값들의 정확한 계산 의미를 여기서 해석하여 확정하지는 않습니다.

[등호별 변환 의미]
- 화면에 `=` 기호가 여러 번 이어진 변형식은 보이지 않습니다.
- 따라서 등호마다 어떤 항이 바뀌는지에 대한 별도 단계 설명은 해당되지 않습니다.

[세부 내용]
- 왼쪽 계산 그림은 세로셈 형식이며, 위쪽에 `869`가 세 자리로 세로 정렬되어 있고 그 아래 오른쪽에 `4`가 곱하는 수로 놓여 있습니다.
- 곱셈 기호 `×`는 `4`의 왼쪽 아래 쪽에 배치되어 있습니다.
- 계산 과정 중간의 숫자 `36`은 가로선 바로 아래 첫 번째 줄에 있고, `240`은 그 아래 줄에서 분홍색 배경으로 강조되어 있습니다.
- `3200`은 그 다음 줄에 있으며, `3476`은 맨 아래 결과처럼 배치되어 있습니다.
- 오른쪽 보기 박스는 주황색 테두리의 직사각형이며, 내부에 4개의 선택지가 2열로 배치되어 있습니다.
  - 왼쪽 위: `6 × 4`
  - 오른쪽 위: `69 × 4`
  - 왼쪽 아래: `60 × 4`
  - 오른쪽 아래: `600 × 4`
- 하단 해설 문장은 `869에서 6은 60을 나타내므로`라는 문구로 시작해, 색칠된 부분과 보기 중 하나가 연결된다는 설명이 붙어 있습니다.

[Python DSL 작성 힌트]
- 문제 텍스트는 상단 한 줄로 배치하고, 번호 `1.`과 지시문을 함께 출력하는 구조가 적절합니다.
- 왼쪽의 세로셈은 자리 맞춤이 중요하므로, 숫자와 기호를 표 형태 또는 고정 폭 정렬로 배치하면 좋습니다.
- 중간 결과 `240` 부분은 연한 분홍색 강조 박스로 표시해야 합니다.
- 오른쪽 선택지는 주황색 외곽선 박스 안에 2행 2열 그리드로 배치하는 것이 적합합니다.
- 하단에는 `(정답)`과 `(해설)`을 별도 텍스트 줄로 분리해 넣는 구성이 알맞습니다.
- 점선 세로 구분선, 가로 기준선, 하이라이트 박스 등 시각 요소를 분리해서 그리는 방식이 좋습니다.

[주의할 점]
- 색칠된 부분의 답을 추론하여 문제를 풀지 말아야 합니다.
- 보기에 있는 네 항목 중 어떤 것이 정답인지 별도로 확정하지 말고, 화면에 보이는 그대로만 기록해야 합니다.
- `240`, `3200`, `3476`의 의미를 계산적으로 단정하지 말고, 보이는 배열과 위치 중심으로 설명해야 합니다.
- 세로셈의 자리 정렬은 매우 중요하므로 숫자 위치를 대략적으로만 적지 말고, 위아래 배치를 명확히 유지해야 합니다.
- 해설 문장은 화면에 보이는 그대로 옮기되, 추가 해석은 최소화해야 합니다.

[불확실한 부분]
- 왼쪽 세로셈의 각 중간 숫자(`36`, `240`, `3200`, `3476`)가 어떤 자릿수 계산에 해당하는지 화면만으로 완전히 확정하기 어렵습니다.
- 특히 `240`이 왜 강조되는지에 대한 정확한 수학적 설명은 문제 해결로 이어질 수 있으므로 여기서는 단정하지 않습니다.
- 오른쪽 보기 박스의 각 항목은 위치상 명확하지만, 이 중 무엇이 정답인지 시각 분석 단계에서는 확정하지 않습니다.

--- RAW VISION DRAFT END ---
