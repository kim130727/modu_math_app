# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_008543.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_008543

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
[전체적인 구성]
- 상단에 문제 번호와 지시문이 있고, 그 아래에 연한 보라색 배경의 긴 직사각형 박스가 하나 크게 배치되어 있습니다.
- 박스 안에는 3개의 계산식이 좌→우로 나란히 놓여 있으며, 각각 앞에 동그라미 기호와 한글 표식(ㄱ, ㄴ, ㄷ)이 붙어 있습니다.
- 박스 아래에는 보기 4개가 두 줄로 나뉘어 배치되어 있고, 각 보기 앞에는 파란색 원형 번호(①~④)가 있습니다.
- 보기 아래에는 `(정답)` 표기와 번호가 따로 있으며, 그 아래 `(해설)` 문장과 식, 그리고 화살표가 있는 설명 문장이 이어집니다.
- 전체적으로 “계산 결과의 크기 순서”를 판단하는 문제 형태이며, 시각 요소는 계산식 3개와 순서 선택지 4개로 구성됩니다.

[보이는 텍스트]
- `□ 5. 계산 결과가 큰 것부터 순서대로 나열한 것을 고르세요.`
- 박스 안:
  - `ㄱ 4 × 28`
  - `ㄴ 3 × 34`
  - `ㄷ 2 × 76`
- 보기:
  - `① ㄱ, ㄴ, ㄷ`
  - `② ㄷ, ㄴ, ㄱ`
  - `③ ㄷ, ㄱ, ㄴ`
  - `④ ㄴ, ㄱ, ㄷ`
- 정답 표기:
  - `(정답) ③`
- 해설:
  - `(해설) ㄱ 4 × 28 = 112   ㄴ 3 × 34 = 102   ㄷ 2 × 76 = 152`
  - 화살표 아이콘 뒤에 `152 > 112 > 102`가 보이고, 이어서 `이므로 계산 결과가 큰 것부터 순서대로 기호를 쓰면 ㄷ, ㄱ, ㄴ입니다.`라고 적혀 있습니다.

[반복되는 시각 패턴]
- 계산식 3개가 동일한 형식으로 반복됩니다.
  - 각 식은 `기호 + 곱셈식` 구조입니다.
  - 곱셈 연산자 `×`가 모두 사용되어 있습니다.
- 보기 4개도 동일한 구조로 반복됩니다.
  - 각 보기마다 파란색 원형 번호 `①, ②, ③, ④`가 먼저 나오고,
  - 그 뒤에 쉼표로 구분된 3개의 기호가 나열됩니다.
- 해설에서는 세 계산식의 결과를 한 줄에 나란히 적고, 그 아래에 부등호 `>`를 사용해 크기 순서를 반복적으로 보여줍니다.

[수식 단계]
- 해설에 보이는 수식 단계는 다음과 같이 구성되어 있습니다.
  - `ㄱ 4 × 28 = 112`
  - `ㄴ 3 × 34 = 102`
  - `ㄷ 2 × 76 = 152`
- 그 다음 단계에서 결과값끼리 비교한 형태가 나타납니다.
  - `152 > 112 > 102`
- 이 단계들은 각각 계산식의 결과를 적은 뒤, 그 결과들 사이의 대소 관계를 나타낸 것으로 보입니다.
- 단, 여기서는 실제 계산 과정을 풀이하지 않고, 화면에 표시된 결과와 비교식의 형태만 관찰할 수 있습니다.

[등호별 변환 의미]
- `4 × 28 = 112` 형태는 `ㄱ`에 해당하는 식의 계산 결과를 등호 뒤에 적은 것입니다.
- `3 × 34 = 102` 형태는 `ㄴ`에 해당하는 식의 계산 결과를 등호 뒤에 적은 것입니다.
- `2 × 76 = 152` 형태는 `ㄷ`에 해당하는 식의 계산 결과를 등호 뒤에 적은 것입니다.
- 이후 `152 > 112 > 102`는 세 결과를 큰 수부터 작은 수까지 배열한 비교식입니다.
- 마지막 문장은 이 비교식에 근거해 기호를 순서대로 쓰는 방식으로 안내하는 설명입니다.
- 여기서는 각 등호가 “같다”는 관계를 나타내며, 앞의 식과 뒤의 수가 대응된다는 점만 확인할 수 있습니다.

[세부 내용]
- 상단 박스는 연한 보라색/회색 톤의 채움색에 보라색 테두리를 가진 큰 직사각형입니다.
- 박스 내부의 3개 계산식은 가로 방향으로 균등하게 배치되어 있고, 가운데 정렬에 가깝게 보입니다.
- 각 계산식 앞의 기호는 동그라미 안에 한글 자모처럼 보이는 표시로, `ㄱ`, `ㄴ`, `ㄷ`이 들어 있습니다.
- 보기 영역은 좌우로 나뉘어 배치되어 있으며, 왼쪽에 ①, ③이 위아래로, 오른쪽에 ②, ④가 위아래로 놓여 있습니다.
- 각 보기의 기호들은 쉼표와 공백으로 구분되어 있으며, 순서를 묻는 형식입니다.
- 해설의 첫 줄은 세 계산식을 한 줄에 붙여 놓은 형태이고, 그 아래에 오른쪽을 향한 회색 화살표가 있어 비교 결과 문장으로 이어지는 흐름을 만듭니다.
- `(정답) ③`은 해설 위쪽에 별도로 제시되어 있어, 정답 번호를 직접 표기하는 구조입니다.

[Python DSL 작성 힌트]
- 큰 직사각형 박스 1개를 먼저 배치하고, 그 안에 3개의 계산식을 가로로 균등 배치하는 구조로 작성하면 좋습니다.
- 각 계산식은 다음 요소를 포함하는 하나의 그룹으로 취급하면 됩니다.
  - 원형 기호(ㄱ/ㄴ/ㄷ)
  - 곱셈식 텍스트
- 보기 4개는 2열 2행 형태로 배치하는 구성이 자연스럽습니다.
- 정답 표기, 해설 텍스트, 화살표 아이콘, 비교식은 본문 아래쪽에 세로로 이어서 배치하면 화면 구조와 유사합니다.
- 비교식 `>`는 해설용 텍스트로만 넣고, 실제 값 계산은 DSL 단계에서 임의로 채우지 않도록 주의해야 합니다.
- 원형 번호(①~④)는 숫자 텍스트가 아니라 시각적 기호로 표현되면 더 유사합니다.

[주의할 점]
- 문제를 풀어 정답을 새로 계산해 넣지 마세요.
- 빈칸은 없지만, 기호 배열을 추론하여 임의로 바꾸지 마세요.
- 해설에 적힌 결과값은 화면에 보이는 텍스트로만 기록하고, 계산 과정을 새로 서술하지 마세요.
- 보기 번호의 배치가 좌우로 교차되어 보일 수 있으므로, 실제 위치를 반영해 적어야 합니다.
- `ㄱ`, `ㄴ`, `ㄷ` 표시는 일반 문자처럼 보이지만, 원형 안에 들어간 기호라는 점을 함께 묘사하는 것이 좋습니다.

[불확실한 부분]
- 상단 문제 번호 앞의 작은 네모 기호 `□`는 체크박스인지 단순 표식인지 화면만으로 확정하기 어렵습니다.
- 박스 내부의 기호가 정확히 원형 테두리 안에 있는지, 혹은 원형에 가까운 작은 표식인지 세부 도형은 해상도상 다소 모호합니다.
- 해설의 화살표는 실제 도형 화살표인지, 안내용 아이콘인지 약간 흐릿하게 보입니다.
- `(정답)`과 `(해설)`의 글자 크기 및 서체는 화면상 유사하지만, 정확한 폰트 종류까지는 판단하기 어렵습니다.

--- RAW VISION DRAFT END ---
