# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_008546.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_008546

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
[전체적인 구성]
- 화면 상단에 문제 문장이 1줄로 배치되어 있고, 그 아래에 연한 연두색 배경의 긴 직사각형 선택 영역이 있습니다.
- 선택 영역 안에는 3개의 보기(①, ②, ③처럼 보이는 원 안 문자 표기)가 가로로 나란히 배치되어 있습니다.
- 선택 영역 아래 왼쪽에는 `(정답)` 표시가 따로 있고, 그 아래에 `(해설)`이 붙은 풀이 영역이 세로로 이어집니다.
- 풀이 영역은 각 보기의 곱셈식을 한 줄씩 나열한 뒤, 마지막에 결론 문장이 제시되는 구조입니다.

[보이는 텍스트]
- `□ 8. 계산 결과가 가장 큰 것을 찾고 기호를 선택하세요.`
- 보기 영역:
  - `ㄱ 18 × 50`
  - `ㄴ 66 × 20`
  - `ㄷ 42 × 30`
- 왼쪽 아래:
  - `(정답) ㄴ`
  - `(해설)`
- 풀이 예시:
  - `ㄱ 18 × 50 = 900`
  - `ㄴ 66 × 20 = 1320`
  - `ㄷ 42 × 30 = 1260`
  - `따라서 곱이 가장 큰 것은 ㄴ입니다.`
- 주의: 원형 기호는 한글 자모처럼 보이는 `ㄱ, ㄴ, ㄷ` 형태이며, 실제로는 각 보기의 선택 기호 역할을 합니다.

[반복되는 시각 패턴]
- 3개의 보기 모두 같은 형식으로 구성되어 있습니다.
  - 기호 1개 + 두 수의 곱셈식
- 각 보기 사이의 간격이 균등하며, 모두 같은 수평선상에 배치되어 있습니다.
- 해설 부분에서도 각 보기의 식이 같은 정렬 방식으로 3줄 반복됩니다.
- 마지막 결론 문장은 일반 문장 형태로, 계산식 나열과 구분되어 있습니다.

[수식 단계]
- 해설에 계산식이 직접 제시되어 있습니다.
  - `18 × 50 = 900`
  - `66 × 20 = 1320`
  - `42 × 30 = 1260`
- 이 문제 화면에서는 중간 변형식이나 여러 단계의 등호 전개는 보이지 않고, 각 곱셈식의 결과만 바로 적혀 있습니다.

[등호별 변환 의미]
- 각 줄의 `=`는 “곱셈식의 계산 결과가 오른쪽 수와 같다”는 의미로 보입니다.
- 단계적 변형이 여러 번 이어진 구조는 아니므로, 등호의 앞뒤로 무엇이 어떻게 바뀌었는지 별도 전개는 관찰되지 않습니다.

[세부 내용]
- 문제 번호 `8.`은 왼쪽 상단에 있고, 그 앞에 작은 사각형 표시가 함께 보입니다.
- 문제 지문은 선택형 문제이며, “계산 결과가 가장 큰 것”을 찾으라고 요구합니다.
- 연두색 배경 박스는 보기 3개를 강조하는 영역으로 보입니다.
- 각 보기의 기호는 곱셈식 앞에 작게 붙어 있으며, 보기 내부에서 수식은 한 줄로 표시됩니다.
- `(정답) ㄴ`이 화면에 직접 노출되어 있어, 답안 표시는 별도로 확인 가능해 보입니다.
- 해설에서는 각 곱셈식 오른쪽에 계산 결과를 적고, 마지막 문장으로 가장 큰 값을 가진 보기를 결론짓습니다.
- 도형, 표, 분수 슬롯, 화살표는 보이지 않습니다.

[Python DSL 작성 힌트]
- 상단에 문제 지문 1줄을 텍스트로 배치합니다.
- 그 아래에 연한 연두색의 긴 사각형 배경을 만들고, 그 안에 3개의 보기를 가로 정렬합니다.
- 각 보기는 `기호 + 식` 구조로 동일한 스타일을 반복해 배치합니다.
- 선택지 아래 왼쪽에는 `(정답)` 텍스트와 기호를 별도로 배치합니다.
- 해설 영역은 `(해설)` 라벨 이후, 3개의 계산식을 세로로 정렬해 넣습니다.
- 마지막 결론 문장은 해설 아래에 일반 텍스트로 넣습니다.
- 식의 표현은 `18 × 50`, `66 × 20`, `42 × 30`처럼 곱셈 기호를 포함한 한 줄 문자열로 처리하면 됩니다.

[주의할 점]
- 답을 추론하여 새로 계산 결과를 만들어 넣지 마세요. 화면에 보이는 텍스트만 기준으로 작성해야 합니다.
- 보기 기호는 원형 숫자처럼 보이지만 실제 인쇄체에서는 `ㄱ, ㄴ, ㄷ` 형태로 읽히는 부분이 있으므로, 보이는 형태를 그대로 옮기되 혼동에 주의하세요.
- `(정답) ㄴ`은 화면에 직접 제시된 정보이므로, 분석 초안에서는 사실로만 적고 이유를 덧붙이지 마세요.
- 계산 과정이 단계별로 분해되어 있지 않으므로, 중간 변형식을 억지로 만들지 마세요.
- 연두색 박스의 정확한 크기나 여백은 이미지 기반으로만 추정 가능하므로, 너무 단정적으로 수치화하지 마세요.

[불확실한 부분]
- 왼쪽 상단의 작은 사각형 표시가 체크박스인지 문제 번호 표식인지 완전히 단정하기는 어렵습니다.
- 보기 기호가 원형 안 문자로 인쇄된 것인지, 화면에서 그렇게 보이는 것인지 약간의 판독 여지가 있습니다.
- 연두색 배경 박스의 정확한 색상값과 폭은 이미지에서 대략적으로만 확인됩니다.
- `(정답) ㄴ` 표시는 인쇄물의 정답 표시인지 해설용 주석인지 맥락상 추정되지만, 분석 초안에서는 표기 그대로 기록하는 것이 안전합니다.

--- RAW VISION DRAFT END ---
