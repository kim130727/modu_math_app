# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_008553.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_008553

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
[전체적인 구성]
- 왼쪽에는 곱셈 세로셈 풀이가 있고, 오른쪽에는 선택지 박스가 따로 배치되어 있습니다.
- 상단에는 문제 번호와 지시문이 한 줄로 적혀 있습니다.
- 가운데 왼쪽의 세로셈에서 특정 부분이 연한 색으로 하이라이트되어 있으며, 그 부분이 빈칸/가려진 답처럼 강조됩니다.
- 오른쪽에는 보라색 테두리의 세로 직사각형 박스 안에 선택지 4개가 세로로 나열되어 있습니다.
- 하단에는 `(정답)` 표시와 `(해설)` 문장이 따로 있습니다.

[보이는 텍스트]
- 상단 지시문: `16. 색칠된 부분은 실제 어떤 수의 곱인지를 찾아 기호를 선택해 보세요.`
- 세로셈:
  - `574`
  - `× 3`
  - 중간 계산값으로 보이는 `12`
  - 색칠된 부분 `210`
  - 다음 줄 `1500`
  - 최종 결과 `1722`
- 선택지 박스 안:
  - `ㄱ 7 × 3`
  - `ㄴ 7 × 30`
  - `ㄷ 70 × 3`
  - `ㄹ 700 × 3`
- 하단:
  - `(정답) ㄷ`
  - `(해설) 색칠된 부분은 곱해지는 수 574에서 십의 자리의 70과 곱하는 수 3의 곱입니다.`
    - 해설 문장은 보이는 그대로 옮기되, 이 초안에서는 의미 해석만 참고용으로 다룹니다.

[반복되는 시각 패턴]
- 선택지는 모두 `기호 + 곱셈식`의 형태로 통일되어 있습니다.
- 세로셈에서는 각 자리의 곱이 아래로 내려가며 적히는 전형적인 자리값 분해 형태가 보입니다.
- 색칠된 부분은 숫자 3개가 가로로 배열된 한 덩어리이며, 다른 계산값들과 같은 글꼴 크기와 정렬을 유지합니다.
- 오른쪽 선택지 박스는 위에서 아래로 4개 항목이 균등한 간격으로 배치되어 있습니다.

[수식 단계]
- 보이는 세로셈은 `574 × 3`의 계산 과정처럼 배치되어 있습니다.
- 첫 번째 중간값으로 `12`가 보이고, 그 아래 색칠된 `210`이 있으며, 그 아래에 `1500`, 마지막에 `1722`가 보입니다.
- 각 단계가 어떤 자리 계산에 해당하는지 화면상 직접 표기되어 있지는 않지만, 자리값 분해에 따른 곱셈 결과를 순서대로 적은 형식으로 보입니다.
- 색칠된 `210`은 다른 줄과 달리 연한 노란색 배경으로 강조되어 있어, 문제의 핵심 대상입니다.

[등호별 변환 의미]
- 화면에 여러 개의 등호로 이어진 변형식은 보이지 않습니다.
- 따라서 등호별 단계 변환 설명은 별도로 필요하지 않습니다.
- 다만 세로셈의 아래쪽 결과가 위에서부터 순차적으로 합쳐져 최종값 `1722`로 이어지는 구조는 보입니다.

[세부 내용]
- 왼쪽 세로셈의 맨 위에는 `574`가 세 자리로 세로 정렬되어 있고, 그 아래 줄에 `× 3`이 있어 곱하는 수가 오른쪽에 놓인 형태입니다.
- 중간 계산값 `12`는 `574` 바로 아래, 첫 번째 계산 줄 위치에 놓여 있습니다.
- 색칠된 `210`은 세 자리 숫자로 가로 정렬되어 있으며, 연한 연두색/노란색 계열의 사각형 배경이 깔려 있습니다.
- 그 아래에는 `1500`이 또 다른 중간 결과로 보이며, 마지막 줄에 `1722`가 최종 답처럼 표시되어 있습니다.
- 오른쪽 선택지 박스는 보라색 외곽선만 있는 단순한 직사각형이며 내부 배경은 흰색입니다.
- 선택지의 기호는 동그라미/기호 스타일이 아니라 한글 자모 형태의 `ㄱ, ㄴ, ㄷ, ㄹ`로 표시되어 있습니다.
- 정답 표시는 본문 아래쪽 왼편에 작게 `(정답) ㄷ`으로 적혀 있습니다.

[Python DSL 작성 힌트]
- 세로셈 영역과 선택지 영역을 분리해서 캔버스에 배치하는 구조로 작성하면 좋습니다.
- `574`, `× 3`, `12`, `210`, `1500`, `1722`는 각각 독립 텍스트 객체로 두고, 숫자 간 가로/세로 정렬을 맞추는 방식이 적절합니다.
- 색칠된 `210`은 배경 사각형을 먼저 그린 뒤 그 위에 숫자를 배치해야 합니다.
- 오른쪽 선택지 박스는 테두리 직사각형 1개와 내부 텍스트 4줄로 구성하면 됩니다.
- 문제 번호와 지시문은 상단 좌측에 한 줄 텍스트로 두되, 본문보다 작은 여백을 주어 배치합니다.
- 하단의 `(정답) ㄷ`, `(해설)` 문구는 별도 텍스트 블록으로 처리하되, 필요 시 숨김/주석 요소로 분리할 수 있습니다.

[주의할 점]
- 색칠된 부분의 수를 답으로 추론해 직접 쓰지 마세요.
- `210`이 무엇을 뜻하는지 풀이를 완성형으로 단정하지 말고, 화면에 보이는 계산 결과로만 다루세요.
- 선택지의 기호와 식을 서로 바꿔 적지 않도록 주의하세요.
- 세로셈의 중간값 배치는 일반적인 교과서 형식이지만, 각 줄의 정확한 계산 의미는 시각 자료만으로는 일부 해석이 필요합니다.
- 정답 표기 `(정답) ㄷ`이 보이더라도, 시각 분석 초안에서는 문제 해결이 아니라 화면 정보 기록에 집중해야 합니다.

[불확실한 부분]
- `12`와 `210`이 각각 어떤 자리의 곱인지 화면만으로는 완전히 명시되어 있지 않습니다.
- `1500`이 자리값 분해의 어떤 항에 해당하는지, 그리고 줄 간의 정확한 계산 연결 방식은 문장으로 직접 적혀 있지 않습니다.
- 색칠된 영역의 정확한 색 이름은 연한 노랑, 연두, 베이지 계열 중 어느 쪽으로 볼지 다소 애매합니다.
- 오른쪽 선택지 박스의 테두리 색은 보라색에 가깝지만, 인쇄 상태에 따라 약간 다르게 보일 수 있습니다.

--- RAW VISION DRAFT END ---
