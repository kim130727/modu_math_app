# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_008608.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_008608

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
[전체적인 구성]
- 한 문제짜리 선택형/판단형 그림 문제입니다.
- 화면 상단 왼쪽에 문제 번호와 지시문이 있고, 그 아래에 사람 그림 3개가 가로로 배치되어 있습니다.
- 각 사람은 노란색 사각형 판을 들고 있으며, 판 안에는 나눗셈 식이 하나씩 적혀 있습니다.
- 각 그림 아래에는 이름표가 따로 붙어 있습니다.
- 화면 하단에는 `(정답)`, `(해설)`이 텍스트로 제시되어 있고, 마지막에 회색 화살표와 함께 결론 문장이 있습니다.
- 전체적으로 “보기 3개 중 묵이가 다른 사람을 고르는” 구조로 보입니다.

[보이는 텍스트]
- `78. 묵이 다른 사람을 선택해 보세요.`
- 노란 판 1: `30 ÷ 3`
- 이름표 1: `은재`
- 노란 판 2: `40 ÷ 2`
- 이름표 2: `성환`
- 노란 판 3: `70 ÷ 7`
- 이름표 3: `기영`
- `(정답)성환`
- `(해설)은재: 30 ÷ 3 = 10, 성환: 40 ÷ 2 = 20, 기영: 70 ÷ 7 = 10`
- `묵이 다른 사람은 성환입니다.`

[반복되는 시각 패턴]
- 사람 3명이 거의 같은 크기와 비슷한 스타일로 반복 배치되어 있습니다.
- 각 인물 위/가운데에는 노란색 직사각형 판이 있고, 그 판 중앙에 식이 1개씩 들어 있습니다.
- 판 아래에는 둥근 모서리의 흰색 이름표가 있으며, 모두 비슷한 형태입니다.
- 세 인물의 구성 방식은 동일하고, 가운데 값만 다르게 제시됩니다.
- 좌→우로 `은재 / 성환 / 기영` 순서의 반복 구조입니다.

[수식 단계]
- 화면 하단 해설에는 등호가 포함된 식이 3개 나란히 제시되어 있습니다.
- `30 ÷ 3 = 10`
- `40 ÷ 2 = 20`
- `70 ÷ 7 = 10`
- 각 식은 “나눗셈식 → 결과” 형태로, 등호 오른쪽에 계산 결과가 붙어 있습니다.
- 다만 이 자료 자체는 풀이 과정을 한 단계씩 전개한 변형식이라기보다, 각 보기의 계산 결과를 한 번에 적어 둔 형태입니다.

[등호별 변환 의미]
- 각 등호는 좌변의 나눗셈 식이 우변의 값과 같다는 뜻으로 보입니다.
- `30 ÷ 3 = 10`은 첫 번째 보기의 계산 결과 표시입니다.
- `40 ÷ 2 = 20`은 두 번째 보기의 계산 결과 표시입니다.
- `70 ÷ 7 = 10`은 세 번째 보기의 계산 결과 표시입니다.
- 여기서 등호는 식의 변형 단계라기보다, 계산 결과를 대응시킨 의미로 읽힙니다.

[세부 내용]
- 문제 번호 `78.`이 좌상단에 작게 표시되어 있습니다.
- 문장 `묵이 다른 사람을 선택해 보세요.`에서 `다른` 부분이 밑줄처럼 강조되어 보입니다.
- 세 사람의 그림은 머리, 얼굴, 상반신만 보이는 반신 캐릭터 형태입니다.
- 각 사람은 두 손으로 노란 판을 잡고 있는 모양이며, 판이 인물의 몸 앞쪽 중앙에 위치합니다.
- 노란 판은 가로로 긴 직사각형이며 테두리가 약간 있는 듯 보입니다.
- 이름표는 판 바로 아래에 있고, 회색/파란 테두리의 둥근 사각형 안에 이름이 들어 있습니다.
- 하단의 `(정답)`과 `(해설)`은 본문보다 작게, 왼쪽 정렬로 배치되어 있습니다.
- 마지막 결론 문장 앞에는 회색의 오른쪽 화살표 아이콘이 있습니다.

[Python DSL 작성 힌트]
- 상단 문제 번호와 지시문을 하나의 텍스트 블록으로 배치하면 좋습니다.
- 인물 3개는 동일한 템플릿을 반복해서 배치하되, 가로 방향으로 균등 간격 정렬하는 구성이 적절합니다.
- 각 인물에 대해:
  - 캐릭터 이미지
  - 그 아래 또는 앞쪽의 노란 직사각형 판
  - 판 중앙의 나눗셈 식 텍스트
  - 판 아래의 이름표
  를 별도 요소로 나누어 구성하면 좋습니다.
- 하단 해설 영역은 문제 본문과 분리된 텍스트 박스로 처리하면 읽기 좋습니다.
- 결론 문장은 화살표 아이콘과 함께 별도 줄에 배치하는 방식이 적합합니다.
- 식 텍스트는 `30 ÷ 3`처럼 연산 기호가 포함된 일반 텍스트로 두는 것이 자연스럽습니다.
- 정답 표시가 이미 시각 자료 안에 있으므로, DSL에서는 정답 영역을 별도 강조 텍스트로 표현해도 됩니다.

[주의할 점]
- 이 자료는 문제 풀이용이 아니라 시각 요소 분석용이므로, 어떤 사람이 “다른 사람”인지 여기서 판정해 설명하지 않는 것이 좋습니다.
- 빈칸이 없는 문제이므로 빈칸 추론을 시도할 필요가 없습니다.
- `묵이`가 문제 문장에 보이지만, 이는 이름 또는 문맥상 오타처럼 보일 수 있으나 단정하면 안 됩니다.
- 하단에 이미 `(정답)`과 `(해설)`이 적혀 있어도, 분석 문서에서는 이를 답안으로 확정해 재진술하지 않도록 주의해야 합니다.
- 인물의 정확한 표정, 머리카락 모양, 옷 색상은 도식화 수준에서만 설명하고 세부 묘사는 과도하게 확정하지 않는 것이 좋습니다.

[불확실한 부분]
- 상단 문장의 `묵이`는 실제 의도된 이름인지, `무엇이` 또는 다른 표현의 일부인지 화면만으로는 확실하지 않습니다.
- 인물의 옷 색상과 머리 모양은 화면에서는 보이지만, DSL 구현에서 꼭 동일하게 재현해야 할 정도로 중요한지 여부는 불확실합니다.
- 하단의 `(정답)성환`은 편집상 예시인지 실제 정답 표기인지 분리해서 판단해야 하나, 이 분석에서는 단정하지 않습니다.
- 결론 문장의 의미는 보이지만, 그 문장을 최종 답으로 사용할지 여부는 별도 판단이 필요합니다.

--- RAW VISION DRAFT END ---
