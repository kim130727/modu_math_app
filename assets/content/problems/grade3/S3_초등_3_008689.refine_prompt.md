# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_008689.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_008689

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
[전체적인 구성]
- 화면 왼쪽 위에 문제 번호와 지시문이 있고, 오른쪽 중앙에 원 그림이 배치되어 있다.
- 원 그림 아래에는 선택지/분류용 문장이 한 줄로 길게 적혀 있고, 그 아래 왼쪽에는 `(정답)`과 `(해설)` 표기가 따로 보인다.
- 전체적으로 “그림을 보고 항목을 고르는” 형태의 초등 수학 문제 레이아웃이다.
- 본문에는 계산식 전개나 등호로 이어지는 수식 단계는 보이지 않는다.

[보이는 텍스트]
- `□ 78. 원의 반지름과 지름을 나타내는 선분을 각각 찾아 기호를 선택해 보세요.`
- 원 그림 안의 기호 표기: `ㄱ`, `ㄴ`, `ㄷ`
- 원 내부 중심 부근에 작은 문자 `O`처럼 보이는 점 표기가 있다.
- 그림 아래 문장:
  - `원의 반지름 ( ㄱ , ㄴ , ㄷ ), 원의 지름 ( ㄱ , ㄴ , ㄷ )`
- 하단 왼쪽:
  - `(정답) ㄷ, ㄴ`
  - `(해설) 원의 반지름 ㄷ, 원의 지름 ㄴ`
- 주의: 위의 `(정답)`과 `(해설)`은 화면에 실제로 보이는 텍스트이며, 여기서는 내용을 풀이하지 않고 시각적으로만 기록한다.

[반복되는 시각 패턴]
- 원 안에 선분 후보가 여러 개 표시되어 있고, 각 선분에 각각 기호 `ㄱ`, `ㄴ`, `ㄷ`가 붙어 있다.
- 선분들은 모두 파란 점선/하늘색 점선처럼 보이는 강조선으로 나타나며, 하나는 거의 세로에 가깝고, 하나는 기울어진 선, 하나는 중심을 지나가는 듯한 긴 선으로 보인다.
- 원의 중심 근처에 작은 점 표시가 있어 반지름/지름을 판단하는 기준점처럼 보인다.
- 선택지 문장에서는 같은 형식의 괄호가 반복된다:
  - `원의 반지름 ( ㄱ , ㄴ , ㄷ )`
  - `원의 지름 ( ㄱ , ㄴ , ㄷ )`
- `(정답)`과 `(해설)`도 같은 형식으로 반복 배치되어 있다.

[수식 단계]
- 해당 사항 없음.
- 등호(`=`)로 이어지는 수식 변형은 보이지 않는다.

[등호별 변환 의미]
- 해당 사항 없음.
- 수식 전개 단계가 없어서 등호별 의미 설명도 없다.

[세부 내용]
- 문제 문장은 “원의 반지름과 지름을 나타내는 선분을 각각 찾아 기호를 선택”하라는 뜻으로 보인다.
- 오른쪽 원 그림은 검은 외곽선의 원이고, 내부에 여러 선분이 겹쳐 그려져 있다.
- 원의 아래쪽 가장자리 부근에서 시작해 위쪽으로 뻗는 선이 하나 있고, 원 내부를 비스듬히 가로지르는 선이 하나 있으며, 왼쪽 쪽에 거의 세로로 놓인 선분도 하나 있다.
- 각 선분 옆에 동그라미 안 글자처럼 보이는 기호 `ㄱ`, `ㄴ`, `ㄷ`가 각각 배치되어 있다.
- 원 중심 표시로 보이는 작은 점이 있고, 그 주변에 한 선분은 중심을 지나가는 구조로 보인다.
- 그림 아래의 문장은 정답을 직접 쓰는 칸이 아니라, 괄호 안에 기호들을 고르는 방식의 안내 문장처럼 보인다.
- 하단의 `(정답)`과 `(해설)`은 출력 예시 또는 교사용 표시처럼 배치되어 있으며, 학습자용 입력 칸은 별도로 보이지 않는다.

[Python DSL 작성 힌트]
- 전체 레이아웃은 상단 문제 문장 + 중앙 우측 원 도형 + 하단 선택지 문장 + 좌하단 정답/해설 문장 구조로 나누면 좋다.
- 원 도형은:
  - 바깥 원 1개
  - 내부 선분 3개 정도
  - 중심점 1개
  - 각 선분에 대응하는 기호 라벨 3개
  로 구성해서 표현하면 된다.
- 선분은 색상/선종류 차이를 반영해 점선 또는 연한 파란색 계열로 표시된 것으로 처리하면 좋다.
- 아래 문장은 텍스트 블록으로 따로 두고, 괄호와 쉼표 간격을 원문 배치에 맞게 유지하는 편이 좋다.
- `(정답)`과 `(해설)`은 별도 텍스트 박스로 좌측 하단에 배치하는 것이 자연스럽다.

[주의할 점]
- 답을 추론해서 특정 기호를 채워 넣지 말 것.
- 그림 속 선분이 실제로 정확히 어느 위치에서 끝나는지, 중심을 지나는지 여부는 이미지 해상도상 일부 애매할 수 있으므로 단정하지 말 것.
- `ㄱ`, `ㄴ`, `ㄷ`의 위치가 원의 어느 선분에 대응되는지는 시각적 연결만 설명하고, 정답 판정은 하지 말 것.
- `(정답)`과 `(해설)`에 적힌 내용은 화면에 보이는 텍스트로만 기록하고, 이를 이용해 문제를 푸는 설명은 피할 것.

[불확실한 부분]
- 원 내부의 작은 중심 표기가 정확히 `O`인지, 또는 점과 문자 표기가 겹쳐 보이는 것인지 해상도상 다소 불확실하다.
- 일부 선분의 끝점이 원 둘레와 정확히 맞닿는지, 아니면 약간 안쪽에서 끝나는지 구분이 흐릿하다.
- 문제 상단의 첫 줄 끝부분은 줄바꿈 때문에 `보세`와 `요.`처럼 나뉘어 보여서, 원문 전체가 한 줄인지 두 줄인지 화면상 약간 애매하다.
- `(정답) ㄷ, ㄴ`, `(해설) 원의 반지름 ㄷ, 원의 지름 ㄴ`은 화면에 보이지만, 이것이 문제 본문에 포함된 것인지 별도 해설 표시인지 맥락상 완전히 분리되지는 않는다.

--- RAW VISION DRAFT END ---
