# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_008708.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_008708

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
[전체적인 구성]
- 화면 상단 왼쪽에 문제 번호와 지시 문장이 있고, 중앙과 하단에 원 그림 2개가 배치되어 있습니다.
- 왼쪽 중간에는 선택 표시를 위한 기호 예시가 세로로 놓여 있고, 그 아래에 `(정답)` 표시와 빈 원형 선택칸이 있습니다.
- 하단 왼쪽에는 `(해설)` 표시가 붙은 원 그림이 있으며, 그 아래에 해설 문장이 한 줄로 적혀 있습니다.
- 전체적으로 “문장 + 그림 + 선택 표시 + 해설” 구조입니다.

[보이는 텍스트]
- `□ 3.`  
  - 문제 번호 앞에 작은 빈 사각형이 보입니다.
- `원의 지름을 그림과 같이 그렸습니다. 바르게 그렸으면 ○, 틀리게 그렸으면 ×표를 선택하세요.`
  - 문장이 두 줄로 나뉘어 보입니다.
- `○`
- `×`
- `(정답)`
- `(해설)`
- `원의 지름을 바르게 그렸습니다.`
  - 하단 해설 문장입니다.

[반복되는 시각 패턴]
- 원 모양이 2개 반복됩니다.
  - 위쪽 원: 화면 중앙 위쪽에 배치.
  - 아래쪽 원: 화면 왼쪽 아래쪽에 배치.
- 두 원 모두 내부에 가로선이 하나 그어져 있습니다.
- 두 원 모두 가로선 중앙 부근에 작은 점이 2개 보입니다.
  - 하나는 회색/검은색의 작은 빈 원처럼 보이는 점.
  - 하나는 분홍색의 작은 점.
- 왼쪽 중간에는 `○`, `×`가 세로로 정렬되어 있습니다.
- `(정답)` 옆에는 선택칸처럼 보이는 작은 빈 원이 있습니다.

[수식 단계]
- 해당 없음. 수식 변형이나 등호 연결은 보이지 않습니다.

[등호별 변환 의미]
- 해당 없음.

[세부 내용]
- 상단의 지시 문장은 “그림에서 원의 지름을 바르게 그렸는지 판단하여 ○ 또는 ×를 고르라”는 의미로 보입니다.  
  - 다만 이는 문제의 의도에 대한 해석이며, 실제 풀이 결과는 적지 않습니다.
- 위쪽 원은 화면 중앙 상단에 있으며, 원 내부를 좌우로 가로지르는 선이 있습니다.
- 아래쪽 원도 같은 형태로, 해설용 예시처럼 보입니다.
- 각 원의 가로선은 원의 중심을 지나가는 것처럼 배치되어 보이지만, 이것이 정확히 어떤 학습 포인트인지 단정하지는 않습니다.
- `(정답)` 표시는 왼쪽 중간의 기호 예시 아래에 있고, 그 오른쪽에 빈 원형 칸이 있어 정답 표시를 넣는 자리처럼 보입니다.
- 하단의 `(해설)` 표시는 아래 원의 왼쪽에 붙어 있으며, 해설용 그림과 문장이 함께 배치되어 있습니다.

[Python DSL 작성 힌트]
- 텍스트 요소를 상단 왼쪽에 먼저 배치하고, 문장이 두 줄로 줄바꿈되는 형태를 반영하세요.
- 원 도형은 2개를 별도 객체로 배치하되, 크기와 선 스타일을 유사하게 맞추세요.
- 각 원 내부의 가로선은 원의 지름처럼 보이는 직선 요소로 표현하되, 원과 정확히 겹치도록 중앙 정렬을 고려하세요.
- 원 내부의 작은 점 2개는 중앙 부근에 서로 가까운 위치로 배치하세요.
- 왼쪽 중간의 `○`, `×`는 세로로 간격을 두고 정렬하세요.
- `(정답)` 텍스트 옆의 빈 원형 칸은 별도 선택 표시 요소로 작성하세요.
- `(해설)` 텍스트와 아래 원 그림, 하단 문장은 서로 묶인 해설 블록처럼 처리하면 좋습니다.

[주의할 점]
- 답을 추론하여 `○` 또는 `×`를 채워 넣지 마세요.
- `지름`이 맞는지 여부를 실제로 판정하지 마세요.
- 원 내부의 작은 점이 정확히 어떤 의미인지 단정하지 마세요.
- 상단 문제 문장이 두 줄로 보이는 점을 유지하세요.
- 도형의 위치 관계를 놓치지 말고, 위쪽 문제 그림과 아래쪽 해설 그림을 서로 다른 블록으로 분리하세요.

[불확실한 부분]
- 위쪽 원과 아래쪽 원의 미세한 선 두께, 점 색상, 정확한 중심 정렬은 이미지 해상도 때문에 다소 불명확합니다.
- `(정답)` 옆의 빈 원형 칸이 실제 선택칸인지, 단순 표시용인지 화면만으로는 완전히 확정하기 어렵습니다.
- 왼쪽의 `○`, `×` 표시는 선택지 예시인지, 인터페이스 기호인지 문맥상 해석이 가능하지만, 화면 요소만으로는 용도가 완전히 명시되지는 않습니다.

--- RAW VISION DRAFT END ---
