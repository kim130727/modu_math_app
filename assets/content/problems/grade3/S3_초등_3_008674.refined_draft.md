[문제 개요]
- 문제 번호는 `55.`로 보인다.
- 지시문은 `원을 바르게 완성한 것을 선택하세요.`로 보인다.
- 보기들은 원의 일부가 그려진 정사각형 그림 여러 개로 구성되어 있다.
- 화면 아래쪽에는 `(정답)` 표시가 붙은 그림이 별도로 보이고, 맨 아래에는 `(해설)` 문장이 한 줄 있다.
- 이 문항은 계산식 문제가 아니라, 도형의 완성 상태를 고르는 선택형 문제로 보인다.

[원본에 보이는 요소]
- 상단 왼쪽에 작은 체크박스 모양 기호와 `55.`가 보인다.
- 그 오른쪽에 `원을 바르게 완성한 것을 선택하세요.`라는 문장이 한 줄로 배치되어 있다.
- 중앙에는 노란색 테두리의 정사각형 안에 원이 들어간 그림들이 여러 개 있다.
- 각 그림 안에는 검은색 원호와 분홍색 중심점이 보인다.
- 일부 그림에는 빨간색 선이 덧그려져 있다.
- 아래 왼쪽 그림 옆에는 `(정답)`이 보인다.
- 맨 아래에는 `(해설)원을 그릴 때에는 누름 못을 이동하지 않고, 시작점과 끝점이 만나도록 그립니다.`가 보인다.
- 그림 내부에는 숫자나 추가 문자는 보이지 않는다.

[수식/관계 구조]
- 수식 구조는 보이지 않는다.
- 등호, 분수, 계산식, 단계식은 없다.
- 대신 도형의 완성 여부를 비교하는 시각적 관계가 중심이다.
- 공통 구조:
  - 정사각형 테두리
  - 원의 일부를 나타내는 검은 곡선
  - 중심처럼 보이는 분홍 점
  - 일부 보기에서만 빨간 보조 곡선 또는 직선

[등호별 변환 의미]
- 해당 없음.
- 등호 연쇄나 식 변환이 보이지 않는다.
- 도형 비교 문제이므로 단계적 변형 설명은 없다.

[빈칸 정책]
- 빈칸에 해당하는 수학적 입력란은 보이지 않는다.
- 따라서 빈칸을 추론해서 채우지 않는다.
- 정답처럼 보이는 그림이 있어도, 이를 풀이 결과로 확정해 서술하지 않고 `정답 표시가 보인다` 수준의 관찰만 적는다.

[반복 패턴]
- 모든 보기에서 노란색 정사각형 테두리와 원형 도형의 기본 틀이 반복된다.
- 검은색 곡선은 원의 대부분을 이루는 공통 요소로 반복된다.
- 분홍색 점은 각 그림의 중심 부근에 반복적으로 배치되어 있다.
- 빨간색 선은 보기마다 다르게 나타나며, 원의 부족한 부분을 보완하는 것처럼 보이기도 하고, 직선처럼 보이기도 한다.
- 이러한 반복 패턴은 `어떤 그림이 원을 바르게 완성했는지`를 비교하도록 유도한다.

[DSL 작성 힌트]
- 화면을 크게 `상단 문제번호/지시문`, `보기 그림들`, `정답 표시`, `해설 문장`의 4개 구역으로 나누면 좋다.
- 보기 그림들은 동일한 크기의 정사각형 컴포넌트로 반복 배치한다.
- 각 정사각형에는 공통으로:
  - 노란색 테두리
  - 검은색 원호
  - 분홍색 중심점
  을 넣는다.
- 보기별 차이는 빨간색 선의 형태와 위치이므로, 빨간 요소는 각 그림마다 별도 지정하는 편이 좋다.
- `(정답)` 표시는 아래 왼쪽 그림의 좌측에 작은 텍스트로 붙어 있다.
- `(해설)` 문장은 하단에 한 줄로 배치한다.
- 정사각형 테두리, 원호, 중심점은 공통 스타일로 묶고, 빨간 선만 변형 요소로 분리하는 구성이 적합하다.
- 렌더링 시 원호의 시작·끝 위치가 중요한 도형 문제이므로, 곡선의 연결 지점이 잘 보이도록 간격을 확보하는 것이 좋다.
- 분홍 점은 각 그림의 거의 중앙에 놓여 있으며, 너무 크게 그리지 않는 편이 자연스럽다.

[semantic/solvable 힌트]
- 이 문항은 원을 그리는 방법을 이해하고, 제시된 그림 중 원이 바르게 완성된 모습을 찾는 유형으로 보인다.
- 해설 문장에 따르면 원을 그릴 때에는 누름 못을 이동하지 않고 시작점과 끝점이 만나도록 그린다고 설명한다.
- 따라서 그림 해석의 핵심은 `끊긴 원호가 자연스럽게 이어져 원이 되는가`를 시각적으로 비교하는 것이다.
- 다만 이 항목은 문제 풀이를 대신하는 것이 아니라, 문항의 의미를 설명하는 수준으로만 기록한다.

[주의 및 TODO]
- 정답을 추론해서 확정하지 말 것.
- 보이는 `(정답)` 표시는 관찰 사실로만 적고, 풀이 결과로 단정하지 말 것.
- 상단 첫 그림이 보기인지 예시인지가 화면 구성상 약간 애매하므로 TODO로 남긴다.
- 빨간 선의 정확한 시작점과 끝점은 해상도에 따라 다소 달라 보일 수 있으므로 단정하지 말 것.
- 노란 테두리의 정확한 두께, 원호의 정확한 각도, 중심점의 좌표는 추정하지 말고 시각적 표현 수준으로만 서술할 것.
- 해설 문장의 정확한 띄어쓰기와 구두점은 OCR 오차 가능성이 있으므로, 필요 시 원본 이미지 기준으로 재확인한다.
