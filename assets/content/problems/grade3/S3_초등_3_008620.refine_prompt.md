# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_008620.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_008620

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
[전체적인 구성]
- 화면 상단 왼쪽에 작은 체크박스 모양과 함께 문제 번호 `90.`이 있고, 그 오른쪽에 문제 지시문이 한 줄로 배치되어 있습니다.
- 지시문 아래에는 연한 하늘색 직사각형 박스가 가로로 길게 놓여 있고, 그 안에 세 개의 나눗셈 식이 좌에서 우로 배치되어 있습니다.
- 하단에는 `(정답)` 표시와 함께 한 식이 따로 제시되어 있으며, 그 아래쪽에는 `(해설)` 표시와 함께 세 개의 세로셈 풀이가 나란히 배열되어 있습니다.
- 전체적으로 위쪽은 선택할 문제, 아래쪽은 정답과 계산 예시/풀이를 보여주는 구조입니다.

[보이는 텍스트]
- 상단 지시문: `90. 묶이 70보다 큰 것을 선택해 보세요.`
- 하늘색 박스 안의 식:
  - `268 ÷ 5`
  - `402 ÷ 4`
  - `397 ÷ 6`
- 중간의 정답 표기: `(정답)402 ÷ 4`
- 하단의 해설 표기: `(해설)`
- 세로셈에 보이는 수:
  - 왼쪽 풀이: `5 ) 268`, 몫으로 보이는 `53`, 중간 계산 `25`, `18`, `15`, 마지막 나머지처럼 보이는 `3`
  - 가운데 풀이: `4 ) 402`, 몫으로 보이는 `100`, 중간 계산 `4`, 마지막 숫자 `2`
  - 오른쪽 풀이: `6 ) 397`, 몫으로 보이는 `66`, 중간 계산 `36`, `37`, `36`, 마지막 숫자 `1`

[반복되는 시각 패턴]
- 하늘색 직사각형 박스 안에 문제 후보 3개가 같은 크기의 글씨로 균등하게 배치되어 있습니다.
- 각 후보는 모두 `세 자리 수 ÷ 한 자리 수` 형태의 나눗셈입니다.
- 아래 해설 부분에서는 세로셈 형식이 반복됩니다.
  - 왼쪽에 나누는 수,
  - 오른쪽 괄호 뒤에 피제수,
  - 그 위쪽에 몫,
  - 아래에 곱셈/뺄셈 중간 수가 단계적으로 내려가며 배치됩니다.
- 각 세로셈은 가로선이 여러 번 그어져 있어 계산 단계가 구분됩니다.
- 세 개의 해설이 가로 방향으로 나란히 정렬되어 있어, 같은 형식의 풀이를 비교하는 구조처럼 보입니다.

[수식 단계]
- 해설 영역에는 등호로 이어진 변형식은 보이지 않고, 세로셈의 단계별 나눗셈 과정만 보입니다.
- 각 풀이마다 몫과 중간 계산 결과가 위아래로 배치되어 있으며, 가로선으로 단계가 구분됩니다.
- 따라서 이 문제에서는 `=`, `→` 같은 변형 기호보다는 세로셈 배치 자체가 단계 표시 역할을 합니다.

[등호별 변환 의미]
- 해당 없음.
- 화면에 등호를 이용한 연속 변형이 보이지 않습니다.

[세부 내용]
- 상단 지시문은 “70보다 큰 묶음”을 고르는 형태의 조건 제시문으로 읽힙니다.
- 하늘색 박스 안의 세 항목은 선택지처럼 보이며, 각각 독립된 나눗셈 식으로 제시되어 있습니다.
- `(정답)` 표기는 중간 아래에 따로 있고, 그 뒤에 `402 ÷ 4`가 이어져 있어 선택 결과를 직접 표시한 구성으로 보입니다.
- `(해설)` 아래에는 세 선택지 각각에 대응하는 세로셈 풀이가 작은 크기로 제시되어 있습니다.
- 왼쪽 풀이에서는 `268 ÷ 5`에 대한 세로셈이, 가운데 풀이에서는 `402 ÷ 4`, 오른쪽 풀이에서는 `397 ÷ 6`의 세로셈이 각각 대응되는 것으로 보입니다.
- 세로셈 내부의 숫자 배치는 다음처럼 보입니다.
  - 몫은 나눗셈 기호 위쪽에,
  - 곱해서 빼는 수는 피제수 아래쪽에,
  - 다음 단계로 내려가는 수는 그 아래에 순차적으로 배치됩니다.
- 다만 일부 숫자는 작은 크기라 간격상 붙어 보일 수 있습니다.

[Python DSL 작성 힌트]
- 상단 문제문은 좌측 정렬 텍스트로 배치하고, 문제 번호 `90.`을 문장 앞에 포함하세요.
- 문제문 아래에 연한 하늘색 직사각형 박스를 가로로 길게 그린 뒤, 내부에 3개의 선택지를 좌우 균등 배치하세요.
- 선택지 텍스트는 박스 중앙 세로축에 맞추어 같은 기준선에 두는 것이 자연스럽습니다.
- 박스 아래에는 `(정답)` 텍스트와 정답 식을 한 줄로 배치하세요.
- 그 아래 `(해설)` 텍스트를 두고, 세로셈 3개를 가로로 나란히 배치하세요.
- 세로셈은 각각:
  - 나누는 수,
  - 괄호 뒤 피제수,
  - 몫,
  - 중간 계산 숫자,
  - 가로선
  을 포함하는 형태로 구성하세요.
- 세로셈의 단계선은 여러 개의 수평선으로 표현하고, 각 계산 단계 숫자는 오른쪽/아래쪽에 정렬되도록 처리하세요.
- 선택지와 해설이 서로 대응되도록 좌우 순서를 유지하세요.

[주의할 점]
- 이 문제는 풀이는 요구되지 않으므로, 어느 식이 왜 70보다 큰지 설명하거나 계산 결과를 확정해서 쓰지 마세요.
- `(정답)` 표기는 이미지에 이미 존재하는 텍스트이므로, DSL에서도 별도로 텍스트로 넣을 수 있지만 해설을 새로 생성하지는 마세요.
- 세로셈의 몫과 중간 수는 작고 일부 겹쳐 보일 수 있으니, 정확한 값은 이미지 기반으로 신중하게 배치해야 합니다.
- 하늘색 박스는 단순 배경 도형이며, 테두리는 두드러지지 않습니다.
- 선택지 사이에 추가 기호나 구분선은 보이지 않습니다.

[불확실한 부분]
- 왼쪽과 오른쪽 세로셈에서 일부 숫자는 해상도 때문에 약간 흐릿하게 보일 수 있습니다.
- 가운데 풀이의 몫 표기와 아래 숫자 배치가 작아서, 세부 정렬은 이미지와 약간 다를 수 있습니다.
- 상단 지시문의 첫 단어는 `묶이`처럼 보이지만, 인쇄체 특성상 일부 글자는 다르게 읽힐 가능성이 있습니다.

--- RAW VISION DRAFT END ---
