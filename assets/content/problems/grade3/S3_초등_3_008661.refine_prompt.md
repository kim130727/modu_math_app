# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_008661.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_008661

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
[전체적인 구성]
- 화면 왼쪽 위에 문제 번호와 질문 문장이 있고, 오른쪽 위에는 원과 여러 개의 가로선으로 이루어진 도형이 배치되어 있습니다.
- 화면 왼쪽 아래와 오른쪽 아래에는 보기 4개가 2열 형태로 배치되어 있습니다.
- 맨 아래에는 `(정답) ②`가 있고, 그 아래에 해설 문장이 한 줄로 있습니다.
- 전체적으로 **문항 본문 + 도형 + 선택지 + 정답/해설** 구조입니다.

[보이는 텍스트]
- `□ 36. 길이가 가장 긴 선분은 어느 것인가요?`
- 보기:
  - `① 선분 ㄱㄴ`
  - `② 선분 ㄷㄹ`
  - `③ 선분 ㅁㅂ`
  - `④ 선분 스ㅅ`
- ` (정답) ②`
- `(해설)중심을 지나는 선분 ㄷㄹ이 길이가 가장 긴 선분입니다.`
- 도형 주변 문자:
  - 위쪽 왼쪽: `ㄱ`
  - 위쪽 오른쪽: `ㄴ`
  - 가운데 왼쪽: `ㄷ`
  - 가운데 오른쪽: `ㄹ`
  - 그 아래 왼쪽: `ㅁ`
  - 그 아래 오른쪽: `ㅂ`
  - 아래쪽 왼쪽: `스`
  - 아래쪽 오른쪽: `ㅅ`
  - 도형 중앙 근처에 `ㅇ`처럼 보이는 작은 원형 표시가 있음

[반복되는 시각 패턴]
- 원 안에 **수평선 4개**가 위에서 아래로 거의 평행하게 그려져 있습니다.
- 각 수평선은 원의 왼쪽 경계와 오른쪽 경계를 가로지르며, 양 끝에 한글 자모가 붙어 있습니다.
- 위에서부터 각각 다른 위치의 선분으로 보이며, 가운데 두 번째 수평선은 중앙을 지나는 선처럼 배치되어 있습니다.
- 중앙부에는 작은 점/원 표시가 있어 중심을 나타내는 것으로 보입니다.
- 보기에서는 `선분`이라는 공통 표현 뒤에 서로 다른 두 글자 묶음이 반복됩니다.

[수식 단계]
- 해당 없음.  
- 화면에는 계산식, 등식 변형, 분수식, 덧셈/뺄셈식 등의 단계가 보이지 않습니다.

[등호별 변환 의미]
- 해당 없음.  
- 등호로 이어진 변형 과정이 제시되어 있지 않습니다.

[세부 내용]
- 오른쪽 도형은 **원** 하나가 크게 그려져 있고, 그 원 내부를 가로지르는 **직선형 선분들**이 위아래로 배열되어 있습니다.
- 가장 위의 선분은 원의 윗부분을 가로지르며 왼쪽 끝에 `ㄱ`, 오른쪽 끝에 `ㄴ`이 표시되어 있습니다.
- 두 번째 선분은 원의 중앙 부근을 통과하며 왼쪽 끝에 `ㄷ`, 오른쪽 끝에 `ㄹ`이 표시되어 있습니다. 이 선분의 중앙 근처에는 작은 점(또는 작은 원) 표시가 있습니다.
- 세 번째 선분은 그 아래에 있으며 왼쪽 끝에 `ㅁ`, 오른쪽 끝에 `ㅂ`이 있습니다.
- 가장 아래 선분은 원의 아랫부분을 가로지르며 왼쪽 끝에 `스`, 오른쪽 끝에 `ㅅ`이 표시되어 있습니다.
- 보기 4개는 좌우 두 열로 나뉘어 배치되어 있고, 각 보기는 앞에 파란색 동그라미 숫자가 붙어 있습니다.
- `(정답) ②`는 보기 아래쪽 왼편에 따로 표기되어 있습니다.
- 해설은 정답 표시 바로 아래에 있으며, “중심을 지나는 선분”이라는 표현이 포함되어 있습니다.

[Python DSL 작성 힌트]
- 문제 본문 텍스트와 선택지 4개를 별도 블록으로 분리해서 배치하면 좋습니다.
- 도형은 다음 요소로 나누어 표현하면 됩니다:
  - 큰 원 1개
  - 원 내부의 수평 선분 4개
  - 각 선분의 양 끝 라벨 8개
  - 중심 표시용 작은 점/원 1개
- 선분들은 서로 거의 평행하며 위아래 간격이 일정하지 않게 배치된 점을 반영하면 좋습니다.
- 보기 영역은 2열 2행 구조로 정렬하는 것이 자연스럽습니다.
- 정답 및 해설 문구는 하단에 작은 글씨 영역처럼 따로 배치하는 방식이 적절합니다.

[주의할 점]
- 선분의 길이를 실제로 비교해 값을 쓰지 마세요.
- 중앙을 지나는 선분이 무엇인지 추론 과정으로 서술할 수는 있지만, 답을 계산하거나 단정적으로 재작성하지는 마세요.
- `스`와 `ㅅ`은 글자 모양이 비슷할 수 있으므로, 화면에서 보이는 대로 정확히 옮겨 적어야 합니다.
- 도형 중앙의 작은 표시가 글자인지 점인지 애매할 수 있으므로, “작은 원/점 표시”처럼 유보적으로 기록하는 것이 안전합니다.
- 문제 번호 앞의 작은 네모(□)는 체크박스처럼 보일 수 있으나, 의미는 단정하지 않는 것이 좋습니다.

[불확실한 부분]
- 원 안 중앙의 표시가 정확히 `ㅇ`인지, 작은 점인지, 혹은 편집용 표시인지 화면만으로는 확실하지 않습니다.
- 가장 아래 라벨의 왼쪽 글자는 `스`처럼 보이지만, 인쇄체 특성상 다른 자모일 가능성을 완전히 배제할 수는 없습니다.
- 보기의 파란 동그라미 숫자와 실제 인쇄 위치가 약간 흐릿하여, 세부 정렬은 원본 해상도에 따라 달라질 수 있습니다.

--- RAW VISION DRAFT END ---
