[문제 개요]
- 문제 번호는 `□ 50.`으로 보인다.
- 문장형 문제이다.
- 주제는 `누름 못과 띠 종이를 사용하여 원을 그리는 방법`이다.
- 원을 가장 크게 그리기 위해 `어느 구멍에 연필을 꽂아야 하는지`를 묻고, `알맞은 기호를 선택`하라고 되어 있다.
- 하단에 `(정답)`과 `(해설)`이 함께 보인다.
- 이미지 기준으로 정답 표시는 `ㄹ`로 보인다.

[원본에 보이는 요소]
- 상단에 문제 문장이 2줄로 배치되어 있다.
- 가운데에 연한 청록색의 가로로 긴 띠 모양 그림이 있다.
- 띠의 왼쪽에는 회색의 작은 누름 못처럼 보이는 도형이 있다.
- 띠 위쪽에는 작은 원형 표시가 4개 보인다.
- 각 원형 표시 아래쪽에는 분홍색 세로 화살표가 내려와 있다.
- 화살표 아래에는 원 안에 들어간 선택지 기호가 4개 있다.
- 선택지 기호는 왼쪽부터 `ㄱ, ㄴ, ㄷ, ㄹ` 순서로 보인다.
- 하단 왼쪽에 `(정답) ㄹ`이 보인다.
- 그 아래에 `(해설) 누름 못과 연필심 사이가 많이 벌어질수록 원을 크게 그릴 수 있습니다.`가 보인다.

[수식/관계 구조]
- 수식이나 등호로 이어지는 계산식은 보이지 않는다.
- 관계는 계산식이 아니라 `거리-크기`의 대응 구조로 보인다.
- 보이는 구조는 다음과 같다.
  - 왼쪽의 고정점처럼 보이는 누름 못
  - 오른쪽으로 이어지는 여러 구멍 위치
  - 각 구멍에 대응하는 선택지 기호
- 추론되는 수학적 의미:
  - 누름 못과 연필심 사이의 거리가 클수록 그리는 원의 크기가 커진다.
- 다만 이 의미는 해설 문장에 근거한 관찰된 설명이며, 추가 계산은 없다.

[등호별 변환 의미]
- 해당 없음.
- 등호를 포함한 변형 과정이나 식의 단계적 전개는 없다.

[빈칸 정책]
- 보이는 빈칸이나 미기입 칸을 추론값으로 채우지 않는다.
- `□ 50.`의 작은 사각형은 의미를 단정하지 않고, 보이는 그대로 유지한다.
- 정답이 이미지에 인쇄되어 보이므로, 그 사실만 관찰 사실로 기록한다.
- 그러나 문제 풀이를 새로 계산해 빈칸을 채우는 방식은 사용하지 않는다.

[반복 패턴]
- 구멍 표시가 좌우로 반복되는 구조이다.
- 선택지 기호도 좌→우로 `ㄱ, ㄴ, ㄷ, ㄹ` 순서로 반복 배치되어 있다.
- 각 구멍 아래에 분홍색 세로 화살표가 하나씩 대응된다.
- 화살표와 기호의 대응이 4회 반복되어 보인다.
- 전체 패턴은 `구멍 4개 + 화살표 4개 + 원형 라벨 4개`의 반복 구조이다.

[DSL 작성 힌트]
- 상단 문장은 문제 번호와 본문 문장을 분리해 배치하면 좋다.
- 본문은 2줄 줄바꿈을 반영해도 자연스럽다.
- 가운데 그림은 다음 요소로 나누어 표현하기 좋다.
  - 긴 가로 띠 1개
  - 왼쪽의 회색 누름 못처럼 보이는 작은 도형 1개
  - 상단의 작은 원형 구멍 표시 4개
  - 각 구멍에서 아래로 내려오는 분홍색 세로 화살표 4개
  - 화살표 아래의 원형 선택지 라벨 4개
- 선택지 라벨은 원 안에 넣고, 좌→우로 `ㄱ, ㄴ, ㄷ, ㄹ`을 배치하는 것이 자연스럽다.
- 정답 표시는 별도 텍스트로 `(정답) ㄹ` 형태로 표시하면 된다.
- 해설 문장은 하단 한 줄 텍스트로 배치하면 된다.
- 레이아웃은 `상단 문제문장 / 중앙 그림 / 하단 정답·해설`의 3단 구조로 잡으면 좋다.
- 그림 안의 화살표는 각 구멍과 선택지의 대응을 보여주는 요소로 취급하면 된다.

[semantic/solvable 힌트]
- 해설 문장에 의해, 핵심 의미는 `누름 못과 연필심 사이가 더 멀수록 더 큰 원`이다.
- 따라서 선택지는 `가장 멀어 보이는 구멍`에 대응하는 기호를 고르는 문제로 해석된다.
- 이미지에는 정답 표시가 보이므로, 정답 사실은 `ㄹ`로 관찰된다.
- 다만 새로 추론하여 정답을 계산하는 절차는 이 문서에서 수행하지 않는다.

[주의 및 TODO]
- 왼쪽 회색 도형의 정확한 명칭은 그림만으로 완전히 단정하지 말 것.
- 상단의 작은 원형 표시가 실제 구멍인지, 도식적 표식인지 엄밀히는 불확실하므로 `구멍 표시` 또는 `구멍처럼 보이는 표식`으로 다루는 것이 안전하다.
- `□ 50.`의 사각형은 체크박스인지 문제 번호 표기의 일부인지 확정하지 말고 TODO로 남길 수 있다.
- 정답 `ㄹ`은 이미지에 인쇄되어 보이는 사실로만 기록하고, 풀이로 재도출하지 않는다.
- 등호식, 분수식, 변환식은 없으므로 수식화해서 과장하지 말 것.
- 좌우 정렬, 간격, 화살표 방향, 라벨 원형 테두리 등 시각적 배치 정보는 DSL에서 중요하므로 누락하지 않도록 주의한다.
