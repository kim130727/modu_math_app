# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_008709.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_008709

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
[전체적인 구성]
- 화면 상단에 문제 번호와 질문 문장이 1줄로 배치되어 있습니다.
- 중앙 상단에는 원 그림이 있고, 원 안팎에 여러 글자 표지가 붙어 있습니다.
- 원 아래쪽에는 연두색 테두리의 큰 설명 박스가 있으며, 그 안에 빈칸 2개가 들어간 문장이 2줄로 배치되어 있습니다.
- 설명 박스 아래에는 선택지 4개가 2열 구조로 배치되어 있습니다.
- 더 아래에는 `(정답)` 표시와 `(해설)` 문장이 이어집니다.
- 전체적으로 “원의 지름” 개념을 고르는 객관식 문제 형식입니다.

[보이는 텍스트]
- 문제 번호와 문장:
  - `□ 5. □ 안에 공통으로 들어갈 선분으로 알맞은 것은 무엇인가요?`
- 원 그림 주변의 글자:
  - `ㄱ`, `ㄷ`, `ㅁ`, `ㄴ`, `ㄹ`, `ㅂ`, `ㅅ`, `ㅈ`
  - 원 안쪽에 `ㅇ`처럼 보이는 작은 표식이 있음
- 설명 박스 안 문장:
  - `선분 [빈칸] 은 원 안에 그을 수 있는 가장 긴 선분입니다.`
  - 화살표 기호 `→`
  - `원의 지름은 선분 [빈칸] 입니다.`
- 선택지:
  - `① ㄱㄴ`
  - `② ㄷㅁ`
  - `③ ㅁㅂ`
  - `④ ㅅㅈ`
- 하단 표기:
  - `(정답)③`
  - `(해설)지름은 원 안에 그을 수 있는 가장 긴 선분이므로 선분 ㅁㅂ입니다.`

[반복되는 시각 패턴]
- 같은 유형의 빈칸이 2개 있습니다.
  - 첫 번째 빈칸은 “선분 [ ]” 뒤에 위치합니다.
  - 두 번째 빈칸은 “선분 [ ] 입니다”의 가운데에 위치합니다.
- 선택지는 모두 두 글자씩 이어진 선분 이름 형태입니다.
- 원 그림에는 원주를 따라 떨어진 여러 문자 표지가 반복적으로 배치되어 있습니다.
- 설명 박스 내부는 위 문장과 아래 문장으로 나뉘며, 가운데에 화살표가 있어 위 설명에서 아래 결론으로 이어지는 구조입니다.

[수식 단계] (해당될 때)
- 이 문제는 수식의 등호 변형이 아니라 문장 설명형 문제입니다.
- 등식 단계는 보이지 않습니다.
- 대신 개념 설명의 흐름이 있습니다:
  - 위 문장: “원 안에 그을 수 있는 가장 긴 선분”
  - 아래 문장: “원의 지름은 선분 [ ]”
- 따라서 단계적 변환이라기보다, 일반 설명에서 용어를 연결하는 구성입니다.

[등호별 변환 의미] (해당될 때)
- 등호가 없으므로 해당 사항이 없습니다.
- 보이는 의미상 연결은 다음과 같습니다.
  - “가장 긴 선분”이라는 설명을
  - “지름”이라는 용어와 연결하고 있습니다.
- 다만 이는 풀이가 아니라 화면에 제시된 설명 구조를 그대로 읽은 것입니다.

[세부 내용]
- 원 그림:
  - 원이 비교적 크게 그려져 있고, 내부에 기울어진 여러 선분이 평행하게 보입니다.
  - 원의 중심으로 보이는 작은 점 표시가 있으며, 그 근처에 분홍색 점도 함께 보입니다.
  - 원의 위쪽, 아래쪽, 왼쪽, 오른쪽 주변에 ㄱ·ㄷ·ㅁ·ㄴ·ㄹ·ㅂ·ㅅ·ㅈ 같은 한글 자모가 각각 배치되어 있습니다.
  - 그중 일부는 원 둘레 바깥쪽에 붙어 있고, 일부는 원 내부 선분의 끝점 근처에 있는 듯 보입니다.
- 설명 박스:
  - 연두색 테두리의 직사각형 박스입니다.
  - 박스 안 첫 문장은 왼쪽에 “선분”이 있고, 그 뒤에 작은 직사각형 빈칸이 1개 있습니다.
  - 두 번째 문장은 회색 화살표 `→` 다음에 시작하며, “원의 지름은 선분” 뒤에 또 다른 직사각형 빈칸이 있습니다.
  - 두 빈칸 모두 검은 테두리의 작은 입력 칸처럼 보입니다.
- 선택지 배치:
  - 왼쪽 열: ①, ③
  - 오른쪽 열: ②, ④
  - 각 번호는 파란색 원형 표시 안에 들어가 있습니다.
  - 번호 오른쪽에 선택한 선분 이름이 있습니다.
- 하단:
  - `(정답)③`이 보이지만, DSL 작성 시에는 정답을 그대로 채우는 용도로 쓰지 않는 것이 좋습니다.
  - 해설 문장은 문제 의도를 확인하는 참고 텍스트입니다.

[Python DSL 작성 힌트]
- 큰 구성요소를 순서대로 배치하면 좋습니다:
  1. 상단 문제 문장
  2. 원 도형과 주변 문자 라벨
  3. 연두색 테두리의 설명 박스
  4. 4지선다 선택지
  5. 정답/해설 텍스트
- 원 도형은:
  - 원 1개
  - 원 내부의 기울어진 선분 여러 개
  - 중심점 표시 1개
  - 주변 자모 라벨 여러 개
  로 분리해서 그리면 좋습니다.
- 설명 박스는:
  - 바깥 사각형 1개
  - 내부 문장 2줄
  - 첫 줄의 빈칸 1개
  - 가운데 화살표 1개
  - 둘째 줄의 빈칸 1개
  로 나누어 구성하면 됩니다.
- 선택지는 2열 2행으로 배치하는 형태가 적절합니다.
- 각 빈칸은 텍스트 치환 슬롯으로 처리하고, 실제 답은 채우지 말고 placeholder로 두는 것이 안전합니다.

[주의할 점]
- 빈칸에 들어갈 선분 이름을 추론해서 채우지 마세요.
- `(정답)③`이 보이더라도, 시각 분석 초안에서는 정답을 완성형으로 쓰지 마세요.
- 원 내부 선분들이 서로 비슷하게 기울어져 있어, 어떤 것이 정확히 지름인지 단정하지 말고 보이는 그대로 묘사하세요.
- 자모 표식은 글자 크기와 위치가 작아서 일부는 원주 바깥/안쪽 경계에 걸쳐 보일 수 있습니다.
- “선분 [ ]”의 빈칸은 실제로는 짧은 직사각형 상자이며, 밑줄형이 아니라 박스형 입력칸으로 보입니다.
- 화살표는 설명 문장 사이의 연결 요소이지, 수식 변환 화살표는 아닙니다.

[불확실한 부분]
- 원 그림 안의 각 자모가 정확히 어느 선분의 양 끝점에 대응하는지 화면만으로는 일부 अस्प명확합니다.
- 중심점처럼 보이는 작은 원형 표시와 분홍 점의 정확한 의미는 문제 맥락상 추정만 가능하며 단정할 수 없습니다.
- 원 안의 여러 기울어진 선분 중 어떤 것이 “지름”인지 시각적으로 후보는 보이지만, 여기서는 결론을 확정하지 않습니다.
- 문제 제목의 첫 번째 네모와 문장 내 빈칸이 정확히 동일한 개념을 가리키는지, 화면 구성상은 같아 보이지만 문구 해석은 단정하지 않습니다.

--- RAW VISION DRAFT END ---
