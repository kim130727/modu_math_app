# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_008657.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_008657

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
[전체적인 구성]
- 화면 상단에 문제 번호와 지시문이 한 줄로 배치되어 있습니다.
- 그 아래 중앙에 연한 청록색 테두리의 큰 직사각형 박스가 있고, 그 안에 그림 선택지 3개가 가로로 나란히 배치되어 있습니다.
- 각 선택지는 왼쪽부터 `가`, `나`, `다`로 표시되어 있으며, 각각 원을 그리는 컴퍼스 그림 1개씩을 포함합니다.
- 큰 박스 아래쪽 왼편에 `(정답)나`가 보이고, 그 아래에 `(해설)` 문장이 이어집니다.
- 전체적으로 “보기 3개 중 올바른 것을 고르는” 객관식 형태입니다.

[보이는 텍스트]
- 상단 문제 문장:
  - `□ 32. 원을 그릴 때 컴퍼스를 바르게 사용하는 것을 찾아 선택해보세요.`
- 그림 안의 보기 표기:
  - `가`
  - `나`
  - `다`
- 하단 표시:
  - `(정답)나`
  - `(해설)컴퍼스의 침을 원의 중심에 꽂아야 하므로 컴퍼스를 바르게 사용한 것은 나입니다.`

[반복되는 시각 패턴]
- 세 보기 모두 같은 종류의 컴퍼스와 원 그림을 사용하고 있어, 동일한 도형을 세 가지 방식으로 비교하는 반복 패턴입니다.
- 각 보기마다:
  - 원 1개
  - 중심으로 보이는 작은 점 1개
  - 컴퍼스 1개
  - 컴퍼스의 한 다리는 침, 다른 다리는 연필심처럼 보이는 끝부분
- 세 그림은 컴퍼스의 꽂는 위치와 각 다리의 벌어진 방향만 다르게 표현되어 있습니다.

[수식 단계]
- 해당 없음: 수식, 등호, 변형식이 보이지 않습니다.

[등호별 변환 의미]
- 해당 없음: 등호를 이용한 단계적 변환이 없습니다.

[세부 내용]
- 큰 박스는 선택지 영역으로 보이며, 내부가 3개의 세로 구획처럼 나뉘어 보이지만 실제 선으로 구획된 것은 아니고 그림 배치로 구분됩니다.
- `가`
  - 왼쪽에 보기 글자 `가`가 있고, 그 오른쪽에 컴퍼스 그림이 있습니다.
  - 컴퍼스는 원 위쪽에 벌어진 상태로 놓여 있으며, 한쪽 끝이 원의 오른쪽 가장자리 부근에 닿아 있는 듯 보입니다.
  - 중심점처럼 보이는 분홍색 점은 원 내부 아래쪽 중앙쯤에 있습니다.
  - 컴퍼스의 침이 원의 중심점에 정확히 꽂힌 모습은 아닙니다.
- `나`
  - 보기 글자 `나`가 컴퍼스 그림의 왼쪽 위쪽에 위치합니다.
  - 컴퍼스의 침이 분홍색 점, 즉 원의 중심으로 보이는 위치에 꽂혀 있습니다.
  - 연필 부분은 원의 오른쪽 가장자리 쪽에 닿아 있어, 원을 그리는 바른 자세처럼 보입니다.
- `다`
  - 보기 글자 `다`가 왼쪽 위에 있고, 컴퍼스는 원의 왼쪽 쪽에 세워진 듯한 배치입니다.
  - 컴퍼스의 침이 중심점에 꽂힌 것처럼 보이지 않거나, 연필 쪽과 위치가 바뀐 듯한 형태로 보입니다.
  - 다른 보기와 비교하면 컴퍼스 사용 자세가 어색하게 표현되어 있습니다.
- 하단 해설은 “컴퍼스의 침을 원의 중심에 꽂아야” 한다는 규칙을 설명하며, 그 규칙에 맞는 보기가 `나`라고 안내합니다.
- 단, 이 해설은 정답 안내 문구로 보이며, 실제 DSL 작성 시에는 정답을 넣는 용도로 사용하지 않고 참고 정보로만 다루어야 합니다.

[Python DSL 작성 힌트]
- 큰 청록색 테두리 박스를 먼저 만들고, 그 안에 3개의 보기 이미지를 가로 정렬하는 구조가 적절합니다.
- 각 보기에는 다음 요소를 분리해 배치하면 좋습니다.
  - 보기 라벨 텍스트 `가/나/다`
  - 원 도형 1개
  - 중심점 역할의 작은 점 1개
  - 컴퍼스 도형 1개
- 컴퍼스는 단순 선분 2개와 상단 연결부, 아래쪽 침/연필 끝처럼 보이는 세부 요소로 나뉘어 표현할 수 있습니다.
- `나` 보기에서만 침이 중심점에 위치한 상태를 강조하는 식으로 그림을 구성하면 원본 배치와 유사해집니다.
- 하단의 `(정답)`, `(해설)` 문구는 별도 텍스트 블록으로 두고, 문제 본문과 시각적으로 분리하는 것이 좋습니다.
- 문제 번호 `32.` 앞의 작은 사각형 기호도 텍스트로 포함할지 여부를 검토하면 좋습니다.

[주의할 점]
- 정답이 `나`로 보이더라도, 이 초안에서는 문제를 푸는 서술을 길게 확장하지 말고 “보이는 안내문” 정도로만 처리하세요.
- `가/나/다`의 컴퍼스 자세는 사진 해상도상 미세한 선 위치가 다소 애매할 수 있으므로, 선의 정확한 각도보다 “중심에 침이 꽂혔는지 여부” 중심으로 설명하는 것이 안전합니다.
- 분홍색 점과 작은 원형 점이 함께 보이는데, 이것들이 각각 무엇을 의미하는지는 그림만으로 100% 단정하지 말고 “중심으로 보이는 점”, “작은 원 표시”처럼 서술하세요.
- 큰 박스의 테두리 색은 연한 청록색 계열로 보이지만, 정확한 색명은 단정하지 않아도 됩니다.
- 해설 문장은 하단에 인쇄된 글자이므로, 그림 요소와 혼동하지 않도록 별도 텍스트로 분리하세요.

[불확실한 부분]
- `가`, `나`, `다` 그림에서 컴퍼스의 각 다리와 원의 접점 위치는 일부 작게 보여 정확한 접촉점은 다소 불확실합니다.
- 원 내부의 분홍색 점과 아래의 작은 원형 점이 각각 어떤 표식인지 명확한 설명은 화면만으로 확정하기 어렵습니다.
- 왼쪽 상단의 작은 사각형 기호 `□`는 체크박스인지 문제 번호 장식인지 맥락상 애매할 수 있습니다.
- 큰 박스 내부의 세 보기 사이에 실제 구분선은 없는 것으로 보이지만, 배치상 구획처럼 느껴질 수 있어 구현 시 어느 정도 간격을 둘지 판단이 필요합니다.

--- RAW VISION DRAFT END ---
