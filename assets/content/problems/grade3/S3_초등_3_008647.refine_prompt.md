# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_008647.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_008647

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
[전체적인 구성]
- 문제 번호 **19.**가 좌상단에 있고, 그 오른쪽에 문제 지시문이 한 줄로 배치되어 있습니다.
- 오른쪽 상단에는 **원과 여러 선분이 그려진 도형**이 있고, 각 선분 위쪽/옆쪽에 작은 원형 표식으로 글자가 붙어 있습니다.
- 중간 왼쪽에는 **(정답) ㄷ** 표시가 따로 보입니다.
- 그 아래 왼쪽에는 **(해설)** 표기가 있고, 해설용으로 같은 유형의 원 그림이 한 번 더 제시되어 있습니다.
- 맨 아래에는 해설 문장이 한 줄(아래쪽은 다음 줄로 이어짐)로 적혀 있습니다.
- 전체적으로 **객관식/선택형 도형 문제**의 형태이며, 도형에서 가장 긴 선분을 찾는 내용으로 보입니다.

[보이는 텍스트]
- `□ 19.`
- `원 안의 선분 중 길이가 가장 긴 선분을 찾아 기호를 선택하세요.`
- 도형 위의 기호들: `ㄴ`, `ㄷ`, `ㄹ`, `ㅁ`, `ㄱ`  
  - 위 도형에서는 각 기호가 원 둘레의 특정 점 근처에 작은 원으로 표시되어 있습니다.
- `(정답) ㄷ`
- `(해설)`
- 아래 해설 그림 위의 기호: `ㄷ`
- 하단 해설 문장:
  - `원 안에 그을 수 있는 선분 중 길이가 가장 긴 선분은 원의 중심을 지나는 원의`
  - `지름이므로 ㄷ입니다.`
- 도형 내부에는 중심처럼 보이는 점 옆에 `ㅇ`처럼 보이는 작은 표기가 있습니다.  
  - 다만 이것이 실제 문자 표기인지, 중심 표시용 주석인지 화면상 약간 अस्प명합니다.

[반복되는 시각 패턴]
- **원형 도형이 두 번 반복**됩니다.
  - 첫 번째는 문제용 도형.
  - 두 번째는 해설용 도형.
- 두 도형 모두
  - 원의 **아래쪽 한 점에서 여러 선분이 위쪽 둘레의 여러 점으로 방사형**으로 뻗어 있습니다.
  - 그중 **가운데를 수직으로 지나는 긴 선분**이 가장 두드러지게 그려져 있습니다.
- 각 끝점에는 **작은 원형 기호(라벨)**가 붙어 있어, 선분과 대응되는 선택지를 나타내는 구조로 보입니다.
- 해설 그림도 문제 그림과 거의 같은 구성을 유지하면서, 특정 기호 하나를 강조하는 역할을 합니다.

[수식 단계]
- 해당 없음.
- 수식 변형식, 등호 연결, 계산식은 보이지 않습니다.

[등호별 변환 의미]
- 해당 없음.
- 등호가 포함된 단계적 변형은 보이지 않습니다.

[세부 내용]
- 첫 번째 도형:
  - 원의 **맨 위**에 하나의 점이 있고, 그 점 근처에 `ㄷ` 표식이 있습니다.
  - 원의 **왼쪽 위**, **오른쪽 위**, **왼쪽 중간**, **오른쪽 중간** 부근에도 각각 다른 기호가 붙어 있습니다.
  - 원의 **아래쪽 한 점**에서 위의 여러 점으로 선분들이 연결되어 있습니다.
  - 가운데에는 **세로로 곧게 내려오는 선분**이 원의 위아래를 관통하며, 중심 부근에 작은 점이 표시되어 있습니다.
- 해설 도형:
  - 구성은 거의 동일하지만, 위쪽 기호 중 **`ㄷ`**만 보이도록 더 단순하게 제시된 느낌입니다.
  - 해설 문장에서는 가장 긴 선분이 **원의 중심을 지나는 선분**, 즉 **지름**이라고 설명합니다.
- 시각적으로 가장 눈에 띄는 선분은
  - 원의 위아래를 잇는 **세로 선분**
  - 나머지 사선보다 길게 보이는 구조
  입니다.
- 다만 이 초안에서는 그 선분이 무엇인지 **이름을 확정하여 답으로 단정하지는 않습니다.**

[Python DSL 작성 힌트]
- 도형은 다음 요소로 나누어 그리면 좋습니다.
  1. **원**
  2. 원의 **아래 한 점**
  3. 아래 점에서 원의 위쪽 여러 점으로 연결된 **여러 선분**
  4. 원을 세로로 지나는 **중심 통과 선분**
  5. 각 끝점 옆의 **작은 원형 기호 라벨**
- 라벨은 선분 끝점 근처에 **작은 원 테두리 + 한글 자모 1개** 형태로 배치된 것으로 보입니다.
- 문제용 도형과 해설용 도형을 **위아래로 분리 배치**하면 원본 레이아웃과 유사하게 만들 수 있습니다.
- 텍스트는 다음과 같이 구획하면 좋습니다.
  - 상단: 문제 번호 + 지시문
  - 중단: 정답 표기와 해설 그림
  - 하단: 해설 문장
- 해설 문장은 줄바꿈이 들어가 있으므로, **두 줄 텍스트 박스**로 처리하면 자연스럽습니다.

[주의할 점]
- **답을 추론해서 채우지 말 것.** 이 초안에서는 단지 화면에 보이는 기호와 배치만 기록해야 합니다.
- 문제의 핵심은 “가장 긴 선분”이지만, 여기서 그것이 무엇인지 **결론을 확정해서 쓰지 않는 것**이 중요합니다.
- 원 내부의 작은 점과 그 옆 문자처럼 보이는 요소는 해상도상 약간 애매하므로, **중심점 표시인지 문자 라벨인지 단정하지 말 것**.
- 해설 그림의 일부 선은 원본보다 흐릿해 보이므로, Python DSL에서는 **선분의 존재와 방향 중심**으로 구현하는 것이 안전합니다.
- 상단 문제 그림과 하단 해설 그림의 기호 위치가 완전히 동일한지 화면상 약간 차이가 있어 보일 수 있으므로, **정확한 좌표는 참고 이미지 기준으로 따로 보정**하는 것이 좋습니다.

[불확실한 부분]
- 원 내부 중심 근처의 `ㅇ`처럼 보이는 표식은 실제 문자 표기인지, 중심을 나타내는 보조 표시인지 불확실합니다.
- 상단 도형의 일부 끝점 라벨(`ㄴ`, `ㄹ`, `ㅁ`, `ㄱ`)은 해상도 때문에 원형 테두리와 겹쳐 보이며, 정확한 위치가 조금 흐립니다.
- 하단 해설 문장의 줄바꿈 위치는 화면 너비에 따라 달라질 수 있어, 정확한 줄 단위 복원은 약간 불확실합니다.
- 오른쪽 아래의 일부 UI 테두리처럼 보이는 요소는 문제 본문과 무관한 화면 자투리일 가능성이 있습니다.

--- RAW VISION DRAFT END ---
