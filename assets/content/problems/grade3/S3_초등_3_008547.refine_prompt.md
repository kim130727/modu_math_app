# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_008547.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_008547

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
[전체적인 구성]
- 화면 왼쪽 상단에 문제 번호와 지시문이 있고, 그 아래 중앙 왼쪽에 세로셈 형태의 곱셈 계산이 배치되어 있습니다.
- 가운데 오른쪽에는 보라색 테두리의 큰 직사각형 선택 상자가 있으며, 그 안에 보기 4개가 세로로 나열되어 있습니다.
- 계산 영역 중간의 한 곱셈 결과 부분이 연한 연두색 배경으로 강조되어 있습니다.
- 화면 왼쪽 하단에는 `(정답)` 표시와 동그라미 친 보기 기호가 있고, 맨 아래에는 `(해설)` 문장이 한 줄로 길게 들어가 있습니다.

[보이는 텍스트]
- 상단 문제 문장:
  - `10. 색칠된 부분은 실제 어떤 수의 곱인지 찾아 선택하세요.`
- 세로셈:
  - 위에 `3 2 4`
  - 아래에 `× 6`
  - 가로선 아래 결과들로 `2 4`, `1 2 0`, `1 8 0 0`, `1 9 4 4`가 क्रम서대로 보임
- 선택 상자 안 보기:
  - `ㄱ 2 × 6`
  - `ㄴ 2 × 60`
  - `ㄷ 20 × 6`
  - `ㄹ 200 × 6`
- 하단 정답 표시:
  - `(정답) ㄷ`
- 하단 해설:
  - `(해설) 색칠된 부분은 곱해지는 수 324에서 십의 자리의 20과 곱하는 수 6의 곱입니다.`

[반복되는 시각 패턴]
- 보기 4개는 모두 원형 기호(ㄱ, ㄴ, ㄷ, ㄹ)가 먼저 나오고, 그 오른쪽에 곱셈식이 하나씩 붙는 형태입니다.
- 세로셈은 위에서 아래로 계산 결과가 단계적으로 내려오는 전형적인 곱셈 정렬입니다.
- 일부 계산 결과가 가로선 아래에 분리되어 배치되어 있고, 그중 `120`만 연한 색 배경으로 강조되어 있습니다.
- 숫자는 자릿수 간격을 두고 배치되어, 자릿값이 보이도록 정렬되어 있습니다.

[수식 단계]
- 세로곱셈의 구조는 `324 × 6` 형태로 보입니다.
- 계산 결과가 여러 줄로 제시되어 있으며,
  - 첫 번째 부분 결과처럼 보이는 `24`
  - 색칠된 `120`
  - 다음 줄의 `1800`
  - 마지막 합계로 보이는 `1944`
  가 순서대로 배치되어 있습니다.
- 다만, 이 화면은 완전한 풀이과정 표기라기보다 결과와 중간값이 함께 제시된 형태로 보입니다. 정확히 어떤 중간 계산 자리값을 가리키는지는 해설 문장을 통해 보완됩니다.

[등호별 변환 의미]
- 화면에 직접적인 등호(`=`)가 반복되는 변형식은 보이지 않습니다.
- 대신 세로셈의 중간 결과가 줄별로 나뉘어 제시되어 있어, 각 줄이 자릿값별 곱셈 또는 누적 결과를 나타내는 것으로 보입니다.
- 연한 색으로 칠해진 `120`은 해설에 따르면 `324`의 십의 자리인 `20`과 `6`의 곱에 해당하는 부분으로 해석됩니다.  
  - 이 부분은 시각적 강조와 해설 문장으로 연결됩니다.
- 단, 실제 등식 전개 과정 자체가 적혀 있는 것은 아니므로, 등호 단계 변환으로 단정해서 쓰지는 않는 것이 좋습니다.

[세부 내용]
- 왼쪽의 세로셈은 숫자 `324`가 위에 있고, 그 오른쪽 아래에 `× 6`이 세로로 배치되어 있습니다.
- 가로선 아래에는 계산 결과가 세로로 정렬되어 있으며, 그 중간에 연한 연두색 직사각형 배경으로 `120`이 강조되어 있습니다.
- 강조된 `120`은 숫자들 사이에 약간 넓은 자간으로 배치되어 있고, 사각형 배경은 숫자보다 약간 크게 둘러싸고 있습니다.
- 오른쪽 선택 상자는 보라색 얇은 선으로 둘러싸인 직사각형이며, 내부 보기들은 왼쪽 정렬에 가깝게 위에서 아래로 일정 간격을 두고 놓여 있습니다.
- 각 보기는 원 안에 한글 자모 기호가 있고, 그 뒤에 수식이 붙는 형태입니다.
- 하단의 `(정답) ㄷ`은 문제의 선택 결과를 알려 주지만, 시각 분석 초안에서는 정답 자체를 새로 추론해 쓰기보다 화면에 보이는 사실로만 기록하는 것이 안전합니다.

[Python DSL 작성 힌트]
- 큰 틀은 다음 요소들로 나누어 배치하면 좋습니다:
  1. 상단 문제 문장
  2. 왼쪽 세로곱셈 영역
  3. 오른쪽 보라색 선택 박스
  4. 하단 정답/해설 텍스트
- 세로곱셈 영역은 숫자들의 가로 정렬과 세로 간격이 중요하므로, `324`, `× 6`, 각 줄 결과를 자릿수 맞춤으로 배치해야 합니다.
- 강조색 박스는 `120` 뒤에 또는 주변에 연두색 배경 사각형을 넣는 방식이 적절합니다.
- 선택지는 박스 내부에 원형 기호 + 식 문자열을 세로로 4줄 배치하는 구조로 표현하면 됩니다.
- 하단 해설은 작은 글씨로 한 줄 배치하고, `(정답)`과 `(해설)`을 구분해 넣는 형태가 좋습니다.

[주의할 점]
- 문제를 실제로 풀어 정답을 유도하지 마세요.
- 색칠된 부분을 숫자값으로 확정해 적되, 그 의미를 해설 문장에 기반한 시각 정보로만 설명하세요.
- `324`의 각 자리와 `120`, `1800`, `1944`의 관계를 너무 단정적으로 풀이하지 말고, 화면에 나타난 배열 중심으로 서술하세요.
- 보기 기호는 동그라미 안 문자처럼 보이지만, 정확한 글꼴은 화면 캡처에 따라 약간 다르게 인식될 수 있습니다.
- 하단의 `(정답) ㄷ` 표시는 화면에 보이지만, DSL 작성 시에는 필요 여부를 별도로 판단하세요.

[불확실한 부분]
- 왼쪽 세로셈에서 `24`가 정확히 어떤 중간 곱인지, 자릿값 정렬상 일부 숫자가 잘려 보이는지 여부는 화면만으로 100% 확정하기 어렵습니다.
- `120`의 정확한 위치가 `324`의 십의 자리와 어떤 방식으로 연결되는지는 해설을 참고한 해석이며, 캡처만으로는 표기 의도를 완전히 단정하기 어렵습니다.
- `1800` 위/아래에 추가적인 기호나 줄이 있는지 여부는 해상도상 뚜렷하지 않습니다.
- 오른쪽 박스의 테두리 두께와 내부 여백은 대략적인 추정이 포함될 수 있습니다.

--- RAW VISION DRAFT END ---
