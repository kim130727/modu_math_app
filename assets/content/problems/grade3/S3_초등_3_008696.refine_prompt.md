# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_008696.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_008696

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
[전체적인 구성]
- 상단 왼쪽에 문제 번호 `85.`가 있고, 그 오른쪽으로 문제 지시문이 한 줄로 이어집니다.
- 중앙에는 원형 도형 3개가 가로로 배치되어 있으며, 각 도형 사이에는 오른쪽 방향 화살표가 있습니다.
- 각 도형 아래에는 설명 문장이 여러 줄로 나뉘어 배치되어 있습니다.
- 하단에는 선택형 문장이 3줄 정도 이어지며, 첫 줄에는 괄호 안에 선택지 형태의 단어가 들어갑니다.
- 전체적으로 “원 모양의 종이를 접는 과정”을 그림과 문장으로 설명한 뒤, 알맞은 말을 고르는 문제입니다.

[보이는 텍스트]
- `85. 원을 둘로 똑같이 나누는 선분을 알아보려고 합니다. 알맞은 말을 선택하세요.`
- 첫 번째 그림 아래:
  - `원 모양의 종이를`
  - `둘로 똑같이 나누어`
  - `도록 접었다가 펼`
  - `칩니다.`
- 두 번째 그림 아래:
  - `다른 방향으로도 둘`
  - `로 똑같이 나누어지`
  - `도록 접었다가 펼칩`
  - `니다.`
- 세 번째 그림 아래:
  - `접었을 때 생기는 선`
  - `분들이 만나는 점을`
  - `점 ○○이라고 표시합`
  - `니다.`
- 하단 문장:
  - `(2) 원을 둘로 똑같이 나누는 선분은 ( 반지름, 지름 )입니다.`
  - `(2) 원을 둘로 똑같이 나누는 선분은 지름입니다.`
  - `(2) 지름`
- 그림 속 추가 표시는:
  - 원 3개
  - 각 원 중심을 가로지르거나 세로로 지나는 점선 접기 표시
  - 그림 사이의 회색 화살표 2개
  - 세 번째 원 안의 작은 점 2개 정도가 보이며, 하나는 중심 근처에 표시됨

[반복되는 시각 패턴]
- 원형 도형이 3번 반복됩니다.
- 첫 번째 원은 세로 점선만 있는 형태, 두 번째 원은 세로·가로 점선이 교차하는 형태, 세 번째 원은 같은 점선 구조에 중심 표시가 추가된 형태입니다.
- 각 단계 사이에 같은 모양의 회색 오른쪽 화살표가 배치되어 과정의 진행을 나타냅니다.
- 아래 설명도 “접었다가 펼칩니다”라는 비슷한 구조가 반복되어, 접기 동작을 여러 방향으로 적용하는 흐름을 강조합니다.

[수식 단계]
- 해당 없음. 보이는 것은 수식 변형식이나 등호가 아니라 도형 설명과 문장형 정보입니다.

[등호별 변환 의미]
- 해당 없음. 등호가 연결된 식이 보이지 않습니다.

[세부 내용]
- 첫 번째 그림:
  - 연한 분홍색 원이 있고, 원의 중앙을 위아래로 지나는 분홍 점선이 있습니다.
  - 점선은 접는 선을 나타내는 듯하며, 원 내부가 두 부분으로 나뉜 상태를 보여줍니다.
- 두 번째 그림:
  - 같은 원에 가로 점선이 추가되어, 세로·가로 두 방향의 접기 결과가 나타납니다.
  - 십자 모양으로 교차하는 점선이 원의 중심에서 만납니다.
- 세 번째 그림:
  - 두 방향의 점선이 유지된 상태에서 중심 부근에 작은 점이 표시되어 있습니다.
  - 점 주변에 작은 원형 표식처럼 보이는 기호도 함께 보이며, 문장에서는 이 점을 `점 ○○`처럼 표기한다고 설명합니다.
- 하단 선택 문장:
  - 괄호 안에 `반지름, 지름`이 나열되어 있어, 둘 중 하나를 고르는 형식입니다.
  - 바로 아래에 `지름입니다.`라는 문장이 별도로 보이지만, 이것이 정답 제시인지 학생 응답 예시인지 화면 구성상 구분이 필요합니다.  
  - 마지막 줄의 `지름`은 짧게 떨어져 있어, 답안 칸 또는 선택 결과를 적는 자리처럼 보일 수 있습니다.

[Python DSL 작성 힌트]
- 상단 문제 번호와 지시문을 한 줄 텍스트로 배치합니다.
- 중앙에는 원형 3개를 가로 정렬하고, 원 사이에 오른쪽 화살표 2개를 넣습니다.
- 각 원은 연한 분홍색 외곽선과 연한 분홍 채움으로 표현하고, 내부 점선은 접는 선처럼 점선 스타일로 그립니다.
- 첫 번째 원은 세로 점선만, 두 번째 원은 세로와 가로 점선이 교차하는 형태, 세 번째 원은 교차점 근처의 작은 점 표식을 추가합니다.
- 각 원 아래 설명 문장은 3~4줄로 나누어 가운데 정렬에 가깝게 배치합니다.
- 하단의 선택 문장은 줄바꿈을 유지해 넣고, 괄호 선택지 `( 반지름, 지름 )` 부분은 하나의 텍스트 블록으로 보존합니다.
- 답안으로 보이는 `지름` 단독 줄은 별도 텍스트 라인으로 처리하되, 실제 정답이라고 단정하지 말고 위치만 재현합니다.

[주의할 점]
- 문제를 풀어서 답을 확정하지 마세요.
- `반지름`, `지름` 중 어느 것이 정답인지 이 분석 단계에서 단정하지 마세요.
- 세 번째 그림의 작은 점과 `○○` 표기는 해상도 때문에 정확한 문자/기호 판독이 다소 불확실합니다.
- 하단의 `(2)` 표기는 문항 번호처럼 보이지만, 실제로는 인쇄상 반복 오류일 가능성도 있어 보입니다.
- 문장 끝의 줄바꿈은 원본에서 임의로 나뉜 것이므로, DSL에서는 줄 단위 배치를 그대로 살리는 것이 좋습니다.

[불확실한 부분]
- 세 번째 그림 안의 점 표기가 `점 ○○`인지, `점 OO`인지, 또는 특정 기호를 가리키는지 명확하지 않습니다.
- 하단에 `지름입니다.`가 이미 적혀 있는 부분이 답 안내인지, 본문 일부인지 화면만으로는 확정하기 어렵습니다.
- 마지막 줄의 `지름`이 별도 입력칸인지, 선택 결과 표시인지, 혹은 다음 문항의 일부인지 확실하지 않습니다.

--- RAW VISION DRAFT END ---
