# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_008628.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_008628

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
[전체적인 구성]
- 흰 배경의 초등 수학 문제 지면이다.
- 좌상단에 문제 번호와 지시문이 있고, 그 아래 중앙 쪽에 연산 후보 3개가 연한 초록색 테두리의 직사각형 박스 안에 가로로 배치되어 있다.
- 박스 오른쪽 아래에는 선택 형식처럼 보이는 괄호 묶음 `( ㄱ , ㄴ , ㄷ )`가 따로 있다.
- 왼쪽 하단에는 `(정답)` 표시가 있고, 그 아래에 `(해설)`이 이어지며 각 보기의 계산 과정이 줄바꿈으로 나열되어 있다.
- 맨 아래에는 “따라서 …”로 끝나는 결론 문장이 있다.

[보이는 텍스트]
- `98.`
- `나머지가 가장 큰 것을 찾는 기호를 선택하세요.`
- 초록색 박스 안:
  - `ㄱ 583 ÷ 5`
  - `ㄴ 584 ÷ 6`
  - `ㄷ 585 ÷ 7`
- 박스 오른쪽 아래:
  - `( ㄱ , ㄴ , ㄷ )`
- 왼쪽 하단:
  - `(정답) ㄷ`
  - `(해설) ㄱ 583 ÷ 5 = 116···3`
  - `ㄴ 584 ÷ 6 = 97···2`
  - `ㄷ 585 ÷ 7 = 83···4`
  - `따라서 나머지가 가장 큰 것은 ㄷ입니다.`

[반복되는 시각 패턴]
- 보기 3개가 동일한 형식으로 반복된다.
  - 각 보기 앞에 동그라미 기호 안의 한글 자모 `ㄱ, ㄴ, ㄷ`가 붙어 있다.
  - 모두 `숫자 ÷ 숫자` 형태의 나눗셈 식으로 구성되어 있다.
- 해설에서도 같은 순서로 각 보기의 식이 다시 한 번 반복되며, 뒤에 몫과 나머지가 `=` 뒤에 함께 적혀 있다.
- 나머지 표기는 가운데 점 3개처럼 보이는 `···` 형식으로 반복된다.
- 정답 표시와 해설 표시가 각각 괄호로 구분되어 있다.

[수식 단계] (해당될 때)
- 해설 부분에 각 보기별로 하나의 등식이 제시되어 있다.
- 각 등식은 `나눗셈 식 = 몫···나머지` 형식이다.
- 보이는 단계는 다음처럼 구조적으로만 확인된다.
  - `583 ÷ 5 = 116···3`
  - `584 ÷ 6 = 97···2`
  - `585 ÷ 7 = 83···4`
- 등호가 연속으로 여러 번 이어지는 변형은 보이지 않는다.
- 계산 과정의 중간 단계나 나눗셈 세로셈은 없다.

[등호별 변환 의미] (해당될 때)
- 각 등호는 왼쪽의 나눗셈을 오른쪽의 몫과 나머지 표기로 바꿔 적은 것으로 보인다.
- 단, 여기서는 실제 계산 결과를 풀이하지 않고, 화면에 적힌 형식만 관찰할 수 있다.
- `···` 표시는 몫과 나머지를 구분하는 기호처럼 사용된 것으로 보인다.
- 정답 문장에서는 해설에서 비교한 나머지들 중 가장 큰 보기 기호를 지칭하는 방식으로 작성되어 있다.
  - 다만 “가장 큰 것”의 판단 자체는 문제 해결 내용이므로 여기서는 결과만 보이는 대로 확인한다.

[세부 내용]
- 문제 번호 `98.`은 좌상단 맨 앞에 작게 표시되어 있다.
- 지시문은 문제 번호 오른쪽에 한 줄로 이어진다.
- 중앙의 초록색 테두리 박스는 가로로 길고, 내부 여백이 넓다.
- 박스 안의 세 보기:
  - 첫 번째는 왼쪽에 `ㄱ`, 그 오른쪽에 `583 ÷ 5`
  - 두 번째는 `ㄴ`, 그 오른쪽에 `584 ÷ 6`
  - 세 번째는 `ㄷ`, 그 오른쪽에 `585 ÷ 7`
- 세 보기 사이에는 세로선이나 구분선 없이 공백으로만 분리되어 있다.
- 박스 바깥 오른쪽에는 `( ㄱ , ㄴ , ㄷ )`가 따로 있어 선택지 표기 또는 답안 표시용으로 보인다.
- `(정답) ㄷ`는 왼쪽 아래에 단독으로 제시되어 있다.
- `(해설)` 아래에는 각 보기마다 한 줄씩 해석이 배치되어 있으며, 각 줄은 기호 + 식 + 결과 형식이다.
- 마지막 문장은 “따라서”로 시작해 결론을 설명하는 형태이나, 실제 답 도출은 생략하고 구조만 인식한다.

[Python DSL 작성 힌트]
- 전체 화면을 문제 영역, 보기 박스, 우측 선택 표기, 하단 정답/해설 영역으로 나눠 배치하면 좋다.
- 초록색 테두리 직사각형은 `box` 또는 `rect` 계열로 표현하고, 내부에 3개의 텍스트 항목을 가로 정렬한다.
- 각 보기 앞의 `ㄱ/ㄴ/ㄷ`는 원형 기호처럼 보이는 작은 마커와 함께 텍스트로 배치한다.
- 해설 영역은 세 줄의 식을 세로 방향으로 정렬하고, 줄 간 간격을 일정하게 둔다.
- `···` 나머지 표기는 별도의 특수 기호로 텍스트 처리하면 된다.
- `(정답)`과 `(해설)`은 일반 텍스트 박스처럼 두되, 앞뒤 공백과 줄바꿈 위치를 반영한다.
- 선택지 괄호 `( ㄱ , ㄴ , ㄷ )`는 오른쪽 하단에 독립 텍스트로 놓는다.
- 문제 번호와 지시문은 상단 좌측 정렬로 처리한다.

[주의할 점]
- 답을 계산해서 새로 채워 넣지 말 것.
- 보이는 `정답` 표기는 그대로 옮기되, 이를 근거로 풀이를 생성하지 말 것.
- `···`는 생략점이 아니라 나머지 표기 방식으로 보이므로 임의로 다른 기호로 바꾸지 말 것.
- 보기의 기호 `ㄱ, ㄴ, ㄷ`는 순서와 형태를 그대로 유지해야 한다.
- 박스의 색상은 연한 초록색으로 보이지만, 정확한 색상 값은 단정하지 말 것.
- 해설의 숫자와 결과는 화면에 보이는 그대로만 옮기고, 추가 계산은 하지 말 것.

[불확실한 부분]
- 초록색 테두리의 정확한 색상 코드와 선 두께는 이미지 만으로 확정하기 어렵다.
- 원형 기호가 실제로는 동그라미 안 한글 자모인지, 인쇄체 특수문자인지는 확대 없이 단정하기 어렵다.
- `(정답) ㄷ`의 배치가 문제에서 원래부터 인쇄된 것인지, 학습지 주석처럼 덧붙여진 것인지 화면만으로는 구분이 어렵다.
- `···`의 정확한 타이포그래피(점의 개수, 간격)는 이미지 해상도상 약간 애매할 수 있다.

--- RAW VISION DRAFT END ---
