# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_008559.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_008559

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
# [전체적인 구성]
- 상단에 문제 번호와 지시문이 한 줄로 배치되어 있습니다.
- 중앙에는 세로셈 형태의 곱셈 과정 그림이 크게 있고, 오른쪽에는 연두색 테두리의 선택지 상자가 있습니다.
- 하단 왼쪽에는 `(정답)` 표시와 함께 답이 적혀 있고, 그 아래에 `(해설)` 문장이 한 줄로 제시되어 있습니다.
- 전체적으로 **왼쪽: 문제 문장 / 가운데: 계산 과정 / 오른쪽: 보기 상자 / 아래: 정답 및 해설**의 구조입니다.

# [보이는 텍스트]
- `□ 23. 색칠된 부분은 실제 어떤 수의 곱인지를 찾아 선택하시오.`
- 가운데 세로셈 그림 안에 보이는 숫자:
  - `7 6 2`
  - `×`
  - `4`
  - `8`
  - `2 4 0`  ← 분홍색으로 색칠된 부분
  - `2 8 0 0`
  - `3 0 4 8`
- 오른쪽 선택지 상자 안의 텍스트:
  - `6 × 4`
  - `60 × 4`
  - `62 × 4`
  - `600 × 4`
- 하단:
  - `(정답)60 × 4`
  - `(해설)7620에서 6은 십의 자리이므로 60 × 4 = 240입니다.`

# [반복되는 시각 패턴]
- 가운데 계산 그림은 **세로선으로 구분된 자리값 칸**이 반복되어 보입니다.
- 숫자 `7`, `6`, `2`, `4`가 각각 서로 다른 열에 정렬되어 있어, **자릿수별 곱셈** 구조임을 시각적으로 강조합니다.
- 중간의 `240`은 **분홍색 배경으로 강조**되어 있으며, 다른 결과값들과 구별됩니다.
- 오른쪽 보기 상자는 **연두색 테두리의 직사각형 박스** 안에 항목들이 세로로 나열된 형태입니다.
- 선택지들은 모두 `숫자 × 4`의 형태로 반복되어 있어, 같은 곱셈 구조에서 **어느 자리를 나타내는 수인지**를 고르게 하는 문제처럼 보입니다.

# [수식 단계]
- 가운데에는 세로셈 곱셈 과정이 보이며, 아래로 내려가며 부분곱이 적혀 있습니다.
- 보이는 순서는 대략 다음과 같은 배열입니다.
  - 위쪽: `762`
  - 오른쪽 아래 쪽에 곱하는 수 `4`
  - 첫 번째 부분곱으로 보이는 `8`
  - 가운데 색칠된 부분 `240`
  - 그 아래 `2800`
  - 맨 아래 최종값처럼 보이는 `3048`
- 다만 이 자료는 **계산 전체를 설명하려는 것이 아니라, 색칠된 부분이 어떤 자리의 곱인지 찾는 데 초점**이 있습니다.

# [등호별 변환 의미]
- 등호가 여러 번 연결된 수식 변형은 보이지 않습니다.
- 대신 하단 해설에서 `7620에서 6은 십의 자리이므로 60 × 4 = 240입니다.`라고 설명하여,
  - `6`이 **십의 자리 숫자**
  - 이를 실제 수로 보면 `60`
  - 그 결과가 색칠된 `240`
  이라는 뜻을 전달하는 것으로 보입니다.
- 여기서의 의미는 **숫자 6 자체가 아니라 자리값 60을 나타낸다**는 점을 설명하는 형태입니다.

# [세부 내용]
- 문제 번호 왼쪽에 작은 네모 모양 기호가 있습니다.
- 문제 문장은 한 줄로 쓰여 있으며, 문장 끝에 선택하라는 지시가 있습니다.
- 중앙의 세로셈은 얇은 회색 점선 같은 세로 구획선이 여러 개 있어, 자리별 정렬을 돕는 레이아웃입니다.
- `240`은 분홍색 사각형 배경 위에 있고, 글자는 진한 남색/검정 계열로 보입니다.
- 오른쪽 상자는 연두색 선으로만 둘러싸인 직사각형이며 내부 항목 간 간격이 넓습니다.
- 하단의 `(정답)`과 `(해설)`은 괄호가 포함된 라벨 형식이며, 해설은 문장형 텍스트입니다.

# [Python DSL 작성 힌트]
- 전체 페이지를 상단/중앙/오른쪽/하단의 4개 영역으로 나누어 배치하면 좋습니다.
- 중앙 세로셈은 다음과 같은 요소를 분리해서 다루면 좋습니다.
  - 상단 숫자열 `7, 6, 2`
  - 곱하는 수 `4`
  - 부분곱 후보 `8`, `240`, `2800`, `3048`
  - 색칠 강조 영역 `240` 주변의 분홍색 박스
- 오른쪽 보기는 하나의 테두리 박스 안에 세로 목록으로 넣는 구조가 적절합니다.
- 텍스트 요소는 가능한 한 **문제문 / 선택지 / 정답 / 해설**로 분리해 배치하세요.
- 계산 그림의 점선 세로 구획선은 실제 칸처럼 보이므로, 필요하면 얇은 선 여러 개로 표현할 수 있습니다.
- 색칠된 `240`은 일반 텍스트와 별도 스타일로 강조 표시해야 합니다.

# [주의할 점]
- 이 자료는 **풀이를 작성하는 문제가 아니라 시각 요소를 분석하는 작업**입니다.
- `60 × 4`가 정답으로 보이더라도, 답을 새로 계산해 채워 넣는 식의 서술은 피해야 합니다.
- `240`을 포함한 가운데 계산 결과의 정확한 산출 과정을 임의로 재구성하지 마세요.
- `7620`이라는 표현이 해설에 보이지만, 이것이 원문에서 어떤 형식으로 제시되었는지(예: 오타인지, 설명용 표기인지)는 단정하지 않는 것이 좋습니다.
- 선택지의 순서와 줄간격은 실제 이미지에서 세로 배열로 보이며, 가로 배열로 추정하지 마세요.

# [불확실한 부분]
- 가운데 세로셈의 정확한 수식 배열은 일부 가려지거나 생략되어 보여서, 각 부분곱이 어떤 자리에서 나온 것인지 전체 구조를 100% 확정하기는 어렵습니다.
- `8`, `240`, `2800`, `3048`이 각각 어떤 연산 단계에 대응하는지는 그림만으로 완전히 명시되어 있지 않습니다.
- 하단 해설의 `7620` 표기는 문제 본문과의 대응 관계가 명확하지 않아, 실제로는 설명용 표기일 가능성이 있습니다.
- 문제 문장 앞의 작은 네모 기호가 체크박스인지, 번호 표시 장식인지 확정하기 어렵습니다.

--- RAW VISION DRAFT END ---
