# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_008625.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_008625

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
[전체적인 구성]
- 화면 왼쪽 상단에 작은 체크박스 모양이 있고, 그 오른쪽에 문제 번호 `95.`가 보입니다.
- 그 뒤로 문제 지시문 `몫이 더 큰 것을 선택하세요.`가 한 줄로 배치되어 있습니다.
- 화면 오른쪽 중앙에는 연분홍색 테두리의 큰 사각형 박스가 있고, 그 안에 두 개의 나눗셈 식이 좌우로 나란히 제시되어 있습니다.
- 화면 왼쪽 아래에는 `(정답)` 표시와 함께 정답 식이 따로 적혀 있습니다.
- 그 아래에는 `(해설)` 표시와 함께 두 나눗셈의 계산 결과가 등호로 연결된 형태로 제시되어 있고, 마지막에 비교 문장이 이어집니다.
- 전체적으로 “문제 제시 → 보기 박스 → 정답 표기 → 해설” 순서의 구성입니다.

[보이는 텍스트]
- `95.`
- `몫이 더 큰 것을 선택하세요.`
- 박스 안: `64 ÷ 4`
- 박스 안: `84 ÷ 3`
- `(정답)84 ÷ 3`
- `(해설)64 ÷ 4 = 16, 84 ÷ 3 = 28`
- `따라서 16<28이므로 몫이 더 큰 것은 84 ÷ 3입니다.`

[반복되는 시각 패턴]
- 나눗셈 기호 `÷`가 두 번 반복되어 보이며, 두 보기 모두 같은 형식의 식으로 제시되어 있습니다.
- 해설에서도 두 식이 `= 숫자` 형태로 각각 제시되어, 같은 비교 대상이 반복적으로 나타납니다.
- 정답과 해설이 문제 본문 아래에 별도로 배치되어 있어, 본문과 풀이/정답 정보가 시각적으로 구분됩니다.

[수식 단계]
- 해설에 보이는 수식은 다음과 같이 읽힙니다.
  - `64 ÷ 4 = 16`
  - `84 ÷ 3 = 28`
- 이어서 두 결과를 비교하는 문장이 붙어 있습니다.
  - `16<28`
- 등호가 있는 각 식은 “나눗셈식 → 결과값” 형태로 정리된 상태입니다.

[등호별 변환 의미]
- `64 ÷ 4 = 16`
  - 보이는 의미: `64 ÷ 4`라는 계산식의 결과를 `16`으로 적어 둔 형태입니다.
  - 해설 문장상: 첫 번째 보기의 몫을 나타냅니다.
- `84 ÷ 3 = 28`
  - 보이는 의미: `84 ÷ 3`라는 계산식의 결과를 `28`로 적어 둔 형태입니다.
  - 해설 문장상: 두 번째 보기의 몫을 나타냅니다.
- `16<28`
  - 보이는 의미: 앞의 두 결과를 크기 비교한 식입니다.
  - 해설 문장상: 더 큰 몫을 판단하기 위한 비교 표현입니다.
- 마지막 문장 `몫이 더 큰 것은 84 ÷ 3입니다.`는 비교 결과를 바탕으로 선택을 적은 문장입니다.
  - 단, 여기서는 문제를 푸는 것이 아니라, 화면에 그렇게 적혀 있다는 사실만 정리합니다.

[세부 내용]
- 문제 번호 `95.`는 본문 맨 앞에 작게 붙어 있습니다.
- 지시문은 일반 크기의 글씨로 왼쪽 상단에 배치되어 있고, 선택형 문제임을 나타냅니다.
- 보기 영역의 연분홍색 사각형 박스는 가로로 넓고 세로는 낮은 형태입니다.
- 박스 내부의 두 식 `64 ÷ 4`와 `84 ÷ 3`은 서로 간격을 두고 좌우로 배치되어 있으며, 세로 정렬은 거의 같은 줄에 맞춰져 있습니다.
- 왼쪽 아래의 `(정답)` 표시는 괄호가 포함된 짧은 라벨이고, 그 뒤에 정답 식이 바로 이어집니다.
- `(해설)`은 괄호 라벨 뒤에 쉼표로 구분된 두 식이 이어지는 형태입니다.
- 해설의 마지막 문장은 비교 부등호 `<`를 포함하고 있으며, 결과 비교를 서술형 문장으로 마무리합니다.

[Python DSL 작성 힌트]
- 문제 텍스트 영역과 보기 박스 영역을 분리해서 배치하면 좋습니다.
- 보기 박스는 `rectangle` 또는 `box` 계열 요소로, 연분홍색 테두리만 있는 큰 사각형으로 표현하면 적절합니다.
- 박스 내부에는 `64 ÷ 4`와 `84 ÷ 3`를 같은 높이 기준선에 맞춰 좌우로 배치합니다.
- 아래쪽에는 `(정답)`과 `(해설)` 라벨을 텍스트로 따로 두고, 정답/풀이 문자열을 그 뒤에 이어 붙이는 방식이 맞습니다.
- 해설 부분은 `=`와 `,`를 포함한 한 줄 또는 두 줄 텍스트로 구성할 수 있습니다.
- 비교 문장 `16<28이므로` 부분은 등호가 아니라 부등호가 포함된 일반 문장 텍스트로 처리하면 됩니다.

[주의할 점]
- 답을 직접 계산해서 새로 채워 넣지 말고, 화면에 보이는 텍스트만 기록해야 합니다.
- `정답` 표기는 문제 풀이 결과를 암시하지만, 여기서는 시각 자료에 적힌 문자열로만 취급해야 합니다.
- 두 보기의 위치 관계(좌우 배치)를 놓치지 말아야 합니다.
- 연분홍 테두리 박스의 존재가 중요하므로 단순 텍스트 목록으로만 보면 안 됩니다.
- 해설의 마지막 문장은 결과를 설명하는 서술문이며, 별도의 수식으로 오해하지 않도록 구분해야 합니다.

[불확실한 부분]
- 화면 왼쪽 맨 앞의 작은 사각형이 실제 체크박스인지, 인쇄용 여백 표식인지 이미지만으로는 확정하기 어렵습니다.
- 정답/해설의 글씨 크기와 정확한 줄 간격은 이미지 해상도상 세밀하게 단정하기 어렵습니다.
- 연분홍 박스의 테두리 두께와 정확한 색상은 화면상 보이는 대로만 추정 가능하며, 픽셀 단위로는 확정할 수 없습니다.

--- RAW VISION DRAFT END ---
