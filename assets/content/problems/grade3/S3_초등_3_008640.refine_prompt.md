# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_008640.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_008640

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
# [전체적인 구성]
- 상단 왼쪽에 문제 번호와 문장형 질문이 있고, 가운데에는 원 도형 그림이 배치되어 있습니다.
- 원 아래에는 연두색 테두리의 큰 선택지 박스가 1개 있습니다.
- 하단에는 `(정답)` 표시와 정답 문장, 그 아래에 `(해설)` 문장이 이어집니다.
- 전체적으로 **객관식 1문항** 형태이며, 그림과 보기 박스를 보고 정답을 선택하는 구성입니다.

# [보이는 텍스트]
- `□ 12. 원에 반지를를 3개 그은 것입니다. 한 원에 반지를를 몇 개 그을 수 있는지 알맞은 것을 선택하세요.`
  - 첫 글자 앞에 작은 빈 사각형 기호가 보입니다.
  - 문장 중 `반지를를`은 이미지상 그렇게 보이지만, 일부 글자는 흐릿하여 오타/인쇄 상태일 수 있습니다.
- 선택지 박스 안:
  - `( 무수히 많이 그을 수 있습니다. , 3개 )`
  - 괄호가 양쪽에 있고, 그 안에 문장이 먼저 나오고 쉼표 뒤에 `3개`가 있습니다.
- 하단:
  - `(정답)무수히 많이 그을 수 있습니다.`
  - `(해설)반지름은 원의 중심과 원 위의 한 점을 이은 선분이므로 무수히 많이 그을 수 있습니다.`
- 해설 문장에서는 `반지름`이라는 용어가 쓰였습니다.

# [반복되는 시각 패턴]
- 원 안에 중심점 하나와, 그 중심에서 원 둘레로 뻗는 선분이 여러 개 보입니다.
- 선분은 색이 서로 다르게 표시되어 있습니다.
  - 왼쪽으로 향한 선분 1개
  - 오른쪽 위로 향한 선분 1개
  - 아래쪽으로 향한 선분 1개
- 모두 같은 출발점(중심점 부근)에서 시작하는 형태로 보이며, 이 패턴이 **“중심에서 원둘레로 연결되는 선분”**이라는 점을 시각적으로 반복 강조합니다.
- 선택지 박스는 하나의 큰 직사각형 안에 문장이 들어가는 반복적 보기 형식입니다.

# [수식 단계]
- 해당 없음.
- 등호를 포함한 계산식이나 변형 과정은 보이지 않습니다.

# [등호별 변환 의미]
- 해당 없음.
- 등호가 이어지는 단계식이 없습니다.

# [세부 내용]
- 원은 화면 중앙보다 약간 위에 배치되어 있고, 원의 테두리는 회색 계열의 가는 선으로 그려져 있습니다.
- 원 내부에는 작은 중심 표시가 있으며, 중심점은 분홍색 점처럼 보입니다.
- 중심 근처 위쪽에는 작은 빈 원형 표시가 하나 더 보입니다.
- 중심에서 다음 방향으로 선이 연결됩니다.
  - 왼쪽 방향: 하늘색/파란색 계열의 선분
  - 오른쪽 위 방향: 연보라색 계열의 선분
  - 아래 방향: 초록색 계열의 선분
- 각 선분은 중심점에서 시작해 원의 둘레까지 닿는 형태로 보입니다.
- 선택지 박스는 연두색 테두리의 긴 가로 직사각형이며, 내부 텍스트는 가운데 정렬에 가깝게 배치되어 있습니다.
- 박스 안 괄호와 쉼표가 포함되어 있어, 보기 두 개를 한 줄에 나열한 형태처럼 읽힙니다.
- 하단 정답/해설은 일반 본문 글씨 크기로, 문제 영역과 분리되어 있습니다.

# [Python DSL 작성 힌트]
- 상단 문제 문장은 좌측 상단에 텍스트 블록으로 배치합니다.
- 중앙에는 큰 원 1개를 그리고, 중심점과 세 개의 방사형 선분을 추가합니다.
- 선분은 모두 같은 시작점에서 뻗도록 설정하고, 색을 서로 다르게 주어 구분합니다.
- 원 아래에는 연두색 테두리의 긴 직사각형 박스를 배치하고, 그 안에 선택지 문장을 중앙 정렬로 넣습니다.
- 하단에는 `(정답)`과 `(해설)` 텍스트를 별도 줄로 배치합니다.
- 도형과 텍스트 사이의 간격을 넉넉하게 두어 원 그림이 문제의 핵심 시각 요소로 보이게 합니다.

# [주의할 점]
- 문제를 풀어서 정답을 새로 계산하거나 추론해 넣지 마세요.
- 보기 괄호 안의 `3개`는 이미지에 보이는 텍스트로만 다루고, 의미 판단은 하지 마세요.
- `반지를를`처럼 보이는 부분은 인쇄가 흐려 정확히 판독되지 않을 수 있으므로, 그대로 보이는 대로 기록하는 것이 안전합니다.
- 중심점 주변의 작은 원형 표시와 색 점은 실제 편집 요소일 수 있으므로 도형 요소로 분리해서 보아야 합니다.
- 정답/해설 영역은 문제 본문과 구분되는 후속 설명 텍스트이므로, DSL 작성 시 별도 블록으로 처리하는 것이 좋습니다.

# [불확실한 부분]
- 문제 문장 중 `반지를를`의 정확한 원문 표기는 이미지 해상도상 다소 불명확합니다.
- 원 내부 중심 근처의 작은 빈 원 표시가 정확히 어떤 기호인지 확정하기 어렵습니다.
- 선택지 박스 안의 문장 앞뒤 공백과 괄호 배치는 이미지 판독상 약간의 오차가 있을 수 있습니다.

--- RAW VISION DRAFT END ---
