[문제 개요]
- 문제 번호는 `62`이다.
- 문제 문장은 `나머지가 더 큰 것을 찾아 보세요.`로 보인다.
- 보기 2개가 연한 연두색 가로 박스 안에 좌우로 배치되어 있다.
- 화면 아래에는 `(정답)`, `(해설)`, 결론 문장이 순서대로 배치되어 있다.
- 이 문항은 두 나눗셈의 나머지를 비교하여 더 큰 보기를 고르는 형식이다.

[원본에 보이는 요소]
- 좌상단에 작은 네모 모양 기호 `□`가 있고 그 옆에 `62.`가 있다.
- 그 뒤에 문제 문장 `나머지가 더 큰 것을 찾아 보세요.`가 한 줄로 이어진다.
- 중앙 상단에 연한 연두색 배경의 긴 직사각형 박스가 있다.
- 박스 안에는 다음 두 보기가 좌우로 놓여 있다.
  - `㉠ 515 ÷ 4`
  - `㉡ 516 ÷ 5`
- 박스 아래 왼쪽에 `(정답)㉠`가 보인다.
- 그 아래 왼쪽에 `(해설)㉠ 515 ÷ 4 = 128···3`가 있다.
- 다음 줄에 `㉡ 516 ÷ 5 = 103···1`가 있다.
- 마지막 줄에 `따라서 3>1이므로 나머지가 더 큰 것은 ㉠입니다.`가 있다.

[수식/관계 구조]
- 보기 1: `㉠ 515 ÷ 4`
- 보기 2: `㉡ 516 ÷ 5`
- 해설 1: `515 ÷ 4 = 128···3`
- 해설 2: `516 ÷ 5 = 103···1`
- 비교 문장: `3>1`
- 구조상 각 나눗셈의 결과에서 나머지만 비교하는 관계이다.
- `128···3`, `103···1`은 몫과 나머지를 함께 나타낸 표기처럼 보인다.

[등호별 변환 의미]
- `515 ÷ 4 = 128···3`
  - 왼쪽의 나눗셈식을 오른쪽에서 몫과 나머지 형태로 옮겨 적은 것이다.
  - 계산 과정은 생략되고 결과만 제시되어 있다.
- `516 ÷ 5 = 103···1`
  - 동일하게 나눗셈 결과를 몫과 나머지로 바로 적은 것이다.
- `3>1`
  - 두 결과의 나머지를 비교한 것이다.
- 마지막 문장 `따라서 3>1이므로 ...`는 위 비교를 바탕으로 결론을 말하는 문장이다.
- 등호가 연쇄적으로 이어지는 변형은 보이지 않는다.

[빈칸 정책]
- 이 이미지에는 빈칸 입력란이 보이지 않는다.
- 정답 표시는 화면에 인쇄되어 보이는 `㉠`만 관찰 사실로 기록한다.
- 보이지 않는 값이나 숨은 계산 결과는 추론해서 채우지 않는다.
- 만약 DSL에서 빈칸 형태로 재현해야 한다면, 보이는 그대로 빈칸을 유지하고 임의 값은 넣지 않는다.

[반복 패턴]
- 보기 기호 `㉠`, `㉡`가 각 항목 앞에 반복된다.
- 상단 보기 영역과 하단 해설 영역에서 같은 기호가 대응되어 반복 사용된다.
- 두 해설 줄 모두 `식 = 몫···나머지`의 형식을 따른다.
- 마지막에 `따라서`로 시작하는 결론 문장이 붙는 패턴이 있다.
- `···` 표기가 나머지 표기처럼 반복되어 보인다.

[DSL 작성 힌트]
- 상단에는 `문제 번호 + 문장`을 한 줄 텍스트로 배치한다.
- 그 아래에 연한 연두색 사각형 박스를 두고, 내부에 보기 2개를 좌우로 배치한다.
- 보기 텍스트는 각각 `㉠ 515 ÷ 4`, `㉡ 516 ÷ 5`처럼 기호와 식을 함께 둔다.
- `(정답)㉠`는 별도 줄에 왼쪽 정렬로 배치한다.
- `(해설)` 아래에는 두 식을 세로로 나열한다.
- 해설 식의 `= 128···3`, `= 103···1`는 보이는 문자열을 그대로 텍스트로 넣는다.
- 마지막 결론 문장은 한 줄 텍스트로 배치하고, `3>1` 비교 부분이 눈에 띄게 이어지도록 둔다.
- 박스 안 보기 2개는 같은 높이에서 좌우 간격을 충분히 두고 정렬하는 것이 중요하다.
- 문제 번호 왼쪽의 작은 `□`는 선택 체크박스처럼 보이므로 필요하면 별도 작은 기호 요소로 처리한다.

[semantic/solvable 힌트]
- 이 문제는 두 나눗셈의 나머지를 비교하여 더 큰 것을 고르는 유형이다.
- 원본 해설에는 각 나눗셈의 몫과 나머지가 이미 제시되어 있다.
- 따라서 의미상 핵심은 `3`과 `1`의 비교이다.
- 다만, 정답을 새로 계산하거나 추론해서 채우는 방식은 사용하지 않는다.
- 원본에 보이는 정답 표시는 `㉠`이다.
- `나누기 n은 분모의 ×n으로 반영된다.`  
  - 이 문장은 본 이미지의 식에서는 직접적인 분수 변형이 보이지 않더라도, DSL 규칙상 해당 형태가 나타날 때 사용할 문장이다.

[주의 및 TODO]
- `···`는 OCR/표기상 나머지 구분 기호처럼 보이지만, 실제 폰트 형태는 이미지 해상도에 따라 다르게 보일 수 있다.
- 연두색 박스의 정확한 테두리 두께와 내부 여백은 확대 상태에 따라 약간 달라질 수 있다.
- `(정답)㉠`가 한 줄로 완전히 붙어 있는지, 약간 간격이 있는지는 화면에서 약간 다르게 읽힐 수 있다.
- `□`의 정확한 의미는 문제 선택용 표식인지 추가 기호인지 맥락상 단정하지 않는다.
- 계산 과정은 보이지 않으므로, 해설을 단계 계산으로 확장하지 말고 원문 그대로 유지한다.
