# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_008558.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_008558

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
[전체적인 구성]
- 화면 상단 왼쪽에 문제 번호와 지시문이 있고, 그 아래 오른쪽 영역에 주황색 테두리 상자 2개가 나란히 배치되어 있습니다.
- 각 상자 안에는 세로셈 형태의 곱셈 계산이 들어 있습니다.
- 화면 중간 왼쪽에는 `(정답)` 표시 아래에 세로셈이 한 번 더 제시되어 있고, 하단에는 `(해설)` 문장으로 풀이 설명이 있습니다.
- 전체적으로 “바르게 계산한 것 선택” 유형의 선택 문제이며, 두 개의 보기 중 맞는 계산 결과를 고르는 구조입니다.

[보이는 텍스트]
- `□ 21. 바르게 계산한 것을 선택하세요.`
- 왼쪽 주황색 상자 안:
  - `5 0`
  - `× 6 0`
  - `3 0 0`
- 오른쪽 주황색 상자 안:
  - `5 0`
  - `× 6 0`
  - `3 0 0 0`
- 아래쪽 왼쪽:
  - `(정답)`
  - 세로셈 `50 × 60` 형태의 계산식
  - 결과 `3000`
- 하단 해설:
  - `(해설)50 × 60은 5 × 6 = 30에 0을 2개 붙여서 줍니다.`

[반복되는 시각 패턴]
- 동일한 곱셈식 `50 × 60`이 두 개의 보기 상자와 정답 예시에 반복됩니다.
- 두 보기 모두 위아래로 숫자가 정렬된 세로셈 형식이며, 위쪽 숫자 `50`, 아래쪽 숫자 `60`의 배치는 동일합니다.
- 결과만 다르게 표시되어 있어, 계산 결과 자리의 자릿수 차이가 비교 포인트로 보입니다.
- 주황색 테두리 상자는 같은 크기와 형태로 반복되어, 선택지 두 개를 시각적으로 구분합니다.

[수식 단계] (해당될 때)
- 화면에는 등호가 연속된 변형식은 보이지 않습니다.
- 다만 하단 해설에서 `50 × 60`을 `5 × 6 = 30`으로 보고, 뒤에 0을 2개 붙이는 방식의 설명이 있습니다.
- 이는 식 변형이라기보다 계산 원리를 말로 설명한 형태입니다.

[등호별 변환 의미] (해당될 때)
- 직접적인 등호 연쇄는 없어서, 등호마다의 변환 단계는 보이지 않습니다.
- 해설 문장 속 `5 × 6 = 30`은 `50 × 60`의 기본 곱을 자릿값으로 해석하는 설명으로 읽힙니다.
- 단, 이 문장은 풀이 설명이므로 실제 정답 도출을 여기서 확정해 쓰지는 않는 것이 좋습니다.

[세부 내용]
- 문제 번호 `21` 앞에 작은 네모 체크박스처럼 보이는 기호가 있습니다.
- 상단의 두 보기 상자는 주황색 얇은 선으로 둘러싸인 직사각형입니다.
- 각 상자 안의 식은 세로셈 배치로:
  - 첫 줄에 `5 0`
  - 둘째 줄에 `× 6 0`
  - 가로줄
  - 셋째 줄에 계산 결과
- 왼쪽 보기의 결과는 `3 0 0`처럼 보이고, 오른쪽 보기는 `3 0 0 0`처럼 보입니다.
- 아래 `(정답)` 영역에도 같은 세로셈이 들어가 있으며, 결과는 `3000`으로 보입니다.
- 해설 문장은 일반 문장형 텍스트로, 곱셈의 자릿수 처리 원리를 설명합니다.

[Python DSL 작성 힌트]
- 문제 번호와 지시문은 상단 좌측 텍스트로 배치합니다.
- 두 개의 주황색 직사각형 박스를 가로로 나란히 배치합니다.
- 각 박스 내부에는 세로셈 스타일로:
  - 첫 줄 숫자 `50`
  - 둘째 줄 앞에 곱셈 기호 `×`와 숫자 `60`
  - 그 아래 가로선
  - 결과 숫자
  를 가운데 정렬에 가깝게 놓습니다.
- 결과 숫자는 박스마다 서로 다르게 렌더링되어 있으므로, 텍스트 길이와 자릿수 간격을 따로 관리하는 것이 좋습니다.
- 아래쪽에는 `(정답)` 라벨과 함께 같은 세로셈 예시를 별도 블록으로 배치합니다.
- 최하단에는 `(해설)` 문장을 일반 텍스트로 한 줄 배치합니다.
- 숫자 사이 공백은 이미지상 글자 간격을 반영해 약간 벌려 보이게 처리할 수 있습니다.

[주의할 점]
- 정답을 추론해서 보기 중 무엇이 맞는지 직접 단정하지 마세요.
- 보기 안의 결과 숫자는 이미지에 보이는 그대로 옮기되, 계산으로 재판정하지 마세요.
- 해설 문장은 풀이 원리 설명일 뿐, 이 초안에서 답을 확정하는 근거로 쓰지 마세요.
- `300`과 `3000`의 차이는 자릿수 차이로 보이지만, 이 문서에서는 그 의미를 설명만 하고 결론은 내리지 않습니다.
- `□ 21.` 앞의 작은 네모는 체크박스처럼 보이나, 정확한 형태는 단정하지 않는 것이 좋습니다.

[불확실한 부분]
- 왼쪽 보기의 결과 숫자 배열이 `300`인지 `3 0 0`인지, 글자 간격 때문에 약간 애매하게 보일 수 있습니다.
- 정답 영역의 세로셈 결과 표기는 `3000`으로 보이지만, 인쇄 해상도상 숫자 간격이 넓어 보일 수 있습니다.
- 상단 왼쪽의 작은 기호는 빈 체크박스인지 단순 장식인지 확정하기 어렵습니다.
- 주황색 박스 내부의 숫자 정렬이 완벽히 중앙인지, 약간 왼쪽 치우침이 있는지는 이미지 해상도상 세밀하게 단정하기 어렵습니다.

--- RAW VISION DRAFT END ---
