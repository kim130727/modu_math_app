[문제 개요]
- 문제 번호는 `□ 30.`으로 보인다.
- 주어진 도형은 원 1개와 중심에서 원둘레의 여러 점으로 뻗는 선분들로 구성되어 있다.
- 문장은 “선분 ㅇㄱ, 선분 ㅇㄴ, 선분 ㅇㄷ은 모두 원의 반지름입니다. 각 선분의 길이를 재어 알맞은 말을 선택하세요.”로 보인다.
- 선택 문장은 “한 원에서 원의 반지름은 모두 ( 같습니다, 다릅니다 ).” 형태이다.
- 하단에 `(정답) 같습니다`, `(해설) ...`이 인쇄되어 보인다.

[원본에 보이는 요소]
- 상단 왼쪽에 문제 번호와 문장 지시가 한 줄 또는 두 줄로 배치되어 있다.
- 중앙에는 원 도형이 크게 배치되어 있다.
- 원의 내부에 중심점이 표시되어 있으며, 중심에서 원둘레의 3점으로 선분이 연결되어 있다.
- 선분 끝점에는 `ㄱ`, `ㄴ`, `ㄷ`로 보이는 표기가 붙어 있다.
- 중심점 근처에는 `ㅇ`로 보이는 기호가 있다.
- 원 아래에는 선택 문장이 한 줄로 중앙 정렬되어 있다.
- 그 아래 왼쪽에는 `(정답)`, `(해설)`이 각각 별도 줄로 적혀 있다.
- 해설 줄에는 `(선분 ㅇㄱ) = (선분 ㅇㄴ) = (선분 ㅇㄷ) = 2 cm`가 보인다.
- 다음 줄 해설 문장으로 “따라서 세 선분의 길이는 모두 같으므로 한 원에서 원의 반지름은 모두 같습니다.”가 보인다.

[수식/관계 구조]
- 해설에는 등호가 연속된 나열형 구조가 있다.
- 보이는 형태: `(선분 ㅇㄱ) = (선분 ㅇㄴ) = (선분 ㅇㄷ) = 2 cm`
- 이 구조는 여러 선분의 길이를 같은 값으로 연결해 나열하는 방식이다.
- 마지막 항 `2 cm`는 화면에 인쇄된 값으로 보인다.
- 다만 이 값이 왜 그런지는 여기서 계산하거나 추론하지 않고, 보이는 형태만 기록한다.

[등호별 변환 의미]
- 첫 번째 `=`: `(선분 ㅇㄱ)`과 `(선분 ㅇㄴ)`의 길이가 같다고 적은 것으로 보인다.
- 두 번째 `=`: `(선분 ㅇㄴ)`과 `(선분 ㅇㄷ)`의 길이가 같다고 적은 것으로 보인다.
- 세 번째 `=`: 세 선분의 길이가 `2 cm`와 같다고 적은 것으로 보인다.
- 전체적으로 같은 종류의 선분 길이를 순서대로 연결해 보여 주는 표현이다.
- 등호는 계산 변형이라기보다 “같은 길이”를 나열하는 관계 표시로 읽는 것이 안전하다.

[빈칸 정책]
- 이 문제에는 답안을 직접 쓰는 빈칸이 보이지 않고, 보기에서 선택하는 형식으로 보인다.
- `( 같습니다, 다릅니다 )`는 선택지이며, 어떤 것을 고를지는 추론하지 않는다.
- 원본에 정답이 인쇄되어 보이더라도, DSL 작성용 초안에서는 선택 결과를 새로 채워 넣지 말고 관찰 사실로만 분리한다.
- 빈칸이나 불명확한 표기는 추정값으로 채우지 않고 그대로 비워 두거나 TODO로 남긴다.

[반복 패턴]
- 원의 중심에서 바깥 점으로 선분이 여러 개 뻗는 방사형 패턴이 반복된다.
- 같은 종류의 길이 비교 대상이 3개 나열되어 있다.
- 선택지도 `( 같습니다, 다릅니다 )`처럼 동일한 괄호 형식의 두 항목으로 반복 제시된다.
- 해설에서는 `A = B = C = D` 형태의 연쇄 등호 패턴이 나타난다.

[DSL 작성 힌트]
- 도형 요소
  - 원 1개
  - 중심점 1개
  - 중심에서 원둘레의 3점으로 이어지는 선분 3개
- 위치 관계
  - 중심은 원 내부의 중앙 부근에 배치
  - 한 선분은 왼쪽 아래 방향, 한 선분은 오른쪽 아래 방향, 한 선분은 오른쪽 방향 약간 위쪽으로 향한다
  - 각 선분의 끝점은 원둘레 위에 놓인다
- 텍스트 배치
  - 상단에 문제 문장 1개
  - 원 아래에 선택 문장 1개
  - 하단 왼쪽에 `(정답)`, `(해설)` 텍스트 블록
- 레이아웃
  - 원 도형은 본문보다 중앙 오른쪽 쪽에 크게 배치된 느낌이다
  - 선택 문장은 원 바로 아래 중앙 정렬
  - 해설은 그 아래에 줄바꿈으로 분리
- 표기 힌트
  - 점 이름은 `ㅇ`, `ㄱ`, `ㄴ`, `ㄷ`로 보이지만 OCR 오독 가능성이 있으므로 정확한 문자 판독은 TODO 처리
  - 해설의 수식은 일반 텍스트 라인 또는 수식 객체로 `A = B = C = D` 형식으로 배치 가능
- 분수/변환 관련 특이사항
  - 해당 문제에는 분수 변환이 보이지 않는다.

[semantic/solvable 힌트]
- 보이는 주제는 “한 원에서 반지름의 길이는 모두 같다”는 성질을 묻는 문제이다.
- 도형에서 중심에서 원둘레까지 연결된 세 선분이 모두 반지름으로 제시되어 있다.
- 해설 텍스트는 세 선분의 길이가 같다고 관찰 사실을 적고 있다.
- 단, 여기서는 문제를 풀어 결론을 새로 만들지 않고, 인쇄된 내용만 정리한다.
- 원본에 정답 `같습니다`가 인쇄되어 보이지만, 작성용 초안에서는 그 사실을 따로 표시하되 추론으로 보강하지 않는다.

[주의 및 TODO]
- `ㅇㄱ`, `ㅇㄴ`, `ㅇㄷ`의 표기가 정확한지 OCR 오독 가능성이 있다.
- 중심점의 `ㅇ` 기호도 실제 문자 판독이 애매하다.
- 상단의 작은 네모 `□`는 장식 또는 체크박스처럼 보이지만 확정하지 않는다.
- 해설의 `(선분 ㅇㄱ)` 등의 괄호 포함 여부는 이미지 판독 기준으로 다시 확인 필요하다.
- 원의 중심과 점 이름의 상대적 위치는 실제 이미지 기준으로 재배치해야 할 수 있다.
- 선택지의 정답이 화면에 인쇄되어 보여도, DSL용 구조화에서는 정답을 새로 채워 넣지 말고 관찰 사실로만 따로 적는다.
