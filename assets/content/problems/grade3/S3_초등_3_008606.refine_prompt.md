# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_008606.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_008606

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
[전체적인 구성]
- 화면 상단 왼쪽에 작은 빈 사각형(체크칸처럼 보임)과 함께 문제 번호 `76.`이 있고, 그 뒤에 문제 지시문이 한 줄로 이어집니다.
- 지시문 아래에는 가로로 나란히 배치된 큰 테두리 박스 2개가 있습니다.
- 왼쪽 박스에는 `491 ÷ 3`, 오른쪽 박스에는 `709 ÷ 6`이 각각 중앙 정렬로 들어 있습니다.
- 박스 아래 왼쪽에는 `(정답)` 표기와 함께 짧은 식이 있고, 그 아래에는 `(해설)` 표기와 함께 나눗셈의 몫과 나머지가 포함된 식이 제시됩니다.
- 하단에는 오른쪽을 가리키는 회색 화살표 아이콘이 있고, 그 옆에 비교 결과를 말하는 문장이 이어집니다.

[보이는 텍스트]
- `76. 나머지가 더 큰 것을 선택하세요.`
- `491 ÷ 3`
- `709 ÷ 6`
- `(정답)491 ÷ 3`
- `(해설)491 ÷ 3 = 163··2, 709 ÷ 6 = 118··1`
- `나머지의 크기를 비교하면 2>1입니다.`

[반복되는 시각 패턴]
- 큰 테두리 박스가 2개가 좌우로 반복 배치되어 있습니다.
- 두 박스 모두 비슷한 크기와 형태를 가지며, 가운데에 나눗셈 식이 하나씩 들어 있습니다.
- 아래 해설 부분에서도 두 식이 `= ... ··...` 형식으로 나란히 제시되어, 같은 비교 대상이 반복적으로 대응됩니다.
- 화살표 아이콘이 해설 문장을 강조하는 방식으로 사용됩니다.

[수식 단계]
- 해설 부분에 등호가 포함된 변형식이 2개 보입니다.
- `491 ÷ 3 = 163··2`
- `709 ÷ 6 = 118··1`
- 여기서 `··` 표시는 나머지를 나타내는 기호처럼 보이며, 나눗셈 결과를 몫과 나머지로 적어 둔 형식입니다.
- 다만 이 문서는 풀이를 확정하는 용도가 아니라 시각 요소 분석이므로, 실제 계산 과정은 전개하지 않습니다.

[등호별 변환 의미]
- 첫 번째 식 `491 ÷ 3 = 163··2`는 `491 ÷ 3`의 결과를 몫과 나머지 형태로 적어 둔 것으로 보입니다.
- 두 번째 식 `709 ÷ 6 = 118··1`도 같은 방식으로 결과를 정리한 형태입니다.
- 마지막 문장 `나머지의 크기를 비교하면 2>1입니다.`는 두 식에서 얻은 나머지를 비교한다는 의미로 읽힙니다.
- 단, 이 해설 문장의 수치 판단 자체를 여기서 검증하거나 재계산하지는 않습니다.

[세부 내용]
- 문제 번호 `76.`은 화면 왼쪽 상단에 있습니다.
- 문제 문장은 번호 뒤에 바로 이어지며, 끝에 마침표가 있습니다.
- 두 선택지는 테두리 박스 안에 들어 있으며, 박스는 연한 보라색 계열의 선으로 그려진 직사각형입니다.
- 왼쪽 박스와 오른쪽 박스 사이에는 충분한 가로 간격이 있습니다.
- 각 박스의 수식은 가운데 정렬되어 있고, 글자 크기가 비교적 크게 보입니다.
- 정답 표시 부분은 본문보다 아래쪽에 왼쪽 정렬로 배치되어 있습니다.
- `(정답)`은 괄호 포함 텍스트로 적혀 있고, 그 뒤에 `491 ÷ 3`이 바로 이어집니다.
- `(해설)`은 정답 줄 아래에 있으며, 두 식이 한 줄로 이어져 있습니다.
- 하단 화살표는 회색이며, 오른쪽을 향합니다.
- 화살표 뒤의 문장은 비교 근거를 설명하는 문장으로 보입니다.

[Python DSL 작성 힌트]
- 화면 레이아웃은 다음과 같이 구성하면 좋습니다.
  - 상단 문제 문장 1줄
  - 그 아래 좌우 2개의 선택 박스
  - 하단 왼쪽 정답/해설 텍스트 블록
  - 맨 아래 화살표와 설명 문장
- 선택 박스는 동일 스타일의 사각형 2개로 처리합니다.
- 박스 내부 텍스트는 중앙 정렬로 배치합니다.
- 해설의 `= ...··...` 형식은 등호를 포함한 수식 텍스트로 유지합니다.
- 화살표 아이콘은 별도 도형 요소로 두고, 설명 문장과 가로로 연결해 배치합니다.
- 만약 DSL에서 나눗셈 기호를 직접 입력해야 한다면 `÷`를 그대로 쓰는 방식이 자연스럽습니다.
- `··` 표기는 나머지 표시로 보이지만, 시각적으로 보이는 그대로 문자열 처리하는 편이 안전합니다.

[주의할 점]
- 답을 추론해서 채우지 마세요.
- `(정답)` 옆의 값이 실제로 맞는지 확인하거나 수정하지 마세요.
- `163··2`, `118··1`의 의미를 과도하게 해석하지 마세요. 보이는 형태 중심으로 기록하세요.
- 보라색 박스의 정확한 색상 값, 선 두께, 글꼴은 이미지에서 완전히 확정하기 어렵습니다.
- 화살표 아이콘의 정확한 크기와 위치는 대략적인 배치로만 반영하는 것이 좋습니다.

[불확실한 부분]
- `(정답)491 ÷ 3`에서 `(정답)`이 특정 선택지를 가리키는지, 혹은 해설용 표기인지 문맥상 확정하기 어렵습니다.
- `··`가 실제로 두 점인지, 나머지 기호를 대체한 표기인지 이미지 해상도상 완전히 단정하기 어렵습니다.
- 문제 지시문이 정확히 어떤 선택 기준을 요구하는지, 시각 분석만으로는 추가 해석이 필요할 수 있습니다.

--- RAW VISION DRAFT END ---
