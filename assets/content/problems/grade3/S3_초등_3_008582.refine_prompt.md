# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_008582.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_008582

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
[전체적인 구성]
- 화면 상단 왼쪽에 작은 네모 표시와 함께 문제 번호 `49.`가 있고, 그 오른쪽에 문제 문장이 한 줄로 배치되어 있다.
- 중앙에는 두 명의 어린이 캐릭터가 좌우로 나란히 배치되어 있으며, 각 캐릭터가 노란색 사각형 판을 들고 있다.
- 왼쪽 캐릭터는 `63×12`가 적힌 판을, 오른쪽 캐릭터는 `24×31`이 적힌 판을 들고 있다.
- 각 캐릭터 옆에 이름표처럼 보이는 작은 둥근 사각형 박스가 있으며, 왼쪽은 `진수`, 오른쪽은 `수호`라고 적혀 있다.
- 중앙 아래쪽에는 괄호로 묶인 `(` 진수 , 수호 `)` 형태의 텍스트가 보인다.
- 화면 하단에는 `(정답)` 표시와 함께 정답 텍스트가 보이고, 그 아래에는 `(해설)`로 시작하는 계산 설명이 한 줄로 제시되어 있다.
- 해설 줄에는 두 식의 계산 결과와 함께 `>` 기호를 사용한 비교가 들어가 있으며, 오른쪽 끝에 회색 화살표 모양이 보인다.

[보이는 텍스트]
- `49. 계산 결과가 더 큰 사람의 이름을 선택해 보세요.`
- `진수`
- `63×12`
- `24×31`
- `수호`
- `( 진수 , 수호 )`
- `(정답)진수`
- `(해설)진수: 63 × 12 = 756, 수호: 24 × 31 = 744 ➡ 756>744`

[반복되는 시각 패턴]
- 사람 이름이 들어 있는 작은 둥근 사각형 박스가 양쪽에 각각 1개씩 반복된다.
- 사람이 들고 있는 노란색 계산 카드가 좌우에 2개 반복된다.
- 계산 카드의 크기와 형태는 비슷하고, 가운데에 곱셈식이 크게 인쇄되어 있다.
- 아래 해설에서는 `이름: 식 = 결과` 형식이 두 번 반복된다.
- 마지막에 두 결과를 비교하는 `>` 기호가 사용된다.

[수식 단계]
- 해설에 식이 두 개 제시되어 있다.
  - `63 × 12 = 756`
  - `24 × 31 = 744`
- 이어서 두 결과를 비교하는 표시가 있다.
  - `756 > 744`
- 보이는 그대로, 각 곱셈식의 계산 결과가 적혀 있고 그 결과끼리 비교하는 흐름이다.

[등호별 변환 의미]
- 첫 번째 등호 `=`는 `63 × 12`를 계산한 결과가 `756`임을 연결한다.
- 두 번째 등호 `=`는 `24 × 31`을 계산한 결과가 `744`임을 연결한다.
- 마지막 비교식에서는 `>`를 사용해 앞의 결과가 뒤의 결과보다 크다는 관계를 나타낸다.
- 다만 이 비교가 어떤 선택으로 이어지는지는 화면 아래의 정답 표기에서 확인되는 형태로 보인다.

[세부 내용]
- 문제는 “계산 결과가 더 큰 사람의 이름을 선택”하는 선택형 문항이다.
- 두 사람의 이름은 각각 계산식과 연결되어 있다.
  - 왼쪽 인물: `진수` / `63×12`
  - 오른쪽 인물: `수호` / `24×31`
- 캐릭터는 단순한 일러스트이며, 계산식은 노란 판 중앙에 크게 표시되어 눈에 띈다.
- 정답 표시 부분에는 `진수`가 적혀 있으며, 해설에서는 두 곱셈 결과를 적은 뒤 비교식을 덧붙였다.
- 화면 하단의 회색 화살표는 해설의 마지막 비교 결과를 강조하는 장식 요소처럼 보인다.

[Python DSL 작성 힌트]
- 전체 화면을 상단 문제 문장, 중앙 캐릭터 2개, 하단 정답/해설의 3개 구역으로 나누어 배치하면 좋다.
- 캐릭터 영역은 좌우 대칭 구조로 잡고, 각 캐릭터 위/옆에 이름표 박스, 손에 든 계산 카드 박스를 따로 둔다.
- 계산 카드는 노란 직사각형으로, 가운데 텍스트 `63×12`, `24×31`을 넣는 방식이 적절하다.
- 이름표는 작은 둥근 사각형 테두리 박스로 표현하면 화면과 유사하다.
- 하단 해설 줄은 텍스트를 한 줄로 이어 쓰되, `이름: 식 = 결과` 형식을 두 번 배치한 뒤 비교 기호를 넣는 구조로 작성하면 된다.
- 정답 표시는 `(정답)`과 선택된 이름을 붙여 표기하는 형태로 두면 된다.
- 좌우 인물 간 간격이 넓으므로 중앙 정렬보다 좌측 캐릭터, 우측 캐릭터를 별도 그룹으로 배치하는 것이 자연스럽다.

[주의할 점]
- 계산 결과를 직접 다시 계산해서 답을 설명문에 넣지 않도록 주의해야 한다.
- 빈칸은 이 문제 화면에는 뚜렷한 입력칸 형태로 보이지 않으므로, 임의로 빈칸 요소를 추가하지 않는 것이 좋다.
- `( 진수 , 수호 )`는 보기나 선택지처럼 보이지만 실제로 어떤 용도인지는 화면만으로 단정하지 말아야 한다.
- 정답과 해설은 화면 하단에 이미 제시된 내용이므로, 이를 별도의 추론 결과로 바꾸지 말고 보이는 그대로 옮겨 적는 방향이 적절하다.
- 화살표는 단순한 도식 표시일 수 있으므로 의미를 과도하게 해석하지 말아야 한다.

[불확실한 부분]
- 하단의 `( 진수 , 수호 )`가 실제 선택지인지, 단순한 안내 문구인지 화면만으로는 확정하기 어렵다.
- 회색 화살표 아이콘이 정확히 어떤 기능을 가지는지는 불명확하다.
- 문제 상단의 작은 네모 표시가 체크박스인지 장식인지 단정할 수 없다.
- 정답과 해설이 인쇄된 위치는 보이지만, 실제 편집 시 같은 줄/같은 블록으로 재현해야 하는지 여부는 원본 레이아웃 세부에 따라 달라질 수 있다.

--- RAW VISION DRAFT END ---
