# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_008631.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_008631

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
[전체적인 구성]
- 화면은 초등 수학 문제 1개로 구성되어 있습니다.
- 상단 왼쪽에 문제 번호와 지시문이 있고, 중앙~오른쪽에 블록 그림(십의 막대와 일의 정육면체)이 배치되어 있습니다.
- 그림 아래에는 나눗셈 식 `69 ÷ 3 = □`가 있으며, 빈칸은 사각형 칸으로 표시되어 있습니다.
- 하단에는 객관식 보기 숫자 5개가 흩어져 배치되어 있고, 맨 아래에는 `(정답) ③`, `(해설) 69 ÷ 3 = 23입니다.`라는 문구가 보입니다.
- 전체적으로 “그림을 보고 빈칸에 알맞은 수 고르기” 유형입니다.

[보이는 텍스트]
- `2.`
- `수 모형을 보고 □ 안에 알맞은 수를 고르세요.`
- `69 ÷ 3 = □`
- 보기:
  - `① 21`
  - `② 22`
  - `③ 23`
  - `④ 24`
  - `⑤ 25`
- 하단:
  - `(정답) ③`
  - `(해설) 69 ÷ 3 = 23입니다.`

[반복되는 시각 패턴]
- 상단의 큰 직사각형 안에는 십의 막대와 일의 큐브가 함께 들어 있는 수 모형이 있습니다.
- 그 아래에는 비슷한 크기의 작은 직사각형 3개가 가로로 나란히 배치되어 있으며, 각 상자 안에도 같은 종류의 막대와 큐브가 들어 있습니다.
- 큰 상자에서 아래쪽 3개 상자로 향하는 아래쪽 화살표가 있어, 전체를 3개로 나누는 상황을 시각적으로 나타냅니다.
- 3개의 작은 상자는 서로 거의 동일한 형태로 반복됩니다.
- 보기 숫자는 동그라미 번호와 함께 나열되어 있으며, 정답 선택지 `③`만 원으로 표시된 번호가 붙어 있습니다.

[수식 단계]
- 이 문제에는 여러 단계의 등호 변형식은 보이지 않습니다.
- 식은 한 줄로 `69 ÷ 3 = □` 형태만 제시되어 있습니다.
- 아래 해설에는 `69 ÷ 3 = 23입니다.`라는 문장이 별도로 적혀 있습니다.  
  - 다만 이는 문제 풀이 결과를 설명하는 문구로 보이며, 단계별 변형 과정은 없습니다.

[등호별 변환 의미]
- 등호가 연속해서 나타나는 변형 과정은 보이지 않습니다.
- 보이는 것은 최종 형태의 식 `69 ÷ 3 = □`뿐입니다.
- 하단 해설의 `69 ÷ 3 = 23입니다.`는 식의 결과를 문장으로 확인해 주는 형태입니다.

[세부 내용]
- 상단 큰 직사각형:
  - 파란 테두리의 가로형 사각형 안에 수 모형이 들어 있습니다.
  - 왼쪽에는 세로로 긴 십의 막대가 여러 개 세워져 있고, 오른쪽에는 작은 정육면체들이 모여 있습니다.
- 큰 상자 아래:
  - 가운데에 회색 아래쪽 화살표 1개가 있습니다.
- 아래의 작은 직사각형 3개:
  - 모두 파란 테두리의 세로형 사각형입니다.
  - 각 상자 안에는 왼쪽에 십의 막대 2개 정도가 세로로 있고, 오른쪽에 일의 큐브가 3개씩 배치되어 있습니다.
  - 세 상자 모두 같은 내용처럼 보이지만, 실제로는 그림상 거의 동일한 형태가 반복되어 있습니다.
- 나눗셈 식:
  - `69 ÷ 3 =` 뒤에 네모 빈칸이 있습니다.
  - 빈칸은 답을 적는 칸처럼 보이며, 선택지 중 하나를 넣는 구조입니다.
- 보기 배치:
  - 왼쪽 아래와 가운데 아래, 오른쪽 아래에 숫자 보기들이 떨어져 배치되어 있습니다.
  - `① 21`, `② 22`, `③ 23`은 위쪽에, `④ 24`, `⑤ 25`는 아래쪽에 배치되어 있습니다.
- 하단 설명:
  - `(정답) ③`이 먼저 보이고, 그 아래 `(해설) 69 ÷ 3 = 23입니다.`가 적혀 있습니다.

[Python DSL 작성 힌트]
- 전체 레이아웃은 다음 요소로 나누어 그리면 좋습니다:
  1. 상단 문제 문장
  2. 중앙의 큰 수 모형 상자
  3. 큰 상자에서 아래로 향하는 화살표
  4. 아래의 3개 소형 수 모형 상자
  5. `69 ÷ 3 = □` 식
  6. 객관식 보기 5개
  7. 하단 정답/해설 문구
- 수 모형은 십의 막대와 일의 큐브를 별도 도형으로 표현하면 좋습니다.
- 상자들은 모두 사각형 테두리로 감싸고, 내부 도형은 좌우로 분리해 배치하는 방식이 적절합니다.
- 빈칸은 작은 사각형 입력칸으로 표현하세요.
- 보기의 동그라미 번호는 일반 숫자 텍스트와 원형 마커를 함께 배치하는 방식이 좋습니다.
- 정답 표시와 해설 문구는 본문과 구분되도록 하단에 작은 크기로 배치하면 됩니다.

[주의할 점]
- 답을 추론해서 빈칸에 직접 넣지 마세요.
- 보이는 수 모형의 개수는 그림 기반으로만 기술하고, 그것이 어떤 계산 결과인지 단정적으로 풀어 쓰지 마세요.
- 하단의 `(정답) ③`과 `(해설) 69 ÷ 3 = 23입니다.`는 화면에 보이는 추가 텍스트이지만, 본문 문제 요소와는 구분해서 다루는 것이 좋습니다.
- 작은 상자 3개의 내부 도형은 인쇄 상태상 약간 비슷하거나 겹쳐 보일 수 있으므로, “비슷한 형태의 모형이 3개” 정도로 안전하게 서술하세요.

[불확실한 부분]
- 큰 상자와 작은 상자들 안의 십의 막대 개수는 인쇄 해상도상 약간 헷갈릴 수 있습니다.
- 오른쪽의 일의 큐브 개수는 일부가 겹치거나 작게 보여 정확한 개별 경계를 단정하기 어렵습니다.
- 따라서 각 상자 내부의 정확한 개수는 단정하지 말고, “여러 개”, “몇 개”, “3개로 보이는 큐브”처럼 시각적으로 관찰되는 범위에서 표현하는 것이 안전합니다.

--- RAW VISION DRAFT END ---
