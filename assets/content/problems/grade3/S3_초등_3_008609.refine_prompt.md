# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_008609.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_008609

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
[전체적인 구성]
- 왼쪽 상단에 문제 번호가 작게 표시되고, 그 오른쪽에 문장형 지시문이 한 줄로 배치되어 있습니다.
- 그 아래에는 주황색 테두리의 긴 가로 직사각형 박스가 있으며, 그 안에 보기 2개가 좌우로 나란히 배치되어 있습니다.
- 박스 아래 왼쪽에는 정답 표기와 해설 문장이 따로 줄바꿈되어 있습니다.
- 전체적으로 객관식 선택형 문제 형식이며, 답은 보기 기호를 고르는 방식입니다.

[보이는 텍스트]
- `79.`
- `바르게 계산한 것을 찾아 기호를 선택해 보세요.`
- 주황색 박스 안:
  - `㉠ 28 ÷ 2 = 14`
  - `㉡ 55 ÷ 5 = 15`
- 박스 아래:
  - `(정답)㉠`
  - `(해설)㉡ 55 ÷ 5 = 11`
  - `따라서 바르게 계산한 것은 ㉠입니다.`

[반복되는 시각 패턴]
- 보기 기호 `㉠`, `㉡`가 각각 식 앞에 붙어 있습니다.
- 두 보기 모두 `나눗셈 기호(÷)`와 등호(`=`)가 포함된 한 줄 수식 형태입니다.
- 보기들은 같은 크기와 같은 유형의 수식으로 구성되어 있어, 계산 결과의 옳고 그름을 비교하도록 유도합니다.
- 아래 해설에서도 `㉡`가 다시 한 번 등장해, 보기 번호와 연결되는 반복 패턴이 있습니다.

[수식 단계]
- 박스 안에는 수식이 2개가 독립적으로 제시되어 있습니다.
  - 첫 번째: `28 ÷ 2 = 14`
  - 두 번째: `55 ÷ 5 = 15`
- 해설 영역에는 또 하나의 수식이 제시되어 있습니다.
  - `55 ÷ 5 = 11`
- 이 수식은 보기의 계산 결과와 비교되는 형태로 보이며, 보기 ㉡의 식이 틀렸음을 보여주기 위한 예시처럼 배치되어 있습니다.
- 다만 실제로 왜 그렇게 되는지는 문제를 풀지 않고, 화면에 보이는 계산식만 기록해야 합니다.

[등호별 변환 의미]
- 박스 안의 두 보기 각각은 `식 = 결과` 형식으로 되어 있어, 왼쪽의 나눗셈 식과 오른쪽의 계산 결과를 직접 연결합니다.
- 첫 번째 보기에서는 `28 ÷ 2`의 결과가 `14`로 적혀 있습니다.
- 두 번째 보기에서는 `55 ÷ 5`의 결과가 `15`로 적혀 있습니다.
- 해설의 `55 ÷ 5 = 11`은 보기 ㉡의 오른쪽 결과와 다르게 제시되어 있어, 해설에서는 이 결과를 바르게 잡아 주는 구조로 보입니다.
- 여기서는 “어떤 값이 맞다”를 계산으로 확정하지 말고, 화면에 적힌 변환 관계만 표현해야 합니다.

[세부 내용]
- 문제 번호 `79.`는 본문보다 왼쪽 위에 작게 붙어 있습니다.
- 지시문은 평서문 형태이며, “기호를 선택해 보세요”라는 문장으로 답을 고르게 합니다.
- 주황색 큰 박스는 보기 영역으로 보이며, 내부에 세로 구분선은 없고 두 식이 좌우로 넓게 분산 배치되어 있습니다.
- 각 보기 앞의 기호는 동그라미 친 한글 자모 형태로 보이며, 수식과 같은 줄에 정렬되어 있습니다.
- 박스 아래의 `(정답)` 표시는 괄호가 포함되어 있고, 그 뒤에 `㉠`만 간단히 적혀 있습니다.
- `(해설)` 뒤에는 기호 `㉡`와 함께 식이 이어지고, 그 다음 줄에 결론 문장이 있습니다.
- 마지막 문장은 “따라서 바르게 계산한 것은 …입니다.” 구조로, 선택 결과를 문장으로 정리합니다.

[Python DSL 작성 힌트]
- 문제 영역을 크게 3개로 나누면 좋습니다: 상단 지시문, 중앙 보기 박스, 하단 정답/해설.
- 중앙 박스는 주황색 외곽선 직사각형으로 그리고, 내부에 좌우 2개의 텍스트 블록을 배치합니다.
- 각 보기 텍스트는 `기호 + 수식` 순서로 한 줄에 배치합니다.
- 해설 영역은 본문 아래 왼쪽 정렬로, `(정답)`과 `(해설)`을 줄바꿈해서 배치합니다.
- 수식의 기호 `÷`, `=`의 간격과 위치를 그대로 반영하면 좋습니다.
- 선택지 기호 `㉠`, `㉡`는 원형 기호로 취급하여 별도 텍스트 객체로 넣는 방식을 고려할 수 있습니다.
- 문제를 풀기 위한 로직보다는, 화면에 보이는 레이아웃과 텍스트를 정확히 재현하는 데 집중하세요.

[주의할 점]
- `28 ÷ 2 = 14`, `55 ÷ 5 = 15`를 임의로 바꾸거나 검산 결과를 추론해서 쓰지 마세요.
- 정답을 해설과 별개로 다시 계산해서 새로 적지 마세요.
- 해설에 나온 식도 화면에 보이는 그대로만 옮겨 적어야 합니다.
- 박스의 정확한 색상은 이미지상 주황색 테두리로 보이지만, 세부 색조는 단정하지 않는 것이 좋습니다.
- 문제 풀이 설명을 덧붙이기보다, 시각적으로 어떤 요소가 어디에 있는지 중심으로 기술하세요.

[불확실한 부분]
- 주황색 테두리 박스의 정확한 선 두께는 이미지 해상도상 다소 애매합니다.
- `(정답)`과 `(해설)`의 글자 크기와 정확한 들여쓰기 폭은 이미지에서 완전히 정밀하게 판독하기 어렵습니다.
- 기호 `㉠`, `㉡`의 폰트 스타일은 일반 인쇄체처럼 보이지만, 정확한 글꼴은 단정할 수 없습니다.
- 하단 해설 문장의 정확한 줄 간격은 이미지 축소 상태라 약간의 오차가 있을 수 있습니다.

--- RAW VISION DRAFT END ---
