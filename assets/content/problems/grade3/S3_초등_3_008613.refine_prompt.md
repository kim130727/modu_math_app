# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_008613.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_008613

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
[전체적인 구성]
- 한 장짜리 객관식/선택형 문제 화면이다.
- 상단 왼쪽에 문제 번호가 있고, 그 오른쪽으로 문제 지시문이 한 줄로 배치되어 있다.
- 그 아래에는 연보라색 테두리의 큰 직사각형 선택지 박스가 가로로 3개 나란히 놓여 있다.
- 각 박스 안에는 작은 네모 칸 1개와 나눗셈 기호, 수가 함께 표시되어 있어 식 형태의 선택지를 이룬다.
- 화면 하단 왼쪽에는 `(정답)` 표시와 함께 빈칸이 포함된 식이 따로 제시되어 있다.
- 맨 아래에는 `(해설)` 문구 뒤로 설명 문장이 한 줄로 이어진다.

[보이는 텍스트]
- `83.`
- `나머지가 3이 될 수 없는 식을 찾아 선택해 보세요.`
- 첫 번째 선택지: `□ ÷ 3`
- 두 번째 선택지: `□ ÷ 5`
- 세 번째 선택지: `□ ÷ 7`
- 하단: `(정답) □ ÷ 3`
- 하단 설명: `(해설) 나누는 수가 3일 때, 나머지가 될 수 있는 수는 0, 1, 2입니다.`

[반복되는 시각 패턴]
- 세 개의 선택지가 동일한 크기의 연보라색 직사각형 박스로 반복된다.
- 각 박스는 중앙 정렬된 형태로 `□ ÷ 숫자` 구조를 가진다.
- 빈칸은 모두 같은 모양의 작은 정사각형 칸으로 표시되어 있다.
- 나눗셈 기호 `÷`가 각 선택지에서 동일한 위치와 방식으로 반복된다.
- 하단 정답 영역도 작은 네모 빈칸이 들어간 `□ ÷ 3` 구조를 반복하지만, 선택지 박스처럼 큰 테두리 박스는 없다.

[수식 단계]
- 이 문항에는 등호 `=`를 이용한 연속 변형식이 보이지 않는다.
- 따라서 수식 단계 비교보다는 선택지별 나눗셈 표현을 읽는 형태로 구성되어 있다.

[등호별 변환 의미]
- 해당 없음.
- 화면에 등호로 이어지는 변형 과정이 나타나지 않는다.

[세부 내용]
- 문제 지시문은 “나머지가 3이 될 수 없는 식”을 고르는 내용이다.
- 선택지 3개는 각각 나누는 수가 3, 5, 7로 다르다.
- 각 선택지의 왼쪽 빈칸은 피나눗셈 자리처럼 보이는 입력칸/빈칸이다.
- 하단의 `(정답) □ ÷ 3`은 별도의 정답 표기 영역으로 보이며, 빈칸은 아직 비어 있다.
- `(해설)` 문장에서는 “나누는 수가 3일 때”라는 조건과 “나머지가 될 수 있는 수는 0, 1, 2”라는 설명이 보인다.
- 이 해설은 보이는 텍스트로만 기록해야 하며, 빈칸에 들어갈 값을 임의로 채우면 안 된다.

[Python DSL 작성 힌트]
- 상단 문제 번호와 지시문을 좌측 정렬로 배치한다.
- 그 아래 가로 방향으로 선택지 박스 3개를 동일한 크기로 배치한다.
- 각 선택지 박스는 연보라색 채움 또는 옅은 배경과 보라색 테두리로 표현한다.
- 박스 내부 중앙에 작은 정사각형 빈칸과 `÷` 기호, 숫자를 한 줄로 놓는다.
- 하단 왼쪽에는 `(정답)` 라벨과 함께 `□ ÷ 3` 형태를 텍스트로 배치한다.
- 그 아래 또는 가까운 위치에 `(해설)` 문장을 한 줄로 표시한다.
- 빈칸은 실제 답을 넣지 말고, 네모 슬롯 형태로만 그린다.
- 선택지 사이 간격을 충분히 두어 3개 박스가 독립적으로 보이게 한다.

[주의할 점]
- 문제를 풀어서 정답을 추론하지 않는다.
- `(정답)` 옆 빈칸에 수를 넣지 않는다.
- 선택지 박스 안의 네모는 숫자 입력칸처럼 보이므로, 일반 도형이 아니라 슬롯으로 처리하는 것이 좋다.
- 해설 문장은 시각적으로 보이지만, DSL에서 필요하면 텍스트로만 옮기고 의미 해석은 최소화한다.
- `□ ÷ 3`, `□ ÷ 5`, `□ ÷ 7`의 순서를 바꾸지 않는다.
- 화면에 보이지 않는 추가 기호나 도형을 임의로 넣지 않는다.

[불확실한 부분]
- 왼쪽 상단의 작은 네모 기호가 정확히 선택 표시용인지 입력칸인지 화면만으로는 완전히 단정하기 어렵다.
- 선택지 박스의 정확한 색상값, 선 굵기, 글꼴은 이미지 해상도에 따라 달라질 수 있다.
- `(정답)` 아래의 `□ ÷ 3`이 실제로는 편집용 표기인지, 인쇄된 정답 영역인지 화면만으로는 확정하기 어렵다.

--- RAW VISION DRAFT END ---
