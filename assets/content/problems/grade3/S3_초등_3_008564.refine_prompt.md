# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_008564.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_008564

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
[전체적인 구성]
- 화면 상단 왼쪽에 작은 빈 사각형 표시와 함께 문제 번호가 있고, 그 오른쪽에 문제 문장이 길게 배치되어 있습니다.
- 중앙에는 두 명의 남자아이 그림이 좌우로 나란히 배치되어 있으며, 각 인물 아래쪽에 노란색 직사각형 판이 있습니다.
- 각 노란 판에는 곱셈식이 크게 인쇄되어 있습니다.
- 왼쪽 인물 쪽에는 이름표처럼 보이는 작은 둥근 사각형 말풍선이 있고, 오른쪽 인물 쪽에도 같은 형태의 이름표가 있습니다.
- 하단에는 `(정답)` 표시와 함께 한 사람 이름이 적혀 있고, 그 아래 `(해설)`이 있는 설명 줄이 이어집니다.
- 해설 줄의 오른쪽 끝에는 회색 화살표가 있으며, 식 비교 결과를 나타내는 문자열이 이어져 있습니다.

[보이는 텍스트]
- `□ 29. 계산 결과가 더 작은 사람을 선택해 보세요.`
- 왼쪽 이름표: `민재`
- 왼쪽 노란 판: `73 × 28`
- 오른쪽 이름표: `서윤`
- 오른쪽 노란 판: `31 × 65`
- 아래쪽: `(정답)서윤`
- 해설 줄: `(해설)민재: 73 × 28 = 2044, 서윤: 31 × 65 = 2015`
- 이어서 회색 화살표 뒤에: `2044>2015`

[반복되는 시각 패턴]
- 두 명의 인물이 거의 같은 형식으로 배치되어 있습니다.
  - 인물 그림
  - 이름표
  - 노란 계산판
- 노란 계산판의 크기, 색, 형태가 좌우에서 동일하게 반복됩니다.
- 이름표도 동일한 둥근 사각형 스타일로 반복됩니다.
- 하단 해설에서는 두 사람의 식이 같은 문장 구조로 나열되어 비교 형식을 반복합니다.

[수식 단계]
- 해설 줄에 두 개의 곱셈식이 각각 등호와 함께 적혀 있습니다.
  - `73 × 28 = 2044`
  - `31 × 65 = 2015`
- 그 뒤에 회색 화살표가 있고, 마지막에 `2044>2015`가 표시되어 있습니다.
- 이 부분은 계산값을 서로 비교하는 흐름으로 보입니다.  
- 다만, 이 초안에서는 실제 계산 과정 자체를 전개하지 않고, 화면에 적힌 결과 표현만 관찰합니다.

[등호별 변환 의미]
- 첫 번째 등호 `=`는 `73 × 28`과 그에 대응하는 값 `2044`를 연결하는 표시로 보입니다.
- 두 번째 등호 `=`는 `31 × 65`와 그에 대응하는 값 `2015`를 연결합니다.
- 화살표 뒤의 `2044>2015`는 두 계산 결과의 크기 비교를 나타내는 표기로 보입니다.
- 화면상으로는 각 곱셈식의 결과를 적은 뒤, 그 결과끼리 비교하여 더 큰 수/더 작은 수를 판단하는 구조입니다.  
  - 단, 실제 정답 판단 과정은 여기서 풀지 않습니다.

[세부 내용]
- 문제 번호 `29`는 왼쪽 상단에 있으며, 앞에 작은 빈 사각형 기호가 함께 있습니다.
- 문제 문장 `계산 결과가 더 작은 사람을 선택해 보세요.`는 상단 한 줄에 크게 인쇄되어 있습니다.
- 가운데 왼쪽 인물은 `민재`로 표시되어 있고, 아래 노란 판에 `73 × 28`이 적혀 있습니다.
- 가운데 오른쪽 인물은 `서윤`으로 표시되어 있고, 아래 노란 판에 `31 × 65`가 적혀 있습니다.
- 두 노란 판은 인물의 손에 들려 있는 형태로 보이며, 판 내부의 수식은 중앙 정렬에 가깝게 배치되어 있습니다.
- 하단의 `(정답)서윤`은 괄호 안 표기 후 이름이 바로 이어져 있으며, 줄바꿈 없이 한 줄에 놓여 있습니다.
- 그 아래 `(해설)` 뒤에는 `민재:`와 `서윤:`이 각각 콜론으로 구분되어 나열됩니다.
- 마지막 비교식 `2044>2015`는 해설 문장 오른쪽에 이어져 있어, 결과 비교를 시각적으로 강조합니다.
- 회색 화살표는 오른쪽을 향하는 굵은 화살표 모양입니다.

[Python DSL 작성 힌트]
- 상단 문제문은 왼쪽 정렬 텍스트로 배치하는 구성이 적절합니다.
- 가운데에는 좌우 2열 구조로 인물 일러스트를 배치하고, 각 인물 아래에 동일한 크기의 노란 사각형을 둡니다.
- 이름표는 인물 옆쪽에 작은 둥근 사각형으로 배치하면 원본 느낌을 살리기 좋습니다.
- 노란 사각형 내부에는 큰 글씨로 곱셈식을 중앙 배치합니다.
- 하단에는 `(정답)` 줄과 `(해설)` 줄을 별도 텍스트 블록으로 배치합니다.
- 해설 줄에서는 두 사람의 이름, 식, 결과, 비교 화살표, 비교식이 한 흐름으로 이어지도록 작성하면 됩니다.
- 비교 결과 표시는 텍스트와 화살표를 함께 쓰는 방식으로 재현하는 것이 좋습니다.
- 전체 배치는 위에서 아래로 읽히는 시험지형 레이아웃입니다.

[주의할 점]
- 정답을 새로 계산하거나 추론해서 채우지 마세요.
- 인물 이름표와 하단 `(정답)`의 이름이 동일한지 여부는 화면에 보이는 그대로만 옮기세요.
- 수식의 결과값은 이미지에 이미 적혀 있는 값만 사용하세요.
- 비교식의 부등호 방향을 임의로 바꾸지 마세요.
- 인물의 정확한 표정, 머리 모양, 세부 색감은 필요 이상으로 단정하지 말고, 보이는 범위 내에서만 기술하세요.

[불확실한 부분]
- 왼쪽 상단의 작은 사각형 기호는 체크박스처럼 보이지만, 실제 용도는 화면만으로 단정하기 어렵습니다.
- 인물 그림의 세부 묘사(눈, 입, 머리카락 형태)는 해상도상 일부가 뚜렷하지 않을 수 있습니다.
- 회색 화살표 뒤의 `2044>2015`는 비교식으로 보이나, 정확한 편집 의도는 이미지 전체 맥락에 따라 달라질 수 있습니다.

--- RAW VISION DRAFT END ---
