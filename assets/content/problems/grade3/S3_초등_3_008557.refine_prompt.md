# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_008557.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_008557

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
[전체적인 구성]
- 전체 화면은 한 개의 곱셈 문제를 중심으로 구성되어 있습니다.
- 상단 왼쪽에는 문제 번호와 지시문이 있고, 상단 오른쪽에는 세로셈 형태의 곱셈 계산 과정 그림이 배치되어 있습니다.
- 중간 하단에는 보라색 테두리의 큰 선택 박스가 있으며, 그 안에 3개의 보기 식이 가로로 나란히 들어 있습니다.
- 가장 아래에는 `(정답)` 표기와 `(해설)` 문장이 이어집니다.
- 이 자료는 “색칠된 부분”을 보고 실제 곱셈식에서 어떤 자리에 해당하는지 고르는 형식입니다.

[보이는 텍스트]
- 문제 번호: `20.`
- 지시문: `색칠된 부분은 실제 어떤 수의 곱인지 찾아 선택하세요.`
- 세로셈 그림 안의 숫자:
  - 위쪽에 `3 9 5`
  - 아래쪽에 `4`
  - 중간 계산 결과로 보이는 `2 0`, `3 6 0`, `1 2 0 0`, `1 5 8 0`
- 선택 박스 안의 보기:
  - `3 × 4`
  - `30 × 4`
  - `300 × 4`
- 하단 표기:
  - `(정답)300 × 4`
  - `(해설)백의 자리의 곱이므로 300 × 4 = 1200입니다.`
- 보이는 범위 안에는 별도의 빈칸 입력란은 없습니다.

[반복되는 시각 패턴]
- 세로셈 계산 영역에서 숫자들이 자릿값별로 세로 정렬되어 있습니다.
- 여러 단계의 부분곱이 위에서 아래로 차례대로 배치되어 있습니다.
- 일부 숫자 열은 연한 세로 점선으로 구분되어 있어 자릿수 위치를 강조합니다.
- 색칠된 부분은 연한 분홍색 배경으로 강조되어 있으며, 한 줄짜리 숫자 묶음(`1 2 0 0`) 전체가 하이라이트되어 있습니다.
- 보기 3개는 모두 같은 크기의 글자로 박스 안에 균등하게 배치되어 있습니다.

[수식 단계]
- 세로셈 그림은 곱셈의 자릿값 계산 과정을 단계적으로 보여줍니다.
- 맨 위의 `395`는 피곱수로 보입니다.
- 아래의 `4`는 곱하는 수로 보입니다.
- 중간의 `20`, `360`, `1200`은 자릿값별 부분곱처럼 세로로 쌓여 있습니다.
- 색칠된 `1200`은 특정 자릿값의 곱을 나타내는 것으로 보이며, 아래 해설 문장에 따르면 백의 자리 곱에 해당합니다.
- 마지막 줄의 `1580`은 전체 곱의 결과처럼 배치되어 있습니다.
- 다만 이 초안에서는 실제 계산값을 확정해 쓰지 않고, 화면에 보이는 배열만 설명합니다.

[등호별 변환 의미]
- 화면에 등호 기호가 여러 번 이어진 수식 전개는 보이지 않습니다.
- 따라서 등호별로 변환 의미를 나누어 설명할 단계는 없습니다.
- 대신 세로셈의 각 줄은 부분곱에서 최종곱으로 이어지는 계산 단계로 해석될 수 있습니다.
- 이 해석은 해설 문장과 배치 구조를 바탕으로 한 의미 설명이며, 문제 풀이 자체는 아닙니다.

[세부 내용]
- 세로셈 그림은 오른쪽 상단에 작게 배치되어 있고, 숫자 열이 좁은 간격으로 정렬되어 있습니다.
- `395`의 오른쪽 아래에 `4`가 붙어 있는 형태라서, 일반적인 세로곱셈 형식입니다.
- 각 부분곱 줄은 아래로 내려갈수록 왼쪽으로 이동한 듯한 정렬이 보입니다.
- 연한 분홍색으로 칠해진 줄은 `1 2 0 0`이며, 이 줄 전체가 강조 대상입니다.
- 선택 박스는 가로로 긴 직사각형이며, 보라색 외곽선과 연한 보라색 배경을 가집니다.
- 보기들은 박스 내부에서 좌, 중, 우로 나뉘어 중앙 정렬되어 있습니다.
- 하단 해설 문장은 `백의 자리의 곱`이라는 표현으로 강조된 줄의 자릿값 의미를 설명합니다.
- `(정답)300 × 4`는 문제의 정답 표시로 보이지만, 초안에서는 이를 정답으로 확정해 풀이하지 않고 화면에 적힌 텍스트로만 기록합니다.

[Python DSL 작성 힌트]
- 상단 문제 문장은 텍스트 객체로 배치하면 됩니다.
- 오른쪽 상단의 세로셈은 자릿수별 숫자들을 개별 텍스트로 놓고, 점선 세로 가이드와 가로 구분선을 추가하는 방식이 적합합니다.
- `1200`이 있는 행은 연한 분홍색 직사각형 배경으로 하이라이트 처리해야 합니다.
- 선택지는 하나의 큰 보라색 테두리 박스 안에 3열 텍스트로 배치합니다.
- 하단의 `(정답)`, `(해설)`은 별도 텍스트 줄로 추가합니다.
- 숫자 간 간격과 세로 정렬이 중요하므로, 글자 위치를 자릿수 기준으로 맞추는 것이 좋습니다.
- 세로셈의 맨 윗줄, 중간 부분곱들, 마지막 결과줄을 각각 독립 요소로 분리하면 구현이 수월합니다.

[주의할 점]
- 색칠된 `1200`을 임의로 다른 값으로 해석하지 마세요.
- 보기 중 어느 것이 정답인지 풀이 과정으로 새로 계산해 확정하지 마세요.
- `395`와 `4`의 곱셈을 직접 계산하는 설명은 피하세요.
- 세로셈의 각 줄이 정확히 어떤 곱셈 규칙으로 나온 것인지 단정적으로 풀이하지 말고, 화면에 보이는 배치 중심으로 설명하세요.
- 문제 지시문은 “어떤 수의 곱인지 찾아 선택”이므로, 선택지 중심의 객관식 형식임을 유지해야 합니다.

[불확실한 부분]
- 세로셈 그림의 일부 숫자 정렬은 화면 해상도상 약간 애매하게 보일 수 있습니다.
- 각 부분곱 줄이 정확히 어떤 자리 이동을 의미하는지는 해설 문장을 통해 추정할 수 있지만, 초안에서는 단정하지 않는 것이 좋습니다.
- 마지막 결과로 보이는 `1580`의 의미는 화면상 보이지만, 문제의 핵심 강조 대상은 색칠된 `1200`으로 보입니다.
- 세로 점선의 정확한 개수와 간격은 이미지 크기에 따라 달라 보일 수 있으므로, 구현 시 대략적인 위치 기준으로 처리하는 것이 안전합니다.

--- RAW VISION DRAFT END ---
