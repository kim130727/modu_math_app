# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_008714.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_008714

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
[전체적인 구성]
- 화면 상단 왼쪽에 문제 번호와 문제 문장이 크게 제시되어 있습니다.
- 중앙 오른쪽에는 원 그림이 하나 있고, 원 위에 자(또는 곧은 막대)와 연필 그림이 겹쳐 그려져 있습니다.
- 원 그림 아래쪽에는 선택지처럼 보이는 텍스트가 가로로 배치되어 있습니다.
- 화면 하단 왼쪽에는 `(정답)` 표시와 함께 정답 단어가 따로 적혀 있고, 그 아래에는 `(해설)` 문장이 한 줄로 제시되어 있습니다.
- 전체적으로 “그림을 보고 이름을 쓰는” 형태의 초등 수학 문제 레이아웃입니다.

[보이는 텍스트]
- `□ 14. 그림과 같이 띠 종이를 이용하여 원을 그릴 때, 누름 못이 꽂힌 점을`
- `무엇이라고 할까요?`
- `원의 ( 중심 , 반지름 , 지름 )`
- `(정답)중심`
- `(해설)원의 한가운데에 있는 누름 못이 꽂힌 점이 원의 중심입니다.`
  
보이는 텍스트만 기준으로 읽으면:
- 문제는 “띠 종이를 이용하여 원을 그릴 때”를 전제로 합니다.
- 질문은 “누름 못이 꽂힌 점”의 이름을 묻고 있습니다.
- 선택처럼 보이는 표현으로 `원의 ( 중심 , 반지름 , 지름 )`가 제시되어 있습니다.
- 하단에는 정답과 해설이 인쇄되어 있습니다.

[반복되는 시각 패턴]
- 원 도형이 단독으로 크게 제시되고, 그 위 특정 위치에 도구 그림이 겹치는 패턴입니다.
- 원 안/원 위의 한 지점을 강조하는 방식으로, 연필과 자가 그 점을 가리키는 형태입니다.
- 선택지 표현이 괄호 안에 쉼표로 나열된 단어 목록 형태로 반복적으로 보입니다.
- 문제 번호 앞에 작은 네모(□)가 있어 체크 박스처럼 보입니다.

[수식 단계]
- 해당 없음.
- 이 문항은 계산식이나 등호를 이용한 변형이 보이지 않습니다.

[등호별 변환 의미]
- 해당 없음.
- 등호가 등장하는 수식 전개는 보이지 않습니다.

[세부 내용]
- 원은 화면 중앙보다 약간 오른쪽에 위치하며, 얇은 회색 선으로 그려져 있습니다.
- 원의 오른쪽 경계 근처에 연한 파란색의 짧은 직사각형 도구가 놓여 있습니다.
- 그 직사각형 안에는 작은 점들이 여러 개 세로가 아니라 가로 방향으로 일정 간격으로 배치되어 보입니다.
- 직사각형의 왼쪽 위쪽에는 누름 못처럼 보이는 작은 표시가 있고, 연필 끝은 직사각형 오른쪽 끝 부근과 원의 둘레가 만나는 지점을 향해 있는 듯 보입니다.
- 연필은 대각선 방향으로 놓여 있으며, 상단이 오른쪽 위를 향합니다.
- 이 그림은 원을 그릴 때 한 점을 중심으로 고정하고 도구를 회전시키는 상황을 시각화한 것으로 보입니다.
- 아래 해설 문장은 “원의 한가운데에 있는 누름 못이 꽂힌 점”이라는 표현으로 원의 내부 중심 위치를 설명하고 있습니다.  
  - 다만 이는 하단의 설명 문구에 적힌 내용이며, 그림만으로 독자가 직접 추론해야 하는 부분은 아닙니다.
- 선택지 텍스트 `원의 ( 중심 , 반지름 , 지름 )`는 괄호 안에서 하나를 고르는 형식처럼 보이지만, 실제로는 선택지 배열인지 문장 중 일부인지 약간 애매합니다.

[Python DSL 작성 힌트]
- 큰 원 1개를 중심 배치로 그립니다.
- 원의 오른쪽 경계 부근에 짧은 가로 직사각형 도구를 겹쳐 배치합니다.
- 직사각형 위에 점 표시 여러 개를 작은 원형 점으로 배열합니다.
- 직사각형 왼쪽 위쪽에는 누름 못처럼 보이는 작은 표시를 추가합니다.
- 직사각형 오른쪽 위에서 왼쪽 아래 방향으로 연필 이미지를 대각선으로 배치합니다.
- 문제 문장은 좌상단에 2줄로 줄바꿈되어 배치합니다.
- 하단에는 `(정답)`과 `(해설)` 텍스트를 각각 अलग 줄로 배치합니다.
- 선택지처럼 보이는 `원의 ( 중심 , 반지름 , 지름 )` 텍스트는 원 그림 아래쪽 오른편에 가로로 놓습니다.
- 원, 도구, 연필의 상대적 위치 관계를 유지하는 것이 중요합니다.

[주의할 점]
- 답을 추론하여 빈칸을 채우면 안 됩니다. 이 초안에서는 정답을 다시 계산하거나 증명하지 않습니다.
- “누름 못이 꽂힌 점”이 무엇인지 문제 해결식으로 단정하지 말고, 화면에 적힌 텍스트와 배치만 기술해야 합니다.
- `(정답)중심`은 인쇄된 텍스트로 보이지만, 작성용 초안에서는 정답을 근거로 서술을 확장하지 않는 것이 좋습니다.
- 도구 그림이 자인지, 띠 종이인지, 연필인지의 표현은 시각적으로 완전히 명확하지 않을 수 있으므로 “자처럼 보이는 직사각형 도구”, “연필처럼 보이는 도형”처럼 완곡하게 적는 것이 안전합니다.
- 선택지 `원의 ( 중심 , 반지름 , 지름 )`의 정확한 의도는 문항 형식상 선택지로 보이지만, 화면 일부만으로는 편집 방식이 확정적이지 않습니다.

[불확실한 부분]
- 원 위에 겹친 직사각형 도구가 정확히 무엇을 상징하는지, 화면만으로는 100% 확정하기 어렵습니다. 자/띠 종이/도구 중 어떤 명칭을 쓸지는 주의가 필요합니다.
- 원 그림의 중심점이 실제로 점으로 표시되어 있는지 여부는 해상도상 뚜렷하지 않습니다.
- 하단의 `원의 ( 중심 , 반지름 , 지름 )`가 단순한 안내 문구인지, 선택지인지, 혹은 편집상의 구성 요소인지는 다소 모호합니다.
- `(정답)중심`의 위치가 문제 본문과 같은 레벨에 있는지, 해설 영역인지 경계가 다소 애매합니다.

--- RAW VISION DRAFT END ---
