# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_008684.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_008684

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
[전체적인 구성]
- 한 장짜리 초등 수학 객관식 문항 이미지입니다.
- 상단 왼쪽에 문제 번호와 문제 문장이 있고, 중앙에는 두 개의 그림이 나란히 배치되어 있습니다.
- 왼쪽 그림은 누름못, 줄, 연필, 손이 포함된 설명 그림이고, 오른쪽 그림은 완성된 원과 기준선이 있는 도식입니다.
- 하단에는 주황색 테두리의 큰 문장 상자가 있으며, 그 아래에 정답과 해설 문장이 작게 따로 적혀 있습니다.
- 전체적으로 “그림을 보고 맞는 말 고르기” 형식이며, 답안 선택지는 괄호 안에 2개만 제시되어 있습니다.

[보이는 텍스트]
- 상단 문제문:
  - `72. 누름 못과 띠 종이를 이용하여 원을 그렸습니다. 그림을 보고 알맞은 말을 선택하세요.`
- 왼쪽 그림 옆의 초록색 설명:
  - `① 한쪽 구멍에`
  - `누름못을 꽂아`
  - `띠 종이를 고정`
  - `해요.`
  - `② 다른 구멍에`
  - `연필을 꽂고`
  - `한 바퀴 돌려`
  - `원을 그려요.`
- 주황색 박스 안 문장:
  - `누름 못이 꽂힌 점에서 원 위에 찍은 4개의 점까지의 길이는`
  - `모두 ( 같습니다, 다릅니다 ).`
- 하단 정답 표시:
  - `(정답) 같습니다`
- 하단 해설:
  - `(해설) 누름 못이 꽂힌 점에서 원 위에 찍은 4개의 점까지의 길이는 모두 같습니다.`

[반복되는 시각 패턴]
- 왼쪽 그림과 오른쪽 그림이 나란히 비교 구조로 배치되어 있습니다.
- 왼쪽 그림에는 초록색 숫자 `①`, `②`가 각각 붙어 있고, 각각에 설명 문장이 여러 줄로 분리되어 있습니다.
- 오른쪽 그림은 원과 정사각형 테두리, 중심의 십자 점선이 대칭적으로 배치되어 있어 기하 도식 느낌이 강합니다.
- 원 위의 표시점이 여러 개 반복되어 나타납니다.
  - 왼쪽 그림에서는 연필이 지나간 것으로 보이는 경로 위에 작은 점들이 반복되어 있습니다.
  - 오른쪽 그림에서는 원의 위, 아래, 왼쪽, 오른쪽에 검은 점이 4개 표시되어 있습니다.
- 하단 선택지 문장에서는 괄호 안에 두 단어가 쉼표로 나란히 제시되어 있어, 둘 중 하나를 고르는 형태입니다.

[수식 단계]
- 이 문항에는 등호가 있는 수식 변형 단계가 보이지 않습니다.
- 따라서 계산식의 단계별 변화는 해당되지 않습니다.

[등호별 변환 의미]
- 해당 사항 없음. 등호를 포함한 수식 전개가 보이지 않습니다.

[세부 내용]
- 왼쪽 그림:
  - 정사각형처럼 보이는 바깥 테두리 안에 큰 원이 들어 있습니다.
  - 원의 한 지점 근처에 손이 누르고 있는 모습이 있고, 그 지점에서 연필 쪽으로 띠 종이가 연결된 형태입니다.
  - 연필은 위쪽 오른편에 들려 있으며, 띠 종이를 따라 원을 그리는 동작을 설명하는 그림입니다.
  - 초록색 곡선 화살표가 두 설명 단계 사이의 진행을 나타내는 듯 배치되어 있습니다.
- 오른쪽 그림:
  - 바깥 테두리는 정사각형이고, 그 안에 원이 정확히 내접한 형태로 보입니다.
  - 원의 중심에는 작은 점이 있으며, 점선 세로선과 점선 가로선이 서로 교차합니다.
  - 원과 점선이 만나는 네 방향 끝점에 검은 점이 찍혀 있습니다.
- 하단 주황색 박스:
  - 박스 안 문장은 중앙 정렬에 가깝게 배치되어 있고, 괄호 안에 두 선택어가 들어 있습니다.
  - 선택어는 `같습니다`, `다릅니다`로 보이며, 빈칸형 선택 구조입니다.
- 정답/해설 영역:
  - 주황 박스 아래 왼쪽에 `(정답)`과 `(해설)`이 이어져 있습니다.
  - 해설은 한 줄로 길게 적혀 있습니다.

[Python DSL 작성 힌트]
- 전체 화면은 상단 문제문 + 중앙 그림 2개 + 하단 선택 문장 + 정답/해설의 4단 구역으로 나누어 배치하면 좋습니다.
- 왼쪽 그림과 오른쪽 그림은 같은 높이에 맞춰 가로로 정렬해야 합니다.
- 왼쪽 그림의 초록색 설명 텍스트는 숫자 `①`, `②`와 함께 각각 여러 줄로 줄바꿈 처리합니다.
- 오른쪽 그림에는:
  - 정사각형 외곽선,
  - 내부 원,
  - 중심 점,
  - 세로 점선,
  - 가로 점선,
  - 네 개의 경계점
  을 별도로 그리도록 분리하는 것이 좋습니다.
- 주황색 선택 문장은 테두리 사각형 박스 내부에 중앙 배치로 넣는 구성이 적절합니다.
- 괄호 안 선택지는 텍스트 두 개를 쉼표로 나란히 배치한 형태로 표현하면 됩니다.
- 정답과 해설은 본문과 분리하여 하단에 작은 글씨로 넣는 레이아웃이 적합합니다.

[주의할 점]
- 문제를 풀거나 정답 선택지를 추론해 넣지 마세요.
- 다만 이미지 하단에 이미 `정답 같습니다`가 보이지만, DSL 작성 시에는 문제 본문과 정답 표시 영역을 분리해서 다루는 것이 좋습니다.
- 왼쪽 설명 그림의 연필 위치, 손 위치, 띠 종이의 곡선 방향은 정확히 대응시켜야 합니다.
- 오른쪽 그림의 점선은 실선이 아니라 점선이며, 중심선이 십자 형태로 교차합니다.
- 바깥 테두리는 원만 있는 것이 아니라 정사각형 틀 안에 원이 들어간 형태입니다.
- 주황색 박스는 단순 텍스트 박스이며, 입력칸처럼 생긴 빈칸은 따로 보이지 않습니다.

[불확실한 부분]
- 왼쪽 그림의 정확한 선 굵기와 곡선 화살표의 시작·끝 위치는 이미지 해상도상 세밀하게는 다소 अस्प명확합니다.
- 연필 끝과 띠 종이가 만나는 지점의 정확한 접점은 작은 크기 때문에 약간 불분명합니다.
- 오른쪽 그림의 중심점은 회색 테두리 원처럼 보이기도 하므로, 실제 색상보다 도형 요소로 인식하는 것이 안전합니다.
- 하단의 `(정답)같습니다` 표기는 원문에서 줄바꿈 없이 붙어 보이지만, 실제 DSL에서는 띄어쓰기와 줄 배치를 조정할 수 있습니다.

--- RAW VISION DRAFT END ---
