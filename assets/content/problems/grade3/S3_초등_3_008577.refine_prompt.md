# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_008577.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_008577

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
[전체적인 구성]
- 화면 상단 왼쪽에 문항 번호와 지시문이 있고, 중앙에는 곱셈식 일부를 배치한 세로형 계산 모양이 보입니다.
- 오른쪽에는 연두색 테두리의 직사각형 박스가 있으며, 그 안에 선택지처럼 보이는 식들이 세로로 나열되어 있습니다.
- 화면 하단 왼쪽에는 `(정답)` 표시와 함께 정답이 직접 적혀 있고, 그 아래에는 `(해설)` 문장이 있습니다.
- 전체적으로 “그림/식에서 실제 곱해진 부분 찾기” 유형의 객관식 또는 선택형 구성으로 보입니다.

[보이는 텍스트]
- `43.`
- `색칠된 부분은 실제 어떤 수의 곱인지 찾아 선택하세요.`
- 오른쪽 박스 안:
  - `90 × 20`
  - `9 × 2`
  - `900 × 2`
  - `90 × 2`
- 하단:
  - `(정답)90 × 2`
  - `(해설)5930에서 9는 십의 자리 숫자이므로 90 × 2 = 180입니다.`
- 중앙 계산 모양 안에는 숫자 `5`, `9`, `3`, `2`가 보이며, 일부는 색칠되어 있습니다.
- 중앙 왼쪽 아래 쪽에 곱셈 기호 `×`가 보입니다.

[반복되는 시각 패턴]
- 오른쪽 선택지 박스는 세로로 항목이 반복 배치된 형태입니다.
- 중앙의 계산 모양은 세로줄/점선 안내선이 여러 개 반복되어 있어 자리값을 구분하는 표처럼 보입니다.
- 숫자 `9`와 `2`가 색칠된 영역으로 강조되어 있으며, 특정 자리의 숫자와 곱하는 수를 연결시키는 시각적 패턴이 있습니다.
- 연두색 배경 강조는 핵심 숫자를 표시하는 용도로 반복적으로 쓰인 듯합니다.

[수식 단계]
- 이 문항 화면 자체에는 등호 `=`가 여러 번 이어지는 변형식은 보이지 않습니다.
- 다만 하단 해설 문장에 `90 × 2 = 180` 형태의 등호가 1회 등장합니다.
- 따라서 “수식 전개 과정”보다는 “정답과 해설 제시”에 가까운 구성입니다.

[등호별 변환 의미]
- 화면에 실제로 보이는 등호는 `90 × 2 = 180`의 한 번뿐입니다.
- 이 등호는 왼쪽의 곱셈식이 오른쪽의 계산 결과와 같다는 뜻으로 읽힙니다.
- 변형 단계가 여러 개 나열된 형태는 아니므로, 단계별 변환 설명은 제한적입니다.

[세부 내용]
- 중앙의 계산 배치는 세로로 놓인 곱셈식의 일부처럼 보입니다.
- `5 9 3`이 가로로 놓여 있고, 그 아래 또는 옆에 `2`가 배치되어 있어 두 수의 곱셈을 나타내는 상황으로 보입니다.
- `9`는 연두색으로 칠해져 있어 자리값을 강조한 것으로 보입니다.
- `2`도 연두색으로 칠해져 있으며, 곱하는 수 또는 선택해야 할 부분을 강조하는 느낌입니다.
- 왼쪽의 `×` 기호는 세로셈 형식의 곱셈을 암시합니다.
- 오른쪽 박스의 보기들은 `90 × 20`, `9 × 2`, `900 × 2`, `90 × 2`로, 자리값을 달리 해석한 곱셈식 후보들로 보입니다.
- 하단 해설 문장에는 `5930에서 9는 십의 자리 숫자`라는 설명이 보이지만, 이 문장 자체는 문제 풀이 설명이므로 시각적으로는 참고용 텍스트로 인식됩니다.

[Python DSL 작성 힌트]
- 전체 레이아웃을 상단 문항번호/지시문, 중앙 계산 도식, 우측 선택지 박스, 하단 정답/해설의 4개 구역으로 나누어 배치하면 좋습니다.
- 중앙 도식은 점선 세로 안내선과 가로 기준선이 있는 계산용 격자 형태로 그리면 좋습니다.
- `5`, `9`, `3`, `2`는 서로 간격을 두고 자리값처럼 배치하고, `9`와 `2`에만 연두색 하이라이트를 넣는 방식이 적절합니다.
- 오른쪽 선택지 박스는 연두색 테두리 사각형 안에 4개의 텍스트를 위에서 아래로 정렬하면 됩니다.
- 하단에는 `(정답)`과 `(해설)` 라벨을 별도 텍스트로 넣되, 정답값은 그대로 보이는 문자열만 사용해야 합니다.

[주의할 점]
- 보이는 답을 빈칸에 끼워 넣는 식으로 재구성하지 마세요.
- 해설의 수학적 의미를 확장해서 추가 계산을 만들지 마세요.
- 중앙의 숫자 배치는 실제 세로셈인지, 단순한 자리값 강조 그림인지 단정하지 말고 “그처럼 보인다” 수준으로 서술하는 것이 안전합니다.
- 오른쪽 선택지의 정확한 선택 여부를 시각 정보만으로 판단하지 말고, 화면에 보이는 텍스트만 그대로 반영하세요.
- 연두색 칠이 무엇을 의미하는지는 문제 맥락상 추정 가능하더라도, 설명에서는 “강조 표시” 정도로만 표현하는 것이 좋습니다.

[불확실한 부분]
- 중앙의 점선/격자선이 모두 동일한 의미를 갖는지, 아니면 단순 배치 보조선인지 화면만으로는 확정하기 어렵습니다.
- `5 9 3`과 `2`의 상대적 위치가 세로셈의 어떤 단계에 해당하는지 정확히 판독하기 어렵습니다.
- 오른쪽의 초록 테두리 박스가 정답 후보 영역인지, 단순 설명 박스인지 화면만으로는 명시적으로 구분되지 않습니다.
- 하단 해설 문장에 적힌 `5930`의 마지막 `0`이 문제 본문과 어떤 직접적 관계를 갖는지, 시각 정보만으로는 완전히 단정하기 어렵습니다.

--- RAW VISION DRAFT END ---
