# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_008563.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_008563

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
[전체적인 구성]
- 상단에 문제 번호와 지시문이 있고, 가운데에는 두 명의 어린이 캐릭터가 좌우로 배치되어 있습니다.
- 왼쪽 어린이와 오른쪽 어린이 각각이 노란색 판넬을 들고 있으며, 판넬 안에 곱셈식이 하나씩 적혀 있습니다.
- 각 어린이 옆에는 이름표처럼 보이는 작은 테두리 박스가 하나씩 붙어 있습니다.
- 하단에는 정답 표시와 해설이 텍스트로 따로 제시되어 있습니다.
- 전체적으로 “두 계산 결과를 비교해 더 큰 사람을 고르는” 유형의 시각 자료입니다.  
  - 이 의미는 문제의 지시문과 해설에 의해 드러납니다.
  - 다만 실제로 어떤 계산을 해야 하는지는 여기서 풀지 않습니다.

[보이는 텍스트]
- 문제 번호: `28.`
- 지시문: `계산 결과가 더 큰 사람을 선택해 보세요.`
- 왼쪽 이름표: `은우`
- 왼쪽 노란 판넬의 식: `24 × 42`
- 오른쪽 이름표: `도윤`
- 오른쪽 노란 판넬의 식: `19 × 78`
- 하단 정답 표기: `(정답)도윤`
- 하단 해설:
  - `은우: 24 × 42 = 1008, 도윤: 19 × 78 = 1482`
  - 이어서 회색 화살표 기호 `→`
  - `1008<1482`

[반복되는 시각 패턴]
- 두 어린이 캐릭터가 거의 같은 구조로 반복 배치되어 있습니다.
- 각각의 캐릭터 아래에 노란색 직사각형 카드가 있고, 카드에는 곱셈식이 가운데 정렬되어 있습니다.
- 각 카드 옆에 이름표처럼 생긴 작은 둥근 사각형 박스가 있어, 인물-식-이름이 한 세트로 대응됩니다.
- 좌우 대칭에 가까운 배치이지만, 왼쪽은 `은우`, 오른쪽은 `도윤`으로 다른 값이 들어 있습니다.
- 하단 해설에서도 `은우:`와 `도윤:`이 같은 형식으로 나열되어 비교 구조를 반복합니다.

[수식 단계]
- 해설 부분에 등호가 포함된 계산 결과가 제시되어 있습니다.
- 첫 번째 식: `24 × 42 = 1008`
- 두 번째 식: `19 × 78 = 1482`
- 그 다음 비교식으로 `1008<1482`가 제시되어 있습니다.
- 이 부분은 계산 과정을 보여주는 형식이지만, 중간 전개 단계는 생략되어 있습니다.
- 즉, 곱셈 결과를 적고, 그 결과를 부등호로 비교하는 구조입니다.

[등호별 변환 의미]
- `24 × 42 = 1008`
  - 왼쪽 곱셈식을 계산한 결과가 오른쪽 숫자로 적혀 있습니다.
  - 이 등호는 “곱셈의 값”을 나타냅니다.
- `19 × 78 = 1482`
  - 오른쪽 곱셈식의 계산 결과를 나타냅니다.
- 이후 `1008<1482`
  - 두 계산 결과를 비교한 부등호 표현입니다.
  - 등호가 아니라 부등호가 사용되어, 두 수의 크기 관계를 보여줍니다.
- 회색 화살표 `→`
  - 앞의 계산 결과에서 뒤의 비교 표현으로 넘어감을 시각적으로 연결합니다.
  - 변형의 방향을 표시하지만, 실제 계산 과정의 상세 단계는 아닙니다.

[세부 내용]
- 상단 문제 문구는 검은색 글씨로, 왼쪽에 작은 사각형 체크칸처럼 보이는 요소가 함께 있습니다.
- 인물은 만화풍 얼굴과 상반신만 보이는 형태입니다.
- 왼쪽 인물은 갈색 머리, 오른쪽 인물은 안경을 쓴 모습입니다.
- 두 인물 모두 노란 카드의 양쪽을 손으로 잡고 있는 듯한 표현입니다.
- 노란 카드의 크기는 가로로 넓고 세로는 비교적 낮은 직사각형입니다.
- 카드 안의 수식은 중앙 정렬이며, `×` 기호가 크게 보입니다.
- 이름표는 카드와 인물 사이에 붙은 작은 테두리 박스로, 약간 입체감 있는 둥근 사각형입니다.
- 하단 해설은 본문보다 작은 글씨로 배치되어 있으며, `은우:`와 `도윤:`이 쉼표로 구분됩니다.
- 정답 표기는 해설보다 위쪽에 따로 한 줄로 제시되어 있습니다.
- 전체적으로 “그림 선택형 문제 + 하단 해설 제공”의 교재 스타일입니다.

[Python DSL 작성 힌트]
- 페이지 상단에 문제 번호 `28.`과 지시문 텍스트를 좌측 정렬로 배치합니다.
- 중앙 영역에는 좌우 2개의 인물 일러스트를 배치하고, 각 인물 아래에 노란 직사각형 박스를 둡니다.
- 각 노란 박스에는 각각 `24 × 42`, `19 × 78` 텍스트를 가운데에 배치합니다.
- 각 인물 옆에는 작은 둥근 사각형 라벨을 넣고, 왼쪽은 `은우`, 오른쪽은 `도윤`을 넣습니다.
- 하단에는 `(정답)도윤` 텍스트를 별도 줄로 표시합니다.
- 그 아래 해설 줄에는 `은우: 24 × 42 = 1008, 도윤: 19 × 78 = 1482 → 1008<1482` 형태의 텍스트를 배치합니다.
- 좌우 대칭 배치, 인물-카드-이름표의 상대 위치를 유지하면 원본 구조를 잘 재현할 수 있습니다.
- 화살표 기호는 회색 계열로 표시하면 원본 분위기와 비슷합니다.

[주의할 점]
- 문제의 핵심은 “더 큰 계산 결과를 가진 사람”을 고르는 것이지만, 여기서는 정답을 풀이하지 않습니다.
- 카드 안의 수식과 하단 해설의 숫자는 보이는 그대로 옮기되, 다른 값으로 바꾸지 마세요.
- 이름표의 위치 관계를 임의로 바꾸지 않는 것이 좋습니다.
- 정답 표기 `(정답)도윤`은 자료에 이미 표시된 내용이므로, 시각 요소로서만 기록해야 합니다.
- 해설의 `1008<1482`는 부등호 표현이며, 이것을 등호식으로 오해하지 않도록 주의합니다.

[불확실한 부분]
- 상단 왼쪽의 작은 사각형 표시는 체크칸처럼 보이지만, 정확히 어떤 UI 요소인지 단정하기는 어렵습니다.
- 인물 그림의 세부 표정과 손 위치는 작게 그려져 있어 일부는 해상도에 따라 달라 보일 수 있습니다.
- 노란 카드의 테두리 두께와 그림자 효과는 정확한 스타일을 단정하기 어렵습니다.
- 하단 해설의 글자 크기와 줄 간격은 화면 캡처 기준이라 실제 인쇄본과 약간 다를 수 있습니다.

--- RAW VISION DRAFT END ---
