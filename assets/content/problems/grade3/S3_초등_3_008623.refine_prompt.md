# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_008623.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_008623

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
[전체적인 구성]
- 흰 배경의 수학 문제 화면이다.
- 상단 왼쪽에 문제 번호 `93.`이 있고, 그 오른쪽에 문제 문장이 한 줄로 배치되어 있다.
- 문제 본문 아래에는 보기 5개가 원형 번호와 함께 2열 비슷한 형태로 흩어져 배치되어 있다.
- 중간 왼쪽에 `(정답) ②`가 따로 표시되어 있다.
- 그 아래에 `(해설)`이 있고, 각 보기의 나눗셈식을 차례로 적은 해설이 세로로 나열되어 있다.
- 맨 아래에는 왼쪽을 향하지 않고 오른쪽 방향의 회색 화살표 아이콘이 있고, 그 뒤에 정답 문장이 이어진다.

[보이는 텍스트]
- `93. 나누어떨어지는 나눗셈은 어느 것일까요?`
- 보기:
  - `① 11 ÷ 2`
  - `② 27 ÷ 3`
  - `③ 35 ÷ 6`
  - `④ 42 ÷ 5`
  - `⑤ 66 ÷ 7`
- ` (정답) ②`
- ` (해설) ① 11 ÷ 2 = 5···1`
- `② 27 ÷ 3 = 9`
- `③ 35 ÷ 6 = 5···5`
- `④ 42 ÷ 5 = 8···2`
- `⑤ 66 ÷ 7 = 9···3`
- 하단 문장:
  - `나누어떨어지는 나눗셈은 ② 27 ÷ 3입니다.`
- 주의: 해설의 나머지 표시는 가운데 점 3개처럼 보이는 `···` 형태로 인쇄되어 있다.

[반복되는 시각 패턴]
- 보기 번호는 모두 파란색의 동그라미 안 숫자 형태로 표시되어 있다.
- 각 보기의 식은 `숫자 ÷ 숫자` 구조로 동일한 형식을 반복한다.
- 해설 부분도 `번호 + 식 + = 결과` 형식이 반복된다.
- 나머지가 있는 경우에는 등호 뒤에 `몫···나머지`처럼 한 줄로 적혀 있고, 나머지가 없는 경우는 등호 뒤에 결과 숫자만 적혀 있다.
- 하단 결론 문장에서는 다시 한 번 `② 27 ÷ 3`이 강조되어 반복된다.

[수식 단계]
- 이 문제에는 단계별 변형식이 여러 등호로 이어지는 형태는 보이지 않는다.
- 각 해설은 한 번의 등호만 사용하여 결과를 적는 형식이다.
- 일부 해설은 나눗셈 결과가 나눠떨어지는지 여부를 보여주기 위해 `= 정수` 또는 `= 몫···나머지` 형태로 표시되어 있다.

[등호별 변환 의미]
- `11 ÷ 2 = 5···1`
  - 보이는 의미: 나눗셈 결과를 몫과 나머지 형태로 적은 것처럼 보인다.
  - 실제 수치 계산은 하지 않음.
- `27 ÷ 3 = 9`
  - 보이는 의미: 나머지 없이 정수 하나로 끝나는 형태로 적혀 있다.
- `35 ÷ 6 = 5···5`, `42 ÷ 5 = 8···2`, `66 ÷ 7 = 9···3`
  - 보이는 의미: 각각 몫과 나머지가 함께 적힌 형태이다.
- `=` 기호는 각 보기의 나눗셈과 그에 대응하는 해설 결과를 연결하는 역할로 보인다.

[세부 내용]
- 문제 문장 `나누어떨어지는 나눗셈은 어느 것일까요?`는 보기 중 나머지가 없는 나눗셈을 고르는 형식으로 읽힌다.
- 보기들은 화면 상단 영역에 넓게 퍼져 있어, 좌우로 간격을 두고 배치되어 있다.
- 첫 줄에는 `① 11 ÷ 2`, `② 27 ÷ 3`, `③ 35 ÷ 6`이 보이고, 다음 줄에 `④ 42 ÷ 5`, `⑤ 66 ÷ 7`이 보인다.
- `(정답) ②`는 해설보다 위에 단독으로 놓여 있어 정답 번호를 먼저 제시하는 형태이다.
- `(해설)` 아래에는 각 번호별 식이 한 줄씩 세로로 정렬되어 있으며, 번호가 왼쪽에 붙고 식이 그 오른쪽에 이어진다.
- 하단의 회색 화살표는 정답 설명을 가리키는 시각적 강조 요소로 보인다.
- 마지막 문장에서는 `②`가 다시 등장하며, 정답 보기의 식 `27 ÷ 3`이 문장 안에 포함되어 있다.

[Python DSL 작성 힌트]
- 문제 영역, 보기 영역, 정답/해설 영역, 하단 결론 문장을 각각 별도 그룹으로 나누어 배치하는 구성이 적절하다.
- 보기 5개는 원형 번호 아이콘과 식 텍스트를 함께 묶어서 표현하면 좋다.
- 해설 부분은 세로 목록 형태로, 각 항목에 번호와 식, 결과를 한 줄씩 두는 방식이 적합하다.
- `=` 뒤의 결과값은 실제 계산으로 채우지 말고, 이미지에 보이는 그대로 입력해야 한다.
- 회색 화살표 아이콘은 하단 결론 문장 앞의 강조 요소로 별도 도형 또는 아이콘 객체로 다루면 좋다.
- 원형 번호는 파란색 테두리 또는 파란색 강조 스타일로 표현하면 시각적으로 유사하다.

[주의할 점]
- 문제를 직접 풀어서 정답을 다시 계산하지 말 것.
- 빈칸은 보이지 않으므로 빈칸 채우기 형식으로 오해하지 말 것.
- 해설의 나머지 표기 `···`는 점 3개처럼 보이지만, 정확한 문자 종류는 단정하지 말고 시각적으로만 처리할 것.
- `②`가 정답으로 표시되어 있지만, 초안에서는 정답의 이유를 서술적으로 확장하지 말 것.
- 보이는 텍스트에 없는 추가 기호나 문장을 만들지 말 것.

[불확실한 부분]
- 해설의 나머지 표기가 실제로는 중점 기호인지, 생략 부호인지 화면만으로는 확정하기 어렵다.
- `② 27 ÷ 3 = 9`의 결과 숫자 `9`는 화면에 선명하게 보이지만, 주변 서체 특성상 약간의 판독 오차 가능성이 있다.
- 하단 화살표 아이콘의 정확한 색조와 세부 모양은 이미지 해상도상 완전히 단정하기 어렵다.

--- RAW VISION DRAFT END ---
