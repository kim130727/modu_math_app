[문제 개요]
- 한 문제짜리 선택형/판단형 화면이다.
- 문제는 “문제를 바르게 설명한 사람의 이름을 선택”하는 형식이다.
- 가운데에는 나눗셈 식 `83 ÷ 6`이 주황색 테두리 박스 안에 제시되어 있다.
- 식 아래에 두 인물과 각각의 말풍선이 좌우로 배치되어 있고, 아래 이름을 보고 누구의 설명이 맞는지 고르게 되어 있다.
- 하단에는 `(정답)`과 `(해설)`이 인쇄되어 있다.
- 원본 PNG 기준으로, 초안의 내용은 대체로 일치해 보인다.

[원본에 보이는 요소]
- 좌측 상단에 작은 사각형 기호 `□`가 보인다.
- 그 옆에 문제 번호 `65.`가 있고, 이어서 지시문 `문제를 바르게 설명한 사람의 이름을 선택하세요.`가 있다.
- 상단 중앙 쪽에 주황색 테두리의 직사각형 박스가 있으며, 그 안에 `83 ÷ 6`이 중앙 정렬로 들어 있다.
- 박스 아래 왼쪽에 남자아이 그림이 있고, 말풍선이 위에 있다.
- 박스 아래 오른쪽에 여자아이 그림이 있고, 말풍선이 위에 있다.
- 왼쪽 인물 아래 이름은 `종우`이다.
- 오른쪽 인물 아래 이름은 `은아`이다.
- 중앙 하단 쪽에 `( 종우 , 은아 )`가 가로로 보인다.
- 하단 왼쪽에 `(정답)종우`가 있다.
- 그 아래에 `(해설)83 ÷ 6 = 13···5이므로 몫은 13이고 나머지는 5입니다. 따라서 문제를 바르게 설명한 사람은 종우입니다.`가 이어진다.

[수식/관계 구조]
- 화면에 직접 제시된 핵심 수식은 `83 ÷ 6`이다.
- 하단 해설에 `83 ÷ 6 = 13···5`가 한 번 더 나타난다.
- 등호를 기준으로 한 변형은 1단계만 보인다.
- 수식 구조상 `83 ÷ 6`의 결과를 몫과 나머지 형태로 적은 표현이 해설에 함께 제시되어 있다.
- 말풍선 내용은 다음 판단 관계를 만든다.
  - `몫은 13이야.` → 몫만 언급
  - `나머지는 0으로 나누어떨어져.` → 나머지에 대한 설명
- 보이는 문장만으로는 어떤 설명이 맞는지 계산을 새로 할 필요 없이, 하단에 이미 정답이 인쇄되어 있다.

[등호별 변환 의미]
- `83 ÷ 6 = 13···5`
  - 왼쪽의 나눗셈 식을, 해설에서 몫과 나머지 형태로 적어 놓은 것으로 보인다.
  - 오른쪽의 `13`은 몫, `5`는 나머지로 해설 문장에 연결되어 있다.
  - 점 표기 `···`는 나눗셈의 몫/나머지 표기처럼 보이며, 원본에 보이는 기호 모양을 그대로 유지하는 것이 좋다.
- 이 문항에서는 등호 연쇄가 더 길게 이어지지 않는다.
- 해설에 수식이 한 번만 더 제시되므로, 단계별 변환은 다음처럼 정리할 수 있다.
  1. `83 ÷ 6`
  2. `83 ÷ 6 = 13···5`
- 주의: 이는 화면에 인쇄된 해설 표현을 관찰한 정리이며, 풀이를 새로 확장한 것은 아니다.

[빈칸 정책]
- 이 문제에는 채워야 하는 수식 빈칸이 보이지 않는다.
- `( 종우 , 은아 )`는 선택 후보를 보여주는 표기처럼 보이지만, 추론값을 넣는 빈칸으로 처리하면 안 된다.
- 원본에 보이는 텍스트는 그대로 유지하고, 빈칸이 있다고 판단되는 부분이 있더라도 추론으로 채우지 않는다.
- 정답이 하단에 인쇄되어 보이더라도, DSL용 초안에서는 이를 새로운 값 생성의 근거로 쓰지 않는다.
- 따라서 “빈칸”은 비어 있는 상태를 유지해야 하며, 추정값 삽입 금지.

[반복 패턴]
- 말풍선 2개가 좌우로 대칭에 가깝게 배치되어 있다.
- 인물 2명이 좌우로 나란히 배치되고, 각각 아래에 이름이 붙어 있다.
- 말풍선의 내용이 각각 인물의 설명을 나타내므로, “누가 바르게 설명했는가”를 비교하는 반복 구조다.
- `(종우 , 은아)`처럼 두 이름을 함께 제시하는 형식이 중간 하단에 다시 나온다.
- 하단에는 `(정답)`과 `(해설)`이 분리되어 반복적으로 표기된다.
- 해설 문장은 수식 → 몫/나머지 설명 → 정답 인물 확인의 순서로 반복적 패턴을 이룬다.

[DSL 작성 힌트]
- 전체 화면은 크게 다음 그룹으로 나누는 것이 적절하다.
  - 상단 문제 지시문 영역
  - 중앙 수식 박스 영역
  - 좌우 인물/말풍선 영역
  - 중간 하단 선택 후보 영역
  - 하단 정답/해설 영역
- 주황색 박스는 `83 ÷ 6` 텍스트를 감싸는 강조 박스로 표현한다.
- 박스 내부 텍스트는 중앙 정렬로 두는 것이 원본에 가깝다.
- 말풍선 2개는 각각 인물의 머리 위에 배치하고, 꼬리가 아래쪽 인물을 향하게 하면 된다.
- 왼쪽 말풍선 텍스트는 `몫은 13이야.` 그대로 둔다.
- 오른쪽 말풍선 텍스트는 `나머지는 0으로 나누어떨어져.` 그대로 둔다.
- 인물 이름은 말풍선과 대응되도록 각각 바로 아래에 배치한다.
- `( 종우 , 은아 )`는 가로 중앙 정렬된 후보 목록/표시 텍스트처럼 배치하는 것이 자연스럽다.
- 하단 `(정답)`과 `(해설)`은 작은 보조 영역으로 분리하면 된다.
- 인물 그림은 캐릭터 아이콘/삽화 객체로 처리하고, 이름은 별도 텍스트 객체로 두는 편이 좋다.
- 정렬 힌트: 전체적으로 좌우 균형이 맞도록 배치하되, 수식 박스가 위쪽 중심축을 잡는다.
- 간격 힌트: 말풍선과 인물 사이, 인물과 이름 사이에는 세로 간격이 비교적 좁다.

[semantic/solvable 힌트]
- 이 문항의 의미는 “나눗셈의 몫과 나머지를 올바르게 설명한 사람 찾기”이다.
- 말풍선 하나는 몫을 말하고, 다른 말풍선은 나머지를 말하고 있다.
- 하단 해설에는 몫과 나머지 값이 문장으로 설명되어 있다.
- 다만 DSL 초안 단계에서는 실제 판단을 새로 계산하기보다, 원본에 인쇄된 설명 구조를 유지하는 것이 우선이다.
- 하단에 정답이 인쇄되어 보이므로, 관찰 사실로는 정답 인물이 표시되어 있다고 적을 수 있다.
- 정답을 추론해서 새로 써 넣지는 않는다.

[주의 및 TODO]
- 원본 기준으로 `□` 기호의 의미는 확정하지 말고 TODO로 남기는 것이 안전하다.
- `13···5`의 점 표기는 인쇄 상태상 정확한 문자 형태가 다소 모호하므로, 원본 모양을 최대한 그대로 보존해야 한다.
- 하단 해설의 문장부호와 띄어쓰기는 원문 흐름을 해치지 않도록 가능한 한 그대로 반영한다.
- 말풍선 문구를 의역하지 말고, 화면에 보이는 그대로 유지한다.
- `( 종우 , 은아 )`는 선택지 목록처럼 보이지만, 단정적으로 UI 기능을 규정하지 말고 “후보 이름 표시 영역” 정도로 처리하는 것이 안전하다.
- 인물의 성별/외형은 그림 관찰에 따른 것이며, DSL에서는 꼭 성별 정보를 넣어야 한다고 단정하지 않는다.
- 정답이 하단에 보이더라도, 빈칸 채우기식 추론은 하지 않는다.
- 나눗셈 표기의 정확한 해석은 해설 문장에 의존하되, 수식 자체는 원본 표기를 우선한다.
