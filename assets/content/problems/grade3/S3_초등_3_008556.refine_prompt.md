# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_008556.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_008556

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
[전체적인 구성]
- 화면 상단 왼쪽에 문제 번호 `19.`가 있고, 그 뒤로 문제 지시문이 한 줄로 이어집니다.
- 중앙 오른쪽에는 세로형 곱셈 풀이 그림이 배치되어 있습니다.
- 곱셈 그림 아래쪽에는 연한 배경색의 큰 선택 상자가 가로로 놓여 있고, 그 안에 3개의 선택지가 나란히 있습니다.
- 화면 하단 왼쪽에는 `(정답)`과 `(해설)`이 텍스트로 따로 표시되어 있습니다.
- 전체적으로 “세로셈 그림 + 선택지 박스 + 정답/해설” 구조입니다.

[보이는 텍스트]
- 상단 문제 문장:
  - `19. 색칠된 부분은 실제 어떤 수의 곱인지를 찾아 선택하세요.`
- 세로셈 그림 안의 숫자:
  - 윗줄에 `5 1 2`
  - 그 아래 곱하는 수로 보이는 `3`
  - 중간 계산 결과로 보이는 `6`
  - 색칠된 칸에 `3 0`
  - 아래쪽에 `1 5 0 0`
  - 맨 아래 결과로 `1 5 3 6`
- 선택지 박스 안:
  - `1×3`
  - `10×3`
  - `100×3`
- 하단 텍스트:
  - `(정답) 10×3`
  - `(해설) 십의 자리의 곱이므로 10×3 = 30입니다.`

[반복되는 시각 패턴]
- 세로셈 부분은 자릿수별로 칸이 나뉜 세로 정렬 구조입니다.
- 곱셈 과정에서 자리값별 곱이 위에서 아래로 누적되는 형태가 보입니다.
- 선택지는 동일한 형식의 곱셈식 3개가 가로로 반복 배치되어 있습니다.
- 색칠된 부분은 연한 살구색/주황빛 배경으로 강조되어 있어, 다른 숫자들보다 시선이 먼저 가도록 처리되어 있습니다.

[수식 단계]
- 세로셈 그림에서 보이는 단계는 다음처럼 읽을 수 있습니다.
  - 첫 번째로 한 자리 수 `3`이 어떤 자리의 숫자와 곱해진 결과로 `6`이 적혀 있습니다.
  - 그 아래 색칠된 부분에는 `3 0`이 보여서, 두 자리 값이 만들어진 부분을 강조합니다.
  - 그 아래에는 `1 5 0 0`이 보이며, 다음 자리 처리 결과가 자리 이동을 포함한 형태로 표시된 것으로 보입니다.
  - 맨 아래 합계/최종 결과로 `1 5 3 6`이 적혀 있습니다.
- 단, 이 문항은 풀이 과정을 설명하는 그림이므로, 각 숫자가 정확히 어떤 위치값을 의미하는지는 주변 맥락과 함께 해석해야 합니다.

[등호별 변환 의미]
- 화면에 직접적인 등호 변형식이 여러 단계로 나열되어 있지는 않습니다.
- 다만 해설 문장에서는 `10×3 = 30`이 제시되어 있습니다.
- 의미상으로는 “십의 자리의 곱”에 해당하는 자릿값 계산을 말하는 것으로 보입니다.
- 하지만 이 문서에서는 문제를 풀지 않고, 등식의 의미만 보이는 수준으로 정리합니다.

[세부 내용]
- 세로셈 그림은 오른쪽에 숫자들이 위아래로 정렬되어 있고, 왼쪽 아래 방향에 × 표시가 있어 곱셈임을 나타냅니다.
- 상단 숫자 `512`와 아래 `3`이 세로로 배치되어 있습니다.
- 중간 계산 영역에는 각 자리 계산 결과처럼 보이는 숫자들이 아래로 내려가며 적혀 있습니다.
- `3 0`이 적힌 부분은 연한 색 배경으로 칠해져 있어 문제의 핵심 대상임을 강조합니다.
- 아래쪽의 `1 5 0 0`과 최종값 `1 5 3 6`은 각 자릿수 계산이 모여 완성된 형태처럼 배열되어 있습니다.
- 선택지 박스는 연한 베이지색 배경과 주황색 테두리로 둘러싸인 긴 직사각형입니다.
- 그 안의 선택지는 가운데 정렬에 가깝게 배치되어 있으며, 서로 간격을 두고 `1×3`, `10×3`, `100×3` 순서로 놓여 있습니다.

[Python DSL 작성 힌트]
- 상단 문제 문장은 좌측 정렬 텍스트로 넣고, 문제 번호 `19.`를 분리해 표현하면 좋습니다.
- 중앙 곱셈 그림은 세로셈 레이아웃으로 구현:
  - 위에 `512`
  - 그 아래 `× 3`
  - 아래쪽에 단계별 결과 숫자 배치
- `3 0` 두 글자는 하나의 색칠 강조 영역 안에 넣어야 하며, 배경색은 연한 주황/살구색 계열로 처리합니다.
- 선택지 영역은 큰 직사각형 박스를 먼저 그리고, 내부에 3개의 텍스트를 가로로 균등 배치합니다.
- 하단의 `(정답)`, `(해설)`은 일반 텍스트로 별도 줄에 배치합니다.
- 세로셈의 숫자들은 자릿수별 칸 간격이 중요하므로, 고정폭 배치 또는 열 정렬을 활용하는 방식이 적합합니다.

[주의할 점]
- 답을 추론해서 빈칸을 채우지 마세요.
- 색칠된 `3 0`을 어떤 계산 결과로 “확정”해서 단정적으로 쓰지 말고, 보이는 그대로 “색칠된 부분”이라고만 표현하세요.
- `1 5 0 0`, `1 5 3 6`의 정확한 의미는 그림 맥락상 해석될 수 있지만, 문제 풀이로 넘어가지는 마세요.
- 선택지의 곱셈식은 수식 형태로 보이지만, 정답 판정은 여기서 하지 않습니다.
- 그림의 세부 정렬이 매우 중요하므로 숫자 사이의 상대 위치를 유지해야 합니다.

[불확실한 부분]
- 세로셈 그림의 각 중간 숫자가 정확히 어떤 자리값 계산에 대응하는지, 이미지 해상도만으로는 일부 애매할 수 있습니다.
- `3 0`의 배경색이 정확히 어느 칸까지 포함되는지, 그리고 숫자 사이 공백이 원래부터 있었는지 여부는 다소 불명확합니다.
- `1 5 0 0`과 `1 5 3 6`이 정확히 어떤 단계의 중간값/최종값인지, 그림만으로 100% 기호적으로 확정하기보다는 “보이는 순서”로 기록하는 것이 안전합니다.

--- RAW VISION DRAFT END ---
