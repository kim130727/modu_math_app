# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_008638.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_008638

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
# [전체적인 구성]
- 화면 상단 왼쪽에 문제 번호 `10.`과 문제 문장이 한 줄로 배치되어 있습니다.
- 오른쪽 상단 쪽에는 정사각형 격자 위 원 그림이 크게 하나 있습니다.
- 원 그림 안에는 분홍색 점 3개와 각 점 옆의 작은 글자 표기가 보입니다.
- 중간 왼쪽에는 `(정답)` 표시가 있고, 그 오른쪽에 한 글자 정도를 쓸 수 있는 빈칸이 있습니다.
- 그 아래에는 `(해설)` 문장과 함께, 아래쪽 왼편에 같은 원 그림이 한 번 더 작게 제시되어 있습니다.
- 아래 그림에는 한 점을 원의 중심으로 강조한 듯한 작은 원 표시가 추가되어 있습니다.

# [보이는 텍스트]
- `10. 원의 중심을 찾아 선택해 보세요.`
- `(정답)` 옆에 빈칸 1개
- `(해설) 원의 중심은 원을 그림 때 누름 못이 꽂혔던 점으로 한 원에는 원의 중심이 1개 있`  
  다음 줄로 이어져 `습니다.`  
  - 문장이 줄바꿈되어 보이며, 전체 의미는 설명 문장처럼 보입니다.
- 그림 속 점 옆의 표기:
  - `가`
  - `나`
  - `다`

# [반복되는 시각 패턴]
- 동일한 스타일의 격자 배경 위 원 도형이 위아래로 2번 반복됩니다.
  - 위: 문제용 큰 그림
  - 아래: 해설용 작은 그림
- 두 그림 모두 같은 원과 같은 위치의 점들이 보입니다.
- 점들은 모두 분홍색 작은 원점처럼 표시되어 있고, 각 점 근처에 한 글자 라벨이 붙어 있습니다.
- 아래 해설 그림에서는 중심으로 보이는 점을 동그라미로 한 번 더 둘러 강조한 형태가 있습니다.

# [수식 단계]
- 해당 없음: 이 문제는 수식 변형식이나 등호가 나타나지 않습니다.

# [등호별 변환 의미]
- 해당 없음: 등호를 포함한 단계적 변환이 없습니다.

# [세부 내용]
- 위쪽 그림:
  - 정사각형 격자 영역 안에 검은색 원이 하나 그려져 있습니다.
  - 원의 중심으로 보이는 위치에 분홍 점이 하나 있으며, 그 근처에 `가`가 있습니다.
  - 원 내부 오른쪽 쪽에 분홍 점 하나가 있고 `나`가 있습니다.
  - 원 내부 아래쪽 쪽에 분홍 점 하나가 있고 `다`가 있습니다.
  - 세 점 중 어느 점이 중심인지 고르는 형식으로 보입니다.
- `(정답)` 옆 빈칸:
  - 한 칸짜리 답 쓰기 영역처럼 보입니다.
  - 실제 글자나 기호는 입력되어 있지 않습니다.
- 아래 해설 그림:
  - 같은 원이 다시 나오고,
  - 중심으로 보이는 점 주변에 작은 원형 강조 표시가 있습니다.
  - 해설 문장은 중심의 개념을 설명하는 내용으로 보입니다.
- 그림 전체는 초등 수학 문제 스타일이며, 선택지 라벨이 그림 안에 직접 들어가 있는 형태입니다.

# [Python DSL 작성 힌트]
- 격자 배경은 `square grid` 또는 점선/연파란 격자 형태로 표현할 수 있습니다.
- 원은 격자 중앙 근처에 위치한 단일 큰 원으로 그리면 됩니다.
- 분홍색 점 3개를 각각 좌표에 배치하고, 각 점 옆에 `가`, `나`, `다` 라벨을 붙이는 구조가 필요합니다.
- 위 그림과 아래 해설 그림은 동일 도형을 재사용하되, 아래 그림에는 중심 강조용 작은 원을 추가하는 방식이 자연스럽습니다.
- `(정답)` 옆 빈칸은 텍스트 입력 박스 또는 빈 자리 표시로 두면 됩니다.
- 문제 문장, 해설 문장, 그림의 상대 위치를 유지하는 것이 중요합니다.

# [주의할 점]
- 정답 라벨이 보이더라도 실제로 어떤 글자가 정답인지 이 초안에서 추론해 적지 마세요.
- 중심으로 보이는 점이 무엇인지는 시각적으로만 설명하고, 문제 해결 결론은 쓰지 마세요.
- 해설 문장은 보이는 대로 옮기되, 맞춤법이나 문장 의미를 임의로 보정하지 않는 편이 안전합니다.
- 아래 해설 그림의 작은 동그라미는 중심 강조 표시로 보이지만, 장식인지 강조인지 단정하지 말고 설명상 그렇게 보인다고만 적는 것이 좋습니다.

# [불확실한 부분]
- 해설 문장 일부가 화면에서 줄바꿈되어 있어, 정확한 띄어쓰기나 일부 글자는 판독이 다를 수 있습니다.
- `(정답)` 옆 빈칸의 정확한 크기와 형태는 화면상 한 글자 정도의 입력칸처럼 보이나, 실제 입력 방식은 확정할 수 없습니다.
- 아래 해설 그림의 중심 강조 표시가 정확히 어떤 도형인지, 단순 원인지 얇은 강조선인지 판독이 약간 불명확합니다.

--- RAW VISION DRAFT END ---
