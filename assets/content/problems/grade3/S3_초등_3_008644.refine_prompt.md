# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_008644.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_008644

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
# [전체적인 구성]
- 화면 상단 왼쪽에 문제 번호와 문장이 있고, 가운데 상단에는 원의 그림이 배치되어 있습니다.
- 원 아래쪽에는 보기 5개가 두 줄로 나뉘어 배치되어 있습니다.
- 하단 왼쪽에는 정답 표기와 해설 문장이 있습니다.
- 전체적으로 **객관식/선택형 문제** 형태이며, 그림에서 옳은 선분을 고르는 구성입니다.

# [보이는 텍스트]
- 상단 문제 문장:
  - `□ 16. 점 O는 원의 중심입니다. 원의 반지름은 어느 것인지 모두 선택해 보세요.`
- 그림 안/주변의 문자:
  - `ㄱ, ㄴ, ㄷ, ㄹ, ㅁ, ㅂ, ㅅ, O`
  - 원의 중심 점 표시로 `O`가 보입니다.
- 보기:
  - `① 선분 ㄱㅅ`
  - `② 선분 OL`
  - `③ 선분 Oㄷ`
  - `④ 선분 Oㅁ`
  - `⑤ 선분 ㄹㅂ`
  - `⑥ 선분 ㄴㅅ`  
  ※ 실제 화면에는 보기 앞에 동그라미 안 한글 기호처럼 보이는 표기가 함께 붙어 있습니다.
- 정답 표기:
  - `(정답) 나, 다, 라`
- 해설:
  - `(해설) 원의 반지름은 원의 중심과 원 위의 한 점을 이은 선분입니다.`

# [반복되는 시각 패턴]
- 원 둘레에 여러 점 이름이 배치되어 있고, 일부 점끼리 선분으로 연결되어 있습니다.
- 선분들은 모두 직선으로 그려져 있으며, 중심 `O`에서 바깥쪽 점으로 뻗는 선분도 포함되어 있습니다.
- 보기에는 모두 `선분`이라는 같은 형식이 반복되며, 각 보기마다 두 점 사이의 선분을 묻고 있습니다.
- 선택지 표기는 좌우 3개씩, 위아래 2줄 형태로 정렬되어 있습니다.

# [수식 단계]
- 해당 없음.
- 이 문제에는 등호로 이어지는 수식 변형 과정이 보이지 않습니다.

# [등호별 변환 의미]
- 해당 없음.

# [세부 내용]
- **그림 구성**
  - 중심으로 보이는 점 `O`가 원 내부 중앙 부근에 있으며, 작은 색 점으로 표시되어 있습니다.
  - 원의 테두리 위에 여러 점이 있고, 각 점은 한글 자모로 표기되어 있습니다.
  - `O`에서 원 위의 점으로 향하는 선분이 여러 개 보입니다.
  - 원 내부 상단에는 원 위의 점들 사이를 잇는 선분이 1개 이상 있습니다.
- **선분 배치**
  - `O`에서 오른쪽으로 수평에 가까운 선분이 원의 오른쪽 점까지 이어집니다.
  - `O`에서 왼쪽 위 방향의 점까지 이어지는 선분이 있습니다.
  - `O`에서 왼쪽 아래 방향의 점까지 이어지는 선분이 있습니다.
  - 오른쪽 위와 오른쪽 아래 쪽 점을 잇는 긴 선분도 보입니다.
  - 상단 쪽의 두 점 사이를 잇는 짧은 선분도 보입니다.
- **보기의 구조**
  - 각 보기는 `선분` 뒤에 두 점 기호가 붙는 형태입니다.
  - 예: `선분 Oㄷ`처럼 점 이름 두 개로 선분을 표현합니다.
  - 일부는 중심 `O`가 포함되고, 일부는 포함되지 않습니다.
- **문항 의도(보이는 내용 기준)**
  - 문제는 “원의 반지름”의 정의에 맞는 선분을 그림에서 고르는 형식입니다.
  - 다만 실제로 어떤 보기가 맞는지는 여기서 판단하지 않습니다.

# [Python DSL 작성 힌트]
- 원 그림은 **중앙 상단**에 크게 배치하고, 그 아래에 보기 6개를 **2행 3열**로 놓는 구성이 적합합니다.
- 중심 `O`는 원 내부 중앙에 점으로 표시하고, 색이 다른 작은 점으로 강조하면 원본과 유사합니다.
- 원 둘레의 점 이름은 원의 위/왼쪽/오른쪽/아래 영역에 적절히 배치해야 합니다.
- 선분은 다음처럼 개별 객체로 분리해 그리는 방식이 좋습니다.
  - 중심에서 둘레로 가는 선분들
  - 둘레의 두 점을 잇는 현처럼 보이는 선분들
- 보기 텍스트는 각 줄마다 정렬이 맞도록 좌측 시작 위치를 일정하게 두는 것이 좋습니다.
- 정답/해설 영역은 하단에 별도 텍스트 블록으로 배치하면 됩니다.
- 문항 번호 `16.`과 문제 문장은 상단 왼쪽에 한 줄 또는 두 줄로 배치하세요.

# [주의할 점]
- 답을 추론하여 표시하지 마세요.
- `O`를 글자 `0`(영)처럼 오인하지 않도록 주의하세요.
- 보기에 있는 한글 점 이름은 위치와 방향을 잘 구분해야 합니다.
- 원 내부의 선분이 실제로 중심에서 출발하는지, 아니면 단순히 교차해 보이는지 구분해서 그려야 합니다.
- 정답 표기 `(정답) 나, 다, 라`는 원문에 보이는 정보이므로, 시각 자료 재현 시 텍스트로 따로 둘 수는 있지만 풀이 설명으로 확장하지는 마세요.

# [불확실한 부분]
- 원 위 점들의 정확한 배치 각도는 이미지 해상도상 약간 흐릿하여, 각 글자 위치를 100% 동일하게 단정하기는 어렵습니다.
- 일부 선분이 원의 테두리와 겹쳐 보여 시작점/끝점이 정확히 어느 점인지 약간 애매합니다.
- 보기 앞의 동그라미 표기가 번호인지 기호인지가 이미지상 다소 작게 보여, 그대로 재현할 때는 위치 중심으로 처리하는 것이 안전합니다.

--- RAW VISION DRAFT END ---
