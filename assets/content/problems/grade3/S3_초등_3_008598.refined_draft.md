[문제 개요]
- 문제 번호는 `67.`로 보이며, 상단에 `나머지가 더 큰 것을 선택하세요.`라는 지시문이 있습니다.
- 가운데에는 가로로 나란한 선택 박스 2개가 있고, 각각 나눗셈 식이 들어 있습니다.
- 아래쪽에는 `(정답)`, `(해설)` 표기와 함께 결과를 비교하는 문장이 이어집니다.
- 전체 구성은 “두 나눗셈 식의 나머지를 비교해 더 큰 쪽을 고르는” 선택형 문제로 보입니다.

[원본에 보이는 요소]
- 상단 왼쪽에 작은 네모 모양 기호가 있고, 그 뒤에 `67. 나머지가 더 큰 것을 선택하세요.`가 한 줄로 표시되어 있습니다.
- 중앙 상단에 같은 크기의 사각형 박스 2개가 좌우로 배치되어 있습니다.
- 왼쪽 박스에는 `504 ÷ 5`
- 오른쪽 박스에는 `467 ÷ 3`
- 하단 왼쪽에 `(정답)504 ÷ 5`
- 그 아래에 `(해설)504 ÷ 5 = 100···4, 467 ÷ 3 = 155···2`
- 맨 아래에 회색 화살표 `→` 뒤로 `나머지의 크기를 비교하면 4>2입니다.`가 있습니다.
- 배경은 흰색이며, 박스 테두리는 연한 보라색 계열로 보입니다.

[수식/관계 구조]
- 선택지 2개가 서로 비교되는 구조입니다.
- 왼쪽 식: `504 ÷ 5`
- 오른쪽 식: `467 ÷ 3`
- 해설 영역에서는 각 나눗셈 결과를 `몫···나머지` 형식으로 적었습니다.
- 비교의 핵심은 두 결과의 나머지 `4`와 `2`를 비교하는 구조입니다.
- 원본에는 계산 과정의 전개식은 없고, 결과 요약만 보입니다.

[등호별 변환 의미]
- `504 ÷ 5 = 100···4`
  - 보이는 의미: `504 ÷ 5`의 결과를 몫과 나머지 형태로 적은 것입니다.
  - 추론되는 수학적 의미: 나눗셈 결과를 `몫 100, 나머지 4` 형태로 표현한 것으로 보입니다.
- `467 ÷ 3 = 155···2`
  - 보이는 의미: `467 ÷ 3`의 결과를 몫과 나머지 형태로 적은 것입니다.
  - 추론되는 수학적 의미: 나눗셈 결과를 `몫 155, 나머지 2` 형태로 표현한 것으로 보입니다.
- `나머지의 크기를 비교하면 4>2입니다.`
  - 보이는 의미: 두 나머지 값을 직접 비교한 문장입니다.
  - 추론되는 수학적 의미: 더 큰 나머지를 가진 식을 고르도록 안내하는 결론 문장입니다.
- 현재 화면에는 등호가 들어간 변환이 2개 있고, 둘 다 결과 요약 단계입니다.
- 별도의 연산 변형 체인은 보이지 않습니다.

[빈칸 정책]
- 이 문제 화면에는 빈칸 입력란이 보이지 않습니다.
- 따라서 빈칸을 추론해서 채우지 않습니다.
- 만약 DSL에서 답안 표시용 빈칸이나 선택 슬롯을 구성하더라도, 관찰되지 않은 값은 비워 둬야 합니다.
- 원본에 명시적으로 보이는 텍스트만 반영하고, 숨은 답은 추정하지 않습니다.

[반복 패턴]
- 동일한 크기의 선택 박스 2개가 좌우로 반복 배치되어 있습니다.
- 각 박스는 얇은 테두리와 중앙 정렬된 수식으로 같은 형식을 유지합니다.
- 해설에서는 `식 = 몫···나머지` 형태가 두 번 반복됩니다.
- 마지막에 화살표 `→`와 비교 문장이 이어져 결론을 강조합니다.
- `···` 표기는 두 식 모두에 반복적으로 등장하며, 나머지 표기처럼 사용된 것으로 보입니다.

[DSL 작성 힌트]
- 상단 문장 영역과 선택 박스 영역, 하단 해설 영역을 분리된 블록으로 구성하는 것이 좋습니다.
- 선택 박스 2개는 동일한 크기, 동일한 간격, 좌우 정렬로 배치합니다.
- 박스 내부 텍스트는 중앙 정렬로 두고, 나눗셈 기호 `÷`가 잘 보이도록 처리합니다.
- 하단의 `(정답)`, `(해설)`은 라벨 블록으로 별도 처리하면 원본 구조와 잘 맞습니다.
- 해설의 두 식은 한 줄에 이어서 배치되어 있으며, 쉼표로 구분됩니다.
- 마지막 비교 문장은 화살표 아이콘과 함께 한 줄 문장으로 배치합니다.
- 박스 테두리는 연한 보라색, 글자는 검정색 계열로 두면 원본 분위기에 가깝습니다.
- 상단 문제 번호 옆의 작은 네모 기호도 별도 아이콘/기호 요소로 고려할 수 있습니다.

[semantic/solvable 힌트]
- 문제 의도는 두 나눗셈의 나머지 크기를 비교하는 것입니다.
- 해설에는 두 결과가 `몫···나머지` 형태로 제시되어 있고, 마지막 문장에서 나머지 비교가 핵심임을 밝히고 있습니다.
- 선택형 구조이므로 DSL에서는 “후보 2개 비교”라는 의미가 드러나도록 배치하는 것이 중요합니다.
- 원본에 정답처럼 보이는 표기가 있지만, 정답을 계산해서 새로 넣지는 말아야 합니다.
- 이미지에 보이는 해설 문장을 그대로 재현하는 방향이 안전합니다.

[주의 및 TODO]
- `100···4`, `155···2`의 `···`는 화면상 점 3개처럼 보이지만, 정확한 인쇄 의미는 TODO로 남겨 두는 것이 안전합니다.
- `(정답)504 ÷ 5`가 실제 정답 표시인지, 해설용 표시인지 화면만으로 완전히 단정하지 마세요.
- 선택 박스가 실제 클릭형 UI인지, 단순한 문제 보기 박스인지 확정하지 말고 레이아웃 요소로만 다루세요.
- 계산 결과를 새로 검증하거나 다른 정답을 추론하지 마세요.
- 빈칸이 없으므로 빈칸 채움 로직을 적용하지 않습니다.
- 화살표 문장의 색상은 회색 화살표와 검정/짙은 파랑 글씨가 섞여 보이므로, 정확한 색상명은 TODO입니다.
