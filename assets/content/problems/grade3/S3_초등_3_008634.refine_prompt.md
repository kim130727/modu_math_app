# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_008634.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_008634

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
[전체적인 구성]
- 왼쪽 상단에 문제 번호 **6.**이 있고, 그 오른쪽에 문제 지시문이 한 줄로 배치되어 있습니다.
- 중앙에는 **파란색 테두리의 큰 직사각형 박스**가 있으며, 그 안에 **나눗셈 식 4개**가 2행 2열 형태로 정렬되어 있습니다.
- 박스 아래에는 선택해야 할 항목을 적은 듯한 **괄호 묶음 표기**가 한 줄로 있습니다. 괄호 안에는 기호/표시 4개가 쉼표로 구분되어 나열됩니다.
- 그 아래 왼쪽에는 **(정답)** 표시가 있고, 바로 옆에 답 기호가 제시되어 있습니다.
- 하단에는 **(해설)**이 이어지며, 각 보기의 나눗셈을 등호와 나머지 표기 형태로 풀어쓴 줄이 가로로 길게 배치되어 있습니다.
- 맨 아래에는 문장형 설명이 한 줄로 있습니다.

[보이는 텍스트]
- `6. 나머지가 다른 하나를 찾아 기호를 선택해 보세요.`
- 박스 안의 보기:
  - `① 29 ÷ 7`
  - `② 31 ÷ 6`
  - `③ 74 ÷ 8`
  - `④ 64 ÷ 9`
- 박스 아래:
  - `( ① , ② , ③ , ④ )`
- 정답 표기:
  - `(정답) ③`
- 해설 표기:
  - `(해설) ① 29 ÷ 7 = 4··1   ② 31 ÷ 6 = 5··1   ③ 74 ÷ 8 = 9··2   ④ 64 ÷ 9 = 7··1`
- 맨 아래 문장:
  - `따라서 나머지가 다른 하나는 ③입니다.`

[반복되는 시각 패턴]
- 보기 4개가 모두 **같은 형식의 나눗셈 식**으로 구성되어 있습니다.
- 각 보기 앞에는 **동그라미 안 숫자 기호**가 붙어 있어 항목 번호를 나타냅니다.
- 해설 줄에서도 각 보기 번호가 다시 반복되며, `÷` 뒤에 **몫과 나머지**가 `= 숫자··숫자` 형식으로 이어집니다.
- 숫자와 기호가 일정 간격으로 배열되어 있어, 보기/해설이 **좌→우 순서**로 대응됨을 알 수 있습니다.

[수식 단계]
- 해설 부분에 각 나눗셈 식이 `= 몫··나머지` 형태로 표시되어 있습니다.
- 등호는 각 보기마다 한 번씩 사용되어 있으며, **원래 나눗셈 식**에서 **몫과 나머지 표현**으로 바뀐 결과를 보여 줍니다.
- 별도의 연쇄 등호 변형식은 보이지 않고, 각 항목마다 독립적인 한 단계 해설만 있습니다.

[등호별 변환 의미]
- `29 ÷ 7 = 4··1`  
  - 보이는 의미: 29를 7로 나눈 결과를 몫과 나머지로 적은 형태입니다.
- `31 ÷ 6 = 5··1`
  - 보이는 의미: 31을 6으로 나눈 결과를 몫과 나머지로 적은 형태입니다.
- `74 ÷ 8 = 9··2`
  - 보이는 의미: 74를 8로 나눈 결과를 몫과 나머지로 적은 형태입니다.
- `64 ÷ 9 = 7··1`
  - 보이는 의미: 64를 9로 나눈 결과를 몫과 나머지로 적은 형태입니다.
- 여기서 `··`는 해설에서 **나머지 표시**로 쓰인 것으로 보입니다. 다만 실제 입력용 표기 방식은 DSL에서 별도 규칙이 필요할 수 있습니다.

[세부 내용]
- 문제는 “**나머지가 다른 하나**”를 찾는 **선택형 문제**입니다.
- 중앙 박스는 4개의 보기로 구성되어 있고, 각 보기의 내용은 다음과 같습니다.
  - ①: `29 ÷ 7`
  - ②: `31 ÷ 6`
  - ③: `74 ÷ 8`
  - ④: `64 ÷ 9`
- 박스 아래의 `( ① , ② , ③ , ④ )`는 보기 번호를 다시 한 번 나열한 것으로 보입니다.
- 정답 표시에는 **③**이 들어가 있으며, 해설 문장도 그와 일치하는 방향으로 적혀 있습니다.
- 배치는 전반적으로 **상단 지시문 → 중앙 보기 박스 → 보기 목록 표기 → 정답/해설** 순서입니다.

[Python DSL 작성 힌트]
- 큰 직사각형 박스를 먼저 만들고, 그 안에 2행 2열로 4개의 텍스트 블록을 배치하는 구성이 적절합니다.
- 각 보기의 시작에는 동그라미 번호 기호를 포함해야 합니다.
- 박스 아래에는 괄호로 감싼 번호 목록을 한 줄로 배치합니다.
- 하단에는 `(정답)`과 `(해설)` 라벨을 별도 텍스트로 배치합니다.
- 해설 줄은 각 항목이 한 줄에 이어지므로, 항목 사이 간격을 일정하게 두고 가로 정렬하는 것이 좋습니다.
- 나눗셈 해설의 `··` 표기는 실제 DSL에서 점 2개인지, 생략/나머지 기호인지 확인 후 대응해야 합니다.

[주의할 점]
- 이 문제는 **정답을 구하는 과정**을 작성하는 것이 아니라, **화면에 보이는 구성과 텍스트**를 옮기는 작업입니다.
- 나머지 값을 추론해 추가 설명을 덧붙이지 마세요.
- `정답 ③`은 화면에 보이는 사실이지만, 풀이를 확장하여 계산 과정을 새로 만들지 마세요.
- 해설의 `4··1` 같은 표기는 정확한 시각적 표현을 우선해야 하며, 임의로 `4 나머지 1`처럼 바꿔 쓰지 않는 편이 좋습니다.
- 번호 기호는 일반 숫자와 구분되는 **원형 번호 마커**로 처리해야 합니다.

[불확실한 부분]
- 해설의 나머지 표기 `··`가 정확히 어떤 글리프인지, 화면상으로는 점 두 개처럼 보이지만 텍스트로는 약간 불명확할 수 있습니다.
- 박스 안 보기의 글자 크기와 정확한 좌표는 이미지 해상도만으로는 정밀하게 단정하기 어렵습니다.
- `( ① , ② , ③ , ④ )`의 간격과 괄호 모양은 보이지만, 정확한 폰트 스타일은 확정하기 어렵습니다.

--- RAW VISION DRAFT END ---
