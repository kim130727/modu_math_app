# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_008587.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_008587

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
[전체적인 구성]
- 문제 번호가 왼쪽 상단에 있고, 그 오른쪽에 문제 문장이 한 줄로 배치되어 있습니다.
- 본문 상단 오른쪽에는 연한 보라색 계열의 큰 직사각형 박스가 있으며, 그 안에 두 개의 보기 수식이 가로로 나란히 들어 있습니다.
- 박스 아래쪽 왼편에는 `(정답) ①`이 따로 표시되어 있고, 그 아래에 `(해설)`이 이어집니다.
- 해설은 두 줄 이상으로 구성되어 있으며, 마지막 문장에서 정답 기호를 다시 한 번 강조합니다.
- 전체적으로 “보기 선택형 문제 + 해설” 형식이며, 빈칸 입력형은 아닙니다.

[보이는 텍스트]
- `54.`
- `나누어떨어지는 나눗셈을 찾아 선택하세요.`
- 보기 박스 안:
  - `① 72 ÷ 4`
  - `② 52 ÷ 8`
- `(정답) ①`
- `(해설) 나머지가 0인 나눗셈을 찾습니다.`
- `① 72 ÷ 4 = 18`
- `② 52 ÷ 8 = 6···4`
- `따라서 나누어떨어지는 나눗셈은 ①입니다.`

[반복되는 시각 패턴]
- 선택지 번호가 동그라미 숫자 기호 `①`, `②`로 반복됩니다.
- 보기 박스 안에서 두 수식이 같은 형식으로 제시됩니다:
  - `번호 + 나눗셈식`
- 해설에서도 각 보기 번호를 다시 한 번 붙여서 결과를 나열합니다.
- 등호 `=`가 결과 확인의 형태로 반복 사용됩니다.
- 두 번째 해설식에는 `···`가 포함되어 나머지를 나타내는 표기처럼 보입니다.

[수식 단계]
- 해설 부분에 계산식이 두 개 제시되어 있습니다.
  - `① 72 ÷ 4 = 18`
  - `② 52 ÷ 8 = 6···4`
- 각 식은 “나눗셈식 → 결과” 형태로 되어 있습니다.
- 두 번째 식은 등호 뒤에 몫과 나머지처럼 보이는 표기가 함께 나타납니다.
- 문제 본문 자체에는 식 변형 과정이 여러 단계로 이어져 있지는 않습니다.

[등호별 변환 의미]
- `72 ÷ 4 = 18`
  - 보이는 그대로는 나눗셈식의 결과를 등호 오른쪽에 적은 형태입니다.
  - 어떤 중간 계산이 생략된 상태로 결과만 제시되어 있습니다.
- `52 ÷ 8 = 6···4`
  - 등호 오른쪽에 `6`과 `4`가 함께 표시되어 있습니다.
  - 시각적으로는 “몫과 나머지”를 함께 적은 형태로 보입니다.
  - 다만 정확히 어떤 기호 체계인지, 점 세 개가 실제 나머지 표기인지 여부는 화면만으로 단정하지 않습니다.

[세부 내용]
- 문제 문장은 “나누어떨어지는 나눗셈을 찾아 선택하세요.”입니다.
- 보기 박스는 연한 보라색 배경의 넓은 직사각형이며, 내부에 두 보기만 배치되어 있습니다.
- 첫 번째 보기는 `① 72 ÷ 4`로, 왼쪽에 동그라미 숫자 1이 있고 오른쪽에 식이 있습니다.
- 두 번째 보기는 `② 52 ÷ 8`로, 첫 번째 보기와 같은 간격과 크기로 오른쪽에 배치되어 있습니다.
- 정답 표시는 본문 아래 왼쪽에 `(정답) ①`로 따로 제시되어 있습니다.
- 해설은 `(해설)` 라벨 뒤에 문장형 설명이 이어지고, 보기별 계산 결과를 한 줄씩 보여 준 뒤 결론 문장으로 마무리됩니다.

[Python DSL 작성 힌트]
- 상단 문제 번호와 문장을 좌측 정렬 텍스트로 배치합니다.
- 오른쪽 상단에는 연한 보라색 직사각형 박스를 만들고, 그 안에 두 개의 선택지를 가로 방향으로 균등 간격 배치합니다.
- 각 선택지는 `동그라미 숫자 + 수식` 형태로 한 줄 텍스트로 넣습니다.
- 박스 아래에는 `(정답) ①`, `(해설)` 텍스트를 순서대로 배치합니다.
- 해설 영역에서는 문장 텍스트와 수식 텍스트를 줄바꿈하여 표시합니다.
- 수식은 일반 텍스트로 처리하되, `÷`, `=`, `···` 기호의 위치가 흐트러지지 않도록 등폭 느낌의 정렬을 고려합니다.
- 마지막 결론 문장은 해설 영역 하단에 별도 문장으로 둡니다.

[주의할 점]
- 문제를 “풀어서 정답을 계산”하는 방향으로 작성하지 말고, 화면에 보이는 정답 표시와 해설 텍스트만 반영합니다.
- `52 ÷ 8 = 6···4`의 점 표기를 임의로 다른 기호로 바꾸지 마세요.
- 정답 표시는 `(정답) ①`으로 보이는 그대로 옮기되, 그 의미를 추가로 추론해 다른 답으로 바꾸지 마세요.
- 보기 박스의 배경색과 직사각형 형태를 놓치지 않는 것이 중요합니다.
- 해설의 수식은 본문 보기와 분리된 영역에 있다는 점을 반영해야 합니다.

[불확실한 부분]
- `6···4`의 점 세 개가 정확히 어떤 교과서 표기 규칙을 따르는지는 화면만으로 확정하기 어렵습니다.
- 보기 박스의 정확한 색상 코드나 픽셀 단위 크기는 알 수 없습니다.
- 글꼴은 세리프체처럼 보이지만, 실제 폰트 이름은 화면만으로 단정할 수 없습니다.

--- RAW VISION DRAFT END ---
