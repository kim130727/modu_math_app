# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_008668.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_008668

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
# [전체적인 구성]
- 화면 상단 왼쪽에 문제 번호 **“45.”**가 있고, 그 오른쪽에 지시문 **“바르게 설명하였으면 ○, 그렇지 않으면 ×를 선택하세요.”**가 한 줄로 배치되어 있습니다.
- 중앙에는 **원 그림**이 크게 있으며, 원 안과 원 주변에 선분과 문자 표기가 함께 들어 있습니다.
- 원 아래쪽에는 판단 문장 1개가 크게 제시되고, 그 아래 왼쪽에 **○ / × 선택 표시**가 세로로 배치되어 있습니다.
- 더 아래에는 **“(정답)×”**가 따로 표시되어 있고, 그 밑에 **“(해설)”** 문장이 이어집니다.
- 전체적으로 **문항 지시문 → 그림 → 판단 문장 → 선택 표시 → 정답/해설** 순서의 구성입니다.

# [보이는 텍스트]
- `45.`
- `바르게 설명하였으면 ○, 그렇지 않으면 ×를 선택하세요.`
- 그림 안 문자 및 표기:
  - `ㄷ`
  - `ㄹ`
  - `ㅁ`
  - `ㅂ`
  - `ㅇ` 또는 중심 표시로 보이는 작은 원 안의 문자 표기
- 판단 문장:
  - `길이가 가장 긴 선분은 선분 ㅁㅂ이고, 원의 지름은 선분 ㄷㄹ입니다.`
- 선택 표시:
  - `○`
  - `×`
- 정답 표기:
  - `(정답)×`
- 해설:
  - `(해설) 길이가 가장 긴 선분은 선분 ㄷㄹ이고, 이 선분이 원의 중심을 지나므로 원의 지름입니다.`

# [반복되는 시각 패턴]
- **○ / × 선택형 문항** 형식이 반복됩니다.
- 선택지 자체는 화면 왼쪽 아래에 **세로로 두 개** 놓여 있고, 각각 파란색 테두리로 표시되어 있습니다.
- 원 그림 내부에는 **직선 선분들이 서로 다른 방향으로 교차**하는 구조가 보입니다.
- 문자 표기 `ㄷ, ㄹ, ㅁ, ㅂ`가 각 선분의 양 끝이나 근처에 배치되어 있어, 선분 이름을 읽도록 만든 패턴입니다.
- 중심점처럼 보이는 작은 표시가 원의 내부 중심 부근에 있고, 그 주변을 기준으로 선분이 지나는 형태가 반복적으로 강조됩니다.

# [수식 단계]
- 해당 없음.
- 등호를 포함한 계산식이나 식 변형 과정은 보이지 않습니다.

# [등호별 변환 의미]
- 해당 없음.
- 식 변환이 아니라 도형 설명의 참/거짓 판단 문제입니다.

# [세부 내용]
- 원은 화면 중앙 상단에 크게 그려져 있고, 외곽은 얇은 검은 원호로 표현되어 있습니다.
- 원 안에는 최소 **두 개의 선분**이 보입니다.
  - 하나는 **왼쪽 위에서 오른쪽 아래로 기울어진 대각선 선분**입니다.
  - 다른 하나는 **거의 수직에 가까운 선분**으로, 원을 위아래로 관통합니다.
- 대각선 선분은 원 내부의 한 점을 지나며, 그 점 근처에 작은 표시가 있습니다.
- 원의 중심으로 보이는 위치에는 **작은 점/표식**이 있고, 그 근처에 `ㅇ`처럼 보이는 문자 표기가 함께 있습니다.
- 위쪽에는 원 위를 가로지르는 짧은 수평선처럼 보이는 선분이 있고, 그 양끝 근처에 `ㄷ`, `ㄹ`이 표시되어 있습니다.
- 오른쪽의 세로 선분 아래쪽 끝 근처에는 `ㅂ`, 오른쪽 아래 대각선 끝 근처에는 `ㄹ` 또는 유사한 글자가 붙어 있습니다.
- 판단 문장은 특정 두 선분의 **길이 비교**와 **지름 여부**를 설명하는 형태입니다.
- 정답 및 해설 영역은 본문과 분리되어 아래쪽에 배치되어 있어, 학생이 먼저 판단하도록 유도하는 구성입니다.

# [Python DSL 작성 힌트]
- `text`로 문제 번호와 지시문을 상단에 배치합니다.
- `circle` 또는 `ellipse` 류 도형으로 중앙의 큰 원을 그립니다.
- 원 내부에:
  - 기울어진 대각선 선분 1개
  - 거의 수직인 선분 1개
  - 위쪽의 짧은 수평 선분 1개
  를 각각 `line` 요소로 표현합니다.
- 각 선분의 끝점 근처에 `label`을 붙여 `ㄷ`, `ㄹ`, `ㅁ`, `ㅂ` 등의 문자를 배치합니다.
- 중심점은 작은 점/원으로 표시하고, 그 주변에 문자 표기를 추가합니다.
- 아래쪽에는 판단 문장을 한 줄 `text`로 넣고, 그 아래에 `○`, `×`를 세로 정렬로 배치합니다.
- 정답과 해설은 별도 텍스트 블록으로 하단에 분리 배치합니다.
- 선분 이름과 문자 위치가 도형과 겹치지 않도록 여백을 넉넉히 둡니다.

# [주의할 점]
- **답을 추론해서 채우지 말아야 합니다.** 이 문항은 참/거짓을 고르는 형식이지만, 초안에서는 판단 자체를 계산해 확정적으로 설명하지 않는 편이 안전합니다.
- 보이는 문자 중 일부는 해상도상 **`ㅁ`, `ㅂ`, `ㄷ`, `ㄹ`의 위치가 헷갈릴 수** 있으므로, 정확한 좌표 배치 전에 다시 확인이 필요합니다.
- 원의 중심 표시와 문자 `ㅇ`처럼 보이는 부분이 겹쳐 보일 수 있으므로, 단정적으로 한 글자로만 해석하지 않도록 주의해야 합니다.
- 해설 문장은 화면에 보이지만, DSL 작성 시에는 **문항 본문과 정답/해설 영역을 분리**하는 것이 좋습니다.

# [불확실한 부분]
- 원 내부 중심점 근처의 문자 표기가 **`ㅇ`인지 다른 기호인지** 선명하지 않습니다.
- 오른쪽 아래/아래쪽의 문자 표기 중 일부는 **`ㄹ`과 `ㅂ`의 위치가 혼동될 수** 있습니다.
- 위쪽의 짧은 선분이 정확히 **어느 문자 쌍에 대응하는지**는 이미지 해상도상 약간 अस्पष्ट합니다.
- 따라서 Python DSL 작성 전에는 각 라벨의 정확한 위치를 한 번 더 대조하는 것이 필요합니다.

--- RAW VISION DRAFT END ---
