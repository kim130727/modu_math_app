# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_008594.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_008594

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
[전체적인 구성]
- 화면 상단 왼쪽에 문제 번호가 있고, 그 오른쪽에 문제 문장이 한 줄로 배치되어 있습니다.
- 그 아래에는 연한 연두색 배경의 가로 긴 박스가 있으며, 그 안에 보기 2개가 좌우로 나란히 배치되어 있습니다.
- 화면 중단 왼쪽에는 `(정답)` 표시가 있고 그 옆에 선택 기호가 하나 적혀 있습니다.
- 그 아래에는 `(해설)`이 있으며, 두 보기 각각에 대한 식이 줄바꿈되어 표시됩니다.
- 마지막 줄에는 두 나머지 값을 비교하는 문장이 있습니다.
- 전체적으로 “보기 → 정답 표시 → 해설 → 비교 결론”의 순서로 구성된 객관식 풀이 설명 형태입니다.

[보이는 텍스트]
- `62. 나머지가 더 큰 것을 선택해 보세요.`
- 연두색 박스 안:
  - `㉠ 515 ÷ 4`
  - `㉡ 516 ÷ 5`
- `(정답) ㉠`
- `(해설)`
  - `㉠ 515 ÷ 4 = 128···3`
  - `㉡ 516 ÷ 5 = 103···1`
- `따라서 3>1이므로 나머지가 더 큰 것은 ㉠입니다.`

[반복되는 시각 패턴]
- 보기 기호 `㉠`, `㉡`가 각각 식 앞에 붙어 있으며, 상단 박스와 해설 문장 모두에서 같은 기호가 대응됩니다.
- 해설에서는 각 나눗셈 식이 `= 몫···나머지` 형식으로 정리되어 있습니다.
- 두 식 모두 같은 형식의 나눗셈 결과를 제시하고, 마지막에 나머지 값을 비교하는 문장으로 연결됩니다.
- `···` 형태가 나머지 표기처럼 반복되어 사용되고 있습니다.
- 정답 표시와 해설이 분리되어 있어, 선택지와 설명이 대응되는 구조가 반복적으로 보입니다.

[수식 단계]
- 첫 번째 식: `515 ÷ 4 = 128···3`
  - 나눗셈 결과가 몫과 나머지 형태로 표시되어 있습니다.
- 두 번째 식: `516 ÷ 5 = 103···1`
  - 역시 몫과 나머지 형태로 표시되어 있습니다.
- 마지막 문장에서는 두 나머지 `3`과 `1`을 비교하여 더 큰 쪽을 고르는 방식이 드러납니다.
- 이 문제에는 등호가 여러 번 이어진 변형식은 보이지 않고, 각각 한 번의 등호로 결과만 제시됩니다.

[등호별 변환 의미]
- `515 ÷ 4 = 128···3`
  - 왼쪽의 나눗셈식을 오른쪽에 몫과 나머지로 바꾸어 적은 형태입니다.
- `516 ÷ 5 = 103···1`
  - 왼쪽의 나눗셈식을 결과 형식으로 옮긴 형태입니다.
- 마지막 비교 문장 `3>1`은 두 결과의 나머지를 비교하는 의미로 쓰였습니다.
- 다만 실제 계산 과정이 단계별로 적혀 있지는 않고, 결과만 제시되어 있습니다.

[세부 내용]
- 문제 번호 `62`는 왼쪽 위에 작게 표시되어 있습니다.
- 문제 문장은 숫자와 문장부호가 포함된 한 줄짜리 지시문입니다.
- 연두색 박스는 보기 영역으로 보이며, 내부에 두 항목이 가로로 넉넉하게 띄어져 있습니다.
- 각 보기는 `㉠`, `㉡` 같은 동그라미 기호 뒤에 식이 붙어 있습니다.
- `515 ÷ 4`, `516 ÷ 5`는 모두 일반 세로셈 그림이 아니라 가로식으로 제시되어 있습니다.
- 해설 부분은 왼쪽 정렬이며, 보기 기호를 먼저 쓰고 그 뒤에 식과 결과를 연결해 설명합니다.
- `(정답) ㉠`는 해설보다 위에 따로 배치되어 있어 정답만 빠르게 확인할 수 있는 형태입니다.
- 마지막 문장은 `따라서`로 시작하여 비교 결과를 말하는 결론 문장입니다.

[Python DSL 작성 힌트]
- 상단 문제문:
  - `문제 번호 + 지시문`을 한 줄 텍스트로 배치하는 구조로 작성하면 됩니다.
- 보기 영역:
  - 연한 연두색 사각형 박스를 만들고, 그 안에 2개의 보기 텍스트를 좌우로 배치합니다.
  - 각 보기 앞에 `㉠`, `㉡` 기호를 넣습니다.
- 정답/해설 영역:
  - `(정답)` 라벨과 정답 기호를 한 줄로 배치합니다.
  - `(해설)` 라벨 아래에 두 줄의 식을 세로로 정렬합니다.
- 식 표기:
  - `= 몫···나머지` 형태를 텍스트로 그대로 표현합니다.
  - 비교 문장은 `3>1`처럼 부등호를 포함한 텍스트 한 줄로 배치합니다.
- 전체 레이아웃:
  - 위에서 아래로 `문제문 → 보기 박스 → 정답 → 해설 → 결론` 순서로 구성하세요.
- 좌우 정렬:
  - 보기 2개는 같은 높이에서 좌우로 분리해 배치하는 것이 핵심입니다.

[주의할 점]
- 이 문제는 나눗셈의 결과를 이용해 나머지를 비교하는 형식이지만, 실제 풀이를 전개하거나 값의 계산 과정을 상세히 재현하지 마세요.
- 빈칸이나 미지의 답을 추론해서 채우지 마세요. 이미 화면에 보이는 정답 기호만 기록하세요.
- `128···3`, `103···1`의 `···`는 보이는 표기 그대로 유지하고, 다른 기호로 바꾸지 마세요.
- 보기와 해설의 기호 대응을 혼동하지 않도록 주의하세요.
- 정답 표시가 화면에 직접 보이므로, 추정이 아니라 관찰 사실로 적어야 합니다.

[불확실한 부분]
- 연두색 박스의 정확한 테두리 유무와 두께는 화면 해상도에 따라 다르게 보일 수 있습니다.
- `···` 표기가 실제로는 나머지 표시인지, OCR에서 그렇게 보이는지에 대한 세부 폰트 형태는 이미지 만으로는 완전히 단정하기 어렵습니다.
- `(정답) ㉠`의 배치가 엄밀히 한 줄인지, 약간 들여쓰기된 줄인지 화면 확대에 따라 차이가 있을 수 있습니다.
- 문제 번호 앞의 작은 네모 기호 `□`는 체크박스처럼 보이지만 정확한 의미는 이미지 밖 맥락이 필요할 수 있습니다.

--- RAW VISION DRAFT END ---
