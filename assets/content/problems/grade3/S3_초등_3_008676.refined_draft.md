[문제 개요]
- 초등 수학의 도형 단원 문제로 보이며, 주제는 **원의 지름 찾기**이다.
- 화면 상단에 문제 번호와 지시문이 있고, 중앙에는 원 그림, 그 아래에는 4개의 선택지가 들어 있는 빨간 테두리 박스가 있다.
- 아래쪽에는 `(정답)`과 `(해설)` 문구가 인쇄되어 있어, 이 문제는 객관식 형태이면서 정답 표시가 함께 보인다.

[원본에 보이는 요소]
- 상단 문장: `59. 원의 지름을 나타내는 선분을 찾아 선택하세요.`
- 원 그림 안팎의 표기 문자: `ㄱ, ㄴ, ㄷ, ㄹ, ㅇ`
- 선택지 박스 안의 보기:
  - `선분 ㄱㄹ`
  - `선분 ㅇㄱ`
  - `선분 ㅇㄴ`
  - `선분 ㄷㄹ`
- 하단 표기:
  - `(정답) 선분 ㄱㄹ`
  - `(해설) 원 위의 두 점을 이은 선분 중 원의 중심을 지나는 선분을 원의 지름이라고 합니다.`

[수식/관계 구조]
- 이 문제에는 등호를 포함한 수식 변형이나 계산식이 보이지 않는다.
- 대신, 도형 관계가 핵심이다.
- 보이는 관계를 구조적으로 정리하면:
  - 원 위의 두 점을 잇는 선분 후보들이 제시됨.
  - 그중에서 **원의 중심을 지나가는 선분**이 지름이라는 설명이 함께 제공됨.
- 원 내부에는 중심처럼 보이는 점 `ㅇ`이 있고, 그 점을 기준으로 여러 선분이 배치되어 있다.
- 선분 `ㄱㄹ`은 원의 왼쪽 아래 점 `ㄱ`에서 오른쪽 위 점 `ㄹ`까지 대각선으로 이어지며 중심 `ㅇ`을 지난다.
- 다른 보기 `ㅇㄱ`, `ㅇㄴ`, `ㄷㄹ`은 중심을 지나지 않는 선분으로 보이거나, 지름 후보가 아닌 짧은 선분 형태로 보인다.

[등호별 변환 의미]
- 해당 없음.
- 등호 연쇄나 식의 변형이 보이지 않으므로 단계별 변환 설명은 필요하지 않다.

[빈칸 정책]
- 빈칸 문제는 아니다.
- 다만 DSL 작성 시, 원문에 보이는 텍스트와 도형 배치를 우선하고, 보이지 않는 내용이나 추론한 답은 채우지 않는다.
- 정답이 화면에 `(정답)`으로 인쇄되어 보이더라도, 새로 계산해 넣지 말고 **보이는 사실로만 기록**한다.
- 이미지에 보이는 텍스트가 불명확한 경우에는 임의로 보정하지 말고 그대로 불확실 항목으로 남긴다.

[반복 패턴]
- 보기 4개가 모두 `선분 + 두 글자` 형식으로 통일되어 있다.
- 선택지들은 빨간 테두리의 큰 가로 직사각형 안에 좌우로 일정 간격을 두고 배치되어 있다.
- 원 그림에는 중심점과 그 중심을 기준으로 한 선분 구조가 반복적으로 강조된다.
- 문자 `ㄱ, ㄴ, ㄷ, ㄹ`은 원의 둘레 근처에, `ㅇ`은 중심 근처에 배치되어 있어 도형 관계를 읽기 쉽게 만든다.

[DSL 작성 힌트]
- 구성 요소는 크게 다음처럼 나누면 좋다:
  - 상단 문제 문장용 `text`
  - 원 도형용 `circle`
  - 중심점용 작은 `point` 또는 작은 원형 표시
  - 선분용 `line_segment` 여러 개
  - 선택지 박스용 `rectangle`
  - 보기 및 하단 문구용 `text`
- 원 그림은 화면 중앙 상단에 크게 배치한다.
- 원 내부 선분은 다음처럼 별도 객체로 분리하면 구조가 분명하다:
  - 왼쪽 아래 `ㄱ`에서 오른쪽 위 `ㄹ`로 가는 대각선
  - 중심 `ㅇ`에서 아래쪽 `ㄴ`으로 내려가는 수직선
  - 오른쪽 위 `ㄹ`에서 아래쪽 `ㄷ`으로 내려가는 수직선
- 중심점 `ㅇ`은 분홍색 작은 점으로 보이며, 문자 `ㅇ`은 그 오른쪽 근처에 작게 배치되어 있다.
- 선택지 박스는 빨간색 외곽선만 있는 직사각형이고 내부는 분할선 없이 텍스트만 놓여 있다.
- 보기 텍스트는 가로 방향으로 4개를 균등 간격에 가깝게 배치하면 원본 느낌에 가깝다.
- 하단의 `(정답)`과 `(해설)`은 선택지 박스 아래쪽에 왼쪽 정렬로 따로 배치한다.

[semantic/solvable 힌트]
- 해설 문장에 따르면, 이 문제는 **원의 중심을 지나는 선분이 지름**이라는 개념을 묻는다.
- 화면에 보이는 정답 표기는 `선분 ㄱㄹ`이다.
- 하지만 DSL 작성 목적상, 이 정답을 계산해서 도출한 것으로 쓰지 말고 **이미 인쇄되어 보이는 텍스트**로만 다루는 것이 안전하다.
- 도형에서 중요한 의미는 “원 위의 두 점을 이은 선분”과 “그 선분이 중심을 지나느냐”의 구분이다.

[주의 및 TODO]
- `ㅇ` 표기는 중심을 뜻하는 것으로 보이지만, 문자 크기가 작아 판독이 아주 선명하지 않을 수 있다.
- 원 내부의 선분과 문자 일부는 원의 경계와 겹쳐 보여, 정확한 좌표 배치에는 주의가 필요하다.
- `(해설)` 문장은 줄바꿈 없이 이어져 보이지만, 화면 오른쪽에서 약간 잘려 보이는 느낌이 있다. 원문 배치 확인이 필요하다.
- 정답이 화면에 인쇄되어 있어도, DSL에는 정답을 새로 생성하지 말고 보이는 그대로만 반영한다.
- 등호 변환이나 계산식 관련 TODO는 없다.
