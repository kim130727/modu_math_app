# provider: openai
# model: gpt-5.4-mini
# image_path: examples\problems\TS_01.초등학교_3학년_01.객관식\S3_초등_3_008649.png

## System Prompt
You are a Korean math worksheet vision-to-DSL planning assistant.
You rewrite a raw vision draft markdown into a cleaner, DSL-friendly refined draft markdown.

Hard rules:
1) Output markdown text only (never JSON).
2) Do not generate Python DSL code.
3) Do not solve the problem unless an answer is visibly printed in the source.
4) Do not fill blank answers with inferred values.
5) Separate visible facts, inferred mathematical meaning, rendering/layout hints, DSL component hints, and TODO/uncertain items.
6) If an equation transformation chain is present, explain step-by-step what changed at each equality.
7) If a transformation like a/b ÷ n -> a/(b×n) appears, explicitly include this sentence:
   "나누기 n은 분모의 ×n으로 반영된다."


## User Prompt
문제 ID: S3_초등_3_008649

아래 raw vision draft를 DSL 작성에 더 직접적으로 쓸 수 있는 refined draft로 다시 작성하세요.
원본 PNG가 함께 제공되므로, 초안과 이미지가 다르면 이미지 기준으로 교정하세요.

반드시 마크다운으로 작성하고, 아래 섹션 헤더를 그대로 포함하세요:

[문제 개요]
[원본에 보이는 요소]
[수식/관계 구조]
[등호별 변환 의미]
[빈칸 정책]
[반복 패턴]
[DSL 작성 힌트]
[semantic/solvable 힌트]
[주의 및 TODO]

작성 규칙:
- JSON 금지, Python DSL 코드 출력 금지.
- 정답 추론 금지. 단, 원본에 정답이 인쇄되어 '보이는 경우'에만 관찰 사실로 언급 가능.
- 빈칸에는 추론값을 넣지 말고 빈칸으로 유지한다는 정책을 분명히 써주세요.
- "보이는 사실"과 "추론되는 수학적 의미"를 분리해서 쓰세요.
- 렌더링/레이아웃 힌트(정렬, 간격, 그룹, 박스, 표, 화살표, 분수 슬롯)를 구체적으로 쓰세요.
- 등호 연쇄 변형이 있으면 각 단계의 변화 내용을 설명하세요.
- a/b ÷ n -> a/(b×n) 류 변환이 보이면 정확히 다음 문장을 포함하세요:
  "나누기 n은 분모의 ×n으로 반영된다."
- 불확실한 항목은 TODO로 남기고 단정하지 마세요.

--- RAW VISION DRAFT START ---
# [전체적인 구성]
- 화면 상단 왼쪽에 문제 번호와 지시문이 있고, 오른쪽에는 원 그림이 배치되어 있습니다.
- 원 그림 아래에는 문장형 설명이 2줄 이상으로 이어져 있으며, 마지막에는 선택지처럼 보이는 짧은 단어가 보입니다.
- 전체적으로 **그림을 보고 문장의 빈칸에 알맞은 말을 선택하는 문제** 형태로 보입니다.
- 수학식이나 계산 과정은 보이지 않고, **도형의 각 부분 이름을 구분하는 언어 문제**에 가깝습니다.

# [보이는 텍스트]
- `□ 20. 그림을 보고 알맞은 말을 선택하세요.`
- `원의 ( 지름, 반지름 )은 원을 둘로 똑같이 나눕니다.`
- `원의 지름은 원을 둘로 똑같이 나눕니다.`
- `지름`

※ 위 문장 중 첫 번째 설명 문장에는 괄호 안에 `지름, 반지름`이 함께 제시되어 있으며, 어떤 말을 고르는 형식으로 보입니다.  
※ 마지막의 `지름`은 보기 또는 선택 결과처럼 표시된 것으로 보이지만, 화면 일부만 보여서 배치 방식은 확실하지 않습니다.

# [반복되는 시각 패턴]
- 원형 도형 안에 **중심점처럼 보이는 점**이 있고, 그 점에서 바깥쪽으로 선분이 여러 개 뻗어 있습니다.
- 원 위의 점들과 내부 선분이 연결되어 있어, **지름과 반지름을 구분하는 학습용 도형** 패턴입니다.
- 도형 오른쪽에는 점 이름이 붙고, 아래쪽과 왼쪽에도 각각 글자가 배치되어 있습니다.
- 원 내부를 가로지르는 긴 선분 1개와, 중심에서 아래쪽으로 내려가는 짧은 선분 1개가 함께 보입니다.
- 선분과 점의 조합이 반복적으로 사용되어, 원의 구성 요소를 설명하는 전형적인 시각 패턴입니다.

# [수식 단계]
- 해당 없음.  
- 등호로 이어지는 계산식이나 변형식은 보이지 않습니다.

# [등호별 변환 의미]
- 해당 없음.  
- 등호 기호가 있는 수식 전개는 관찰되지 않습니다.

# [세부 내용]
- 오른쪽 상단의 그림은 원으로 보이며, 원의 가장자리와 내부 선분이 연필선처럼 그려져 있습니다.
- 원의 중심 부근에 작은 점이 하나 있고, 그 점은 분홍색으로 강조되어 있습니다.
- 원 위쪽 왼쪽 부분에서 오른쪽 끝점까지 이어지는 선분이 보입니다.
- 또 다른 긴 선분은 왼쪽 아래의 원 둘레 점에서 중심 부근을 지나 오른쪽의 원 둘레 점까지 연결되어 있습니다.
- 중심 부근의 점에서 아래쪽 원 둘레 방향으로 짧은 선분이 하나 더 뻗어 있습니다.
- 원 둘레 근처의 글자 표시는 다음처럼 보입니다:
  - 위쪽 근처: `가`처럼 보이는 글자 1개
  - 오른쪽 근처: `라` 또는 비슷한 글자 1개
  - 왼쪽 아래 근처: `나` 또는 비슷한 글자 1개
  - 아래쪽 근처: `다`처럼 보이는 글자 1개
- 다만 글자 모양이 작고 흐려서 정확한 자모 판독에는 약간의 불확실성이 있습니다.
- 본문 문장에서는 `원의 ( 지름, 반지름 )은 원을 둘로 똑같이 나눕니다.`라고 되어 있어, 괄호 안에서 하나를 선택하게끔 되어 있습니다.
- 아래 줄의 `원의 지름은 원을 둘로 똑같이 나눕니다.`는 선택 후 문장을 다시 적어 놓은 형태처럼 보입니다.
- 맨 아래의 `지름`은 화면 하단 일부만 보여서, 보기 목록인지 정답 표시인지 단정하기 어렵습니다.

# [Python DSL 작성 힌트]
- 문제 유형은 **도형 + 문장 선택형**으로 분류하는 방식이 적절합니다.
- Python DSL에서는 다음 요소를 분리해 표현하면 좋습니다:
  - 문제 번호와 안내 문장
  - 원 그림
  - 원 내부의 선분 3개 정도
  - 중심점 표시
  - 원 둘레의 문자 라벨 4개
  - 괄호가 포함된 문장 1개
  - 아래쪽 보조 문장 또는 선택 결과처럼 보이는 텍스트
- 도형 설명 시에는:
  - 원의 외곽선
  - 중심점
  - 중심을 지나는 긴 선분
  - 중심에서 아래로 뻗는 짧은 선분
  - 위쪽에서 오른쪽으로 향하는 선분
  을 각각 별도 객체로 두는 것이 좋습니다.
- 텍스트 배치는 좌측 상단, 우측 상단 그림, 하단 문장 영역으로 나누어 정리하면 좋습니다.
- 괄호 안의 선택어는 실제 정답으로 확정하지 말고, **보기 텍스트**로만 처리하세요.

# [주의할 점]
- `지름`이 맞는지 여부를 이 초안에서 판단하거나 확정하지 마세요.
- 원 내부의 글자 표시는 이미지가 작아 혼동될 수 있으므로, 정확한 글자 판독은 주의가 필요합니다.
- 아래쪽 `지름` 텍스트가 문제의 보기인지, 학생이 선택한 결과인지, 별도 설명인지 화면만으로는 확실하지 않습니다.
- 도형의 선분이 정확히 원의 지름인지 반지름인지 해석하지 말고, **보이는 위치와 연결 관계만** 기록하세요.
- 화면이 잘린 부분이 있어 하단 텍스트 전체를 완전히 복원했다고 단정하지 마세요.

# [불확실한 부분]
- 원 주변의 문자 라벨이 `가/나/다/라`인지 정확한 판독이 다소 불확실합니다.
- 하단 문장의 마지막에 보이는 `지름`이 선택지인지 정답 표시인지 확실하지 않습니다.
- 왼쪽 하단의 작은 기호나 점선처럼 보이는 부분은 문제 본문과 무관한 배경 잡음일 가능성도 있습니다.
- 그림의 선분 일부가 원의 중심을 정확히 통과하는지 여부는 해상도상 완전히 단정하기 어렵습니다.

--- RAW VISION DRAFT END ---
